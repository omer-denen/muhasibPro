# MuhasibPro - Karar Logu ARSIV (2026-08-21 - 2026-09-15)

> Arsiv (2026-09-15, Oturum 271). Guncel kararlar: docs/ROADMAP.md Â§4

## 4. Karar Logu (Yeni)

| Tarih | Karar | Gerekçe |
|---|---|---|
| 2026-08-21 | Master referans, kod iyileştirerek taşınacak — **2026-08-28’de silindi, yerini `muhasibpro-WebToXaml` aldı** | Kullanıcı kararı — alışılan yapı korunacak |
| 2026-08-21 | `MuhasibPro.Domain` korunacak (Core değil), `MuhasibPro` (App) korunacak (MuhasibPro.App değil) | Familiar namespace |
| 2026-08-21 | `UIContracts` ayrı proje yok — kontratlar `Business/Contracts/UIServices` içinde | Alışılan yapı, HATALAR.md Business UI bağımlılığı kabul |
| 2026-08-21 | ViewModel isimleri master gibi (`FirmaListViewModel`, `MaliDonemDetailsViewModel` vb., `*Yonetimi` değil) | Kullanıcı isteği |
| 2026-08-21 | Önce App (Aşama A), sonra muhasebe modülleri tek tek (Aşama B) | Token/modüler çalışma |
| 2026-08-21 | Eski proje komple temizlendi, `src/Libraries` → `Libraries` köke alındı, `MuhasibPro.slnx` → klasik `MuhasibPro.sln`’e dönülecek | Alışılan klasör yapısı |
| 2026-08-21 | `ServiceLocator` pencere-başına scope + `IContextService.IsMainView`/`ViewModelBase.IsMainWindow` dallanması (MainWindow ise `CreateNewViewAsync` ile DetailsWindow, değilse `Navigate`; `CanGoBack`/`IsButtonVisible` koşulları) `WINUI-MIMARISI.md:4.1`’de belgelendi — tekrar anlatım önlendi | Pencere yönetimi olmazsa olmaz, MainWindow’a göre buton/menu farkı |
| 2026-08-21 | `DbContext` Extensions iyileştirildi + `DbContextAnalysisExtensions.cs` merkezi analiz eklendi; `MigrationExtension` finalAnalysis bug düzeltildi (`Array.Empty` → `tablesToCheck` taze analiz); `Backup` haftalık oto yedek (`EnsureWeeklyBackupAsync` 7 gün); `ApplicationPaths` 433 satır god-class 4 provider’a bölündü + facade; `CalculateSimpleHash` overflow düzeltildi | Derin analiz — kullanıcı isteği, modüler ve test edilebilir yapı |
| 2026-08-23 | İlk Kurulum ekranı + LoginView iyileştirmeleri görsel doğrulandı (gradient+glass, True/False→Mevcut/Geçerli/Başarılı, inline hata, CanLogin, Splash→Kurulum→Login, EnsureCreated→Migrate history onarımı) | Kullanıcı dili, Migrate doğru yol |
| 2026-08-23 | Login Reveal (`E052↔E7B3`, `PasswordRevealMode`) + `FirmaShellView` `IsMainWindow→CreateNewViewAsync` canlı doğrulandı + `MaliDonemSagaManager` `Migrate` + `NU1903` `<NoWarn>` ile **0 uyarı 0 hata** | Göz ikonu kullanıcı talebi, saga tutarlılık, build 0/0 hedefi |
| 2026-08-23 | Web temizliği: `src` 22 + `dist`/`node_modules`/`vite`/`tsconfig` + `.cr/.vs` kaldırıldı, saf WinUI3 iskelet + `docs`/`logs` korundu, çekirdek akış için AI Studio’ya sadece WinUI3 paket | Harici dosya temizliği, tek tasarım kanalı |
| 2026-08-23 | Yeni `muhasibpro-new-viewpackage` renk stili mevcut fonksiyonel view'lara giydirildi (Splash+Login açık tema); **fonksiyon ↔ stil ayrımı**: binding/komutlar dokunulmadan sadece renk/görünüm değişti; Acrylic token'ları MSB3073 nedeniyle alınmadı — **2026-08-26’da paket `muhasibpro-new-viewpackage` kaldırıldı, stil kaynağı `muhasibpro-webToWinui3/MuhasibPro/Views`** | Kullanıcı kararı — aynı fonksiyonlar yeni renk stiline çevrilecek |
| 2026-08-24 | **GOD-CLASS YASAĞI genel kural (AGENTS.md Kurallar'da kesin dille):** hiçbir sınıf — ViewModel, Service, Manager, Control, View code-behind dahil — god-class olmayacak; tek sorumluluk zorunlu; kalıtım ile VM birleştirme yasak, composition kullanılacak; ihlal build hatası gibi ele alınır, ihlal edilen faz kapatılmaz. Örnekler: `ApplicationPaths`→4 provider, `LoginViewModel`→`QuickLoginAccountsViewModel`, `FirmaShellViewModel`→`FirmalarVM/MaliDonemVM` composition | Kullanıcı kararı — sınıf bazlı kararlar (O3/O14/O21) projeye genel kesin kurala yükseltildi; kod incelemesinde zorunlu madde |
| 2026-08-24 | **XAML MODÜLERLİĞİ + STİL TUTARLILIĞI (god-class yasağının XAML ayağı):** ezbere yüzlerce satır XAML yazılmayacak; tekrar eden/bağımsız sorumluluklu görsel bloklar esnekliği ve kod analizi hızını artırıyorsa UserControl'e bölünecek; tüm stil değerleri DesignTokens TEK KAYNAK'tan — istenen token yoksa ÖNCE DesignTokens'a eklenir, hardcode `#FF...` yasak (ThemeResource hariç) | Kullanıcı kararı — O18 static renk kararı + QuickLoginPanel/FirmalarList UserControl pratiği genel kurala bağlandı |
| 2026-08-25 | **🔒 ÇEKİRDEK KİLİDİ:** Çekirdek `Aşama A (Altyapı→GlobalDb→Login→FirmaShell→MaliDonem→MainShell + UpdateService/Velopack/SistemMigrationManager)` **en az kusurda** tamamlanmadan Faz B (Cari/Stok/...) açılmayacak; güncelleme almaya başlayan uygulama çekirdeği tamamlanmış sayılır, sonrası küçük fix | Kullanıcı kararı — `LOG Oturum 25` + `KONTROL 🔒` + `ROADMAP Aşama A` kilitlendi; Faz B öncesi 0 kusur hedefi |
| 2026-08-29 | **Db katmanı mühürleme + tenant entity sözleşmesi:** DbContext analizi tek kaynağa (`AnalyzeDatabaseCoreAsync<T>`) bağlandı, migration akışı gerçek `finalAnalysis` (bayat `DatabaseValid`/sahte rollback) ile düzeltildi; `tablesToCheck` ayrı sınıfa (`TenantTablesToCheck`) alındı; 69 muhasebe entity'si **interface sözleşmesine** (`ITenantMuhasebeEntities`) taşındı — Faz B açılınca `AppDbContext` implemente eder, migration o zaman üretilir (çekirdek AppDbContext'i artık şişmez, kaynak-israf yok) | Kullanıcı kararı — migration üretimi PK/ilişki belirsizlikleri yüzünden uzuyordu; hızlı/boşa gitmeyen çözüm |
| 2026-08-31 | **Toast kanalı pencere-scoped (`IToastService` Scoped) + MainWindow root Grid'de ToastHostControl:** MainWindow (Login/FirmaShell/MainShell) ile DetailsWindow (ShellView) aynı toast'u ikisinde birden göstermesin; her pencere kendi scope'unun bildirimini kendi üstünde gösterir | Kullanıcı kararı — "MainWindow'a da koyalım her zaman işe yarar"; duplike önlendi |
| 2026-08-31 | **⭐ Toast sistemi TAMAMEN kaldırıldı:** `IToastService`/`ToastService`/`ToastHostControl`/converter'lar/DI/`ICommonServices.ToastService`/`ViewModelBase.ToastService` silindi; 9 tüketicinin çağrısı kaldırıldı. Bildirimler artık `DialogService` + `StatusBar/StatusMessage` + satır içi feedback ile; QuickSistemDbDiagDialog Yedek Al → buton içi ProgressRing + `BackupStatusText`. `MainWindow.MainFrame` + `AnimatedInfoBorder` korundu | Kullanıcı kararı — "tüm toast'ları kaldır, Service de dahil, istemiyorum" |
| 2026-08-31 | **OS Toast tek kanal (CommunityToolkit.WinUI.Notifications 7.1.2):** kanal ayrımı — karar→Dialog, işlem sonucu→OS toast, form bağlamı→satır içi, global durum→StatusBar. `INotificationService` (Business) + `NotificationService` (App): unpacked AUMID registry + Start Menu shortcut + `ToastNotificationManager.CreateToastNotifier(Aumid)` (toolkit `RegisterAumidAndComServer<T>` COM activator gerektirdiği için ham desen). 5 aksiyon re-wire, satır içi istisnalar korundu | Kullanıcı kararı — "kendi içimizde yapamıyoruz, dialog eskidi, OS'a devredelim" |
| 2026-08-31 | **OOBE görsel dili `docs/OOBE-TASARIM-SABLONU.md`'ye alındı (tek kaynak):** `muhasibpro-WebToXaml` çekirdek ekranlar için referans DEĞİL — özellikle SistemKurulum dark/diagnostik tasarımına bakılmaz. Login/Splash/SistemKurulum deseni (AppBackgroundBrush, MuhasibCardStyle, MuhasibPrimaryButtonStyle, MuhasibOnPrimaryBrush, Loaded gölge, çok-panel düzen) şablonda; yeni ekran oraya göre yazılır | Kullanıcı kararı — "bu şablonu logla her zaman tekrar anlatmayayım, WebToXaml'a bakma" |
| 2026-08-31 | **Ana kartlarda `ModernCard` kesin komut:** `Styles/Cards.xaml:66` `ModernCard` (Login referans) bundan sonra tüm ana kartlarda TEK kaynak; `MuhasibCardStyle` sadece yardımcı iç kartlarda (metric pill) | Kullanıcı kararı — "loglarını tut. bundan sonraki tasarım stilimiz bu. kesin komut olarak yaz" |
| 2026-09-02 | **Dönem kartı vitrin verisi `MaliDonemModel`'e saçılmış skalar alan yerine DB-seçim modeli (`TenantDetailsModel TenantDetails`):** kaç/katman temiz; `GetMaliDonemlerWithFirmaId` (per-firma) korunacak, `GetUserTenantsForSelectionAsync`'ye geçilmeyecek | Kullanıcı yönlendirmesi — "saçılmış alan yerine TenantDetailsModel'i doldur, temiz olur"; soru → kullanıcı per-firma akışını seçti |
| 2026-09-02 | **Titlebar her view'da kopyalanmaz — ortak `ShellTitleBar`:** özel title-bar (logo+başlık+pencere kontrolleri) tek UserControl; FirmaShell/SistemKurulum `SetupCustomTitleBar`+min/max/close kopyası silindi, kapat onayı `WindowHelper.RequestCloseAsync` tek kaynak (özelleştirilmiş titlebar'da `Window.Close()` `AppWindow.Closing`'i tetiklemediği için onay atlanıyordu) | Kullanıcı isteği — "böyle her view içinde titlebar konulmaz, refactor yapalım" |

| 2026-09-02 | **Dönem kartında "DB Durumu" rozeti analiz sonucundan gösterilir** (`GetTenantDatabaseStateAsync` — `Güncel`/`Güncelleme Gerekli`/`DB Dosyası Yok`/`Kontrol Gerekli`); analiz `MaliDonemListViewModel` listeleme akışına bağlandı (opsiyonel servis, sadece FirmaShell paylaşımlı listede) | Kullanıcı isteği — "tablo/kayıt yerine analizden gelen Güncel/güncellenmesi gerekiyor bilgisi olsun"; `TabloKayitMetni` placeholder'ı yerine gerçek durum |

| 2026-09-03 | **XAML içinde tema zorlanamaz (`RequestedTheme` hardcode yasak — dialog dahil):** `ContentDialog` Light garantisi kodda tek kaynakta (`DialogHelper.ShowCenteredAsync` / `DialogService.CreateDialog` → `ElementTheme.Light`); dialog içerikleri tema fırçalarını `ThemeResource` ile izler | Kullanıcı kararı — "xaml kodları içinde tema rengi zorlanamaz, loglara ekle"; sistem Dark iken silme guard'ın koyu açılması bu kural ihlalinden geliyordu (StaticResource + servis overwrite + helper bypass) |

| 2026-09-03 | **Dosya-yok dönem kartı: Kurtar (yedek varsa) + Sil; arşiv onaylı Kapat + "Arşivden Çıkar":** dosya bulunamayınca kart girişe kapanır, Yedek/Arşivle gizlenir; kayıt silme saga korunarak yapılır (doğrulama satır-düzeyi) | Kullanıcı önerisi — "yedeği varsa Kurtar göster, yedek listesinden geri yükle; saga bozulmadan kayıt sil"; "Arşivle bu kadar kolay olmamalı, kapanmış kartta Arşivle yazmasın, arşivden çıkarma lazım" |

| 2026-09-04 | **Mali Dönem ayrı yönetim penceresi; kart hafif (sadece Yedek); sağ içerik kart değil spec-sheet; "Öksüz"→"Bilinmeyen":** tek karttaki sorumluluk firmanın yönetim penceresine taşındı (ComboBox dönem + rail bölümler + flat analiz) | Kullanıcı kararı — god-class/sorumluluk dağılımı; "tek sayfa düzeni gerek yok"; dosyasız dönem ⚠ ile listelenir |
| 2026-09-04 | **Motor 3-sayfa tasarım elenen işlevler tasarımda da yok:** kilit kartı düşer (5 kart), analizde kategori/son-yazma/I-O kartı/anomali raporu çıkarıldı, arşiv bayrak (dosya taşıma yok) — görsel 3 sayfa birebir bunlarla kurulur | Kullanıcı kararı — "kullanılmayacak dediklerimiz aynen geçerli, tasarımda da yer almayacak" |
| 2026-09-05 | **InventEase dili Login pilotu; global palet değişmedi:** EkranKaydi'ndan örneklenen teal/ink/sage yeni token ailesi (yalnız Login); mavi primary diğer ekranlarda korunur, pilot onayı sonrası rollout. Sol taşıyıcı acrylic buzlu anakart, beyaz iç kartlar üstte; lime/kapasite/sparkline dashboard'a bırakıldı (ölü token yasağı) | Kullanıcı kararı — "tasarım dilini al, fonksiyonellik değil"; kart-için-kart + acrylic görünürlük vurgusu |
| 2026-09-05 | **InventEase TÜM projenin ANA tasarım dili (MÜHÜR):** Splash'tan başlayarak tüm Page/UserControl/Dialog/Controls çevrilecek; eski stiller (Panel/Modern/MuhasibCard, mavi primary, hover/tooltip desenleri korunur) yeni dile taşınır; hardcode yasak. Recurra panel dili yürürlükten kalkar (geçiş bitene dek referans). | Kullanıcı kararı — "bu tasarımı ana tasarım olarak kabul et, mühürle, eski stilleri çevir" |
| 2026-09-06 | **ContentDialog kalıbı = QuickDialog + butonlar tek kaynak `Buttons.xaml`:** chrome (`CardBackgroundFillColorDefault + Card16` + setters-only ink/danger/info/secondary, `DefaultButton Primary` yasak) QuickDialog'da mühürlendi, 4 dialog kalıba geçirildi; sayfa `Button` yerelleri merkezi varyantlara taşındı (30→8 istisna) | Kullanıcı kararı — "bundan sonraki şablonlar Quick gibi olacak" + "butonlar tek yerden kontrol edilsin" |
| 2026-09-07 | **Vurgu rengi mavi-uç petrol, teal isimleriyle silindi:** `MuhasibTeal*` → `MuhasibPetrol*` (Light Base `#0B5560`/Deep `#073A42` — koyu revizyon, Dark Base `#6FB3BC`/Deep `#4E8A93`); küçük buton/ikon/metinlerde koyu uç kullanılır, kart/zemine dokunulmaz | Kullanıcı kararı — seçili firma kartı altı butonların yeşil-uç renginden nefret; "proje büyümeden isimlerle hallet" |
| 2026-09-10 | **Denetim Masası (tek kapı):** pre-dönem tüm ayarlar FirmaShell içi sekmede; per-view Expander haritası yürürlükten kalktı (`AYARLAR-PANEL-PLAN` revize); `YonetimAyarlarDialog` kalkacak | Kullanıcı kararı — "FirmaShell'de genel ayarlar sekmesi + Windows Ayarlar görünümü + Yonetim ayarları tek yerde" |
| 2026-09-10 | **Ayarlar kullanıcı bazlı (`Anahtar:U{id}`):** firma seçimi yokken kullanıcı anahtarı; giriş-öncesi global fallback; Identity GLOBAL kalır (kilit/hash login-öncesi + yönetici-kritik) | Kullanıcı kararı ("kullanıcı bazlı, KaydedenId zinciri") + güvenlik istisnası |
| 2026-09-10 | **Avatar:** `PersonPicture` + `ResimSource ?? Resim` passthrough + `ObjectToImageConverter` byte[] decode; yoksa baş-harf (entity `Resim/ResimOnizleme` deseni) | Kullanıcı kararı — "profil resmi veya varsayılan avatarlar" |
| 2026-09-12 | **Görsel dil = saf WinUI Fluent (tüm proje):** yeni işler sistem stilleri (`TableView`, `NavigationView`, standart dialog/button) ile; özel token yalnız Fluent'te karşılık yoksa. `OOBE-TASARIM-SABLONU` + InventEase mührü (6.42) yürürlükten kalktı — mevcut ekranlar çalışıyorsa yerinde kalır, fırsatçı migrate; toplu re-skin yok. Sebep: stabilite. | Kullanıcı kararı — "en stabil tasarım winui fluent dil tasarımıyla devam edeceğiz", "saf fluent tüm proje için geçerli" |
| 2026-09-12 | **Önce çekirdek stabilizasyonu, sonra Faz B:** muhasebe modülleri çekirdek mühürlenmeden açılmaz (Aşama A kilidi teyit); mühür = build 0/0 + test yeşil + smoke + canlı E2E onayı | Kullanıcı kararı — "önce çekirdek yapıyı kurmak istiyorum, şuan heryerde bug var" |
| 2026-09-12 | **Tüm tasarımlar Windows 11 Fluent + Mica-uyumlu:** "toplu re-skin yok" kaydı kalktı (Faz 6.75 açıldı); **Kural 14** — view'e dokunmadan ÖNCE o işlemin tasarımı internette araştırılır (MS Fluent + Toolkit öncelikli), sonra uygulanır | Kullanıcı kararı — "mica uyumlu olacak şekilde tüm tasarımları fluent tasarımına geçireceğiz" + "ilgili view'in işlemi önce internetten aratıp sonra uygulama agent kuralı olarak geçsin" |
| 2026-09-13 | **Kural 14 genişletme — sektör bağlamı + davranış değişikliği:** araştırma "genel WinUI ekranı" değil **muhasebe/ERP yazılımı** bağlamında yapılır (QuickBooks/Sage/MYOB/Tally/Logo referans); araştırma sonucu mevcut sayfa ile uyuşmuyorsa view sadece görsel değil **fonksiyon ve işleyiş olarak da** yeniden tasarlanır — "davranış değişikliği yok" kısıtı sektör gerçeğine tabidir | Kullanıcı kararı — "bu proje ile biz hangi tarz uygulama geliştiriyoruz da sen ona göre araştırma yapıyorsun" + "araştırma sonucu uyuşmuyorsa davranış değişikliği yapılır" |
| 2026-09-13 | **Tema ayarlanabilir + fırça kuralı (Oturum 230):** varsayılan sistemi takip eder (`Default`); Denetim Masası'ndan `Default/Light/Dark`; dialog'lar mirası alır (Light zorlama kalktı); tema-bağımlı fırça kullanım yerinde `ThemeResource` olur (Static load-time kilitlenir — beyaz-beyaz metin yapar), `ThemeDictionaries` içinde `StaticResource` (MS kuralı) | Kullanıcı kararı — "OS Dark + Light zorlama çakışması" + "DesignTokens sistem-öncelikli" |
| 2026-09-13 | **Kural 15 — bilmeden yapma, dürüstçe sor:** araştırılmamış yapı uydurulmaz; emin değilse kod yazılmaz, kullanıcıya açıkça söylenir ve sorulur; geriye dönük doğrulama kurtarmaz | Kullanıcı kararı — "araştırma yapılmadan uydurma; büyük geri dönüş/token israfı; dürüst olunup bilgi verilecek, kesin agent kuralı" |
| 2026-09-13 | **Kural 17 — iki-katmanlı sayfa yapısı (kesin):** her yeni/güncellenen view'da kök tek `Border` (tema-resimli `Image` + ~%85-90 opak tint) + içerik kartları ayrı `Border` (`CardBackgroundFillColorDefaultBrush` + `ControlCornerRadius` + `CardStrokeColorDefaultBrush` + `ThemeShadow` + Padding 16, kart arası 12px); sadece Fluent ThemeResource, hardcode hex yasak; kök Grid+Background ve >2 seviye iç içe kart yasak; uymayan sayfa önce taşınır | Kullanıcı kararı — "Bu kuralları her yeni view/sayfa oluştururken veya mevcut olanı güncellerken uygula ... önce bu yapıya taşı, sonra içeriği ekle" |
| 2026-09-13 | **Denetim Masası FirmaShell'den ayrılıp ayrı modal pencere (Oturum 244):** FirmaShell içi `Seçim\|Ayarlar` toggle + gömülü Denetim Masası kaldırıldı; `DenetimMasasiView` bağımsız `Page` olarak `Views/DenetimMasasi/`'ya taşındı (30 dosya) ve `DetailsWindow` modal penceresinde açılıyor; kayıtlı Page/VM olduğu için her yerden `CreateNewViewAsync<DenetimMasasiViewModel>` ile çağrılabilir | Kullanıcı kararı — "Denetim Masasını Firmashell'den ayır. esnek ve yönetilebilir bir yapı istiyorum. ayrı bir pencere aç" (QuickBooks Preferences / Sage Settings ayrı pencere sektör deseni) |
| 2026-09-13 | **Kural 18 — canlı testi agent yapar, kanıt kullanıcı onayına sunulur (kesin):** biten view/özellik için agent uygulamayı açıp akışı kendisi test eder; ekran görüntüsü + UIA + referans açıklaması kanıtı `LOG`'a işlenir; kullanıcı yalnızca onay/ret verir. "Canlı test kullanıcıda" diyerek kapatma yasak; kanıt/onay yoksa build 0/0 olsa bile faz kapanmaz; ortam açamıyorsa agent "canlı test yapılamadı + neden" der | Kullanıcı kararı — "benim bilmediğim, görmediğim tasarımı ben nasıl canlı test edebilirim" |
| 2026-09-13 | **Kural 19 — referans defteri `docs/REFERANSLAR.md` (kesin):** internetten/sektörden alınan her tasarım/akış referansı (kaynak + bulgu) uygulanmadan önce deftere, uygulandığı view/özellikle yazılır; uygulama bitince `Durum` güncellenir. Kayıtsız referansla tasarım uygulanmaz; aynı yer için tekrar araştırma yapılmaz; çelişkide kullanıcıya sorulur | Kullanıcı kararı — "hangi oturumda hangi tasarım referans alınmış, nereye uygulanmış nereden bileceğiz; sonraki oturumlar/agentlar aynı işi tekrar yapmasın; stabiliteyi koruyalım" |
| 2026-09-13 | **Kural 17 Mica istisnası — Denetim Masası (Oturum 249):** Windows Ayarlar görünümü isteyen ayar pencerelerinde tema-resimli opak zemin yerine pencere Mica'sı (`DetailsWindow` `MicaBackdrop Base`) görünür; kök yine tek `Border` (transparent). Diğer view'larda Kural 17 resim zemini geçerli | Kullanıcı kararı — "denetim masası görünümünü Windows ayarlar görünümü gibi olsun" + MS "opak arka plan Mica'yı kapatır" kuralı |
| 2026-09-13 | **Ayarlar kartı = resmi `CommunityToolkit.WinUI.Controls.SettingsControls`; Denetim Masası içerik+metin komple yeniden tasarım (Oturum 249):** `DevWinUI 10.4.1` net10 olduğu için `net8` projede çalışmıyordu → `DevWinUI 10.0.0` (net8) + resmi Toolkit `SettingsCard` (8.2.251219) kullanılır; **sahte ayar** (tüketicisiz, "ayrı fazda" notlu) ve **"Kaydedildi" metni** yasak; içerik/başlıklar sade dille yeniden yazılır | Kullanıcı kararı — "settingsCard communityToolkit içinde de var, daha stabil ise onu kullan" + "içerikleri yazıları komple yeniden tasarla" + "sahte ayar, kaydedildi text gibi saçma sapan işlemler istemiyorum" |
| 2026-09-13 | **Veritabanı ayarları tek view (Oturum 249 devam 2):** Denetim Masası'nda `Veritabanı → Mali Dönem Veritabanları → Yedek Saklama` 3-seviye yerine **tek `Veritabanı` sayfası + iki `SettingsExpander`** (`Sistem Veritabanı` + `Mali Dönem Veritabanları`); ayrıca **tema sahte→gerçek** (`ThemeSelectorService` `AppSettingsChangedEvent` aboneliği ile canlı + açılışta `AppBackgroundRequestedTheme`); `TextFillColorTertiaryColor`/`ControlFillColorSecondaryColor` geçersiz token fix | Kullanıcı kararı — "veritabanı ayarlarını tek view içinde Sistem ve dönem olarak göstersek daha mı iyi olur" + MS SettingsExpander deseni |
| 2026-09-13 | **Denetim Masası Giriş sayfası = kart kapsız `DevWinUI RichButton` durum satırı + "Kullanıcıya Ait Firmalar" expander listesi (Oturum 251):** "ayar içinde ayar" olmaz (Ayarlar bölümleri navbar'da, sayfada çoğaltılmaz); arama pane'de kalır; hesap kartı pane üstünde (foto/avatar destekli) + **rol** gösterilir; üst öğeler kart kabı olmadan RichButton (Win11 "Windows Update" deseni) + oransal yerleşim; Sistem.db analizi ve güncelleme denetimi **busy ring'li**, güncelleme **her Denetim Masası girişinde** 20 sn tavanla taranır (UI kilitlenmez) | Kullanıcı kararı — "ayarlar içinde ayarların ne işi var", "arama çubuğunu geri navbar'a koy", "güncelleme için DevWinUI RichButton kullan", "giriş paneli için relative yapı kullan", "Yerel hesap yerine kullanıcı bilgisinden Rol yaz", "her denetim masasına girişte arama yapsın ama uygulamayı kilitlemeden ring/progress ile" |
| 2026-09-13 | **Kural 17 hairline + tek görsel/tema perdesi (Oturum 251 devam):** ana border kenarı `MuhasibAnaBorderCizgiBrush` (Light `#E6DFE1E4` / Dark `#45C9CCD1` — beyaz deneme "patlıyor" geri bildirimiyle yumuşak griye çekildi; 1px + `Translation Z 32`); zemin **tek görsel `light.jpg` + tema perdesi** `MuhasibZeminPerdeBrush` (Light `#59F3F3F3` ≈%35 / Dark `#D9202020` ≈%85) — iki ayrı zemin görselinden stabil; pilot `Splash`+`Login` canlı onaylı ("budur") | Kullanıcı kararı — "ana border dark'ta açık gri light'ta beyaz olsa daha mı ön planda", "light resmin üzerine dark perde daha stabil olmaz mı", "biraz daha griye", "beyaz patlıyor gibi" |
| 2026-09-13 | **Dynamics 365 tasarımına geçiş İPTAL (Oturum 251 kapanış):** "bir sonraki oturumda 365 tasarımına geçeceğiz" notu geçersiz; **gri yön yok** — "gri yapmayacağız, mevcut kartlarımızı kullanacağız"; ana border içinde mevcut kart yapısı uyumlu kabul edildi | Kullanıcı kararı — "log kaydına ekle ... 365 tasarımına geçecegiz => iptal" + "zaten uyumlu ana border içinde kart" |
| 2026-09-14 | **Transfer tespiti makine-öncelikli (Oturum 252):** makine farklı → gerçek transfer (uyarı + geri-yükleme amber hükmü); makine aynı → kurulum kimliği yenilenmiş = **kimlik kaybı**, sessizce onarılır (tek damga kimliği geri alınır, karışık damga güncel kimliğe eşitlenir). Uyarı dialogu yalnız DEBUG; release'de bilgi bloklayan geri-yükleme hükmünde | Kanıt: `LocalSettings.json` silinmesi yeni kimlik üretti, aynı makinede sahte "taşınmış veri" alarmı çıktı; kullanıcı "bloklamıyorsa anlamı yok" dedi |
| 2026-09-14 | **Dialog teması tek kapıda uygulama temasından yazılır:** `DialogHelper.ApplyAppTheme` (+ `DialogService.CreateDialog`); ContentDialog popup katmanı kök eleman temasını izlemediği için açık temada dark dialog açıyordu | Kullanıcı tespiti — "transfer dialog açık temada bile dark gösteriyor"; AGENTS "XAML'de RequestedTheme hardcode yasak, dialog uygulama temasını izler" ilkesinin kodla garantisi |
| 2026-09-14 | **Tek örnek koruması:** named Mutex + "zaten açık" uyarısı + mevcut pencereyi öne getirme; uygulama aynı anda tek pencere | Kullanıcı isteği — "uygulamanın çift açılmasını da engelle; zaten açık uyarısı verebilirsin" |
| 2026-09-14 | **Dev-mode = derleme zamanı DEV yeteneği (kullanıcı ayarı DEĞİL):** DEBUG'da `IDevModeProvider`; yöneticinin bile yapamadığı işlemleri açmak için; kapsam: kimlik/transfer + teşhis araçları; restore/göç zorlama ve silme/saklama kilidi bypass'ı kapsam DIŞI; her aksiyon `DEV` log damgalı | Kullanıcı kararı — "dev-mode özelliği koyalım mı, yöneticinin bile yapamayacağı işlemleri açmak için" |
| 2026-09-14 | **🔒 ANA TASARIM DİLİ MÜHÜRLENDİ (Oturum 254):** `zemin → ana border (ana kart) → içerik kartları` mimarisi kilitli; bundan sonra oluşturulacak/güncellenecek **her View, UserControl ve Dialog** bu yapı üzerine inşa edilir. Görünüm işlerinde kapsam yalnızca: (a) View içindeki kontrol yerleşimleri, (b) araştırma sonucu eklenen/çıkarılan buton-kontroller. **Ana kart (Katman 2) ve iç kart (Katman 3) stilleri — renk/radius/gölge/padding/hairline/kart arası boşluk — mührün parçası; habersiz değiştirilmez** (yalnız kullanıcı kararıyla mühür revizyonu) | Kullanıcı kararı — "Ana tasarım dilimizi mühürle. Bundan sonra oluşturulacak View, usercontrol vs. görünümler bu mimari üzerine inşa edilecek. Ana kart ve içindeki kart stili tarzını diyorum... Görünüm tarafında bundan sonraki işlemler sadece View içindeki controllerin yerleri ve araştırma sonucu eklenecek/çıkarılacak butonlar vs. olacak" |
| 2026-09-14 | **Faz 6.83 açıldı — TenantDatabaseUpdateView canlı doğrulama turu:** 6.80 kapsamında Katman-2 dönüşümü yapıldı ama göç bekleyen dönem olmadığından canlı açılamadı; ayrı fazda kontrollü test dönemi (şema damgası geri + test sonrası geri alma) ile Kural 18 kanıtları alınacak | Kullanıcı kararı — "TenantDatabaseUpdateView için ayrı faz aç, unutmayalım" |
| 2026-09-14 | **Mühür revizyonu talebi (Faz 6.84 — FirmaShell seçim deneyimi):** mali dönem `RadioButton` kart stili kaldırılıp **seçimli liste/accordion** (accent seçim işareti) + firma seçimi **seçici buton + Flyout** (arama + "Firma bulunamadı") + seçili firma kartı; CTA solunda firma•dönem özeti; ink siyah hap ve alias renkleri → Fluent accent/system sözleşmesi | Kullanıcı talebi — "radiobutton stilini beğenmiyorum / text-icon renkleri tasarımla uyumlu değil / butonun solunda seçim belirtilsin / firma kartı butonları konforlu olsun"; MS kılavuzu ("bağlama göre değişen seçeneklerde liste kullan") + sektör (Sage/Tally) araştırması |
| 2026-09-14 | **Kullanıcı Yönetimi ayrı faz (6.85) + MODAL pencere:** FirmaShell/Ayarlar içine gömülmez; kendi tasarımı kendi yüzeyinde. Kullanıcıya has işlemler (Hesabım: bilgiler + şifre) ile admin kullanıcı yönetimi (liste/oluştur/düzenle/aktif/şifre/sil) **birbirine karışmaz**; UserInfo menüsü iki grup; sahte madde yok | Kullanıcı kararı — "kullanıcı yönetim kartı ayrı olsun, tasarımlar iç içe geçmesin", "modal olarak açılsın"; QB/MYOB sektör deseni (yalnız admin yönetir; rol yönetimi ayrı yüzey) |
| 2026-09-14 | **Görünür ad "Ayarlar":** Denetim Masası penceresi/butonu kullanıcıya **"Ayarlar"** olarak görünür (kod sınıf/dosya adları `DenetimMasasi*` kalır) | Kullanıcı kararı — "Denetim Masası adı ayarlar olarak geçsin, kod tarafı önemli değil, kullanıcı Ayarlar olarak görsün" |
| 2026-09-14 | **FirmaShellView çekirdek yapısı modüler dashboard gibi çalışacak:** Firma seçici / dönem listesi / güncelleme InfoBar'ı / CTA özeti ayrı modüler bloklar; header dashboard başlığına ayrılır | Kullanıcı kararı (Oturum 256 kapanış) — "modüler tasarım olarak yaptığımız için FirmaShellView çekirdek yapısının dashboard gibi çalışacak" |
| 2026-09-14 | **Mühür revizyonu — Light hairline beyaza + Login form sol hizası (Oturum 258):** `MuhasibAnaBorderCizgiColor` Light `#E6DFE1E4`→`#FFFFFFFF`; Dark `#45C9CCD1` aynen (tema sözlükleri ayrı, Dark bozulmaz; orta renge gerek yok). Login: `NamePasswordControl` `Center→Stretch` (`MaxWidth 380` korunur; logo/adım barına girinti yok) — build 0/0 + test 483/483 | Kullanıcı kararı — "dark aynı kalacak, sadece açık temada çizgi beyaza dönsün" + "logo/adım barı dahil form başlangıç hizası" (netleştirme: logo bloğu sabit, form sola yaslanır) |
| 2026-09-15 | **Dönem seçim stili: mavi tam-çerçeve kalktı — sol hub + dönen kuyruk (Oturum 267):** seçilince accent tam-çerçeve yerine nötr hairline + sol dikey hub (3px, mevcut ölçü) + accent kuyruk hub'dan çıkıp kartı saat yönünde dolanıp hub'da dinlenir (1.8s süpürme + 1.6s bekleme, yalnız seçili kartta); firma listesi eski stilde (kapsam dönem listesi) | Kullanıcı talebi — "mavi border thickness sevmiyorum; orjinal stildeki soldaki dikey kutuyu aynı boyut ve renkte kullanalım; hub'dan çıkıp etrafında dönüp yine hub'da dursun"; araştırma (MS #7432 stok gösterge + EventTrigger + StrokeDashOffset bağımlı animasyon + Composition alternatifi) REFERANSLAR'da; kullanıcı kararıyla Storyboard yolu seçildi |
| 2026-09-15 | **Seçim animasyonu iptal edildi; seçili kart ana kart rengi (içeride) + ana border 1.5px (Oturum 268):** orbit ve periyodik nabız denemeleri kaldırıldı (kullanıcı "yapay" buldu) ve **animasyon sınıfları silindi**; seçili dönem/firma satırı **ana kart rengini** alır (`CardBackgroundFillColorSecondaryBrush` → panel üstüne binince içeride/çukur), seçimsiz saydam, seçim yalnız sol accent hub; seçili firma kartı da aynı; hover iki temada görünür (tema-farkında `MuhasibHoverOverlayBrush`); **ana border `1→1.5` (tüm Katman-2 view'ler, mühür revizyonu)** | Kullanıcı kararı — "animasyon iyi olmadı, yapmak için yapılmış gibi; seçili kart ana kartın rengini alacak ki içerideymiş gibi duracak; seçili firma kartına da uygula; animasyon sınıflarını sil"; MS Motion (kalıcı hareketten kaçın) |
| 2026-09-15 | **Faz 6.84 kapandı — kullanıcı onayı ("gayet başarılı"):** FirmaShell seçim deneyimi tamamlandı (seçili kart ana kart rengi = içeride; sol accent hub; hover tema-farkında token; ana border 1.5px; animasyon yok). 6.82 dev-mode başladı. | 6.84 kapıları (build 0/0 + 492/492 + canlı Dark/Light + onay) tamamlandı; kullanıcı açık işleri sayısal sırayla (6.82→6.83→6.85) tamamlamayı istedi |
| 2026-09-15 | **Faz 6.82 kapandı — Dev-mode (yalnız DEBUG) tamamlandı:** `IDevModeProvider` derleme kapısı + `IDevAraclariService` (kimlik onarım/sıfırlama + transfer taraması + şema damgası) + `ILogSeviyesiYoneticisi` (çalışma-zamanı log eşiği) + `IYolAciciService` (klasör açma) + Denetim Masası "Geliştirici Araçları" bölümü (yalnız DEBUG; onaylı aksiyonlar; `DEV` damgalı log; Kural 13 yardım). Kapsam dışı (restore/göç/silme bypass) uygulanmadı. Mimari bekçi dersi: Installation modülü DatabaseServices'e yaslanamaz → dev araçları `SistemServices/DevServices` altına taşındı. | Kullanıcı kararı — açık işleri sayısal sırayla (6.82→6.83→6.85) tamamla; sınıf planı "tamamını onayla"; Kural 14 araştırması + Kural 18 canlı kanıt + 498/498 test |
| 2026-09-15 | **Faz 6.83 kısmi + Faz 6.87 açıldı (veritabanı güncelleme sayfası komple redesign):** kontrollü test dönemi (`db-TEST_2099`, eski şema) ile `TenantDatabaseUpdateView` açılıp doğrulandı (1.0.0→1.1.0, 1 göç, Yedek→Göç→Doğrulama); kullanıcı ekranı görünce **komple yeniden tasarım** istedi → 6.87; test dönemi temizlendi | Kullanıcı kararı — "güncelleme sayfası komple yeniden tasarlanacak" |
| 2026-09-15 | **Faz 6.86 açıldı (StatusBarService modernizasyonu):** mevcut `StatusBarService`/`StatusMessageService` (440 satır) + `ShellStatusBar` incelendi; hardcode hex renk, `Default` tema bug'ı, ölü üyeler ve eksik erişilebilirlik tespit edildi; yön (InfoBar / mevcut barı modernize / hibrit) bekliyor | Kullanıcı kararı — "StatusbarService incelenecek, geliştirilecek, daha modern bir duruma getirilecek" |
| 2026-09-15 | **Yardım `?` butonu stili: tam opak (arkayı göstermez) + accent'siz border (Oturum 269):** `CircleIconButtonStyle` (tüm `?` butonları) → Normal `SolidBackgroundFillColorBaseBrush`, hover `SolidBackgroundFillColorTertiaryBrush`, pressed `SolidBackgroundFillColorSecondaryBrush` (**tam opak** — üstüne bindiği ana border 1.5px çizgisi artık görünmez) + border `ControlStrokeColorDefaultBrush` (hover `ControlStrokeColorSecondaryBrush`), accent kaldırıldı. Elenen denemeler: `CardBackgroundFillColorDefaultBrush`, `Transparent`, `ControlFillColor*` (yarı saydam → arkayı gösteriyordu). Canlı kanıt `ot288_yardim.png` + `ot288_login.png` | Kullanıcı isteği — "butonun arkasından geçen border net görünüyor; butonun arkası görünmesin" |
| 2026-09-15 | **`CircleIconButtonStyle` dark tonu düzeltildi (Oturum 270):** 269'un opak `SolidBackgroundFillColorBaseBrush`'ı Dark'ta `#202020` → buton "siyah nokta" gibiydi. Butona özel tema-farkında opak token (`MuhasibCircleIconArkaplan*`): Light aynen (`#F3F3F3`/`#F9F9F9`/`#EEEEEE`), **Dark `#333A3E` / `#3F464B` / `#2A3033`** (ana border yüzeyi ≈`#333A3E` ile uyumlu, göze batmaz); opaklık korunur (arkadaki 1.5px çizgi görünmez). `DesignTokens.xaml` + `Buttons.xaml`. 0 hata/0 uyarı; canlı Dark kanıt `ot289e`/`ot289f`/`ot289g` | Kullanıcı isteği — "dark mode için çok siyah oldu; tema rengiyle uyumlu olsun, göze de çok batmasın" |
| 2026-09-15 | **Faz 6.86 genişletildi — Durum çubuğu + StatusMessage + Notification komple redesign + refactor (Oturum 270):** status bar **kalır** ama Fluent'e modernize (sıfırdan, "yakışıklı"/sade); **olay/state mesajları InfoBar**'a; OS toast arka plan bildirimi; yön **Hibrit**. `StatusMessageService` (440→bölünür) + `StatusBarService` (ölü üyeler + DB bool bug'ı) + `NotificationService` (tip-ikon) refactor; `ShellStatusBar` sıfırdan; hardcode hex/`Colors.*` → `{ThemeResource}` severity. `StatusMessageService` (uygulama-içi alt bar) ile `NotificationService` (OS toast) **ayrı sorunluluklarda ayrı kalır**. Eski 6.86 + 6.71/m.4 + 6.72 Dalga 3 tek hatta. Kaynak: `REFERANSLAR` Oturum 270 (MS modern WinUI 3 + WPF→WinUI3 StatusBar→InfoBar + InfoBar/Progress/VS Code) | Kullanıcı kararı — "statusbar'ı Fluent'e modernize et + olay-mesajlarını InfoBar'a ayır … progress + bildirim düzeni + statusbar birleşsin … Statusbar yakışıklı olsun … mevcut statusbar tasarımını yokmuş gibi davran"; büyük iş → yeni context |
| 2026-09-15 | **Notification → In-App message; OS toast terk edildi (Oturum 270):** mevcut toast **legacy** `ToastNotificationManager`/`ToastContentBuilder` (`CommunityToolkit.WinUI.Notifications 7.1.2`) + elle AUMID registry/Start-Menu kısayolu hack'i kullanıyor. Karar: bildirim **In-App message (InfoBar tabanlı)** olur; **OS toast + CommunityToolkit paketi tamamen kaldırılır**. Gerekçe: sektör/MS "kullanıcı uygulamadayken in-app, dışındayken OS" ayrımı + "toast hafif iş için ağır" + legacy API. **Trade-off:** uygulama arka planda/minimize iken bildirim görünmez (kritik arka plan işleri ayrıca değerlendirilecek). **Çökme/başlatma bildirimi:** OS toast yerine **dialog** (shell/in-app host yokken de çalışan bağımsız yol) — OS toast yolu tamamen kalkar. | Kullanıcı kararı — "o zaman In-App message yapısına geçiyoruz ... CommunityToolkit Notification kullanıyor, kullanmayacaksan onu da sil" + "uygulama çökerse dialog ile bildir" |
| 2026-09-15 | **Güncel iş sırası:** **6.86 (aktif — durum/bildirim/progress redesign+refactor)** → **6.87** (update sayfası redesign); eski aktif fazlar (6.72 Dalga 1-3, 6.73, 6.74, 6.75 kalan, 6.76, 6.78 Adım 4) rafta | Kullanıcı kararı (Oturum 270) — 6.86 büyük iş, yeni context'te önce o |
| 2026-09-15 | **Faz 6.86 Chunk-1 tamamlandı (Oturum 271):** `IStatusMessageService`/`IStatusBarService` ölü üye temizliği; `StatusMessageService` (440) → `StatusMesajDurumu`+`MesajOtoGizleme`+`MesajYurutucu`+facade (yetim `StatusAutoHideMs` bağlandı, dispatch `App._dispatcherQueue`); DB bool bug'ı (`ShellViewModel` gerçek durumdan besliyor); `ShellStatusBar` sıfırdan Fluent footer (tema kaynaklı severity, hardcode 0); ölü converter'lar silindi. 0 hata + 498/498 + canlı Dark/Light (`ot290b_*`). **Karar bekliyor:** durum çubuğu ana pencerede görünmüyor çünkü ana akış `ShellView` kullanmıyor (`LoginViewModel.EnterApplication` → doğrudan `FirmaShellView`); "ana pencereye de bağla" / "DetailsWindow-only kalsın" kararı kullanıcıya bırakıldı ("şimdilik not düş, sonra karar"). Chunk-2: In-App message/InfoBar + OS toast/CommunityToolkit kaldırma + progress | Kullanıcı onayı (Chunk-1 sınıf planı "onayla") + Kural 18 canlı kanıt |

