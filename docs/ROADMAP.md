# MuhasibPro — Yol Haritası (Yeni Başlangıç)

## 1. Genel Bakış
MuhasibPro **sıfırdan değil, WebToXaml referansı ile** yeniden inşa ediliyor. `muhasibpro-WebToXaml` tek görsel kaynak — web `src/components/*.tsx` → WinUI3 XAML dönüşümü, doğrudan kopyada bug'lar düzeltilir. `MuhasibPro-master` 2026-08-28’de silindi.

```
Mimari (master ile aynı, alışılan yapı):
  Libraries/MuhasibPro.Domain    (Entity + Enum + Common)
  Libraries/MuhasibPro.Data      (DbContext + Repository + Database)
  Libraries/MuhasibPro.Business  (Contracts + Services + Validators)  ← UIContracts ayrı değil
  Libraries/MuhasibPro.ViewModels(Infrastructure + ViewModels)
  MuhasibPro                     (WinUI3 App, MuhasibPro namespace)
```

```
Veritabanı (iyileştirilmiş, AKIS-PLANI.md):
  %LocalAppData%/MuhasibPro/Databases/ (RELEASE) | Databases/ (DEBUG)
  ├── Sistem.db              → Kullanici, KullaniciRol, KullaniciFirmaRol, RolPermission, Firma, MaliDonem meta, Lisans, AuditLog
  ├── Tenants/{DatabaseName}.db → tenant (TenantDatabaseVersiyonlar: SemVer + kimlik damgası)
  └── Backups/{DatabaseName}_{zaman}_{guid}.backup
```

### Zorunlu kavramlar (korunan)
- `IFirmaWithMaliDonemSelectedService` seçim aynası + `IAppDbContextFactory` (tenant bağlantıları)
- Rol **Kullanici+Firma** (`KullaniciFirmaRol`) — `Kullanici.RolId` kaldırıldı
- Aksiyon bazlı `Permission` + `IPermissionService` + `IModuleLicenseService` + `ILisansService`
- `IDatabaseManagementService` ayrı yönetim penceresi (ileride bağımsız process’e taşınabilir)
- Yedek `VACUUM INTO`, backup-önce-migrate, SemVer şema sürümleri (`DbSchemaVersions`)

## 2. Öncelik — Modüler

### Aşama A — Çekirdek (önce app ayağa kalksın)
Sırasıyla: **Altyapı → GlobalDb migrate+seed → Login → Firma Seç → Mali Dönem Seç → MainShell + UpdateService birleşik (Velopack + SistemMigrationManager)**. Bu fazlar 0–6. **🔒 2026-08-25 ilkesi (LOG Oturum 25):** Çekirdek **en az kusurda** ve güncelleme altyapısı dahil tamamlanmadan **Faz B açılmayacak**.

### Aşama B — Muhasebe (app ayakta sonra, tek tek)
Her modül çekirdek üzerine bağımsız eklenir, öneri: **Cari (B1) → Stok (B2) → Fatura/İrsaliye (B3) → Kasa/Banka (B4) → Çek/Senet (B5) → Sipariş/Teklif (B6) → Personel (B7) → Rapor/Dönem Sonu (B8)**. Sıra tartışmaya açık.

## 3. Faz Tablosu (Yeni)

| Faz | Açıklama | Durum |
|---|---|---|
| 0 | Dokümantasyon seti (bu dosyalar) + `MuhasibPro-master` envanter + iskelet kopyalama + `MSBuild 18` 0 hata | ✅ Eklendi 🧪 Test edildi |
| 1 | Domain iyileştirme (Permission, DonemDurum, AuditLog, KullaniciFirmaRol, RolPermission, MaliDonem genişletme; Kullanici.RolId kaldır) — `WINUI-MIMARISI.md:4.1` IsMainWindow belgelendi | ✅ Eklendi 🧪 Test edildi |
| 2 | Data iyileştirme (GlobalDbContext 14 tablo, AccountingDbContext 69 entity, ITenantContext, IAccountingDbContextFactory, ApplicationPaths 4 provider, Repository Scoped, Migration) | ✅ Eklendi 🧪 Test edildi |
| 3 | Data yönetim (MaliDonemSagaManager, TenantSQLiteBackupManager, TenantLockManager, ModuleType) | ✅ Eklendi 🧪 Test edildi |
| 4 | Business iyileştirme (IPermissionService, ModuleLicenseService, Authentication/Firma/MaliDonem/Context, DI) | ✅ Eklendi 🧪 Test edildi |
| 5 | Entegrasyon (sln build 0/0 + FileLogger + SmokeTest) — `dotnet build` 0 hata, `tsc --noEmit` 0 hata, `vite build` 0 hata, `FileLoggerProvider`/`ModuleLicenseViewModel` entegre | ✅ Eklendi 🧪 Test edildi |
| 6 | WinUI App (WindowHelper/ServiceLocator/Navigation/DetailsWindow+ShellView, Login/FirmaShell/MainShell, Firma/MaliDonem/Kullanici yonetim — master ViewModel isimleriyle, *Yonetimi değil) + Web Migration (React/Vite/Tailwind 8 modül) | ✅ Eklendi 🧪 Test edildi |
| 6.5 | İlk Kurulum & Login İyileştirmeleri (SistemKurulumView gradient+glass, Login inline hata + CanLogin, Splash→Kurulum→Login akışı, AppDbVersion history onarımı) — görsel doğrulandı | ✅ Eklendi 🧪 Test edildi |
| 6.6 | Login Reveal (göz ikonu) + FirmaShell yeni firma görsel test + MaliDonemSaga `Migrate` fix + `NU1903` suppress ile **0 uyarı 0 hata** — UIAutomation ile `revealButton`/`Firma Bulunamadı`/`Yeni Firma` doğrulandı | ✅ Eklendi 🧪 Test edildi |
| 6.7 | Web temizliği — saf WinUI3: `src` 22 + `dist`/`node_modules` (127 MB) + `vite/tsconfig/package` + `.cr/.vs` kaldırıldı, `dotnet build` tek kaynak **0/0** — AI Studio’ya sadece WinUI3 görünüm paketi brief’i | ✅ Eklendi 🧪 Test edildi |
| 6.10 | Yeni viewpackage renk stili — Splash+Login açık tema (radial accent, Bloom elipsler, ThemeResource metinler, primary logo kartları, QuickLogin açık kart, TitleBar Light); fonksiyon korundu, sadece görünüm; Acrylic alınmadı (MSB3073); `ThemeSelectorService` fallback `Default`→`Light` (XAML'de tema yok) — `dotnet build` 0 hata + UIA/screenshot görsel doğrulandı | ✅ Eklendi 🧪 Test edildi |
| 6.12 | FirmaShellView yeni tasarım — viewpackage kart mimarisi: radial+Bloom zemin, header kullanıcı pill'i, sol/sağ `MuhasibCardStyle` kartlar (monogram header + sayaç/"Hazır" pill), dönem `AKTİF/KAPALI` pill'leri, yeşil Devam Et; tüm `x:Bind` korundu — `dotnet build` 0 hata (PRI263 hariç); canlı görsel test bekliyor | ✅ Eklendi 🧪 Test edildi |
| 6.13 | FirmaShellControls kart kontrolleri (`MaliDonemKart`/`FirmaKart`/`FirmaKartListesi` UserControl'leri — viewpackage'a sadık) + `TumunuYukle` veri yolu + repo projeksiyon düzeltmeleri + `DonemSecCommand`/`YeniDonemCommand`; canlı test: Login→kartlar→Yeni Firma saga→Yeni Dönem saga (guard✅ rollback✅) — **SAGA/LOG DI FIX 2026-08-25 ile çözüldü, E2E bekliyor** | ✅ Eklendi 🧪 Test edildi |
| 6.14 | **MaliDonem sadeleştirme** — `MaliDonemDetails` 523→216 (-59%) `MuhasibCardStyle` + viewpackage 04 kart mimarisi; `MaliDonemOlusturProgress`+`SilProgress` (236+236) → tek `MaliDonemProgressView` (97+88) birleştirme + `MaliDonemCard` (183) ölü kaldırıldı — **1178→403 (-66%)**, `dotnet build` 0 hata | ✅ Eklendi 🧪 Test edildi |
| 6.15 | **SistemKurulum webToWinui3 stil — Splash⇒Kurulum akışının ilk adımı** — Top Bar Card + `DiagnosticsMetricsGrid` 4 metrik + `DatabaseTablesList` 6 kart + `DiagnosticsTerminalLog` dark terminal → `SistemKurulumView` üzerine bizim VM (`StatusMessage/DbPath/Logs/TestSonuclari/Progress/PrimaryButtonMetni`) ile giydirildi — tüm `x:Bind` korundu, `dotnet build` 0 hata | ✅ Eklendi 🧪 Test edildi |
| 6.16 | **Login webToWinui3 stil — akışın ikinci adımı** — `BrandBannerControl` + `LoginView MaxWidth 960 3-kolon ThemeShadow 16` + `NamePasswordControl/QuickLoginPanel 8` + `Initial` → `LoginView` üzerine bizim VM (`Username/Password/CanLogin/ErrorMessage/QuickLogin/reveal/Hello`) ile giydirildi — `QuickLoginPanel` kart içine taşındı, `dotnet build` 0 hata | ✅ Eklendi 🧪 Test edildi |
| 6.17 | **FirmaShell webToWinui3 stil — akışın üçüncü adımı** — `FirmalarListControl` + `MaliDonemlerListControl` + `SagaPipelineDialog` → `FirmaShellView 5*|7* 1200` üzerine bizim VM (`FirmaList.Items/MaliDonemList.Items/SelectedFirma/HasSelection/DonemSec/YeniDonem/Delete/DevamEt`) ile giydirildi — radial→Solid, iç-içe→2-panel + fallback kart, `dotnet build` 0 hata | ✅ Eklendi 🧪 Test edildi |
| 6.18 | **MainShell webToWinui3 stil — akışın son adımı** — `TitleBarControl` + `NavSidebarControl 240` + `Breadcrumb 52 + Snapshot` → `MainShellView Solid + TitleBar + NavSidebar + Breadcrumb` üzerine bizim VM (`NavigationItems/NavigateTo/IsPaneOpen/ContentFrame`) ile giydirildi — `NavigationView Collapsed`, `dotnet build` 0 hata | ✅ Eklendi 🧪 Test edildi |
| 6.19 | **UpdateService birleşik + Splash ince ayar — çekirdeğin son kilidi** — `UpdateService IUpdateService Scoped (LocalSettings + SistemMigrationManager + Velopack stub) + ExtendedSplash ShouldRedirectToSetupAsync` → `Splash ⇒ Kurulum ⇒ Login ⇒ FirmaShell ⇒ MainShell + UpdateService` çekirdek tamam, `dotnet build` **0 uyarı 0 hata** | ✅ Eklendi 🧪 Test edildi |
| 6.20 | **Business DB birim testleri + 5 bug fix** — `BusinessDatabaseTests.cs` 41 test (TenantHelperExtensions / TenantSQLiteSelectionService / TenantSQLiteDatabaseSelectedDetailService, Moq tabanlı, `Success=false+Data!=null` regresyonu dahil); fix: `&&`→`||` guard, ölü kod/`;`/ternary temizliği, `ToUpper`→`ToUpperInvariant` (TR kültür `İ` bug'ı) — `dotnet test` **62/62**, `dotnet build` 0 hata | ✅ Eklendi 🧪 Test edildi |
| 6.21 | **DbContext extensions çekirdek mühürleme + tenant entity sözleşmesi** — Analysis tek kaynak + `DatabaseAnalysisResult` dedupe; Operations migrate/backup/restore akışı (sahte rollback + sonsuz döngü fix, `TryWithRetryAsync`); Diagnostics wrapper; `TablesToCheck` ayrı sınıf (`TenantTablesToCheck`) + gerçek çoğul tablo adları; `ITenantMuhasebeEntities` (67 DbSet, Faz B'de AppDbContext implemente eder); `Ayarlar/BelgeNumara/VarsayilanDegerler` `[Key] KullaniciId`; `DataMigrationFlowTests` 8 test — `dotnet test` **73/73**, build 0 hata | ✅ Eklendi 🧪 Test edildi |
| 6.22 | **SistemKurulumView final rötüş + Login yönlendirme** — Status bar sabit (`CardBackground + alt çizgi`, Row 0), log sabit footer değil Scroll içinde, `Giriş Ekranına Git` `Visibility IsKurulumTamamlandi` ile Status'ta, İşlem Günlüğü `TextBox→TextBlock` hover kaldırıldı; `Splash→SistemKurulum→Login` akışı tamamlandı — `dotnet build` **0 Hata 2 Uyarı (NU1903)** | ✅ Eklendi 🧪 Test edildi |
| 6.23 | **DesignTokens Light+Dark + Splash kart + SistemKurulum sadeleştirme** — DesignTokens temiz: sadece Light+Dark dict, semantic renkler dict'lerde (ThemeResource fırçalar), `AppBackgroundBrush` 3-stop gradient (kullanıcı); Splash v2-tarzı ortalanmış gölgeli kart (ThemeShadow `Loaded`'da receiver — ctor'da pencere görünmez yapıyordu, fix); No-DB → `SetFirstSetupMode` "Veritabanı bulunamadı — kurulum gerekiyor" + tek tık; SistemKurulum ViewModel'inden güncelleme-migration kaldırıldı (sadece ilk kurulum; Diagnostics testleri korundu) — `dotnet build` 0 Hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.24 | **Görsel OOBE: yeni arkaplan + kartlar Cards.xaml + gölge + teşhis dialogu** — `AppBackgroundBrush` **ImageBrush→app_background.png** (kullanıcı görseli, 1. resim); çalışmayan 3 stil silindi (MuhasibCardElevatedStyle/GlassPanel/MuhasibAcrylicCardStyle); **kart stilleri tek kaynak Cards.xaml** (MuhasibCardStyle taşındı, DesignTokens artık `<Style>` içermiyor); Login/Splash `ModernCard`+`ThemeShadow`+`Receivers.Add` gölge; FirmaCard `ElevatedCard` bizim OOBE palete; teşhis `QuickSistemDbDiagDialog` → `MuhasibAcrylicBackgroundBrush` (ModernCard reddedildi) — `dotnet build` 0 Hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.25 | **Bildirim mimarisi: Toast SİSTEMİ kaldırıldı → OS Toast tek kanal (CommunityToolkit)** — ayrı pencere (ToastWindow/ToastOverlayWindow) + UserControl overlay (ToastHostControl) denemeleri sonrası kullanıcı "toast istemiyorum" dedi: `IToastService`/`ToastService`/`ToastHostControl`/converter'lar/DI silindi, 9 çağrı kaldırıldı; **ardından** `CommunityToolkit.WinUI.Notifications` 7.1.2 ile `INotificationService`/`NotificationService` (unpacked AUMID registry + Start Menu shortcut + `ToastNotificationManager.CreateToastNotifier(Aumid)`); aksiyon sonuçları OS toast, karar→Dialog, form→satır içi, durum→StatusBar. Build 0 Hata, test 73/73, smoke + REG OK + LNK True | ✅ Eklendi 🧪 Test edildi |
| 6.26 | **Tasarım stili ModernCard kesin komut + FirmaDetails modern iskelet:** `LoginView ModernCard` → `SistemKurulum` hero+log + `DatabaseInfoPanel`/`SystemTestsPanel` + `FirmaShell` 4 kart `ModernCard` (tek sarmal kaldırıldı `MaxWidth 1440`); header daraltma `24,24` + step `12` + firmaShell auto-fit `MaxHeight 560`; `FirmaDetails` modern OOBE `AppBackgroundBrush` + `AppTitleBar` 16 `BackButton` + iç border kaldırıldı + `FirmaCard` logo master’a döndü; `ShellView AppTitleBar` OOBE `32×32` + `AppTitleBarText 16` dinamik `WindowTitle` (`Yeni Firma`); `Helpers/DialogHelper` generik ortalı (sabit boyut yok, `WindowHelper.CurrentXamlRoot` garantili) + `FirmalarListControl` `IsMainWindow` yeni pencere (logo dahil). Build 0 Hata 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.27 | **FirmaShell seçim onarımı + OOBE yerleşim + arşiv:** uygulama çalıştırılıp teşhis — **iki dönem kartında da mavi rozet** (`SelectionMode="None"`+`model.Selected` desync); seçim `SelectionMode="Single"`+`SelectedItem TwoWay`+VSM'e alındı (tek seçim garantisi, tıklamayla 2026↔2027 taşıma doğrulandı); `FirmaRepository.GetFirmalarAsync` projeksiyonuna `MaliDonemler` (0 Dönem fix); `MaliDonemModel.TenantDetails` (skalar yerine DB-seçim modeli) + `TenantDetailsModel` Durum/ArsivlendiMi/DosyaBoyutu/SonYedekTarihi/ArsivliMi + `OnArsivleClick`; FirmalarList (KAYITLI FİRMALAR + `FirmaKodu` tile + düzenle) + MaliDonemler (seçili firma şeridi + 2-kol `ItemsWrapGrid` dönem grid + Yedek/Arşivle/Sil) image'e göre; durum pill `Aktif Seçim/AÇIK/KAPALI`. Build 0 Hata | ✅ Eklendi 🧪 Test edildi |
| 6.28 | **Kapat onayı fix + ortak ShellTitleBar refactor:** kapat X `window.Close()` → `AppWindow.Closing` tetiklemiyor (WinUI3, `AppWindow.Close()` yok) → onay dialogu atlanıyordu; `WindowHelper.RequestCloseAsync(Window)` tek kaynak (system X + özel X delege); **`Views/Components/ShellTitleBar`** UserControl (logo + Title/Subtitle DP + TrailingContent + min/max/close; `Loaded`'da SetTitleBar+gölge) — FirmaShellView + SistemKurulumView titlebar kopyası (SetupCustomTitleBar/UpdateMaximizeIcon/OnMin-Max-Close/ShowWindow/HeaderShadow/3 stil ölü kod) silindi. Build 0 Hata (kapat onayı kullanıcıda son doğrulama) | ✅ Eklendi 🧪 Test edildi |
| 6.29 | **MaliDonem kart vitrini gerçek zamanlı fix (Yedek→SonYedek + Boyut):** `OnBackupClick` yedek sonrası sadece bellek içi `TenantDetails.SonYedekTarihi` atayıp `RefreshAsync()` çağırıyordu → Global.db satırı hiç güncellenmiyordu (arşiv DB'ye yazıyordu, Yedek'te yoktu) ve yeni dönem akışı `DosyaBoyutu` satıra işlemiyordu → kart `-`. Fix: `OnBackupClick` → `SonYedekTarihi=now`+`DosyaBoyutu=BackupFileSizeBytes` → `IMaliDonemService.UpdateMaliDonemAsync` DB'ye yaz → refresh; `TenantSQLiteBackupManager` başarıda `BackupFileSizeBytes` (kaynak boyutu); `TenantSQLiteDatabaseService` `PersistTenantFileSizeAsync` (DB dosyası oluşunca satıra boyut). Kullanıcı kararı: kapsam SonYedek+Boyut, kaynak DB satırı. Build 0 Hata, test 73/73 (canlı görsel kullanıcıda) | ✅ Eklendi 🧪 Test edildi |
| 6.30 | **MaliDonem kart: hover/seçim fix + DB Durumu rozeti (analiz) + kart büyütme:** VSM `Pressed`/`PointerOverSelected`/`PressedSelected` (seçili kart üzerinde hover seçimi ezmiyor — Donem+Firma kart stili); kart `240→300` + `Padding 15`; bilgi satırları ile Yedek/Arşivle/Sil arasına ayraç; **"Tablo/Kayıt" → "DB Durumu" rozeti** (`MaliDonemModel DbAnaliz*` alanları, ölü `TabloKayitMetni` silindi) — `MaliDonemListViewModel` opsiyonel `ITenantSQLiteDatabaseService` ile dönem listesi yüklenirken her tenant DB `GetTenantDatabaseStateAsync` analizi (Güncel/Güncelleme Gerekli/DB Dosyası Yok/Kontrol Gerekli); `TenantSQLiteDatabaseLifecycleService` ters şart bugfix; ölü `OnDonemGirClick` silindi. Build 0 Hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.31 | **Dialog Light garantisi kodda + XAML RequestedTheme yasağı:** sistem Dark iken silme guard koyu açılıyordu — shared fırça + StaticResource app temasına çözülüyor, DialogHelper servis temasıyla Light'ı eziyor, 3 çağrı helper'ı bypass ediyordu. Fix: 8 XAML'den RequestedTheme silindi, 6 dialog ThemeResource'e geçti, DialogHelper/DialogService `ElementTheme.Light` tek kaynak, bypass'lar helper'a alındı. Canlı UIA (sistem Dark) DeleteGuard beyaz doğrulandı. Build 0 Hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.32 | **MaliDonem kart: boyut/Kurtar/Sil/arşiv turu:** "0 MB"→B/KB/MB/GB + backfill; dosya-yok karta banner + Kurtar/Sil + giriş engeli; yetim kayıt silme (saga korunur, doğrulama satır-düzeyi); `GetBackupsAsync` tam-yol desen fix; arşiv onaylı Kapat + "Arşivden Çıkar". Canlı: 2028 silindi, 2026 Kurtar→Güncel, 2027 arşiv turu. Build 0 Hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.33 | **Mali Dönem Yönetim penceresi + kart hafifletme:** firma kartı "Mali Dönem İşlemleri" → DetailsWindow (ComboBox dönem seçici + sayaçlı rail + kart-dışı spec-sheet + Yedekler/Arşivlenenler/Bilinmeyenler panelleri); shell kart Yedek-only. `IMaliDonemListHost` + 3 alt VM + `GetAllBackupsAsync`; ölü kod (RestoreBackupDialog, kart handler'ları, IsYonetimModu) silindi. Canlı doğrulandı. Build 0 Hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.34 | **Şablon Aşama 1 (Genel Bakış + bakım + derin analiz):** KPI + batch + dönem tablosu + 4 bakım kartı + derin analiz paneli | ✅ Eklendi 🧪 Test edildi |
| 6.35 | **Motor iskelet (Aşama A):** başlık + PRAGMA butonu + 2 sekme (Global DB yok) + 3 segmented + panel regroup; rail silindi; FirmaOzet + KPI açılış fix'leri | ✅ Eklendi 🧪 Test edildi |
| 6.36 | **Motor 3-sayfa tasarım (B/C/D):** hero + 4 KPI + toplu bant + 7 kolon tablo; banner + 5 kart (kilit yok) + parametre bandı; 2 skor kartı + dağılımlı tablo (ölçülemeyenler yok); BakimPanel/spec-sheet silindi — build/test Windows'ta yapılacak | ✅ Eklendi 🧪 Bekliyor |
| 6.37 | **Widget görsel dili temel + ölü stil temizliği:** Widget token/stil Light+Dark (tek kaynak) + hardcode 0 (Views) + Cards/Buttons/DataGrid/ToolBar/Icons ölü temizlik + 4 stil dosyası silindi — build 0 hata | ✅ Eklendi 🧪 Test edildi |
| 6.38 | **Recurra panel dili + Login giydirme:** opak panel kartları (radius 20, flat) + kart gruplama kuralı + Login 2-kolon; fonksiyon aynen — build 0/0 | ✅ Eklendi 🧪 Test edildi |
| 6.39 | **Recurra rollout + 3 açık madde fix:** FirmaShell/Yönetim/9 panel/haplar/WindowTitle x:Bind + hover token aralığı (094D8F/073A6C, Ghost PrimaryLight) + 8 tooltip (kök neden: Giriş'te tanımsızdı) + `_dbGate` concurrency serileştirme + VM ölü kod temizliği — build 0 hata, test 73/73, smoke OK | ✅ Eklendi 🧪 Test edildi |
| 6.40 | **Login tek logo + tooltip saga kapanışı + Yedekler Dönem içine:** BrandBanner silindi (sürüm footer); 6 probe → tooltip sağlam kanıtı (odakta açıldı), sentetik hover geçersiz; Ghost 1.5px hover halkası; `DonemYedeklerPanel` modern (başlık+sayaç+CTA+satırlar+boş-durum), 5→4 kart 2×2, Arşiv sayacı — build 0 hata, 73/73, Yedekle E2E (2→3 + toast) | ✅ Eklendi 🧪 Test edildi |
| 6.41 | **InventEase pilotu (Login):** EkranKaydi paleti piksel örnekleme (teal/lime/zemin/ink) + yeni token ailesi Light+Dark (eski mavi korunur) + **TEK** buzlu acrylic kap (sol+sağ kartlar üstte, sol saydam iç taşıyıcı) + kart-için-kart + siyah hap submit; lime/kapasite dashboard'a — build 0 hata, 73/73, canlı render OK | ✅ Eklendi 🧪 Test edildi |
| 6.42 | **InventEase rollout (ANA DİL):** mühür + stiller + Splash + SK + FirmaShell + Yönetim + MainShell + Controls/Dialog-içerik ✅ · dialog buton gizemi KAPANDI (Oturum 96) ✅ · FirmaDetails/FirmaCard ✅ · WindowTitle gradyan + segment VSM (Oturum 97) ✅ · fare-tooltip teyidi + legacy 6 dosya + süpürme (Dark/checkbox) ⬜ (Oturum 98 Login denemesi geri alındı) | 🔨 Aktif |
| 6.43 | **MaliDonemYönetim master-detail (EkranKaydi dili):** sekmeler kalktı → sol AÇIK/ARŞİVLİ seçim listesi + sağ 2-kolon konu kartları (hero/Sağlık/Yedek/Bakım/Analiz/Arşiv+Toplu/Parametre); `GenelBakisPanel` silindi, `AcikDonemler` VM prop'u; Grid pile-up fix — build 0 hata, canlı seçim+tooltip doğrulandı; **Oturum 108:** sol seçim görseli fix (`SecimiListeyeYenidenDuyur`) + banner+sağlık+hızlı-yedek TEK kartta birleşti (`DonemBannerPanel` silindi); **Oturum 109:** hızlı-yedek bölüm çıktı, Yedekle Sil yanına, DURUM/VERİTABANI/YEDEK şeridi + font büyütme — build/test Windows'ta | ✅ Eklendi 🧪 Test edildi |
| 6.44 | **Teal → mavi-uç petrol (isimlerle rename):** 32 dosya ~150 nokta (`MuhasibPetrol*/GhostTintPetrol`), Light `#0B5560/#073A42/#D2E6E9/#A3CBD0` (koyu revizyon) + Dark karşılığı, radio Checked + Ghost stilleri yeni değerlerde; kaynakta `Teal` 0 — build 0 hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.45 | **Zeytin ikinci vurgu + küçük buton görünürlüğü:** zeytin ailesi + `PetrolButtonStyle` (dolu) + `PetrolTintButtonStyle`; FirmaListControl İşlemler dolu petrol + Düzenle tint + N Dönem/sayaç zeytin — build 0 hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.46 | **Yedek listesi boş bug fix:** yazma çıplak ad / okuma `.db`'li desen uyuşmazlığı; `GetBackupsAsync` iki desen+dedupe+filtre, `ParseDatabaseName` kanonik ad; +Bilinmeyen bayat-tarama guard kaldırıldı — build 0 hata, test 73/73 | ✅ Eklendi 🧪 Test edildi |
| 6.47 | **Kimlikli yedek/geri-yükleme + kurulum kaydı:** A0 `IMakineKimligiProvider`+`IKurulumKayitService`+`IGlobalAyarlarService` + D1 TransferDialog + D2 KurulumKayitPanel + A entity+`20260908_AddTenantIdentity` + B damga (`Yönetici` enum, çok-admin serbest) + C kimlikli liste (`Kimliksiz (eski)` rozeti) + D3 `RestoreVerdictEvaluator` saf hüküm (eski `Warning`+auto-migrate / yeni `Block`) + `RestoreVerifyDialog` 6-haneli kod + tek kapı + birleşik kart separator/zeytin — build 0 hata, test 86/86 | ✅ Eklendi 🧪 Test edildi |
| 6.48 | **FirmaShellViewModel god-class split + güncelleme gerçek yapı:** `TenantSelection` + `TenantDatabaseUpdateCoordinator` (`ITenantDatabaseUpdateService`) + `TenantUpdateProgress` (orkestratör 465→183) + `TenantMigrationDescriber` (hardcoded "5 kolon" → gerçek migration yapısı) — build 0 hata, test 96/96 | ✅ Eklendi 🧪 Test edildi |
| 6.49 | **Uygulama güncelleme hattı:** manuel `FeedUrl` + `UpdateFeedSourceFactory` (github/feed ayrımı) + gerçek Velopack Check/Download + `UpdateView` (Pivot Güncelleme\|Ayarlar) — build 0 hata, test 102/102 | ✅ Eklendi 🧪 Test edildi |
| 6.50 | **DB güncelleme sayfası:** ön-dialog (Güncelle/Daha sonra/Vazgeç) + full sayfa (Yedek→Göç→Doğrulama→oto geri alma) + `ValidateAsync` — build 0 hata, test 111/111 | ✅ Eklendi 🧪 Test edildi |
| 6.51 | **Canlı doğrulama + revizyonlar:** bin/obj temizliği, sürüm-hash/notify/komut fixleri, "Şema Değişikliği" dili, firma/yıl + tür headline + secondary fix + sayfa redesign (yeni pencere) — gerçek göç DB kanıtlı, test 117/117; Continue anomalisi açık | ✅ Kısmi |
| 6.52 | **MaliDonemYonetimView canlı revizyon + pagination + Veritabanı Ayarları:** birleşik kart sadeleştirme (StackPanel 3×*), Güncelleme Gerekli ortala + Güncelle→TenantDatabaseUpdateView, header Korkut Mermer büyük taşıma, hamburger SplitView 900, 4 liste pagination (Acik 8/Arsiv 5/Yedek 4/Bilinmeyen 4), Bilinmeyen case-insensitive fix, Veritabanı Ayarları sayfası (FIFO 5 + Toggle'lar) + Display Otomatik Temizleme — kod eklendi, build Windows'ta teyit bekliyor | 🔨 Kısmi |
| 6.53 | **Ölü/tekrar sınıf süpürmesi + Oturum 120 build fix:** 14 dosya + 1 klasör silindi (ölü saga/VACUUM/lock/update + boş 3 repo + boş FirmaShell) + 7 birleştirme (GenerateDatabaseName tekleme, ölü ctor paramları ×3, SafeFileCopy yönlendirme, async void→Task, ölü backup metodu) + count-Where bug fix ×2 + `ShellArgs` namespace fix (WMC kaskadı dağıldı, build 0 hata bu ortamda) — build 0 hata, test 117/117 | ✅ Eklendi 🧪 Test edildi |
| 6.54 | **M1 AppPlatform + Kurulum/SystemDb ayrımı + mimari bekçi:** `SplashRoutingService/TenantVersionReader` (View EF'siz) + `AppPlatformSettings/Provider` + `ThemeSelector` model fallback + `IEventBus` + `Installation` modülü (4 dosya taşındı) + `ArchitectureTests` 5 bekçi (ihlal = derleme hatası) — build 0 hata, test 137/137 | ✅ Eklendi 🧪 Test edildi |
| 6.55 | **Faz 2 (M4 + M2-çekirdek) + ayar yetki kuralı + ortak-altyapı:** `Database/Identity/EntityRegistry/TenantSettings` + kilit/hash modelde + rename + `LegacyPbkdf2Verifier` + sayfa boyutu 4 VM + Backfill ortak sözleşmeye + rollback Sistem.db fix + bekçi genişletme — build 0 hata, test 148/148 | ✅ Eklendi 🧪 Test edildi |
| 6.56 | **Faz 3 (M3 EntityRegistry):** ayar genişletme + provider + `FirmaKodHelper` + `FirmaService` split (348→57/141/92/95) + `ITenantConnectionInfo` + `EntityEvents` birleşimi + 16 test — build 0 hata, test 180/180 (borcu Oturum 127'de kapandı; canlı kullanıcıda) | ✅ Eklendi 🧪 Test edildi |
| 6.57 | **Faz 4 (M5 Tenant):** `TenantSettings` genişletme + provider + E1 olay/yayın/2 abone + derin bağlantı (conn-string busy/pooling + bakım/cmd timeout + göç retry threading, `DefaultTimeout` ms→s fix) + 16 test — build 0 hata, test 180/180 (canlı kullanıcıda) | ✅ Eklendi 🧪 Test edildi |
| 6.58 | **Faz 5 ilk adım (M2-politika):** KFR/RP + Permission/ModuleLicense Scoped DI + PermissionService seçim/auth aynalarına + ölü `ITenantContext` silindi + 6 test — build 0 hata, test 186/186 (**Oturum 129 devamı:** LicenseSettings + KaydedenId + E2 yayınları + Login/DetailsWindow temizliği, test 199/199; **Oturum 130:** KaydedenId tek sabit `SeedYoneticiId`, test 201/201; **Oturum 131:** SemVer geçişi (SemanticVersion + harita, app 1.1.0), test 216/216; **Oturum 132:** Faz 5 kapanışı (Kullanici/Lisans servisleri + E2 abonelikleri), test 235/235 | ✅ Eklendi 🧪 Test edildi |
| 6.59 | **Faz 6 Mühür:** 0 uyarı süpürmesi (70→0) + AKIS/WINUI/ROADMAP güncelleme + abonelik matrisi (5 tipli olay) — build 0/0, test 235/235; canlı toplu E2E kullanıcıda | ✅ Eklendi 🧪 Test edildi |

## 4. Karar Logu (Yeni)

| Tarih | Karar | Gerekçe |
|---|---|---|
| 2026-08-21 | Master referans, kod iyileştirerek taşınacak — **2026-08-28’de silindi, yerini `muhasibpro-WebToXaml` aldı** | Kullanıcı kararı — alışılan yapı korunacak |
| 2026-08-21 | `MuhasibPro.Domain` korunacak (Core değil), `MuhasibPro` (App) korunacak (MuhasibPro.App değil) | Familiar namespace |
| 2026-08-21 | `UIContracts` ayrı proje yok — kontratlar `Business/Contracts/UIServices` içinde | Alışılan yapı, HATALAR.md Business UI bağımlılığı kabul |
| 2026-08-21 | ViewModel isimleri master gibi (`FirmaListViewModel`, `MaliDonemDetailsViewModel` vb., `*Yonetimi` değil) | Kullanıcı isteği |
| 2026-08-21 | Önce App (Aşama A), sonra muhasebe modülleri tek tek (Aşama B) | Token/modüler çalışma |
| 2026-08-21 | Eski proje komple temizlendi, `src/Libraries` → `Libraries` köke alındı, `MuhasibPro.slnx` → klasik `MuhasibPro.sln`’e dönülecek | Alışılan klasör yapısı |
| 2026-08-21 | `ServiceLocator` pencere-başına scope + `IContextService.IsMainView`/`ViewModelBase.IsMainWindow` dallanması (MainWindow ise `CreateNewViewAsync` ile DetailsWindow, değilse `Navigate`; `CanGoBack`/`IsButtonVisible` koşulları) `WINUI-MIMARISI.md:4.1`’de belgelendi — tekrar anlatım önlendi | Pencere yönetimi olmazsa olmaz, MainWindow’a göre buton/menu farkı |
| 2026-08-21 | `DbContext` Extensions iyileştirildi + `DbContextAnalysisExtensions.cs` merkezi analiz eklendi; `MigrationExtension` finalAnalysis bug düzeltildi (`Array.Empty` → `tablesToCheck` taze analiz); `Backup` haftalık oto yedek (`EnsureWeeklyBackupAsync` 7 gün); `ApplicationPaths` 433 satır god-class 4 provider’a bölündü + facade; `CalculateSimpleHash` overflow düzeltildi | Derin analiz — kullanıcı isteği, modüler ve test edilebilir yapı |
| 2026-08-23 | İlk Kurulum ekranı + LoginView iyileştirmeleri görsel doğrulandı (gradient+glass, True/False→Mevcut/Geçerli/Başarılı, inline hata, CanLogin, Splash→Kurulum→Login, EnsureCreated→Migrate history onarımı) | Kullanıcı dili, Migrate doğru yol |
| 2026-08-23 | Login Reveal (`E052↔E7B3`, `PasswordRevealMode`) + `FirmaShellView` `IsMainWindow→CreateNewViewAsync` canlı doğrulandı + `MaliDonemSagaManager` `Migrate` + `NU1903` `<NoWarn>` ile **0 uyarı 0 hata** | Göz ikonu kullanıcı talebi, saga tutarlılık, build 0/0 hedefi |
| 2026-08-23 | Web temizliği: `src` 22 + `dist`/`node_modules`/`vite`/`tsconfig` + `.cr/.vs` kaldırıldı, saf WinUI3 iskelet + `docs`/`logs` korundu, çekirdek akış için AI Studio’ya sadece WinUI3 paket | Harici dosya temizliği, tek tasarım kanalı |
| 2026-08-23 | Yeni `muhasibpro-new-viewpackage` renk stili mevcut fonksiyonel view'lara giydirildi (Splash+Login açık tema); **fonksiyon ↔ stil ayrımı**: binding/komutlar dokunulmadan sadece renk/görünüm değişti; Acrylic token'ları MSB3073 nedeniyle alınmadı — **2026-08-26’da paket `muhasibpro-new-viewpackage` kaldırıldı, stil kaynağı `muhasibpro-webToWinui3/MuhasibPro/Views`** | Kullanıcı kararı — aynı fonksiyonlar yeni renk stiline çevrilecek |
| 2026-08-24 | **GOD-CLASS YASAĞI genel kural (AGENTS.md Kurallar'da kesin dille):** hiçbir sınıf — ViewModel, Service, Manager, Control, View code-behind dahil — god-class olmayacak; tek sorumluluk zorunlu; kalıtım ile VM birleştirme yasak, composition kullanılacak; ihlal build hatası gibi ele alınır, ihlal edilen faz kapatılmaz. Örnekler: `ApplicationPaths`→4 provider, `LoginViewModel`→`QuickLoginAccountsViewModel`, `FirmaShellViewModel`→`FirmalarVM/MaliDonemVM` composition | Kullanıcı kararı — sınıf bazlı kararlar (O3/O14/O21) projeye genel kesin kurala yükseltildi; kod incelemesinde zorunlu madde |
| 2026-08-24 | **XAML MODÜLERLİĞİ + STİL TUTARLILIĞI (god-class yasağının XAML ayağı):** ezbere yüzlerce satır XAML yazılmayacak; tekrar eden/bağımsız sorumluluklu görsel bloklar esnekliği ve kod analizi hızını artırıyorsa UserControl'e bölünecek; tüm stil değerleri DesignTokens TEK KAYNAK'tan — istenen token yoksa ÖNCE DesignTokens'a eklenir, hardcode `#FF...` yasak (ThemeResource hariç) | Kullanıcı kararı — O18 static renk kararı + QuickLoginPanel/FirmalarList UserControl pratiği genel kurala bağlandı |
| 2026-08-25 | **🔒 ÇEKİRDEK KİLİDİ:** Çekirdek `Aşama A (Altyapı→GlobalDb→Login→FirmaShell→MaliDonem→MainShell + UpdateService/Velopack/SistemMigrationManager)` **en az kusurda** tamamlanmadan Faz B (Cari/Stok/...) açılmayacak; güncelleme almaya başlayan uygulama çekirdeği tamamlanmış sayılır, sonrası küçük fix | Kullanıcı kararı — `LOG Oturum 25` + `KONTROL 🔒` + `ROADMAP Aşama A` kilitlendi; Faz B öncesi 0 kusur hedefi |
| 2026-08-29 | **Db katmanı mühürleme + tenant entity sözleşmesi:** DbContext analizi tek kaynağa (`AnalyzeDatabaseCoreAsync<T>`) bağlandı, migration akışı gerçek `finalAnalysis` (bayat `DatabaseValid`/sahte rollback) ile düzeltildi; `tablesToCheck` ayrı sınıfa (`TenantTablesToCheck`) alındı; 69 muhasebe entity'si **interface sözleşmesine** (`ITenantMuhasebeEntities`) taşındı — Faz B açılınca `AppDbContext` implemente eder, migration o zaman üretilir (çekirdek AppDbContext'i artık şişmez, kaynak-israf yok) | Kullanıcı kararı — migration üretimi PK/ilişki belirsizlikleri yüzünden uzuyordu; hızlı/boşa gitmeyen çözüm |
| 2026-08-31 | **Toast kanalı pencere-scoped (`IToastService` Scoped) + MainWindow root Grid'de ToastHostControl:** MainWindow (Login/FirmaShell/MainShell) ile DetailsWindow (ShellView) aynı toast'u ikisinde birden göstermesin; her pencere kendi scope'unun bildirimini kendi üstünde gösterir | Kullanıcı kararı — "MainWindow'a da koyalım her zaman işe yarar"; duplike önlendi |
| 2026-08-31 | **⭐ Toast sistemi TAMAMEN kaldırıldı:** `IToastService`/`ToastService`/`ToastHostControl`/converter'lar/DI/`ICommonServices.ToastService`/`ViewModelBase.ToastService` silindi; 9 tüketicinin çağrısı kaldırıldı. Bildirimler artık `DialogService` + `StatusBar/StatusMessage` + satır içi feedback ile; QuickSistemDbDiagDialog Yedek Al → buton içi ProgressRing + `BackupStatusText`. `MainWindow.MainFrame` + `AnimatedInfoBorder` korundu | Kullanıcı kararı — "tüm toast'ları kaldır, Service de dahil, istemiyorum" |
| 2026-08-31 | **OS Toast tek kanal (CommunityToolkit.WinUI.Notifications 7.1.2):** kanal ayrımı — karar→Dialog, işlem sonucu→OS toast, form bağlamı→satır içi, global durum→StatusBar. `INotificationService` (Business) + `NotificationService` (App): unpacked AUMID registry + Start Menu shortcut + `ToastNotificationManager.CreateToastNotifier(Aumid)` (toolkit `RegisterAumidAndComServer<T>` COM activator gerektirdiği için ham desen). 5 aksiyon re-wire, satır içi istisnalar korundu | Kullanıcı kararı — "kendi içimizde yapamıyoruz, dialog eskidi, OS'a devredelim" |
| 2026-08-31 | **OOBE görsel dili `docs/OOBE-TASARIM-SABLONU.md`'ye alındı (tek kaynak):** `muhasibpro-WebToXaml` çekirdek ekranlar için referans DEĞİL — özellikle SistemKurulum dark/diagnostik tasarımına bakılmaz. Login/Splash/SistemKurulum deseni (AppBackgroundBrush, MuhasibCardStyle, MuhasibPrimaryButtonStyle, MuhasibOnPrimaryBrush, Loaded gölge, çok-panel düzen) şablonda; yeni ekran oraya göre yazılır | Kullanıcı kararı — "bu şablonu logla her zaman tekrar anlatmayayım, WebToXaml'a bakma" |
| 2026-08-31 | **Ana kartlarda `ModernCard` kesin komut:** `Styles/Cards.xaml:66` `ModernCard` (Login referans) bundan sonra tüm ana kartlarda TEK kaynak; `MuhasibCardStyle` sadece yardımcı iç kartlarda (metric pill) | Kullanıcı kararı — "loglarını tut. bundan sonraki tasarım stilimiz bu. kesin komut olarak yaz" |
| 2026-09-02 | **Dönem kartı vitrin verisi `MaliDonemModel`'e saçılmış skalar alan yerine DB-seçim modeli (`TenantDetailsModel TenantDetails`):** kaç/katman temiz; `GetMaliDonemlerWithFirmaId` (per-firma) korunacak, `GetUserTenantsForSelectionAsync`'ye geçilmeyecek | Kullanıcı yönlendirmesi — "saçılmış alan yerine TenantDetailsModel'i doldur, temiz olur"; soru → kullanıcı per-firma akışını seçti |
| 2026-09-02 | **Titlebar her view'da kopyalanmaz — ortak `ShellTitleBar`:** özel title-bar (logo+başlık+pencere kontrolleri) tek UserControl; FirmaShell/SistemKurulum `SetupCustomTitleBar`+min/max/close kopyası silindi, kapat onayı `WindowHelper.RequestCloseAsync` tek kaynak (özelleştirilmiş titlebar'da `Window.Close()` `AppWindow.Closing`'i tetiklemediği için onay atlanıyordu) | Kullanıcı isteği — "böyle her view içinde titlebar konulmaz, refactor yapalım" |

| 2026-09-02 | **Dönem kartında "DB Durumu" rozeti analiz sonucundan gösterilir** (`GetTenantDatabaseStateAsync` — `Güncel`/`Güncelleme Gerekli`/`DB Dosyası Yok`/`Kontrol Gerekli`); analiz `MaliDonemListViewModel` listeleme akışına bağlandı (opsiyonel servis, sadece FirmaShell paylaşımlı listede) | Kullanıcı isteği — "tablo/kayıt yerine analizden gelen Güncel/güncellenmesi gerekiyor bilgisi olsun"; `TabloKayitMetni` placeholder'ı yerine gerçek durum |

| 2026-09-03 | **XAML içinde tema zorlanamaz (`RequestedTheme` hardcode yasak — dialog dahil):** `ContentDialog` Light garantisi kodda tek kaynakta (`DialogHelper.ShowCenteredAsync` / `DialogService.CreateDialog` → `ElementTheme.Light`); dialog içerikleri tema fırçalarını `ThemeResource` ile izler | Kullanıcı kararı — "xaml kodları içinde tema rengi zorlanamaz, loglara ekle"; sistem Dark iken silme guard'ın koyu açılması bu kural ihlalinden geliyordu (StaticResource + servis overwrite + helper bypass) |

| 2026-09-03 | **Dosya-yok dönem kartı: Kurtar (yedek varsa) + Sil; arşiv onaylı Kapat + "Arşivden Çıkar":** dosya bulunamayınca kart girişe kapanır, Yedek/Arşivle gizlenir; kayıt silme saga korunarak yapılır (doğrulama satır-düzeyi) | Kullanıcı önerisi — "yedeği varsa Kurtar göster, yedek listesinden geri yükle; saga bozulmadan kayıt sil"; "Arşivle bu kadar kolay olmamalı, kapanmış kartta Arşivle yazmasın, arşivden çıkarma lazım" |

| 2026-09-04 | **Mali Dönem ayrı yönetim penceresi; kart hafif (sadece Yedek); sağ içerik kart değil spec-sheet; "Öksüz"→"Bilinmeyen":** tek karttaki sorumluluk firmanın yönetim penceresine taşındı (ComboBox dönem + rail bölümler + flat analiz) | Kullanıcı kararı — god-class/sorumluluk dağılımı; "tek sayfa düzeni gerek yok"; dosyasız dönem ⚠ ile listelenir |
| 2026-09-04 | **Motor 3-sayfa tasarım elenen işlevler tasarımda da yok:** kilit kartı düşer (5 kart), analizde kategori/son-yazma/I-O kartı/anomali raporu çıkarıldı, arşiv bayrak (dosya taşıma yok) — görsel 3 sayfa birebir bunlarla kurulur | Kullanıcı kararı — "kullanılmayacak dediklerimiz aynen geçerli, tasarımda da yer almayacak" |
| 2026-09-05 | **InventEase dili Login pilotu; global palet değişmedi:** EkranKaydi'ndan örneklenen teal/ink/sage yeni token ailesi (yalnız Login); mavi primary diğer ekranlarda korunur, pilot onayı sonrası rollout. Sol taşıyıcı acrylic buzlu anakart, beyaz iç kartlar üstte; lime/kapasite/sparkline dashboard'a bırakıldı (ölü token yasağı) | Kullanıcı kararı — "tasarım dilini al, fonksiyonellik değil"; kart-için-kart + acrylic görünürlük vurgusu |
| 2026-09-05 | **InventEase TÜM projenin ANA tasarım dili (MÜHÜR):** Splash'tan başlayarak tüm Page/UserControl/Dialog/Controls çevrilecek; eski stiller (Panel/Modern/MuhasibCard, mavi primary, hover/tooltip desenleri korunur) yeni dile taşınır; hardcode yasak. Recurra panel dili yürürlükten kalkar (geçiş bitene dek referans). | Kullanıcı kararı — "bu tasarımı ana tasarım olarak kabul et, mühürle, eski stilleri çevir" |
| 2026-09-06 | **ContentDialog kalıbı = QuickDialog + butonlar tek kaynak `Buttons.xaml`:** chrome (`CardBackgroundFillColorDefault + Card16` + setters-only ink/danger/info/secondary, `DefaultButton Primary` yasak) QuickDialog'da mühürlendi, 4 dialog kalıba geçirildi; sayfa `Button` yerelleri merkezi varyantlara taşındı (30→8 istisna) | Kullanıcı kararı — "bundan sonraki şablonlar Quick gibi olacak" + "butonlar tek yerden kontrol edilsin" |
| 2026-09-07 | **Vurgu rengi mavi-uç petrol, teal isimleriyle silindi:** `MuhasibTeal*` → `MuhasibPetrol*` (Light Base `#0B5560`/Deep `#073A42` — koyu revizyon, Dark Base `#6FB3BC`/Deep `#4E8A93`); küçük buton/ikon/metinlerde koyu uç kullanılır, kart/zemine dokunulmaz | Kullanıcı kararı — seçili firma kartı altı butonların yeşil-uç renginden nefret; "proje büyümeden isimlerle hallet" |

## 5. Referanslar
- `docs/OOBE-TASARIM-SABLONU.md` — Login/Splash/SistemKurulum görsel dili TEK kaynak (WebToXaml'a bakılmaz, 2026-08-31 kararı)
- `docs/AKIS-PLANI.md`, `docs/WINUI-MIMARISI.md`

