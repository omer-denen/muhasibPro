# MuhasibPro — Oturum Günlüğü — Cilt 7 (121-140)

> Önceki cilt: `LOG-101-120.md` (dolu, 20/20). Kitap indeksi: `docs/LOG.md`.

---

## 2026-09-08 — Oturum 121 (Ölü/tekrar sınıf süpürmesi + Oturum 120 build kırığı fix — ✅🧪)

### İstek (kullanıcı, 3 tur)
1. "Şu an tüm sınıfları inceliyorsun. Benzer sınıfları ya sil, ya da birleştir."
2. "Bu refactoring loguna kaydet: XAML içinde tasarım kolaylığı ve tutarlılığı açısından aynı işlemi yapacak tüm fonksiyonlar için ortak /Controls içine sınıflar oluşturulacak. Nerde neyin kullanıldığı loglara kaydedilecek." → `CEKIRDEK-MODUL-PLAN.md` kural 8 + Ortak Controls Kaydı tablosu.
3. Faz 1 (M1) yarım kaldı — yeni dosyalar (`AppPlatformSettings`, `ISplashRoutingService`, `IEventBus`, `ITenantVersionReader` contract) ağaçta duruyor, implementasyon + View sadeleştirme sonraki oturuma.

### Yöntem
- 3 paralel tarama (Business / Data / ViewModel+App) → 30+ aday → her biri `grep` caller + içerik okuma ile doğrulandı. 3 "ölü" iddiası YANLIŞ çıktı, korundu (aşağıda).

### SİLİNEN (14 dosya + 1 klasör, 0 caller kanıtlı)
- `Data/Managers/MaliDonemSagaManager.cs` + `Contracts/Managers/IMaliDonemSagaManager.cs` (ölü 5-adım saga; canlı hat Business `MaliDonemSagaStep+TenantDatabaseSagaStep`; kayıtsız, caller yok)
- `Data/Managers/TenantSQLiteBackupManager.cs` + `Contracts/Managers/ITenantSQLiteBackupManager.cs` (VACUUM yedekleyici; `BackupResultDto` ile; tek referans DI satırıydı)
- `Data/Managers/TenantLockManager.cs` + `Contracts/Managers/ITenantLockManager.cs` (kayıtsız, caller yok)
- `Data/Managers/LocalUpdateManager.cs` + `Contracts/Managers/ILocalUpdateManager.cs` (kayıtlı ama enjeksiyon yok) + DI satırı
- Boş repolar + contract'ları: `Lisans/AuditLog/GlobalAyarlar` (sadece ctor, DI'da bile kayıtlı değil)
- Boş `MuhasibPro/Views/FirmaShell/` klasörü
- Yan etki: `AddDbManagerHostBuilderExtensions` içinde `using Data.Managers` + `using Data.Contracts.Common` (namespace'i taşıyan silinen dosyalarmış) boşa düştü → temizlendi.

### BİRLEŞTİRİLEN (7)
- `Lifecycle.GenerateDatabaseName` silindi → tek kaynak `TenantHelperExtensions.GenerateDatabaseName` (testli); `Lifecycle` ctor'undan `_applicationPaths` düştü.
- `TenantDatabaseSagaStep` ölü ctor paramı (`tenantSQLieBackupManager`, typo dahil) → yerine `IDatabaseBackupManager` (kullanılıyor).
- `TenantDatabaseSagaStep.SafeFileCopyAsync` (zayıf kopya) silindi → `IDatabaseBackupManager.SafeFileCopyAsync` (kilit+tmp+doğrulamalı).
- `TenantSQLiteSelectionManager` ölü `dbContextFactory` paramı silindi (yorum "çıkarıldı" diyordu ama param duruyordu).
- `LogServiceExtensions` `async void→Task` (3 AppLog metodu; caller yok, Sistem* üçlüsüyle simetrik).
- `TenantBackupService.BackupCurrentTenantIfNeededAsync` silindi (hiç çağrılmıyor — SagaStep kendi safety-copy yapıyor); `_selectionService` bağımlılığı düştü.
- `BusinessCoreTests` çift-kayıt geçidi → tek-kayıt assert'ine çevrildi.

### BUG FIX (süpürmede yakalandı)
- `FirmaRepository:198` + `MaliDonemRepository:67,71` atananmamış `Where`/`Include` → filtreli sayımlar toplam dönüyordu. `HATALAR.md`'ye işlendi.

### Oturum 120 build kırığı (bu oturumda kapandı)
- `MaliDonemYonetimView.xaml.cs:147` `ShellArgs` yanlış namespace → tek satır fix → **bu ortamda da build 0 hata** (Oturum 120 "ortam kırık" teşhisi yanlışmış; WMC0001×19 + WMC9999 kaskadı dağıldı). `HATALAR.md`'ye işlendi.

### KORUNANLAR (yanlış alarmlar)
- `TenantDatabaseUpdateDialog` YAŞIYOR (`DialogService:135` new'liyor).
- `FirmaDetailsWithMaliDonemlerViewModel` + `Views/Firma/*` YAŞIYOR (`FirmaView` kullanıyor).
- `MaliDonemViewModel`/`FirmalarViewModel` YAŞIYOR (FirmaShell composition + `FirmalarView:47` navigasyon).
- `Permission/ModuleLicense` + KFR/RP repolar (bağlı ama DI'sız özellik — Faz 5 M2-politika'ya); log ikizleri + backup/migration aynaları (Faz 4'e, büyük generic-baz işi).

### Gözlem (davranış değişmedi)
- Canlı tenant bağlantılarında `journal_mode=WAL` set eden kod yok (tek yazan ölü saga idi); fabrika WAL parametresi kullanmıyor. Faz 4 `TenantSettings`'e alınacak.

### Doğrulama
- `dotnet build MuhasibPro.slnx` **0 hata** (bu ortam). `dotnet test` **117/117**.
- Uyarılar: hepsi eski aileler (CA1416/CS8625/xUnit1031/CS0618) — yeni dosyadan 0.

### Sonraki adım
- Faz 1 devamı (yarım): `SplashRoutingService + TenantVersionReader + AppPlatformSettingsProvider + EventBus` implementasyonları (contract'lar yazıldı) → `SplashNavigator` sadeleştirme → DI → ThemeSelector bağlama → build/test + canlı Splash→Login.
- Büyük birleştirmeler Faz 4'e: `BaseBackupOrchestrator` (Sistem/Tenant %80 klon), log generic base, migration orchestrator.

---

## 2026-09-08 — Oturum 122 (Faz 1 M1 + Kurulum/SystemDb ayrımı + mimari bekçi — ✅🧪)

### İstek (kullanıcı)
1. "Bağımlılık sırasına göre devam et." 2. "Uygulama kurulumu ile sistem.db yönetimini ayır." 3. "Modüler tasarımı bozacak her işlem derleme hatası olarak kabul edilecek. Loglara kaydedilsin."

### Uygulanan (Faz 1 kapanışı)
- `SplashRoutingService` (`DecideRouteAsync + CheckTransferAsync`, Backfill + `TransferDetectedEvent`, best-effort) + `TenantVersionReader` (Data, damga taraması) → `SplashNavigator` 135 satır EF'siz ince yönlendirici (`grep DbContext` 0).
- `AppPlatformSettings/Provider` (clamp + `AppSettingsChangedEvent`) + `ThemeSelector` fallback modele (davranış aynı).
- `IEventBus` (`Business/Services/CommonServices`, Singleton transport üstünde idempotent) + `DomainEvent` + `ModuleEvents` + 2 canlı yayınlı kayıt. Not: impl önce App'e yazıldı, test projesi App'e erişemediği için Business'a taşındı (katman kuralı kazandırdı).
- `IUpdateService` kaydı açıldı; `ISettingsService` kayıtsız (impl yok — notu eklendi).
- **Ayrım:** `Contracts/Installation` + `Services/Installation` (KurulumKayit/GlobalAyarlar 4 dosya taşındı, 7 tüketici using'i). Kurulum kimliği ↔ sistem.db yönetimi artık fiziksel ayrı.
- **Bekçi:** `ArchitectureTests.cs` 5 test (VM/View EF bilmez, Business WinUI bilmez, Data Business bilmez, Kurulum↔SistemDb ayrık) + `AGENTS.md` "kızaran test = derleme hatası" hükmü.
- Temizlik: `SistemLogListViewModel` ölü EFCore using'i (bekçi öncesi son ihlal).

### Doğrulama
- `dotnet build` **0 hata**, `dotnet test` **137/137** (+20: routing 8 + settings 3 + eventbus 4 + arch 5).
- Canlı (Splash→Kurulum→Login + transfer + tema) kullanıcıda.

### Sonraki adım
- Faz 2 (M4 + M2-çekirdek, paralel): `SystemDbSettings` genişletme + `IdentitySettings` + `Authetication` rename + PasswordHasher tekleme → Oturum 123'te kapandı (aşağıda).
- Büyük birleştirmeler Faz 4'e aynen (`BaseBackupOrchestrator`, log generic base, migration orchestrator).

---

## 2026-09-08 — Oturum 123 (Faz 2 + ayar yetki kuralı + Tenant/Sistem ortak-altyapı — ✅🧪)

### İstek (kullanıcı)
1. "Kaldığımız yerden devam" (Faz 2). 2. "Hardcoded limitler her modülün ayarlar sınıfından kullanıcı tarafından yönetilebilmeli." 3. "İlgili sınıfın modeli tarafından request edilen değer ayarlar sınıfına eklenecek; kritik işlemler admin, normal işlemler kullanıcı (result model fark etmez)." 4. "Tenant ↔ Sistem.db birbirine karışmayacak; ortak altyapıyla konuşacak." 5. "Faz bitince log kayıtları, oturumu kapatacağım."

### Uygulanan
- **M4:** `DatabaseSettingsModel` genişledi (SistemKeepLast/BusyTimeout/Journal/Synchronous/WeeklyBackupDays/VacuumOnBackup + clamp helper'lar); QuickDialog/ DonemYedekler/SettingsViewModel fallback'ları modele; `keepLast=10` default'u silindi (iki imzada).
- **M2:** `IdentitySettings` + provider + `AuthenticationService` kilit eşikleri modelden; `Authetication→Authentication` rename (klasör+namespace+2 using+test yolu); ölü `PasswordHasher.Hash` silindi → `LegacyPbkdf2Verifier`; `PasswordHasherOptions` model varsayılanından. `SessionTimeoutMin` alınmadı (tüketici yok — ölü prop yasağı).
- **Yetki kuralı:** `[YoneticiAyari]` + `AyarYetkiDenetimi` (rol: `KullaniciRolTip.Yönetici`; girişsiz = kullanıcı seviyesi) 3 Save yolunda (2 provider + SettingsViewModel→StatusError). Kritik: keep/limit/timeout/journal/kilit/iterasyon/yol; normal: sayfa boyutları/bildirim/hatırlatma.
- **Limitler:** `EntityRegistrySettings` (Acik 8/Arsiv 5) + `TenantSettings` (Yedek 4/Bilinmeyen 4/Backfill 100) + 4 VM + Backfill boyutu modele. Data protokol sabitleri (buffer/kilit/UID) kapsam dışı — mühendislik sabiti.
- **Ortak-altyapı:** Backfill `SistemDbContext` → `IMaliDonemService` sayfalama (ctor -2 bağımlılık); **rollback Sistem.db hedefi fix'i** (veri kaybı riski — HATALAR'da); bekçi +1 kural (Tenant↔Sistem). Bekçi ilk koşuda `MaliDonemSagaStep` UoW kullanımını yakaladı — kural rafine edildi (yasak: somut query/field, serbest: paylaşımlı `IUnitOfWork<T>`).
- **Singleton düzeltmesi:** `AppPlatformSettingsProvider` Scoped→Singleton.

### Doğrulama
- `dotnet build` **0 hata**, `dotnet test` **148/148** (+11).
- Canlı: tüm fazlar bitince toplu (kullanıcı kararı).

### Sonraki adım
- Faz 3 (M3 EntityRegistry): Oturum 124'te kapandı (aşağıda).

---

## 2026-09-09 — Oturum 124 (Faz 3 M3 EntityRegistry — ✅ kod, 🧪 Windows'ta)

### Uygulanan
- **Ayar:** `EntityRegistrySettings` genişledi (`FirmaKodPattern=F-XXXX`, `DefaultDurum=Acik`, `ValidationStrict=true` + mevcut sayfa boyutları; desen/strict `[YoneticiAyari]`, durum/sayfalar normal) + `GetFirmaKodPattern` fallback.
- **Sağlayıcı:** `IEntityRegistrySettingsProvider` + `EntityRegistrySettingsProvider` (M2 deseni: Get/Save + Clamp + `AyarYetkiDenetimi`) + DI Singleton.
- **Kod deseni:** `FirmaKodHelper` saf fonksiyonlar (`X`=rakam regex + `Generate` sıfır-dolgulu) — `FirmaKayitService.UpdateFirmaAsync` strict iken desene uymayan kodu reddeder (boş kod + gevşek mod dokunulmaz).
- **Durum default'u:** `MaliDonemService.CreateNewMaliDonemForFirmaAsync` yeni modele `TenantDetails.Durum`u sağlayıcıdan yazar (opsiyonel ctor paramı — mevcut kurulumlar kırılmaz).
- **Split:** `FirmaService` 348 → facade 57 + `FirmaListelemeService` 141 + `FirmaKayitService` 92 + `FirmaSilmeService` 95 (3 yeni contract + DI Singleton; `IFirmaService` imzası aynı — mock'layan testler etkilenmez). Projeksiyonlar `FirmaServiceExtensions`a taşındı (`ToHafifDonemListesi`/`ToDonemliFirmaModel`).
- **BUG FIX (taşımada yakalandı):** `GetFirmalarPageAsync` guard'ı `items == null && items.Count < 0` → `||` (null'da NRE, 6.20 ailesi). `HATALAR.md`'ye işlendi.
- **Seçim aynası:** `ITenantConnectionInfo` (Domain) + `TenantContext` implemente eder; `IFirmaWithMaliDonemSelectedService.ConnectedTenantDb` artık arayüz — M3 contract/impl'te `Data.DataContext` import'u 0 (`TenantDatabaseUpdateService:84` ataması + `VerifySet` testi derlenir).
- **Olay birleşimi:** `EntityEvents.FirmaChanged/MaliDonemChanged` (`= ItemChanged`, aynı hat) + kayıp `ItemRangesDeleted` sabiti; 13 dosyada 37 ham string → sabit (M3 siteleri semantik alias kullanır, davranış aynı).
- **VM sayfa boyutları:** zaten ayara bağlıydı (`GetXPageSize` + LocalSettings yükleme) — doğrulandı, kutu tick'lendi.
- **Test:** `EntityRegistrySettingsTests.cs` 16 test (8 fact: varsayılan/clamp/desen-fallback/bozuk-düzeltme/yetki×4 + 5 kod-desen + 3 kod-üretim).

### Doğrulama
- `dotnet` bu kum havuzunda çalışmıyor (`dotnet.exe` WSL interop `UtilBindVsockAnyPort` kırık — Oturum 120 ailesi): **build + test Windows'ta yapılacak**.
- Statik: 4 sınıf 150 altı (`wc`), ham-string `grep` yalnız sabit tanımlarında, `case` çakışması yok (alias'lar ayrı switch'lerde), bekçi-yasaklı string 0, nullable `annotations` modunda uyarı üretmez.

### Sonraki adım
- Windows'ta `dotnet build` 0 hata + `dotnet test` (164/164 bekleniyor: 148+16) + canlı Firma CRUD + kod-deseni reddi + yeni dönem default durumu.
- Faz 4 (M5 Tenant) — L3.

---

## 2026-09-09 — Oturum 125 (Faz 3 M3 statik doğrulama — ✅ statik temiz, 🧪 gerçek build/test hâlâ Windows'ta)

### Durum
- `dotnet` bu kum havuzunda hâlâ çalışmıyor (`dotnet`, `/mnt/c/Program Files/dotnet/dotnet.exe` symlink'i; WSL interop `UtilBindVsockAnyPort:309 socket failed 1` — doğrudan çağrı da aynı; Linux SDK yok). Oturum 120 ailesiyle aynı engel: **gerçek `dotnet build`/`dotnet test` Windows'ta yapılmaya devam ediyor**.

### Statik gözden geçirme (Oturum 124 çıktısı, 12 dosya + test)
- ✅ Namespace/using çözümlemesi: `DonemDurum` (`Domain.Enum`), `DatabaseType` (`Domain.Enum.DatabaseEnum`), `YoneticiAyari` (aynı namespace), `AyarYetkiDenetimi`/`ILocalSettingsService`/`IAuthenticationService` import'ları tamam.
- ✅ İmzalar: repo metotları (`GetFirmalarAsync`, `GetFirmaKeysUserIdAsync→IList`, `DeleteRangeAsync(params T[])`), extension'lar, response ctor'ları (`data/message/resultCount`), `GetCurrentUserId` property, log extension'ları (`source/action/message/description`) — çağrı siteleriyle uyumlu.
- ✅ Projeksiyon üyeleri entity+model'de mevcut (`Firma.MaliDonemler: ICollection<MaliDonem>`, `MaliDonem: BaseEntity`, `MaliDonemler: ICollection<MaliDonemModel>` — `ToList()` atanabilir).
- ✅ Facade 9/9 `IFirmaService` üyesini forward'lıyor; `IFirmaService` değişmedi; DI'da 4 Singleton kaydı var.
- ✅ `TenantContext` 6/6 `ITenantConnectionInfo` üyesini karşılıyor; `SwitchTenantAsync→ConnectedTenantDb` ataması tip-uyumlu; impl `Data.DataContext` import'suz.
- ✅ `case` çakışması yok: alias'lar (`FirmaChanged`/`MaliDonemChanged` = `"ItemChanged"`) kendi switch'lerinde tekil; `case EntityEvents.ItemChanged` hiçbir yerde yok; yayıncı/abone hat değerleri eşleşiyor (13 dosya, ham-string `grep` 0).
- ✅ Test mantığı: 16 testin beklentisi implementasyonla birebir (clamp/yetki/desen/üretim); `KullaniciRolTip`, `HesapModel` grafı, `InlineData(null)` emsali, warnings-as-errors yokluğu doğrulandı.
- ✅ Mimari bekçi desenleri tetiklenmiyor (`ITenantConnectionInfo.cs`'deki tek `MuhasibPro.Data` geçişi doc-comment içinde; `using` yok).

### Kayıt
- KONTROL 6.56 Windows kutusu açık kalıyor (bu oturum statik onayı ekler, 🧪'yi kapatmaz).

### Sonraki adım
- Faz 4 (M5 Tenant) — L3: Oturum 126'da kısmi kapandı (aşağıda).

---

## 2026-09-09 — Oturum 126 (Faz 4 M5 Tenant kısmi — ✅ kod, 🧪 Windows'ta)

### Uygulanan
- **Ayar:** `TenantSettings` genişledi (`MigrationRetry=2`, `BakimTimeoutSec=120`, `CommandTimeoutSec=30`, `BusyTimeoutMs=5000`, `Pooling=true` — beşi de `[YoneticiAyari]`; sayfa/backfill korundu) + Get-helper'lar.
- **Sağlayıcı:** `ITenantSettingsProvider` + `TenantSettingsProvider` (M2/M3 deseni: Get/Save + Clamp + `AyarYetkiDenetimi`) + DI Singleton.
- **E1 olay:** `TenantUpdateAvailableEvent(DatabaseName, FromVersion, ToVersion) : DomainEvent("M5")` — yayın noktası `TenantDatabaseUpdateService.CheckUpdateRequiredAsync` (NeedsUpdate'te, `IEventBus = null` opsiyonel ctor paramı — mevcut 3-arg kurulumlar kırılmaz).
- **E1 M3 bacağı:** `MaliDonemListViewModel` abone olur (opsiyonel bus ctor paramı; 5 kurulum sitesi derlenir) — eşleşen kart `DbAnalizYapildi=true + DbDurum=RequiredUpdating + DbAnalizDetay="From → To"`, refresh yok (UI thread `RunAsync`).
- **E1 M1 bacağı:** `FirmaShellViewModel` abone olur (bus + `_localSettings` saklanır, paylaşımlı listeye bus aktarılır) — `UpdateSettingsModel.ShowNotifications` açıksa durum satırı + toast, kapalıysa sessiz. `UpdateSettingsModel.SettingsKey` public const'a alındı, `UpdateService` private const'u silindi (tek kaynak).
- **Süpürme:** `TenantUpdated` 2 ham-string → `TenantEvents.Updated` (değer aynı, akış korunur).
- **Test:** `TenantSettingsTests.cs` 7 test + E1 yayın testleri 2 (`TenantDatabaseUpdateTests` E1 bölgesi) — beklenti **173/173** (148+16+7+2).

### Doğrulanıp kapatılan plan maddeleri (kod değişikliği yok)
- **keepLast birleşimi:** `DonemYedeklerVM:166` zaten `dbSettings.GetManuelKeep()` (M4) — hardcoded keep kalıntısı 0.
- **Ctor sadeleşmesi:** `TenantSQLiteDatabaseService` 14 bağımlılığın 14'ü canlı (en düşük 3 kullanım) — ölü param yok, facade bölünmez (plan hükmü).
- **SplashNavigator:** 86 satır, factory/DbContext artığı 0 (Faz 1'de taşınmış) — 5. madde kapanmış.
- **BackupFilePattern:** uygulanmadı — dosya-adı grameri parse-sözleşmesi (Oturum 112 dersi) + kural 9 protokol istisnası; kullanıcı-düzenlenebilir desen okuma tarafını kırar. Karar olarak loglandı.

### Oturum 127'ye kalan (derin bağlantı)
- Connection-string seçenekleri (`BusyTimeout/Pooling`), bakım `CommandTimeout`, göç `MigrationRetry` threading — Data imzaları + test güncellemeleriyle ayrı odaklı oturum.

### Doğrulama
- `dotnet` hâlâ kırık (Oturum 125 ile aynı) — build + test Windows'ta. Statik: using/imza/üye/enum/kayıt/ctor-uyumluluğu + `Mock<IEventBus>` deseni + `null!` uyarı temizliği gözden geçirildi.
- Boyut notu: `FirmaShellViewModel` 225 + `MaliDonemListViewModel` 393 satır (ikisi de önce de 150 üstüydü — orkestratör/liste rolleri; eklenen handler'lar abonelik sorumluluğunda, yapay ayrım açılmadı).

---

## 2026-09-09 — Oturum 127 (Faz 4 M5 derin bağlantı + kalan testler — ✅🧪)

### Kalan testler (Oturum 124-126 borcu kapandı)
- Bu ortamda `dotnet` çalışıyor (kum-havuzu WSL engeli yok): `dotnet build` **0 hata** (uyarılar yalnız eski aileler), `dotnet test` **173/173** — Faz 3 M3 + M5-kısmi statik onayları gerçek doğrulamaya dönüştü.

### Uygulanan (derin bağlantı)
- **Domain:** `TenantSettings` statik clamp'leri (`ClampMigrationRetry/ClampBakimTimeoutSec/ClampCommandTimeoutSec/ClampBusyTimeoutMs` + `BusyTimeoutMsToSeconds`; `Get*`'lar statiklere bağlandı — davranış aynı).
- **Data imzaları (hepsi opsiyonel, varsayılan = eski davranış):** `CreateConnectionString/ValidateConnectionStringAsync` (+busyMs/pooling), `IAppDbContextFactory/IAccountingDbContextFactory.CreateDbContext` (+cmdSec/busyMs/pooling), `InitializeTenantDatabaseAsync` (+cmdSec/retry, manager+migrationManager), `CreateNewTenantDatabase(+cmdSec)`, `BakimCalistirAsync` (+timeoutSec), extension'larda `commandTimeoutSec` (dakikayı ezer) + `maxAttempts` (retry threading).
- **BUG FIX (birim hatası):** `TenantSQLiteConnectionStringFactory` `DefaultTimeout = 5000` yazıyordu — builder **saniye** ister (eski kod ~83 dk busy-timeout demekti). Artık `BusyTimeoutMsToSeconds` (5000 ms → 5 sn). `HATALAR.md`'ye işlendi.
- **Business threading (opsiyonel provider — providersız kurulumlar kırılmaz):** `TenantSQLiteSelectionService.SwitchTenantAsync` (validate'e busy/pooling, initialize'a cmdSec/retry), `TenantSQLiteDatabaseOperationService.BakimCalistirAsync` (bakım sn), `TenantSQLiteDatabaseService` 2 doğrudan context açılışı (damga/backfill). Sağlayıcı yoksa null taşınır → Data varsayılanları.
- **Kapsam dışı (bilinçli):** oluşturma hattı (SagaStep→Lifecycle→Manager) varsayılanlarda kalır (5 dk); `TenantVersionReader`/scoped `AppDbContext` varsayılanlarda; `BackupFilePattern` yok (Oturum 126 kararı).
- **Test:** `TenantDerinBaglantiTests.cs` 6 test (conn-string varsayılan/taşıma/clamp, statik clamp, gerçek temp-dosyada VACUUM + bozuk-komut, switch threading verify) + `DataMigrationFlowTests` retry-2 testi — `dotnet test` **180/180**.
- **Test tamirleri:** `BusinessDatabaseTests` 2 Moq `Setup` CS0854 verdi (ifade ağacı opsiyonel argüman alamaz) → açık `It.IsAny` (HATALAR'a ders olarak işlendi); `DataMigrationFlowTests` `[Fact]` yorum satırına yapışmıştı → ayrıldı.

### Doğrulama
- `dotnet build MuhasibPro.slnx` **0 hata** (24 uyarı, hepsi eski aileler; yeni dosyalar 0). `dotnet test` **180/180**.
- Canlı (switch→migrate→yedek→bakım gerçek eşiklerle + Oturum 119 anomalisi) kullanıcıda.

### Sonraki adım
- Faz 4 kapanışı: oluşturma-hattı threading (istenirse) + canlı toplu doğrulama (kullanıcı kararı: tüm fazlar bitince toplu).

---

## 2026-09-09 — Oturum 128 (Faz 5 ilk adım: politika DI + ITenantContext ölümü — ✅🧪)

### Keşif (Faz 5 L4)
- `PermissionService` + `ModuleLicenseService` var ama DI kayıtsız; KFR/RP repoları var ama kayıtsız (Oturum 121 notu doğrulandı).
- **Ölü arayüz:** `Data/DataContext/ITenantContext` — 0 implementasyon, 0 DI kaydı, tek tüketici `PermissionService` (plan L2 notundaki "SetTenant 0 caller" aynen). `IPermissionService`'in 0 tüketicisi var → ctor değişimi güvenli.

### Uygulanan
- `PermissionService` ölü `ITenantContext`'ten koparıldı → `IFirmaWithMaliDonemSelectedService` (seçim aynası, `SelectedFirma.Id`) + `IAuthenticationService` (`IsAuthenticated` + `GetCurrentUserId`; girişsiz → false, repo çağrılmaz). Sözleşme aynı, cache aynı.
- `ITenantContext.cs` silindi (ölü kod kural 4).
- DI: `IKullaniciFirmaRolRepository` + `IRolPermissionRepository` **Scoped** (DbContext tutar — Singleton captive olurdu) + `IPermissionService` + `IModuleLicenseService` **Scoped** (planın dediği gibi).
- **Test:** `PolitikaTests.cs` 6 test (2 DI-kayıt taraması + yetki doğru/yanlış + cache/ClearCache + girişsiz + firmasız) — `dotnet test` **186/186**.
- Bilinen borç (dokunulmadı): `ModuleLicenseService:150 KaydedenId = 1` hardcoded (kural 7 — auth'tan gelmeli, sonraki adımda).

### Doğrulama
- `dotnet build MuhasibPro.slnx` **0 hata** (5 uyarı, hepsi eski: CS0618 Velopack + IL2072). `dotnet test` **186/186**.

### Sonraki adım
- Faz 5 devamı: `KullaniciService` + `LisansService` (yeni) + `LicenseSettings` + E2 yayın eksikleri (`DatabaseSettingsVM`, `UpdateVM`, Login Unsubscribe, DetailsWindow kapanış).

---

## 2026-09-09 — Oturum 129 (Faz 5 devam: LicenseSettings + KaydedenId + E2 yayın/abonelik temizliği — ✅🧪)

### Kapsam kararı
- Planın "sonraki adım" dörtlüsünden üçü kapandı; `KullaniciService` + `LisansService` Oturum 130'ya bırakıldı — ikisi de greenfield (Lisans: 0 tüketici, sadece entity; Kullanici: repo var, servis yok) ve `LicenseSettings` önce gelmeliydi (LisansService türü modelden okuyacak).

### Uygulanan
- **LicenseSettings:** `Domain/Models/LicenseSettings.cs` (Tur `[YoneticiAyari]` default Deneme + CheckIntervalDays default 7, clamp 1-90) + `ILicenseSettingsProvider` + `LicenseSettingsProvider` (M2/M3 deseni: Get/Save + Clamp + `AyarYetkiDenetimi`; bozuk enum → Deneme) + DI Singleton + `LicenseSettingsTests.cs` 7 test.
- **KaydedenId borcu (KONTROL 6.58):** `ModuleLicenseService` ctor'a opsiyonel `IAuthenticationService` → yeni satırda auth id'si, girişsiz bootstrap'ta sistem kaydı 1. `ModuleLicenseTests.cs` 3 test (gerçek sqlite in-memory: auth 42 → satır 42; paramsız → 1; ikinci kayıt aynı satırı günceller).
- **E2 yayınları:** `DatabaseSettingsViewModel.SaveAsync` + `UpdateViewModel.SaveSettingsAsync` sonrası `AppSettingsChangedEvent` yayını (opsiyonel bus ctor paramı, mevcut kurulumlar kırılmaz); `DatabaseSettingsViewModel` yerel `SettingsKey` const'u silindi → `DatabaseSettingsModel.SettingsKey` tek kaynak; `UpdateViewModel.Unsubscribe()` no-op'tan gerçek bus-unsubscribe'a + `UpdateView.OnNavigatedFrom` bağlandı.
- **Login Unsubscribe (gerçek bug):** `LoadAsync` her çağrıda `MessageService.Subscribe` ediyordu — ikinci çağrı `Dictionary.Add` çift anahtarla `ArgumentException` atar (Teşhis→Kapat yolu `RefreshDbStatusAsync`'e hiç ulaşamıyordu). `_quickLoginAbone` guard + `Unsubscribe()` + `LoginView.OnNavigatedFrom`.
- **DetailsWindow kapanış:** `ServiceLocator.DisposeScope(windowId)` (id-bazlı; `DisposeCurrent` delege olur, ölü kod yok) + `WindowHelper.UnregisterWindow` kapanışta scope dispose (try/catch). `DisposeCurrent` 0-caller ölü adaydı — bağlandı. MessageService/EventBus Singleton olduğu için Closed-handler'lar etkilenmez.

### Doğrulama
- `dotnet build MuhasibPro.slnx` **0 hata** (2 uyarı, ikisi de eski CS0618 Velopack). Ara not: ServiceLocator edit'imde qualifier düşürüp CS0103 çıkardım → WMC0001×19 + WMC9999 kaskadı; HATALAR dersi aynen (önce gerçek CS).
- `dotnet test` **199/199** (+13: License 7 + ModuleLicense 3 + AyarYayin 3).
- Canlı (lisans ayarı + Teşhis→Kapat refresh + pencere kapatma) kullanıcıda.

### Sonraki adım
- Oturum 130: `KullaniciService` (repo sarmalar) + `LisansService` (LicenseSettings.Tur'ü okur) + tipli `TenantBackupCompleted/TenantRestoreCompleted` olayları + yedek-listesi/Bilinmeyen abonelikleri. Ayrı borç: diğer `KaydedenId = 1` kalıntıları (`GlobalAyarlarService:41`, `MakineKimligiProvider:65`, `AuthenticationRepository:50`, log repoları).

---

## 2026-09-09 — Oturum 130 (KaydedenId tek sabit — kullanıcı sorusu → ✅🧪)

### Soru (kullanıcı)
- "KaydedenId null olamaz, varsayılan değer vermemiz gerekiyor. Başka uygulamalar nasıl yapıyor, fikrin var mı?"

### Bulgu (cevap öncesi araştırma)
- Seed admin `Id = 5413300800` (sabit, migration'a gömülü); `KaydedenId`'de FK yok (yanlış değer patlamaz, iz yalan söyler).
- 4 sihirli sayı, hiçbiri gerçek kullanıcı değil: `1` (3 yer) / `800` (2 log reposu) / `241341L` (Register) / `24134175366` (81 il seed'i).
- Karar (kullanıcı): seed admin sabiti (ayrı SİSTEM satırı değil).

### Uygulanan
- `Domain/Entities/KullaniciSabitleri.SeedYoneticiId = 5413300800L` tek kaynak.
- 7 yanlış: ModuleLicense fallback + GlobalAyarlar + MakineKimligi + 2 log reposu → sabit; Register → kendi Id'si (self-registration); İl seed'leri (81×) → sabit.
- 6 dosyadaki doğru `5413300800` literal'i + seed adminin kendi `Id`'si sabite (değer aynı, migration etkilenmez).
- `KaydedenIdVarsayilanTests` bekçi (sabit-değer pini + literal taraması; Migrations/test-fixture hariç).

### Doğrulama
- `dotnet build` **0 hata** (dokunulan dosyalardan 0 uyarı), `dotnet test` **201/199→201** (+2 bekçi).

### Sonraki adım
- Oturum 131: `KullaniciService` + `LisansService` + tipli Backup/RestoreCompleted + abonelikler.

---

## 2026-09-09 — Oturum 131 (SemVer geçişi: tenant/sistem/app — ✅🧪)

### İstek (kullanıcı)
- Sıradaki oturumdan önce tenant + sistem DB + uygulama versiyonu SemVer'e çevrilecek.

### Tasarım
- **Tek kaynak ikiye ayrıldı:** `Domain/Utilities/SemanticVersion` (ayrıştır/normalleştir/karşılaştır, 3-parça sayısal) + `Data/Database/Common/Helpers/DbSchemaVersions` (migration→SemVer haritası: iki Initial → `1.0.0`, AddTenantIdentity → `1.1.0`; bilinmeyen → `1.0.0`). Kural: yeni migration = bir satır (MINOR uyumlu, MAJOR kırıcı).
- **Takvim şeması (`1.yyyy.MMdd.HHmm`) emekli:** `ExtractVersionFromMigration` + `CalculateSimpleHash` silindi (ölü kod); 5 çağrı noktası haritaya bağlandı.
- **Eski damgalar okunur:** `1.2026.0908.0` → `0.908.0` bandına iner — her SemVer'den küçük, kendi içinde sıralı (restore-hüküm eski yedekleri Block yerine Warning'e düşürür, doğru olan bu).
- **App 1.0.0 → 1.1.0** (csproj + manifest; seed'ler donmuş tarih, ellemedi).
- **Bug fix:** `:355` ilk-kayıt dalı sabiti değil hesaplananı yazar.
- `RestoreVerdictEvaluator` el-yordamı karşılaştırması `SemanticVersion`'a devredildi.

### Doğrulama
- `dotnet build` **0 hata** (dokunulan dosyalardan 0 uyarı), `dotnet test` **216/216** (+15: harita 4 + SemVer 14 − emekli takvim 3).

### Sonraki adım
- Oturum 132: `KullaniciService` + `LisansService` + tipli Backup/RestoreCompleted + abonelikler.

---

## 2026-09-09 — Oturum 132 (Faz 5 kapanışı: Kullanici/Lisans servisleri + E2 abonelikleri — ✅🧪)

### Uygulanan
- **KullaniciService:** repo sarmalar (Get/GetByUsername/Liste/Update/SetAktif/SifreBelirle/Delete); guard'lar: girişsiz ret, self-delete ret, seed-yönetici silme/pasif ret, başkasının şifresi admin-dışı ret; `MinPasswordLength` (6, normal seviye) `IdentitySettings`'e eklendi (kural 7 — hardcode yok) + DI Scoped + 8 test.
- **LisansService:** tür ayardan + satırlar tablodan (`GetLisansDurumu`: Deneme satırsız-geçerli, diğerleri tarih-geçerli satır ister + KalanGun; `Kaydet` admin + tarih guard + KaydedenId auth'tan; `Liste`) + DI Scoped + 6 sqlite testi.
- **E2:** `TenantBackupCompletedEvent` + `TenantRestoreCompletedEvent` (M5) → `TenantSQLiteDatabaseOperationService` başarı dallarında yayın (opsiyonel bus); `DonemYedeklerVM` (db-eşleşmeli `YukleAsync`) + `BilinmeyenVM` (`TaraAsync`) abone + `YonetimVM` orkestrasyona bağlandı (Subscribe/Unsubscribe eşit) + 5 test.
- **Moq CS0854** tekrar yakalandı (ifade ağacında `new PasswordHasher`) → hasher dışarı alındı (HATALAR dersi aynen).

### Doğrulama
- `dotnet build` **0 hata** (dokunulan dosyalardan 0 uyarı), `dotnet test` **235/235** (+19).
- Canlı (kullanıcı yönetimi + lisans + çift-pencerede yedek tazeleme) kullanıcıda.

### Sonraki adım
- Faz 6 (Mühür): AKIS/WINUI doc güncellemeleri + abonelik matrisi + full canlı E2E.

---

## 2026-09-09 — Oturum 133 (Faz 6 Mühür: 0 uyarı + docs + matris — ✅🧪)

### Uygulanan
- **0 uyarı süpürmesi:** full `--no-incremental` sayımda 70 uyarı (benzersiz ~35) kapatıldı — test `null!`'ları, `?.ToString()`, async test, `string?` desen + null-tolerant `IsValid`, Velopack `UpdatePendingRestart != null`, gerekçeli IL2072 pragma, `IsWindows()` guard. Sonuç: **0 uyarı 0 hata**, test 235/235.
- **Docs:** AKIS-PLANI §2 (Sistem.db/Tenants/Backups + SemVer) + §3 (Splash-ilk akış) + §4 (Update/Verify/Transfer/FIFO/kimlik) + §7 abonelik matrisi (5 tipli olay; AppSettings/Transfer abonesiz-sözleşme; ThemeChanged bus-dışı; legacy string-bus grandfathered); WINUI-MIMARISI §7 (Coordinator/Yönetim/UpdateView/kapanış); ROADMAP §1 ağaç + kavramlar (ITenantContext/Global.db kalıntıları gitti).
- **Denetim:** `Send("` ham-string yayın Libraries+App'te 0 (yeni kod tipli bus'ta); Subscribe = Unsubscribe (test kilitli + scope dispose).

### Canlı (kullanıcıda — toplu E2E)
- Splash→SistemKurulum→Login→FirmaShell→Yönetim→MainShell + güncelleme sayfası + geri yükleme + FIFO 5 + çift-pencere yedek tazeleme + lisans/kullanıcı ekranları.

### Sonraki adım
- Faz B açılışı (kullanıcı kararıyla): B1 Cari.

---

## 2026-09-09 — Oturum 134 (Tasarım brief bakımı — ✅ docs)

### İstekler (kullanıcı, canlı-test öncesi)
1. Sayfa tasarım planı loglarda var mı? → `OOBE-TASARIM-SABLONU.md` özeti verildi.
2. Arkaplan resmi logda var mı? → `app_background.png` (Oturum 52) yerine kodda `background_muhasib.png` (Oturum 102) kullanılıyormuş; şablon §1 düzeltildi.
3. AI Studio'ya komple atıp tek-tip tasarıma geçmek için brief güncelle.

### Uygulanan
- **Dil kararı (kullanıcı):** hedef = güncel kod dili (Custom* + warm + petrol); frost dili tarihçeye kaldırıldı → şablona "Güncel dil" bölümü eklendi, InventEase başlığı tarihçe işaretlendi.
- **`AI-STUDIO-VIEW-BRIEF.md` sıfırdan yazıldı:** hedef dil + okunacak/yüklenmeyecek listesi + 7 ekranlık kapsam tablosu + sözleşme-koruma kuralları + çıktı formatı (viewpackage.zip + RAPOR.md) + aynen-yapıştır prompt. Eski brief bayattı (mavi primary, silinmiş dosyalar, acrylic yasağı).

### Sonraki adım
- AI Studio viewpackage.zip + RAPOR.md gelince inceleme (Oturum 137); token kısıtı — parça parça bakılabilir.

---

## 2026-09-09 — Oturum 136 (AI Studio tek-tip geçiş başladı — 🔨 devam)

- Brief §7 prompt'u AI Studio'ya verildi; repo importlu çalışıyor, hazırlık yapıyor.
- Beklenen: `viewpackage.zip` (mevcut ağaçla aynı yollar) + `RAPOR.md` (değişen stiller + eksik tokenlar + bölünen kontroller).
- İnceleme kuralları (gelince): sözleşme (x:Bind/Click/Name/AutomationId birebir mi), hardcode `#` yok, eksik token listesi → DesignTokens'a ekleme, seçerek `MuhasibPro/Views`'a alma.

---

## 2026-09-09 — Oturum 135 (Git + AI Studio hazırlığı — ✅)

- `.gitignore`: `*.db/*.backup/Databases/conversation-*.txt` eklendi; `xamlout.txt` ölü dosya silindi.
- İlk push: `github.com/omer-denen/muhasibPro` (main, 720 dosya). Secret notu: dev admin kimliği kodda açık (private repo varsayımı).
- AI Studio brief §7'ye import-senaryosu hüküm sırası eklendi; kullanıcıya kopyala-yapıştır prompt verildi.
