# MuhasibPro — Oturum Günlüğü (Oturum 81-100)

> Ana indeks: [docs/LOG.md](../LOG.md)

---

## 2026-09-04 — Oturum 80 (Veritabanı Motoru 3-sayfa tasarım: Tümü + Dönem + Analiz — ✅ kod, 🧪 DERLENMEDİ)

### Bağlam
- Kullanıcı repos üstüne 3 tasarım görseli ekledi (`VeritababıYonetimiAnaEkran`, `DonemeOzelIslemler`, `VeritabaniAnalizi`): 7 kolon tablo + 6 kart + skor kartları. Renkler OOBE'ye sadık, renklere takılınmadı.
- Karar netleştirme: daha önce elenenler (kilit kartı, kategori/son-yazma/I-O/anomali, dosya-taşıma arşiv) **tasarımda da YOK** — 6→5 kart, analiz 2 skor kartı + 3 kolon tablo, arşiv bayrak bazlı.

### Yapılanlar (Aşama B — Tümü)
- `DonemGenelBakisViewModel`: `KayitliDbAltMetni` (aktif sayısı) + `OrtalamaDepolamaMetni` + `ButunlukAltMetni` (PRAGMA hata sayısı) + `GosterilenMetni` (N/M sayacı).
- `TumuHeroPanel` yeni (hero kart + firma pill + 3'lü segment içinde; aktif-buton görünümü panel içinde, `Root.PropertyChanged` + `Unloaded` unsubscribe).
- `GenelBakisPanel` restyle: 4 KPI kartı (ikon + alt metin) + toplu işlem bandı (başlık + 3 buton) + arama + sayaç + başlıklı 7 kolon tablo (yıl rozeti seçilide mavi + Yönet + yedek/durum/analiz ikonları). Satır Sil kaldırıldı (silme dönem banner'da).

### Yapılanlar (Aşama C — Dönem)
- `DonemBannerPanel` yeni (büyük yıl + unvan + pill'ler + Hedef Dönem Combo + Sil + dosya-yok Kurtar satırı).
- `DonemIslemKartlariPanel` yeni (5 kart: Yedek primary + VACUUM + REINDEX + WAL + Arşivle/Arşivden Çıkar bayrak).
- `DonemParametrePanel` yeni (SQLite Format + Page Size + AccountingDbContext + Migration Durumu).
- Sayfadaki spec-sheet + aksiyon şeridi silindi; `DonemBakimPanel` dosyaları silindi (ölü kod); sayfa handler'ları panellere taşındı (sayfada ölü `OnYedekle/OnKurtarGoto/OnArsivle/OnDonemSil` silindi).

### Yapılanlar (Aşama D — Analiz)
- `DerinAnalizPanel` restyle: analiz bandı (açıklama + Tam Analiz Çalıştır primary) + 2 skor kartı (Bütünlük: hata sayısı + integrity/journal/dosya satırları; Sayfa: sayfa sayısı + boyut/freelist/tablo satırları) + tablo (Tablo/Kayıt/Depolama + Dağılım ProgressBar, `ElementName=KokPanel` ile DosyaBoyutu'na oranlı) + hata metni.
- Kategori / son-yazma / I-O kartı / anomali raporu YOK (ölçülemiyor kararı).

### Ders (HATALAR.md'ye işlendi)
- `ModernCard` `TargetType=Border` — `Grid`'e `Style` ile verilemez; `Border` sarmala + iç `Grid`. Statik grep + XML parse ile yakalandı, derleme yapılamadan düzeltildi.

### Doğrulama
- `dotnet` bu ortamda YOK (Linux sandbox + WSL interop vsock hatası) → **build + test yapılamadı**. Statik: 7 XAML well-formed (xml parse), `Grid-ModernCard` 0, ölü referans 0 (`BakimPanel/Seg*Buton/OnSatirSec` grep temiz), tüm binding adları VM/model/DTO'da mevcut.
- ⬜ Windows'ta `dotnet build MuhasibPro.slnx` + `dotnet test` + canlı görsel (3 segment + dağılım barı + sekme geçişi) kullanıcıda.

### Sonraki Adım
- Build/test sonrası: sekme/segment manuel tıklama (Oturum 79 ⚠️ aynen duruyor) + Yedek dosya üretimi + Kapalı pilleri + aside2026.

### Üst şerit sadeleştirme (istenen karşılaştırması)
- İstenende üstte dönem seçici yok (seçim yalnızca Dönem banner'daki "Hedef Dönem"de) → sayfadaki DÖNEM şeridi + firma başlık bloğu silindi (görünmez `WindowTitle` korundu); "Yeni Dönem Aç" PRAGMA yanına taşındı. Satırlar 5→3.

### Tooltipler
- Yönetim sayfasındaki 20 butona (sekme/segment/toplu işlem/5 kart/banner/Arşiv/Yedekler/Bilinmeyen) `ToolTipService.ToolTip` eklendi; tooltipsiz buton kalmadı (grep 0).

### Düzeltme (canlı ekran görüntüsü incelemesi)
- `EkranKaydi/proje-gorunumu` vs `istenen-gorunum`: arşivli satırlarda Açık + Arşivlendi pill'leri üst üste biniyordu (KapaliMi + DbGuncelMi aynı anda true). Durum sütunu `KapaliMi FalseToVis` sarmala alındı + arşiv pill'i istenendeki gibi nötr gri "Kapalı" yapıldı. Crash yok (Transparent fix canlı doğrulandı).
- "Son Yedekleme: Yedek bulunamadı." veri gerçeği (yedek yok); Toplu Yedekle sonrası dolması Yedek-dosyası-üretimi açık maddesiyle izlenecek.

### Seçim kaybı fix + alt liste tooltip kontrolü
- REINDEX sonrası `SelectedDonem` null: ComboBox TwoWay ara-reset'i (`ItemsSource.Clear` anında null yazma). Setter guard + `EsitleListeSecimi()` eklendi (`HATALAR.md`'de).
- Alt listeler (Yedekler/Arşiv/Bilinmeyen) satır butonlarında tooltip kodda mevcut (Geri Yükle/Yedeği Sil/Arşivden Çıkar/Kalıcı Sil) — görünmüyorsa rebuild sonrası tekrar bakılacak.

### Segment sızıntısı fix (kök neden)
- Dönem/Analiz altında Tümü tablosu görünüyordu: `GenelBakisPanel` elementindeki `IsTumuSegmenti` binding'i, code-behind `DataContext=GenelBakisVM` sonrası ölüydü (converter çalışmaz, Visible kalır). Visibility sarmalayıcı `StackPanel`'e taşındı. `HATALAR.md`'de kural olarak işlendi.

### Görsel birebir uyum (istenen 1-* serisi)
- Banner Combo: "2027 — Açık (28 KB)" formatı (Kapalı/Açık + Boyut). Arama kutusuna büyüteç ikonu. Satır ikon butonlarına renkli zemin (yedek PrimaryLight, durum WarningBg, analiz InfoBg).
- Kalan bilinçli farklar: kilit kartı yok (5 kart), analizde I-O/kategori/parçalanma/son-yazma/anomali yok, arşiv bayrak, Sil + arşiv kısayol şeridi (işlevsel zorunluluk).

### Arşivden çıkar null + bilgi animasyonu + null guard'ları
- Kısayoldan "Arşivden Çıkar" → seçim null + şerit gitmiyordu: yenileme penceresi koruma bayrağı (`_secimKoruma`), koruma-sonrası Id ile geri alma, liste-başarısız null kabulü korundu. `DonemSeciliMi()` guard + `IsDonemSeciliDegil` null barı ("Dönem Listesine Git").
- Başarıda animasyonlu bilgi çubuğu: "2027 arşivden çıkarıldı, tekrar kullanıma açık." (3.5sn + 0.6sn fade, token'lı).
- Banner + 5 kart handler'ları try/catch: hata → Danger bildirim + Tümü'ne yönlendir.

### Section erteleme (karar)
- `Controls/Section` incelendi (HeaderButton + DetailsViewSection + Composition animasyonlar). OOBE diline uymuyor + `OnApplyTemplate` çoklu abonelik riski → yönetim ekranına sokulmadı, ileriye ertelendi.

### Analiz dönem bağlamı (istenen eksikliği tamamlandı)
- İstenen resimde de yoktu: analiz sayfasına "ANALİZ EDİLEN DÖNEM" kartı (yıl + unvan + Açık/Kapalı/Dosya Yok + db adı + Hedef Dönem Combo "2027 — Açık (28 KB)"). Combo seçimi tüm sayfayı günceller (DerinAnaliz sıfırlanır, Çalıştır ile yeniden üretilir).

### Tablo tooltip: ListView -> ItemsControl
- Tümü satır butonlarında tooltip hiç görünmüyordu (diğer her yerde çalışıyor). Çalışan örnekle (FirmaShell) tek fark: satır host'u `ListView` vs `ItemsControl`. Tablo `ItemsControl`'e çevrildi (seçim zaten butonlarla; dışta ScrollViewer varken iç içe kaydırma da kalktı), tooltip'ler kanıtlı desene döndürüldü.

### Yedekler dönem bağlamı + ListView temizliği
- "Hangi dönemi yedekleyecek" belirsizdi: Yedekler başlığına dönem adı + "Dönem:" seçici eklendi (`Root` DP + ElementName binding; seçim değişince yedek listesi tazelenir).
- Tüm Yonetim listeleri `ListView`→`ItemsControl` (Tümü tablosu tooltip şüphesi + iç içe kaydırma; seçimler zaten Tag/buton tabanlı, kayıp işlev yok).

## 2026-09-04 — Oturum 81 (Tooltip teşhisi: SekmeGorunumu hipotezi çürütüldü — kod değişikliği yok)

### İncelenen
- Kullanıcı hipotezi: "tooltip'ler code-behind manuel stillerden (TumuHeroPanel.xaml.cs SekmeGorunumu) bozuluyor".
- `TumuHeroPanel.xaml.cs:51-65` + `MaliDonemYonetimView.xaml.cs:66-80` okundu; `*.cs` genelinde `ToolTip` araması **0 sonuç** — code-behind hiçbir tooltip'e dokunmuyor.
- SekmeGorunumu yalnızca 5 butonda `Background` (+`ClearValue`) ve `Foreground` set ediyor (SegTumu/SegDonem/SegAnaliz + Tenant/Arsiv tab); satır butonları (Yönet/Yedekle/Durum/Analiz, Geri Yükle/Sil…) metoda hiç uğramıyor — buna rağmen onlarda da görünmüyor raporu var → ortak neden olamaz.
- Global `ToolTip` stili yok (App.xaml + Styles + Themes temiz); `MuhasibCardRadioButtonStyle` hover katmanı `IsHitTestVisible=False` (temiz).
- `dotnet build MuhasibPro.slnx` bu ortamda **0 Hata** (20 uyarı: WMC1506 x2 bilinen + IL2072 + test CS8625/xUnit1031) — 33 `ToolTipService` bildirimi binary'de mevcut.

### Gerçek bulunan vaka (tekil)
- `MaliDonemlerListControl.xaml:155` `ToolTip="{Binding DbAnalizDetay}"` — analiz bitene kadar `DbAnalizDetay=null` (`MaliDonemModel.cs:132`, analiz `MaliDonemListViewModel.cs:188-267`'de sonradan dolduruyor) → o grid'de tooltip çıkmaması normal, statik butonları açıklamaz.

### Sonraki adım (kullanıcıda)
- Clean rebuild + "Yeni Dönem Aç" üzerinde 1-2 sn bekleme testi; çıkarsa bayat binary, çıkmazsa pencere-düzeyi canlı teşhis (hangi buton grubu: segment / satır / tümü).

### Düzeltme (Oturum 81 devamı — manuel stil XAML'e taşındı, şüpheli kod silindi)
- Kullanıcı şüphesi giderildi (kanıtlanamasa da): `SekmeGorunumu` + `GuncelleSegmentGorunum`/`GuncelleAktifGorunum` (fırça `resources[]` + `ClearValue` + `Foreground` döngüsü) **tamamen silindi** — `TumuHeroPanel.xaml.cs` + `MaliDonemYonetimView.xaml.cs`.
- Yerine: renkler SADECE XAML'de — `SegmentStates` (Tumu/Donem/AnalizAktif + SegmentPasif) + `SekmeStates` (Tenant/ArsivAktif + SekmePasif) `VisualStateGroup`ları (token `Setter`lar, binding yok); code-behind yalnızca `VisualStateManager.GoToState` ile durum adı seçer (fırçaya dokunmaz). Pasif görünüm XAML'de bildirimsel (Secondary foreground + varsayılan zemin) — piksel-aynı.
- `StateTrigger+Binding` bilerek KULLANILMADI (trigger görsel ağaçta değil, DataContext şüphesi) → tetikleme VM bildiriminden (`AktifSegment/AktifSekme` zaten `Is*` flag'lerini bildiriyor).
- Doğrulama: Release build **0 Hata** (18 uyarı: bilinen WMC1506/IL2072/test uyarıları). Debug build dosya kilidinden kopyalayamadı (uygulama + VS açık) — derleme hatası değil.
- ⬜ Canlı test kullanıcıda: segment/sekme vurguları aynı mı + tooltip'ler (şüpheli kod artık yok).

## 2026-09-05 — Oturum 82 (Widget görsel dili temel + ölü stil temizliği — ✅🧪)

### Bağlam
- Kullanıcı: ana görünüm kökten değişsin (Win11 Widgets panosu referans görseli) → tüm paneller dahil, tutarlılık tek kaynak token/kart, hardcode yok, koyu tema destekli, kullanılmayan stiller silinsin.

### Yapılanlar (temel — kırılım yok)
- **Widget token'ları** (`DesignTokens.xaml`, Light+Dark): `MuhasibWidgetBackground/Border/HostColor` + fırçalar, `MuhasibDangerHover/PressedColor` (QuickLogin sil-X hover'ı token'a alındı) + fırçalar, `MuhasibWidgetPadding/HostPadding`. Koyu tema: widget kart `#1E293B`, host `#141B2B`, danger-hover/pressed `#33/#55F87171`.
- **Widget stilleri** (`Cards.xaml`, tek kaynak): `WidgetHostStyle` (Acrylic host + widget border), `WidgetCardStyle` (Border, widget bg/border + ThemeShadow), `WidgetTitleText/TimestampText/HeroValueText/SubtitleText` (12/10/28/11).
- **Hardcode sıfır:** `QuickLoginPanel` 4 `Button.Resources` rengi → token (`PrimaryLight/Border`, `DangerHover/Pressed`); `ToolBar.xaml` `#FFAAAAAA` → `MuhasibTextTertiaryColor`. Views'da `#...` 0 (Styles'ta sadece token tanımları).
- **Ölü stil temizliği (grep ile tek tek doğrulandı):** Cards 19 (CardSuperHeader/Header/Section/Text/Separator, CompactSectionHeader, CompactInfoIcon/Label/Value, InfoRow, Hero/GridHero varyantları 7, SectionPadding, SmallCardCornerRadius) + Buttons 4 (PaneToggle + 3 Compact) + DataGrid 6 (sadece `GridItemStyle` kaldı) + ToolBar 3 (Collapsed + 2 storyboard) + Icons 32 (30 kullanılmayan + 2 geometri; Faz B'de git'ten geri alınır) + **4 dosya silindi:** `FontSizes/TextBlock/Thickness/CalendarPicker.xaml` (23 margin + 7 text stili + 2 ölçü + 165 satır picker hepsi 0 kullanımlı) + App.xaml 4 merge satırı. `DetailToolbar` çift-boşluk normalize.
- Korunanlar (canlı kanıtlı): `GlassPanel`, `ElevatedCard`, `FormsSection`, `ModernSectionHeader`, `CompactInfoRow`, `InfoIcon/Label/Value`, `DetailsViewSection`, `PivotHeaderItem` (implicit, FirmalarView Pivot'u var), `ToolBarStyle/ModernToolBarButtonStyle`, `Primary/Danger/SecondaryButtonStyle`, `PlainButtonStyle` (Themes template'i kullanıyor).

### Doğrulama
- `dotnet build MuhasibPro.slnx` **0 Hata** (yalnız bilinen IL2072 + test uyarıları). Fonksiyonel XAML'e dokunulmadı (sadece stil/token).

### Sonraki Adım
- Pilot giydirme: tek ekran (öneri LoginView → 3 widget) ile widget dilini kanıtla → FirmaShell/SistemKurulum/MainShell → analiz 9 panel + dialoglar. `OOBE-TASARIM-SABLONU.md` widget bölümü eklenecek.

## 2026-09-05 — Oturum 83 (Login Mica + glass deney — ✅🧪 build 0 hata)

### Bağlam
- Kullanıcı widget dilini istemedi (kendi araştırması + görsel gönderecek); arada Login'de deney: pencere Mica (duvar kağıdı rengi otomatik) + ana panel glass.

### Yapılanlar
- Tespit: `MainWindow` zaten `MicaBackdrop (Base)` + Acrylic fallback kullanıyor — duvar kağıdı rengi pencerede mevcut, Login bunu opak `AppBackgroundBrush` ile kapatıyordu.
- `Cards.xaml`: yeni `GlassCardStyle` (Border; `MuhasibAcrylicCardBrush` tema-reaktif + border/padding token).
- `LoginView`: kök `Transparent` (Page + RootGrid, Mica görünür) + ana kart `ModernCard → GlassCardStyle`. Binding/VM'ye dokunulmadı.
- Renk doğrulaması (kullanıcı sorusu): evet — `DesignTokens` Light+Dark dict + tüm fırçalar `{ThemeResource}` (tema değişince otomatik); Acrylic fırçalar Tint+Fallback tema-bağlı; Views'da hardcode 0. Açılış Light (ThemeSelector fallback) → Mica Light + Light tokenlar uyumlu.

### Doğrulama
- `dotnet build MuhasibPro.slnx` **0 Hata** (bilinen IL2072 tek uyarı). Canlı görsel (duvar kağıdı tonu + cam efekti) kullanıcıda.

### Sonraki Adım
- Kullanıcının göndereceği referans görsele göre yön netleşecek.

### Devamı (ana panel blur + iç kontroller kirli beyaz)
- Kullanıcı: ana panel hâlâ beyaz görünüyor → arka plan rengini daha blur alsın; iç kontroller kirli beyaz olsun.
- `MuhasibAcrylicCardBrush` TintOpacity 0.88→0.55 (tek tüketici `GlassCardStyle` → yalnız Login; global etki yok).
- Yeni token `MuhasibOffWhiteColor` (Light `#E9EDF3`, Dark `#26314B`) + `MuhasibOffWhiteBrush`; uygulandı: Login DB kartı + QuickLoginPanel kökü (CompactCard üstüne local Background) + kullanıcı adı/şifre kutuları.
- Build **0 Hata**. Canlı görsel kullanıcıda (blur derecesi + kirli beyaz tonu).

## 2026-09-05 — Oturum 84 (Recurra panel dili + Login giydirme — ✅🧪 0/0)

### Bağlam
- Kullanıcı Recurra referansını onayladı (widget/masonry/cam yok; opak düz dil). Kurallar: ilişkili işlemler tek kart, ek işlevler ayrı kart; `...` menü yok (kullanıcılar buton ister); login'e yeni fonksiyon yok (Yeni Firma/arama eklenmedi), yalnız stil.

### Yapılanlar
- **Token:** `MuhasibPanelCornerRadius` 20; widget/offwhite/acrylic ölü token'ları silindi (tüketici 0).
- **Cards:** `PanelCardStyle` (opak, radius 20, flat) + `PanelMiniStyle` (subtle iç kutu); `GlassCardStyle` + 6 widget stili silindi.
- **LoginView:** opak zemin + logo/adım üst barı (komutsuz) + sol form kartı (BrandBanner + divider + NamePassword) + sağ kolon (DB kartı + Hızlı Giriş kartı); 900px VSM dar/geniş. Tüm `x:Bind`/handler aynen.
- **QuickLoginPanel:** başlık + kart stili kaldırıldı (ebeveyn kart sağlıyor); satırlar aynen.
- **NamePasswordControl:** hap inputlar (PanelMini sarmal + transparent/borderless kutu, ikonlar kolonda), Giriş butonu hap. Reveal/Beni hatırla/hata/demo aynen.

### Hata (HATALAR.md'ye işlenecek düzeyde ders)
- `LoginCardShadow` code-behind'de kalıp XAML'den silinince **CS0103 → XamlCompiler kaskadı** (App.xaml'de 19 sahte WMC0001 + WMC9999). Tek gerçek hata CS0103'tü; ölü shadow bloğu silinince build **0 Uyarı 0 Hata**. Ders: kart stili değişince code-behind gölge receiver'ı aynı anda temizlenir.

### Sonraki Adım
- Kullanıcı canlı görsel onayı (Login) → FirmaShell/SistemKurulum/MainShell + analiz 9 panel aynı dile.

### Devamı (form sağa)
- Kullanıcı: form (NamePasswordControl) sağ kartta, marka + DB + hızlı giriş sol kartta olsun. Kolonlar `1.2*|*` yapıldı, binding/handler aynen. Build **0 Hata** (bilinen IL2072 tek uyarı).

### Devamı (Splash Recurra)
- `ExtendedSplash`: opak zemin + `PanelCardStyle` (gölge receiver code-behind ile birlikte silindi — Oturum 84 dersi uygulandı); logo AppIcon resim → `M` primary tile; progress/storyboard/binding aynen. Build **0 Hata**.

### Devamı (SistemKurulum + Teşhis Dialog)
- `SistemKurulumView`: opak zemin + hero/log `PanelCardStyle`, hero gradient şerit silindi (flat); ShellTitleBar + binding/handler aynen.
- `DatabaseInfoPanel` (2×) + `SystemTestsPanel` (1×) kök `ModernCard → PanelCardStyle`; iç miniler token'lı, dokunulmadı.
- `QuickSistemDbDiagDialog`: opak card bg + panel radius; mini kutular radius 12; yedek bölümü token fırçalara; Yedek Al hap. Tüm `x:Name`/handler aynen. Build **0 Hata**.

### Devamı (SistemKurulum header)
- Üst bar trailing: durum hapı aynen; `Giriş Ekranına Git` hap buton (pill radius, geniş padding); bozuk Türkçe metinler düzeltildi (`Cekirdek veritabanini...` → `Çekirdek veritabanını...`, `Giris Ekranina Git` → `Giriş Ekranına Git`). Build **0 Hata**.

### Devamı (FirmaShell kabuk + liste hapları)
- `FirmaShellView`: opak zemin + Info/sol/sağ `PanelCardStyle` (3 gölge + receiver + `OnRootLoaded` silindi); Yeni Firma hap buton. Binding/VSM/handler aynen.
- `FirmalarListControl`: aksiyon stili + arama + seçim pili hap formuna. `MaliDonemlerListControl`: aksiyon stili + Yeni Dönem + DevamEt hap; 10 mini rozet radius 6→10. Seçim VSM'ine dokunulmadı.
- Not: derleme bir kez MSB3027/3021 verdi — sebep açık duran uygulama exe kilidi (kod hatası değil); `taskkill` + rebuild **0 Uyarı 0 Hata**, test **73/73**.

### Devamı (Mali Dönem İşlemleri: ana sayfa + 9 alt panel, atlanan yok)
- `MaliDonemYonetimView`: resim zemin → opak; sekme/aksiyon butonları + rozetler hap; segment kabı radius 12.
- 6 panelde 19 `ModernCard → PanelCardStyle` (TumuHero 1, GenelBakis 6, DonemBanner 1, IslemKartlari 5, Parametre 1, DerinAnaliz 5); Yedekler/Arşiv/Bilinmeyen kart kökü yoktu (miras zemin) — butonları haplandı.
- Tüm `CornerRadius="6"` → hap token; bilgi barları/kutu 8→12. Binding/handler/VSM aynen. Gölge receiver yoktu.
- Kalan: `ModernCard` stili yalnız `FirmalarView`'da yaşıyor (legacy sayfalar: Firma/Firmalar/MaliDonemView sırası gelince).
- Build **0 Hata** (18 bilinen uyarı, full rebuild).

### Devamı (Yönetim header resmi + WindowTitle + hover/tooltip altyapısı + liste kartları)
- **Header resmi:** `ShellView.AppTitleBar` (DetailsWindow kapsayıcısı) resim + gölge kullanıyordu → flat opak + hairline; `OnRootLoaded`/receiver silindi. Sayfa zemini zaten opaktı.
- **WindowTitle ölüydü:** `Title="{Binding FirmaBaslik}"` gevşek binding sessizce çözülmüyordu → `{x:Bind ViewModel.FirmaBaslik, Mode=OneWay}` (VM notify mevcut). DetailsWindow başlığı firma Milan dönem yönetimi başlığını gösterir.
- **Hover/tooltip kök neden:** 22 küçük butonun `Style`'ı yoktu → varsayılan chrome beyaz kartta görünmez, hover belli belirsizdi. Yeni `GhostPillButtonStyle` (şablonlu, VSM hover/pressed garantili, token) tümüne takıldı: Banner 4 + Kartlar 5 + PRAGMA 1 + Bakış 6 + Yedekler 3 + Arşiv 1 + Bilinmeyen 2. VSM sekmeler (aktif vurgu) ellemedi. Tooltip'ler aynı pointer hattında, canlıda doğrulanacak.
- **Liste kartları:** Yedekler/Arşiv/Bilinmeyen satırları `PanelMiniStyle` karta alındı (screenshot'taki kartsız görünüm giderildi).
- Ara not: `Buttons.xaml`'a stil eklerken kapanış tag'i erken yazıldı, Primary stili dışarı taştı — okuma ile yakalanıp düzeltildi (derlemeden önce göz kontrolü).
- Build **0 Hata**.

## 2026-09-05 — Oturum 85 (Recurra rollout devam + canlı çalıştırma + MaliDonemListView sorunları — 🔨 код, hover/tooltip + concurrency AÇIK)

### Yapılanlar (kod, build 0/0)
- **FirmaShell:** opak zemin + Info/sol/sağ `PanelCardStyle` (3 gölge + receiver + `OnRootLoaded` silindi); Yeni Firma hap. Liste kontrolleri: aksiyon stilleri + arama + seçim pili hap; 10 dönem rozeti radius 10. Seçim VSM korundu.
- **Seçili kart:** `MuhasibCardRadioButtonStyle` Checked → 1.5px + tema primary border (iki listede ortak).
- **Login ortalama:** `VerticalContentAlignment` işe yaramadı (screenshot kanıtlı) → `LoginScroll.ViewportHeight` x:Bind + iç Grid `MinHeight` kalıbı.
- **Titlebar tuşları:** 46×32 → 40×28 kompakt + şablonlu stil (kapat hover/pressed kırmızı-beyaz).
- **ShellTitleBar/ShellView header:** resim → flat opak; 2 receiver + `GetAncestors` + ölü using silindi.
- **Test:** `dotnet test` **73/73**.

### Canlı çalıştırma bulguları (kullanıcının oturumunda gözlem)
- Uygulama açıldı; Login Recurra dilinde render OK; yönetim pencere başlığı **"Korkut Mermer — Mali Dönem Yönetimi"** → WindowTitle x:Bind fix CANLI doğrulandı.
- **Hover:** primary butonda renk farkı (`#0F6CBD`→`#115EA3`) gözle ayırt edilemiyor — hover token'ları birbirine çok yakın (algısal sorun).
- **Tooltip:** 1.8sn hover sonrası primary butonda görünmedi — **doğrulanamadı/kırık şüphesi sürüyor** (koordinat UIA ile kesinleştirilemedi; EnumWindows P/Invoke kalıbı PowerShell'de tip hatası verdi, kök UIA filtrelemesine geçildi).
- **Süreç notu:** tek-instance handoff yüzünden kullanıcının canlı oturumuna tutundum (ekran görüntüleri ondan). Canlı fare testleri kullanıcıyı rahatsız edebilir — bundan sonra hover/tooltip canlı doğrulaması kullanıcıda veya ayrı oturumda.

### MaliDonemListView sorunları (kod incelemesi — DÜZELTİLMEDİ, kullanıcı "durdur" dedi)
1. **EF concurrency (Event Log kanıtlı, KRİTİK):** `Application` logunda bugün çok sayıda `EventId 10100` — `SistemDbContext` üzerinde *"A second operation was started on this context instance before a previous operation completed"*. Aday kaynaklar: `AnalyzeDbStatusAsync` fire-and-forget (`_ =`) + `BackfillFileSizeAsync` yazımı + üst üste `RefreshAsync` (mesaj + kullanıcı) aynı Scoped context'i paylaşıyor. `EnrichTenantDbStatusesAsync` içindeki `Task.WhenAll` deseni de aynı riski taşır. Öneri: VM-içi `SemaphoreSlim` ile Refresh/Backfill serileştirme.
2. **Ölü kod (silinmedi):** `EnrichTenantDbStatusesAsync` + `AnalyzeAndStampAsync` hiç çağrılmıyor (LoadDataAsync çağırmıyor); `OnDeleteSelection` içinde büyük yorum bloğu; çağrılmayan `DeleteRangesAsync` (NotImplementedException); kullanılmayan `using static ...DbLoggerCategory`.
3. **Hover/tooltip:** Ghost stil takıldı ama canlı kanıt eksik (yukarıya bak).

### Sonraki Adım
- Kullanıcı onayı ile: (a) hover token farkını açma, (b) tooltip canlı teşhis, (c) MaliDonemListView concurrency + ölü kod düzeltmesi.

### Devamı (resim artığı + titlebar tuşları)
- Resim (`AppBackgroundBrush`) `ShellTitleBar` kökünde yaşıyordu → flat opak header (beyaz + hairline); gölge + receiver + `GetAncestors` + ölü using silindi. Diğer resim kullanımları (FirmaShell/MainShell/Yönetim) ilgili ekranlarda dönüşecek.
- Pencere tuşları kompakt (46×32 → 40×28, aralık daraltıldı) + şablonlu stil: normal/min/max hover subtle, **kapat hover/pressed kırmızı + beyaz ikon**. Build **0 Hata**.

## 2026-09-05 — Oturum 86 (3 açık madde fix: hover + tooltip + EF concurrency — ✅🧪)

### 1. Hover token farkı (ÇÖZÜLDÜ)
- Light `MuhasibPrimaryHoverColor #115EA3 → #094D8F`, `Pressed #0B4A8A → #073A6C` — primary `#0F6CBD`'den belirgin koyu basamaklar (mavi kanal 189→143→108). Dark zaten ayrık (`#3B82F6/#60A5FA/#2563EB`), ellemedi.
- `GhostPillButtonStyle`: PointerOver zemini `Subtle #F8FAFC` (beyaz kartta görünmez) → `PrimaryLight`; Pressed `PrimaryLight → PrimaryBorder` (beyaz→açık tint→orta 3 basamak). Token adları aynı → `MuhasibPrimaryButtonStyle` + Login `Button.Resources` override'ları otomatik devraldı.

### 2. Tooltip (ÇÖZÜLDÜ — kök neden: test edilen butonda tooltip yoktu)
- Oturum 85'te hover'lanan Login Giriş primary butonunda `ToolTipService.ToolTip` hiç tanımlı değildi (`NamePasswordControl.xaml:173`) → "görünmedi" beklenen davranıştı, framework bug'ı değil. Yönetim'deki 33+ tooltip XAML'de tanımlı, engelleyen kod yok (Oturum 81 kanıtı geçerli).
- Eklenen 8: Giriş ("Kullanıcı adı ve şifre ile giriş yap") + Reveal ("Şifreyi göster / gizle") + Teşhis + QuickLogin hesap/sil-X + Yeni Firma + Yeni Dönem + Çalışma Alanına Geç.
- `DbAnalizDetay` binding tooltip'e `TargetNullValue='Analiz bekleniyor — kart seçilince çalışır'` (null iken boş yerine anlamlı metin). Not: disabled butonda tooltip WinUI platform gereği gösterilmez (DevamEt seçimsizken).

### 3. EF concurrency + ölü kod (ÇÖZÜLDÜ)
- `MaliDonemListViewModel`: `_dbGate = new SemaphoreSlim(1,1)` — `RefreshAsync` (okuma) + `BackfillFileSizeAsync` (yazım) aynı kapıdan serileşti → EventId 10100 "second operation started" deseni kapandı (üst üste Refresh dahil). Kapı `ConfigureAwait`'siz (Oturum 78 dersi: `NotifyPropertyChanged` UI thread'inde). Tenant dosya analizi kendi bağlantısını açtığı için kapı dışında, fire-and-forget korundu.
- Ölü kod silindi (AGENTS kural 4): `EnrichTenantDbStatusesAsync` + `AnalyzeAndStampAsync` (0 caller) + `OnDeleteSelection` yorum bloğu + `DeleteItemsAsync` + `DeleteRangesAsync` (NotImplementedException) + `using static DbLoggerCategory`.

### Doğrulama
- `dotnet build` **0 Hata** (18 bilinen uyarı: test CS8625/xUnit1031 + IL2072, yeni yok), `dotnet test` **73/73**, smoke OK (açılıyor + Responding + HWND görünür).
- Canlı görsel (hover basamakları + tooltip'ler + 10100'in kesilmesi) kullanıcıda.

## 2026-09-05 — Oturum 87 (Login tek logo + tooltip/hover soruşturması + Yedekler Dönem içine — ✅🧪)

### Login çift logo (kullanıcı bildirimi — ÇÖZÜLDÜ)
- Üst bar (M + MuhasibPro + adımlar) + sol kart `BrandBannerControl` (AppIcon + Muhasib Pro + sürüm) çift logo → karttaki kaldırıldı; `BrandBannerControl.xaml(.cs)` silindi (tek kullanıcı Login'di, csproj kaydı yok); sürüm mono caption olarak içeriğin altına footer'a taşındı. Drive-by: `Teshis → Teşhis`. Canlı screenshot ile tek logo doğrulandı.

### Tooltip/hover soruşturması (6 probe — SAGA KAPANDI)
- Yöntem: UIA + `SetCursorPos`/`SendInput` hover + screenshot + piksel diff + UIA `ToolTip` araması; DetailsWindow UIA ağacında görünmüyor → `EnumWindows + FromHandle` kalıbı (`.ps1`'e BOM gerekli, PS 5.1 Türkçe desenler için).
- Bulgular: sentetik hover'da piksel diff 0 + UIA tooltip 0 (Login dahil); ScrollViewer içi/dışı fark etmedi; **kontrol: Notepad'de (WinUI3) de aynı** → sentetik girdi bu makinede XAML hover süremiyor, metodoloji geçersiz.
- **Kanıt (probe6): klavye odağı (Tab) tooltip'i açtı** — UIA `ToolTip: 1 [Şifreyi göster / gizle]` + render screenshot'ta görünür → servis + timer + render sağlam; gerçek farede çalışır. Ders `HATALAR.md`'de: XAML hover/tooltip otomasyonla doğrulanamaz, klavye-odak probu kullanılır.
- Gerçek hover açığı (kod-kanıtlı): satır ikon butonlarında yerel `Background` stil hover'ını eziyordu (Yedekle `PrimaryLight→PrimaryLight` = görünmez) → `GhostPillButtonStyle` PointerOver/Pressed'e **1.5px Primary halka** eklendi (zeminden bağımsız geri bildirim). Canlı teyit gerçek fareyle kullanıcıda.

### Yedekler Dönem içine (kullanıcı isteği — ✅🧪)
- `YedeklerPanel` (Arşiv sekmesi) silindi → yeni `DonemYedeklerPanel` Dönem segmentinde (banner → 4 kart → yedekler → parametreler): başlık (ikon tile + `{db} • N yedek` + primary "Şimdi Yedekle") + modern satırlar (ikon tile + mono dosya + meta + "Geri Yükle" + çöp-ikon Sil) + boş-durum kartı. Dönem ComboBox kalktı (bağlam zaten SelectedDonem).
- `DonemIslemKartlariPanel`: Yedek kartı silindi → 4 kart 2×2; ölü `OnYedekClick` silindi. Banner dosya-yok satırı: Kurtar butonu (Arşiv'e gidiyordu) → yönlendirme metni + uyarı ikonu; ölü `OnKurtarClick` silindi.
- Arşiv sekmesi: "Yedekleme Arşivi" → **"Arşiv (N)"**; sayaç `Yedekler+Bilinmeyen` → `ArsivliDonemler+BilinmeyenYedekler` (ikisi de null-güvenli).

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072), `dotnet test` **73/73**.
- Canlı E2E: Dönem render (4 kart + yedek paneli 2 yedek) + Arşiv render (Yedekler yok, sayaç 0) + **"Şimdi Yedekle" tıklama → OS toast "Yedek Alındı" + sayaç 2→3**. Geri Yükle/Sil aynı VM yolu (Oturum 75'te canlı testli), tıklanmadı.

## 2026-09-05 — Oturum 88 (EkranKaydi InventEase dili → Login pilotu — ✅🧪)

### Bağlam
- Kullanıcı: üst klasör `EkranKaydi/` PNG'sindeki (InventEase dashboard) **tasarım dilini** projeye uyarla — fonksiyonellik değil, yalnız görsel dil.
- Kapsam kararları (soru-cevap): **pilot LoginView** + palet birebir + screenshot zemini + **Dark dahil**.

### Piksel örnekleme (System.Drawing, kanıtlı)
- Teal `#40989A` (aktif daire buton) · lime CTA `#EAEE5B` · kapasite `#DEEA74` · zemin `#E7EAEE→#F4F4E6` (soğuk sol-üst → sıcak sağ) · kart `#FFFFFF` · ink `#141414`/`#0C0C0C` · yazı `#101010` · soluk panel `#E1E7E9` · sparkline dolgusu `#C5DFDD`.
- Sol blok taraması: yatay/dikey satırlarda **keskin sınır yok** (örn. `#E1E8E8→#E6EBE9` stepsiz akış) → sol taşıyıcı ayrı opak kart değil.

### Kararlar
- **Global primary değişmedi:** mavi tokenlara dokunulmadı; yeni `MuhasibTeal/Ink/Invent/Sage` ailesi eklendi, yalnız Login kullanıyor. Pilot onayı sonrası global geçiş küçük iş (stiller hazır).
- **Lime/kapasite/sparkline tokenları dashboard'a bırakıldı** (Login'de lime'lık öğe yok → ölü token yasağı, kural 4). Aynı gerekçeyle `InventPillStyle` ve `TealHover` çifti eklendi→silindi (0 ref).
- **Sol taşıyıcı 3 tur:** (1) soluk opak kart → (2) kullanıcı "kart-için-kart" uyarısı + sınır-yok kanıtı → saydam → (3) kullanıcı düzeltmesi **"acrylic anakart + üstte beyazlar, görünürlük artar"** → `InventFrostBrush` (Acrylic, pale tint 0.55) + `InventFrostPanelStyle`. Buzlu kenar zeminle bütünleşik (screenshot'taki gibi).
- **Kart-için-kart kuralı:** buzlu/zemin üstü = beyaz kart; beyaz kart içi = soluk hap (`InventPillMutedStyle`). Ayraç çizgisi yok, kartlar boşlukla ayrılır; bölüm başlıkları small-caps (`SİSTEM DURUMU`, `CharacterSpacing 80`).
- **Aksiyon dili:** "git" = siyah hap (`InventDarkButtonStyle`, şablonlu hover/pressed + Disabled 0.55); teal = marka/vurgu (logo, link, hint). Submit'teki `Button.Resources` override'ları silindi (HATALAR dersi: şablonlu stil).

### Yapılanlar
- `DesignTokens`: Invent ailesi Light+Dark (bg 3-stop + `InventEaseBackgroundBrush` gradyan, InventCard/InventPale, SoftBorder, Teal/Pressed/Light/Border/OnTeal, Ink/Hover/Pressed/OnInk, SageSecondary/Tertiary) + `MuhasibHeroCornerRadius 24` + `MuhasibInventInnerCornerRadius 16`.
- `Cards`: `InventCardStyle` (24) + `InventFrostPanelStyle` (acrylic) + `InventPillMutedStyle`; `Buttons`: `InventDarkButtonStyle`. Paylaşılan eski stillere dokunulmadı (0 etki alanı).
- `LoginView`: gradyan zemin + beyaz logo hapı (teal M) + adım hapı (aktif = siyah hap) + buzlu sol (DB beyaz iç kart + HIZLI GİRİŞ satırları) + beyaz sağ kart. `NamePassword`: soluk hap inputlar + siyah hap submit (metin + `ChevronRight`) + teal demo hint. `QuickLogin`: beyaz satır kartları (16) + teal avatar + teal hover. Tüm `x:Bind`/handler/tooltip aynen.
- Drive-by: 3 hardcode `CornerRadius` (10/10/6) token'a alındı; Login'de `#...` 0.

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072), `dotnet test` **73/73**.
- Canlı render OK (`Temp/opencode/ot88_login.png`): gradyan + buzlu sol + beyaz iç kartlar + siyah hap buton; binding'ler canlı (DB durumu, hatırlanan hesap, kullanıcı adı dolu).
- Açık: Dark görsel + checkbox mavi tiki (sistem accent, polish adayı) + pilot onayı sonrası rollout (FirmaShell/Yönetim/MainShell) ve lime/kapasite tokenları kullanıcıda.

## 2026-09-05 — Oturum 89 (Login: kartlar TEK acrylic kap içine — ✅🧪)

### Bağlam
- Kullanıcı düzeltmesi: "sadece kartları tek bir acrylic kart içine al, resme bakarsan anlarsın" → Oturum 88'de buz yalnız sol paneldeydi; resimde TÜM kartlar tek buzlu tabakada yüzüyor.

### Yapılanlar
- `LoginView`: `ContentGrid` (sol + sağ) `InventFrostPanelStyle` tek `Border` içine alındı; `LeftPanel` buz stili bırakıp saydam iç taşıyıcıya (`StackPanel`) döndü (çift-blur yok). Üst bar + footer gradyanda kaldı. VSM/binding/handler aynen; yeni token/stil yok, ölü kod yok.

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072); test değişikliği yok (XAML-only, 73/73 Oturum 88'de).
- Canlı render OK (`Temp/opencode/ot89_login.png`): tek buz tabaka + üstte beyaz kartlar. Not: hızlı giriş satırı screenshot'ta teal — fare o an satır üstünde park etmiş (gerçek-mouse hover kanıtı, bug değil).

## 2026-09-05 — Oturum 90 (InventEase ANA DİL ilanı + mühür + paylaşılan stiller + Splash refactor — ✅🧪)

### Karar (kullanıcı — loglara işlendi)
- InventEase tüm projenin ana dili; Splash'tan başlayarak tüm Page/UserControl/Dialog/Controls çevrilecek; eski stiller taşınır, kalıntı yasak, hardcode yasak. Recurra yürürlükten kalktı (geçiş referansı). Mod: otonom rollout (kullanıcı izliyor), ekran-ekran doğrulama.
- ROADMAP karar logu + Faz 6.42 (🔨) + KONTROL checklist (10 madde) + OOBE MÜHÜR bölümü yazıldı.

### Envanter
- 43 View XAML + 7 Controls XAML; ~380 eski-token referansı (32 dosya); stil kullanımları: PanelCard 28, Ghost 20, MuhasibPrimaryButton 9, Elevated 7, MuhasibCard 6, Glass 4, PanelMini 3, Radio 2, ModernCard/Primary/Secondary/Danger 1, CompactCard 0.

### Dil mühürleme
- Token ailesi mevcut haliyle mühürlü: `TealHover` iade EDİLMEDİ (0 ref adayı, kural 4 — Ghost Light/Border, primary ink kullanır); lime = spec-only (kapasite `#DEEA74`, CTA `#EAEE5B`, üstü `#101010`, spark `#C5DFDD`), token ilk kullanımda. Mavi tokenlar süpürmede ölecek (erken silme derlemeyi kırar — sıralama notu).

### Paylaşılan stiller (tek noktadan tüm app)
- `Cards`: Invent stilleri dosya başına (BasedOn forward-ref kuralı) + `PanelCard/ModernCard→InventCard` alias + `MuhasibCard→iç kart` (16) + `PanelMini` soluk iç kutu + `GlassPanel→frost` + `ElevatedCard` Invent tokenlar (ölü `ThemeShadow` setter silindi — `Receivers` 0) + `Radio` teal (seçili tint+teal çerçeve, 12→16 token) + `CompactCard` silindi.
- `Buttons`: `InventDarkButton` dosya başına + `PrimaryButtonStyle→InventDark` alias (ölçü override'lı) + `Ghost→teal` (hover tinti + halka + sage yazı) + `MuhasibPrimaryButtonStyle→ink` (hap radius). `Secondary/Danger` aynen (nötr/semantik). Styles'ta mavi 0 (anahtar adı hariç).

### Splash çevirme + detaylı refactoring
- Giydirme: gradyan + `InventCard` + teal logo/progress + teal % hapı + soluk rozet + sage yazılar; `CornerRadius 13→12` token.
- Kaçanlar (kullanıcı uyarısı): `On→Ön Muhasebe`, `Baslatiliyor→Başlatılıyor…`, ASCII yorumlar, ölü `xmlns:local` (şimdi kullanılıyor), mesaj hayaleti.
- **Mesaj hayaleti kök neden:** çift-TextBlock crossfade + yarışan async geçişler (yarım kalmış yarı-saydam durumlar). Fix: `SplashStatusControl` (tek blok, sıralı sön→değiştir→belir 120/180ms + sıra no guard + `Stop()` determinizmi) + `MinHeight 28` (wrap kırpılma fix'i).
- **God-class bölünme (339 satır):** `SplashNavigator` (hedef+frame+tema, ~85) + `SplashStatusControl` (~45) + sayfa ~125 (şablon yorumu/özet/`StatusMessages`/`ApplicationName`/scaffolding yorumları/ESKİ-YENİ bloğu/`Mark` sarmalı silindi; bildirim `DialogHelper`'a alındı 35→20 satır; `Debug.` normalize; gereksiz `Notify` + 6 using silindi).
- **Eklemeler (tasarım uzmanı):** karta yumuşak `ThemeShadow` (`Translation 0,0,32`, `Loaded`+try/catch) + kart giriş animasyonu (400ms fade+rise, `From`-only — çalışmazsa kart görünür kalır).

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072), `dotnet test` **73/73**.
- Canlı: `ot90_splash2.png` (gölge + Ö + hayaletsiz mesaj + %10) + `ot90_login2.png` (regresyon temiz, Navigator Login'e ulaştırdı).
- Notlar: hızlı mesajlarda ~120ms metin boşluğu (normal, hayalet değil); Teşhis'te odak halkası (odak davranışı, önceden var); ProgressBar köşe yarıçapsız (WinUI şablonu, ekran-kanıtlı sorun yok).

## 2026-09-05 — Oturum 91 (Splash buz anakart + header sökümü + SistemKurulum çevirme — ✅🧪)

### Splash revizyonu (kullanıcı: "buz üstünde beyaz hub her yerde")
- Desen adı mühürlendi: **"buz üstünde beyaz hub"**. Splash tek beyaz kart → buz anakart + logo/başlık (buz üstü) + progress beyaz kartı (bar+% hapı yatay satır) + rozet beyaz hapı. Gölge/giriş buz kapıya taşındı (isimler aynı, kod 0 değişiklik).
- Düzeltme (kullanıcı: "animasyonlu texte beyaz hub kullanma, çirkin"): durum yazısı buz üstünde çıplak → mühür kuralı olarak işlendi.
- Araç notu: pencere arkadaysa fullscreen kare boş çıkar — `SetForegroundWindow` + öne alım şart; `PrintWindow` alternatifi hazır (`ot91_pw.png` kanıtı).

### Header sökümü (kullanıcı: "SK ve diğer pagelerdeki header olayını kaldır")
- Mimari tespit: `MainWindow` bare-Frame + `ExtendsContentIntoTitleBar` (sistem caption zaten var); `ShellTitleBar` yalnız 2 sayfada (SK + FirmaShell) — Login/Splash'sız yaşıyor. Çift-krom (sistem + özel) + `SetTitleBar(null)` karmaşası bitti.
- SK: header bloğu silindi (logo/başlık/subtitle/sürükleme/özel tuşlar) + `xmlns:ui` + code-behind `SetTitleBar` temizliği (caption tema çağrıları korundu); durum hapı hero'ya, "Giriş Ekranına Git" içerik altına (ink hap) taşındı. `ShellTitleBar` dosyaları FirmaShell turunda silinecek (son kullanıcı).
- Kapsam: pencere-düzeyi `DetailsWindow`/`ShellView AppTitleBar` (sürükleme) + Login üst barı (akış göstergesi) korunur; MainShell `TitleBarControl` kendi turunda.

### SistemKurulum çevirme (4 dosya, 0 kalıntı)
- View: gradyan + TEK buz kap (hero + 2 kolon + aksiyon) + teal tint haplar + solid teal görsel + daire tikler + terminal konsolu (Inner 16); tam-dosya temiz yazım.
- `DatabaseInfoPanel`: beyaz kartlar + soluk iç kutular + daire ikonlar (teal) + `Analiz Et→Ghost` + ink `Kurulumu Başlat` + banner aksanları (`MuhasibAccentCornerRadius` 16,0,0,16 yeni token) + `ProgressBar CornerRadius 3` hardcode silindi. `OnPrimary` eşleşme kuralı: ink→OnInk, teal→OnTeal, beyaz-sabit/terminal→OnPrimary (Dark-güvenli).
- `SystemTestsPanel` + `QuickSistemDbDiagDialog` (dialog `ThemeResource` disiplini korunarak) + code-behind 1 satır (`Tertiary→SageTertiary`); semantikler aynen.
- Tarama: mavi 0 (stil anahtar adı hariç) + `#` 0 + sayısal radius 0.

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072).
- Canlı: UIA zinciri (`TeshisButton` AutomationId + `Detayl?*` Invoke — `shot_sk.ps1` kalıcı) → `ot91_sk.png`: headersız frost + hero 3 hap + metrikler + testler + canlı log, veriler canlı.

## 2026-09-05 — Oturum 92 (FirmaShell çevirme + ShellTitleBar silme — ✅🧪)

### Yapılanlar
- **View:** header bloğu silindi + frost kap (InfoCard + 2 panel) + `UserInfoControl` header'dan InfoCard sağına (Yeni Firma yanı) + teal ikon/progress + ink butonlar; code-behind `SetTitleBar` temizliği + ölü `OnLogoutClick` silindi + şablon yorumu/özet.
- **FirmalarList:** `FirmaActionButtonStyle→Ghost` (stil silindi) + teal sayfalama (sayaç/search) + solid teal FirmaNo + ink "Aktif Seçim" + beyaz dönem hapları + teal ghost aksiyonlar + daire ikonlar; Tertiary/Secondary süpürme.
- **MaliDonemlerList:** `DonemActionButtonStyle→Ghost` (silindi) + teal sayaç (çerçeveli) + ink butonlar + soluk boş-durum + daire yıl rozeti + ink "Aktif Seçim" + hap rozetler (Kapalı/Açık/DB durumu) + teal Yedek.
- **UserInfo:** soluk chip + teal avatar + soluk halka + teal rol hapı + daire köşeler.
- **ShellView:** gradyan kök + beyaz sürükleme şeridi (işlev korunur) + daire geri butonu + solid teal logo + `White` hardcode→token. `ShellStatusBar` temiz. **`ShellTitleBar.xaml(.cs)` silindi** (0 ref).
- Tarama: mavi 0 (stil anahtarı + Dialog turu hariç) + `#` 0 + sayısal radius 0.

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072).
- Canlı: `shot_fs.ps1` (şifre kutusu temizle-yaz + Enter — ilk deneme üstüne-yazma + QuickLogin Invoke no-op dersleri) → `ot92_fs.png`: frost + InfoCard (kullanıcı chip + siyah buton) + seçili teal kartlar + ink haplar + `Çalışma Alanına Geç`, veriler canlı.

## 2026-09-05 — Oturum 93 (MaliDonemYönetim çevirme: view + 9 panel + Details — ✅🧪)

### Yapılanlar
- **View:** gradyan kök + başlık gradyanda + sekmeler/içerik TEK buz kap + teal sekme VSM + ink/Ghost aksiyonlar; tam-dosya temiz yazım.
- **9 panel:** Hero (teal VSM + solid logo + çerçeveli hap) · Banner (ink yıl + hap rozetler + Ghost Uyarı-uyumlu) · GenelBakış (KPI teal ikonlar + ink toplu buton + hap arama + teal/soluk yıl rozetleri + tintli satır butonlar) · DerinAnaliz (ink değerler + ink çalıştır + teal ring/bar) · Yedekler (teal başlık + ink CTA + soluk satırlar + daire ikon) · Kartlar (4 beyaz + teal ring) · Parametre (soluk iç kutular) · Arşiv/Bilinmeyen (**buz üstü BEYAZ satırlar** — hap kuralı uygulaması).
- **Details (522 satır legacy):** `White`×5→token + aksanlar teal (başlık ink) + daire tikler + iç kutular 16 + `CornerRadius 2` silme + Grid sadeleştirme + ölü 2 xmlns silme; sistem nötr/semantikler korundu.
- Uzman dokunuşları: büyük değerler ink (ekran-dili), `ProgressRing`/`TextBox` sistem-aksan kaçakları kapatıldı (teal/hap), hayalet `CornerRadius 3` barlar temizlendi.
- Tarama: mavi 0 + `#` 0 + sayısal radius 0. Build **0 Hata** (1 bilinen IL2072) — ara `WMC9997` (`>` düşürme, anında fix).

### Doğrulama
- Canlı: `shot_yonetim.ps1` (giriş + `Mali D?nem *lemleri` Invoke + EnumWindows başlık-bulma — kalıcı) → `ot93_yonetim.png`: chrome + frost + hero + KPI + bant + hap arama, veriler canlı.
- Not (fonksiyon, stil değil): sekme/segment aktif-vurgusu görünmüyor — VSM tetikleme şüphesi; KONTROL 6.35 ⬜ maddesiyle aynı konu, renkler doğru yerde.

### Görünüm ve akış modeli (Oturum 96'da derlendi — bu sayfanın bütünsel kaydı)

**Görünüm şeması** (`MaliDonemYonetimView.xaml:40-154`):
- `DetailsWindow` (ShellView kapsayıcı) içinde `Page`: gradyan kök → başlık satırı (sol: "Veritabanı & Yedekleme Motoru" + açıklama; sağ: `Yeni Dönem Aç` ink + `Bütünlük Kontrolü` ghost) → içerik **TEK buz kap** (`InventFrostPanelStyle`).
- Buz içinde: 2 sekme (`Tenant DB Yönetimi & Analizi` | `Arşiv (N)` — Global DB sekmesi YOK, karar) + Tenant altında 3'lü segment (hero içinde: Tümü | Döneme Özel | Analiz).
- Segment görünümleri: **Tümü** = `TumuHeroPanel` + `GenelBakisPanel` (4 KPI + toplu bant + 7 kolon tablo); **Dönem** = `DonemBannerPanel` (yıl + Combo + Sil) + `DonemIslemKartlariPanel` (4 kart 2×2, Yedek yok) + `DonemYedeklerPanel` + `DonemParametrePanel` (Expander, kapalı); **Analiz** = `DerinAnalizPanel` (2 skor + dağılım tablosu) + `ANALİZ EDİLEN DÖNEM` kartı; **Arşiv** = `ArsivPanel` + `BilinmeyenPanel` (buz-üstü-beyaz satırlar).
- VSM: `SekmeStates` (TenantAktif/ArsivAktif teal) + hero `SegmentStates` — renkler SADECE XAML'de, code-behind yalnız `GoToState` (Oturum 81 dersi). `GenelBakisPanel` Visibility'si SARMALAYICI `StackPanel`'de (satır 140 — DataContext alt VM'de, `IsTumuSegmenti` orada yok; Oturum 80 dersi).

**DataContext dağıtımı** (`.xaml.cs:26-46`): sayfa `MaliDonemYonetimViewModel`; Hero/Banner/Kartlar/Parametre/Analiz → ana VM; `DonemYedekler→YedeklerVM`, `Arsiv→ArsivVM`, `Bilinmeyen→BilinmeyenVM`, `GenelBakis→GenelBakisVM` (+ `Root=ana VM` host). `Subscribe/Unsubscribe` + `PropertyChanged→GuncelleSekmeDurumu`.

**VM orkestrasyonu** (`MaliDonemYonetimViewModel : ViewModelBase, IMaliDonemListHost`): paylaşılan `MaliDonemList` (tek instance) + 4 alt VM (Yedekler/Arsiv/Bilinmeyen/GenelBakis) + `SelectedFirma/SelectedDonem` + `AktifSekme/AktifSegment` + sayaç başlıkları (`Tümü (N)`, `Arşiv (N)`) + `DerinAnalizYukleAsync/BakimCalistirAsync/RefreshAllAsync`.

**Akış modeli:**
1. Açılış: FirmaShell firma kartı `Mali Dönem İşlemleri` → `DetailsWindow` (`MaliDonemYonetimArgs`) → panellere DataContext + `LoadAsync` → Tenant/Tümü.
2. Tümü→Dönem: satır `Yönet` → `AktifSegment=Dönem` + `SelectedDonem` set (+ `EsitleListeSecimi` Combo senkronu; null-guard `IsDonemSeciliDegil` barı).
3. Dönem: banner Combo (`TwoWay SelectedDonem`) tüm sayfayı günceller; Yedekle→OS toast+sayaç; Sil→`DeleteGuard` (onay kodu=yıl)→refresh; dosya-yok→Kurtar (yedek listesi→restore); Arşivle/Arşivden Çıkar→durum+bayrak.
4. Yeni Dönem: `YeniDonemDialog` (sayı+açıklama, EnterOnay)→`SagaPipelineDialog` (5 adım canlı)→`SagaCompleted→RefreshAllAsync`.
5. Analiz: `DerinAnalizYukleAsync` (PRAGMA okumaları) → 2 skor kartı + oranlı dağılım tablosu.
6. Arşiv: `ArsivVM/BilinmeyenVM` + sekme sayacı (`Arsivli+Bilinmeyen`).
- **Otomasyon kanıtı (Oturum 96):** giriş → firma seç (`SelectionItemPattern`) → `Mali Dönem İşlemleri` Invoke → `EnumWindows` başlık-bulma (`*Mali D?nem*`, DetailsWindow UIA'da görünmez) → `Yönet` → Combo Expand + 2027 `Select` → `DonemSilButton` (`AutomationId`) → DeleteGuard kırmızı → ESC (silme yapılmadı). Scriptler: `shot_del96.ps1` (tam zincir), `shot_dumpd96.ps1` (buton dump).

## 2026-09-05 — Oturum 94 (MainShell çevirme + TitleBarControl silme — ✅🧪)

### Kararlar (tasarım uzmanı)
- **`TitleBarControl` silindi** (header yasağı + tam fonksiyon-tekrarı: logo/firma/dönem/WAL/ayarlar breadcrumb+sidebar+tenant kartında var; 2 ölü butonsuz buton içeriyordu). `MainShellView` tam-dosya temiz yazım.
- Ölü çökmüş `NavigationView` (Collapsed 20 satır) silindi; ölü zil ikonu silindi; breadcrumb firma tekrarı atıldı ("Genel Bakış" yalnız).
- Placeholder veriler (`ÖRNEK...`, `Ömer Korkut`) BAĞLANMADI — VM'de karşılık yok, kural 5 zinciri Faz B işi (KONTROL notu). Ölü logout/zil/Snapshot Faz B wire-up bekliyor (görseller duruyor).

### Yapılanlar
- View: gradyan + beyaz sidebar + yüzen başlık satırı + buz kap (tenant kartı + frame) + teal Snapshot Ghost + teal tenant döşemesi.
- Sidebar: beyaz ray + soluk tenant/kullanıcı kutuları + daire avatarlar + **ink aktif buton** (`InventDark` + 12 override) + teal RBAC.
- Tarama: mavi 0 + `#` 0 + sayısal radius 0. Build **0 Hata** (1 bilinen IL2072).

### Doğrulama
- Canlı: `shot_main.ps1` (giriş + `DevamEtButton` Invoke — kalıcı) → `ot94_main.png`: sidebar + yüzen başlık + buz + tenant kartı + placeholder.

## 2026-09-05 — Oturum 95 (Controls + Dialog içerikleri + dialog buton gizemi [AÇIK] — 🔨 kısmi)

### Controls (7 dosya — TAMAM ✅)
- Envanter: `DataList/DataGrid/Details/DetailToolbar/ListToolbar/Search/CustomContentDialog`. Hepsi canlı (`DataList`→2 legacy view, `Details`→2 details, toolbarlar içeriden kompoze, `CustomContentDialog`→`DialogService`, `DataGrid` Faz B altyapısı — SİLİNMEDİ).
- Değişiklik: `CustomContentDialog` 6 hardcode hex→token (Success/Danger/Warning/Teal×3) + Hero 24 + beyaz zemin + iç radius token; `DataList` aksan→teal. Geri kalan sistem-nötr (temiz). Köşe taraması 0.

### Dialog içerikleri (4 dosya — TAMAM ✅)
- `SagaPipeline`: Hero 24 + beyaz zemin + iç kartlar 16 + daire rozet + teal footer + sage yazılar; `SagaStepStatusToBrushConverter` teal/soluk eşleşmesi (Pending soluk, InProgress teal tint); code-behind `Primary→Teal` string.
- `DeleteGuard`/`YeniDonem`: Hero 24 + beyaz zemin + iç kutular 16 + Info banner (kenarlık `BloomBlue→InfoBrush`) + soluk hap inputlar (pill radius, `NumberBox` dahil — derlendi) + sage yazılar.
- `YeniFirma`: Hero 24 + beyaz zemin + teal bölüm çipleri (hap radius); `MuhasibCardStyle` aliası yerinde bırakıldı (doğru hedef).
- **Ölü silme:** `YeniMaliDonemDialog.xaml(.cs)` — `YeniDonemDialog` ile birebir aynı + 0 kullanım → silindi.
- **Form hardcode'ları:** `FormAutoSuggestBox/FormCalendarDatePicker/FormComboBox/FormTextBox` doğrulama-hata kırmızısı (`FromArgb 211,47,47`) → `MuhasibDangerBrush` resource okuma (4 dosya, `QuickDiag` emsali).

### Dialog buton gizemi (AÇIK ❓ — içerik mavisi değil, chrome buton mavisi)
- Gözlem (`ot95_dialog.png`): içerik çevrili; ama `PrimaryButtonText` butonu ("Saga Pipeline Başlat") sistem MAVİSİ render oluyor.
- Denenenler (hepsi `YeniDonemDialog` üstünde, hepsi derlendi):
  1. XAML `PrimaryButtonStyle="{StaticResource InventDarkButtonStyle}"` (4 dialoga) → etkisiz, mavi sürüyor.
  2. Code-behind `Loaded` + `PrimaryButtonStyle = InventDarkButtonStyle` → etkisiz, mavi sürüyor.
  3. Code-behind `Loaded` + `PrimaryButtonStyle = DangerButtonStyle` → **KIRMIZI render oldu** (mekanizma çalışıyor!).
- Sonuç: özellik/mekanizma sağlam; **`InventDarkButtonStyle` (şablonlu) dialogda uygulanınca mavi düşüyor, şablonsuz `DangerButtonStyle` uygulanıyor.** Şablonun dialog kapsamındaki davranışı çözülemedi (teoriler elendi: key çakışması yok, `TargetType` doğru, `RequestedTheme` Light=app Light, Login'de AYNI stil siyah render oluyor).
- **Güvenli durum:** deney artığı (`Loaded` hack) GERİ ALINDI; XAML `PrimaryButtonStyle` öznitelikleri yerinde duruyor (etkisiz ama zararsız, niyeti belgeliyor). Derleme **0 Hata**. Yapı bozulmadı.
- Sonraki oturum seçenekleri: (a) Live Visual Tree ile butonun `Style`/`Background`/`Template` değerlerini çalışma anında okuma (kök neden), (b) dialog-butonlarına özel setters-only ink stili deneme, (c) içerik-içi özel butonlara geçme (`QuickDiag BackupButton` emsali — ama `result==Primary` akışı `Accepted` bayrağına taşınmalı).

### Süreç dersleri
- `MSB3027/3021` (exe kilidi): çekişen `MuhasibPro` prosesi öldürülmeden derleme yapılmaz — otomasyon başına kill şart.
- `Yeni Firma` penceresi yavaş açılıyor (5 sn EnumWindows kaçırdı, sonradan görüldü) + aynı-başlıklı pencereler hashtable'da çakışır — script beklemeleri 8 sn + HWND listesi.
- `Write` ile `.ps1` + Türkçe literal = PS 5.1 mojibake → ASCII joker (`?`, `*`) kalıbı (`shot_*.ps1` hepsinde).

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072). Dialog içerik + kırmızı-test karesi `ot95_dialog.png`.
- Kalan (sonraki oturum): buton gizemi + legacy `Firmalar/Firma` 8 dosya + süpürme (mavi token ölümü + Dark + checkbox) + 6.42 kapatma.

## 2026-09-06 — Oturum 96 (Yarım oturum tamamlama + dialog buton gizemi KAPANDI — ✅🧪)

### Yarım oturum tespiti (loglanmamış iş)
- `docs` son yazım 00:48, `Views/Styles` 14:11-14:56 arası 20+ XAML değişmiş ama kayıt yok → `forfiles` ile tespit. İçerik: `FirmaDetails.xaml` kökü çevrilip içi yarım bırakılmış + stil rötuşları.

### Stil inceleme (taramalı, kanıtlı)
- Temiz: `RequestedTheme` 0, `#` hardcode 0 (Views+Controls), ölü `Panel*/Widget*` referans 0.
- Bug (2): `Style="{ThemeResource ...}"` ile stil verilemez → `YeniFirmaDialog.xaml:36` (`MuhasibCardStyle`) + `QuickSistemDbDiagDialog.xaml:127` (`MuhasibPrimaryButtonStyle`) `StaticResource`'a çevrildi.
- `FirmaDetails` yarım çevirimi bitirildi: 2 bölüm çipi `Primary*`→teal + `CornerRadius 8`→hap token + (kullanıcı uyarısıyla) fırçalar `StaticResource`→`ThemeResource` (Dark'ta güncellenme kuralı). Final: Primary 0 + sayısal radius 0 + hardcode 0.

### Dialog buton gizemi — KÖK NEDEN BULUNDU + KAPANDI
- Kanıt zinciri (`shot_dlg96.ps1` + piksel + probe dosyası): XAML `PrimaryButtonStyle` property'DE duruyordu (`setters=10`, null değil) ama render mavi `#3285CC` → stil atanmış ama UYGULANMIYOR.
- Kök neden: **`DefaultButton="Primary"`** — WinUI default butona `AccentButtonStyle` basıp `PrimaryButtonStyle`'ı eziyor. Kaldırılınca setters-only stil uygulandı (siyah hap, gözle doğrulandı). DeleteGuard'da `DefaultButton="Close"` olduğu için Vazgeç mavi + Dönemi Sil kırmızı — teori %100 doğrulandı.
- Çözüm: `InventDialogPrimaryStyle`/`InventDialogDangerStyle` (setters-only, Buttons.xaml) + 3 dialogda `DefaultButton="Primary"` kaldırıldı (YeniDonem/YeniFirma/SagaPipeline); DeleteGuard'da Close default korundu (Enter=iptal güvenli varsayılan). YeniDonem'de Enter=onay `KeyDown→Hide()+EnterOnay` bayrağı + 2 caller koşulu ile korundu.
- Canlı: YeniDonem ink ✅ + Enter→pipeline ✅ + SagaPipeline ink ✅ + DeleteGuard kırmızı ✅ (`ot96_dialog/enter/deleteguard.png`).
- Yönetim otomasyon zinciri: giriş→firma seç→`Mali Dönem İşlemleri`→EnumWindows→`Yönet`→Combo 2027→Sil→DeleteGuard→ESC (`shot_del96.ps1`; ayrıntı Oturum 93 ekinde).

### FirmaCard çevirme (kullanıcı kararı) + YeniFirma kırığı
- `Yeni Firma` penceresi boş açılıyordu → logda `XamlParseException: Cannot find Resource CardPictureRadius` (ölü temizlikte silinmiş, `FirmaCard` 5 yerde istiyordu).
- `FirmaCard` InventEase'e çevrildi: 68 sayısal boyut + teal overlay/border + teal hap + fırçalar ThemeResource + 24px daire ikonlar; binding/yapı aynen. Pencere açıldı, kart+form mühürde render (`ot96_yenifirma.png`).
- `YeniFirmaDialog` (ContentDialog): 0 caller/0 XAML ref/0 kayıt → ÖLÜ (gerçek akış DetailsWindow+FirmaDetails) → kural 4 ile silindi (2 dosya).
- `DonemBannerPanel` Sil butonuna `AutomationProperties.Name="Dönemi Sil"` (UIA erişilebilirliği).

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072), `dotnet test` **73/73**, smoke OK (hwnd≠0, Responding).
- Kalan: legacy 6 dosya (`FirmaView/FirmalarView/FirmaMaliDonemler/FirmalarDetails/FirmalarCard/FirmalarList` — el değmemiş) + süpürme (Dark görsel + checkbox accent) → 6.42 kapanışı.

## 2026-09-06 — Oturum 97 (GorunumTest kayıtları + tooltip fare-hOLOGI + WindowTitle + segment VSM — ✅🧪)

### GorunumTest incelemesi (6 kayıt, `EkranKaydi/GorunumTest/`)
- Referans (`Ekran görüntüsü 2026-09-05 231934.png`, InventEase dashboard) ile karşılaştırma: buz kap + beyaz kart + hap + teal + ink CTA + small-caps + daire ikonlar — dil tutarlı ✅.
- Bug değil: kırmızı X (hover anı, sistem kromu normal), `4 Tablo • 2 Kayıt` (analiz verisi), boş Yetkili/İletişim (veri yok), çökmüş dönem kartları (Unchecked soluk zemin, tasarım).
- Bug (3): WindowTitle beyaz şeridi + segment/sekme aktif vurgusu yok + ikon-buton fare-tooltip'i yok (aşağıda).

### Tooltip fare-hover soruşturması (kullanıcı: "çözmüştün, tekrar oldu — stil çakışması?")
- Ayırıcı test (`shot_tip97.ps1`): satır `Yönet` butonuna `SetFocus` → UIA `ToolTip: 1 [Yönet]` AÇILDI → servis+tanım+render sağlam (Oturum 87 ile tutarlı). Blok taraması: yönetimdeki TÜM buton bloklarında `ToolTip` tanımlı. Yani sorun tanımda değil, fare-hover hattında.
- Hipotez (kod-kanıtlı): Oturum 87'de eklenen 1.5px hover halkası `BorderThickness` değiştiriyordu → fare sabitken buton BÜYÜR → enter/exit döngüsü → `ToolTip` timer'ı sürekli sıfırlanır → hover tooltip'i HİÇ açılmaz. Tıklama + odak etkilenmez (gözlemle birebir uyumlu).
- Fix: `GhostPillButtonStyle` PointerOver/Pressed'te ÖLÇÜ değişimi kaldırıldı (yalnız renk: zemin tinti + border teal; kalınlık hep 1 sabit) + 4 satır butonundaki `BorderThickness="0"` yereller silindi (tint `Background`ler korundu). Odak probu regresyon temiz.
- ⬜ Fare teyidi kullanıcıda: satır ikon butonunda (Yedekle/Durum/Analiz/çöp) fareyi 1-2 sn sabit tut.

### WindowTitle beyaz şeridi (kullanıcı bildirimi)
- `ShellView AppTitleBar` beyazdı (`InventCardBrush`) → gradyan (`InventEaseBackgroundBrush`, hairline korunur, sürükleme korunur) + şeritteki 6 `StaticResource` fırça → `ThemeResource` (Dark kalıntısı). Canlı doğrulandı (`ot97_segment.png`).

### Segment/sekme aktif vurgusu (Oturum 93'ten açık)
- Probe kanıtı: `GuncelleSegmentGorunum state=TumuAktif ok=False loaded=False` → `GoToState` görsel ağaç bağlanmadan çağrılıyordu (Root setter `OnNavigatedTo`'da). Sonradan değişiklik olmadığı için vurgu hiç uygulanmıyordu.
- Fix: `TumuHeroPanel` + `MaliDonemYonetimView` ctor'larına `Loaded => Guncelle*Durumu()` (gölge-receiver kalıbı). Canlı: Tümü segmenti + Tenant sekmesi teal vurgulu (`ot97_segment.png`).
- Kural (HATALAR.md): VSM `GoToState` ilk çağrısı `Loaded`'da yapılır (ctor/navigate erken).

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072), `dotnet test` **73/73**.
- Kalan: fare-tooltip teyidi (kullanıcı) + legacy 6 dosya + süpürme (Dark + checkbox) → 6.42 kapanışı.

## 2026-09-06 — Oturum 98 (Login kart-dışı bölüm başlığı + 3. referans stili — ✅🧪)

### İstek (kullanıcı)
- `LoginView` içindeki header'lar beyaz kart içinden çıkarılsın + `EkranKaydi/` 3. resimdeki başlık stili eklensin.
- Referans (`Ekran görüntüsü 2026-09-06 172057.png`): gri küçük üst satır ("Renewal Reminder") + kalın siyah büyük başlık ("Lease Expiration Notice").

### Tespit
- Beyaz kart İÇİNDEKİ tek header: `NamePasswordControl` "Kullanıcı Girişi" bloğu (sağ panel beyaz kartın içindeydi). Sol başlıklar (`SİSTEM DURUMU`/`HIZLI GİRİŞ`) zaten buz üstünde kart dışındaydı — ellemedi (ikincil bölüm etiketi; referans dashboard'da da büyük/küçük başlık karışık).

### Yapılanlar
- `Styles/Cards.xaml`: `InventSectionEyebrow` (Body 14 + sage tertiary) + `InventSectionTitle` (Display 24 Bold + **ink `#141414`** — referans pikseliyle birebir) TextBlock stilleri (`ModernSectionHeader` yanına; fırçalar ThemeResource, boyutlar ilk kez `MuhasibFontSize*` token'larından).
- `NamePasswordControl.xaml`: kart-içi header bloğu silindi (uzun açıklama satırı referans 2-satır desenine uymadığı için düştü).
- `LoginView.xaml`: `RightPanel` Border→StackPanel (başlık buz üstünde + form beyaz kartta; VisualState `Grid.*` setter'ları aynen çalışır, sol panel yapısıyla simetrik). Başlık: eyebrow "Kimlik Doğrulama" + title "Kullanıcı Girişi".
- `OOBE-TASARIM-SABLONU.md`: kart-dışı başlık deseni mühür bölümüne işlendi.

### Doğrulama
- `dotnet build` **0 Hata** (1 bilinen IL2072), `dotnet test` **73/73**, `ot98_login.png`: eyebrow+title buzda + form kartta, binding'ler canlı.
- Kalan (6.42 kapanışı): fare-tooltip teyidi (kullanıcı) + legacy 6 dosya + süpürme (Dark + checkbox).

### GERİ ALMA (kullanıcı isteği — aynı oturum)
- Kullanıcı önce formun genişletilmesini istedi (`MaxWidth 1060→1160`, kolonlar `1.2*/1*→*/*`, form `380→440` — build 0 hata, render OK), ardından "loginView içindeki yaptığın değişiklikleri geri al" dedi (2× mesaj).
- Oturum 98'in TÜM Login değişikliği geri alındı: `LoginView.xaml` (orijinal kolon/MaxWidth/kart-içi yapı) + `NamePasswordControl.xaml` (header bloğu + 380 geri) + `Cards.xaml` (`InventSectionEyebrow/Title` silindi, ölü referans 0) + `OOBE-TASARIM-SABLONU.md` kart-dışı başlık maddesi silindi.
- Doğrulama: build **0 Hata**, `ot98_login.png` güncel (orijinal Login render). KONTROL/ROADMAP 6.42 satırları geri alındı; net kod değişikliği YOK.

## 2026-09-06 — Oturum 99 (Cards Custom* stilleri + Splash/Login/FirmaShell background — log kaydı — ✅🧪)

### Bağlam (kullanıcı — log dışı değişiklik)
- Kullanıcı: "log kayıtlarında olmayan bazı değişiklikler yaptım. DesignToken ve card stillerini incele. Splash, login, firmaShell tarafının background rengini değiştirdim." → `Cards.xaml` ile 3 sayfada log dışı ek bulundu, inceleme yapıldı. Karar: yeni stiller **silinmeyecek**, kullanım şekli loga işlenecek; duplicate varsa ayrı ele alınacak.

### Tespit — Cards.xaml (246 satır, 15 stil + 7 token)
- **Mevcut Invent ailesi (korundu):** `InventHeroCardStyle:11` (24, hero), `InventCardStyle:20` (16, iç), `InventFrostPanelStyle:30` (acrylic frost), `InventPillMutedStyle:39`, `MuhasibCardStyle:48` (Invent 14 alias), `ModernCard:53` (Hero 24 alias), `MuhasibCardRadioButtonStyle:56`, `GlassPanel:167` (frost aliasi, padding 10), `ElevatedCard:176` (Grid, 24, InventCard).
- **Yeni eklenen Custom* ailesi (log dışı, 5 stil):**
  - `CustomModernCard:185` (Border; `CardBackgroundFillColorDefaultBrush` + `CardStrokeColorDefaultBrush` + `CardCornerRadius 16` + `CardPadding 10`, Margin 0,0,0,12)
  - `CustomCompactCard:194` (Border; `CardBackgroundFillColorSecondaryBrush` + `CardStrokeColorDefaultBrush` + `CompactCardCornerRadius 6` + `CompactPadding 12`, Margin 0,0,0,8)
  - `CustomGlassPanel:203` (Border; `LayerOnAcrylicFillColorDefaultBrush` + `CardStrokeColorDefaultBrush` 0.5 + `CardCornerRadius 16` + padding 10)
  - `CustomElevatedCard:210` (Border; `AcrylicBackgroundFillColorDefaultBrush` + `CardBackgroundFillColorTertiary` + `CardCornerRadius 16` + `CardPadding 10`, `ThemeShadow`)
  - `CompactElevatedCard:225` (Grid; `CardBackgroundFillColorSecondaryBrush` + `CompactCardCornerRadius 6` + `CompactPadding 12`, `ThemeShadow`)
- **Yeni tokenlar (3 ölçü + 4 alias):** `CardPictureRadius:239` (68), `CardCornerRadius:240` (16), `SmallCardCornerRadius:241` (8), `CompactCardCornerRadius:242` (6), `CardPadding:243` (10), `SectionPadding:244` (16), `CompactPadding:245` (12).
- **Fırça kaynağı farkı:** Invent ailesi `MuhasibInvent*`/`InventFrostBrush` (DesignTokens Light+Dark ThemeResource); Custom ailesi WinUI sistem fırçaları (`CardBackgroundFillColor*`/`LayerOnAcrylic*`/`AcrylicBackground*` + `CardStrokeColorDefaultBrush`) — `XamlControlsResources` üzerinden tema-reaktif, Invent paletinden bağımsız.
- **Duplicate analizi (kullanıcı talimatı: silme yok, duplicate ayrı):**
  - Anahtar (x:Key) çakışması **0** — tüm key'ler tekil.
  - İşlevsel çakışma: `GlassPanel:167` (InventFrostBrush) vs `CustomGlassPanel:203` (LayerOnAcrylic) — aynı "cam panel" rolü, **farklı fırça** (acrylic pale tint vs LayerOnAcrylic) → duplicate değil, varyant olarak korundu.
  - `ElevatedCard:176` (Grid, InventCard, gölgesiz) vs `CustomElevatedCard:210` (Border, Acrylic, gölgeli) vs `CompactElevatedCard:225` (Grid, Secondary, gölgeli) — farklı TargetType/ölçü/fırça → korunacak.
  - `InventHeroCardStyle` (24) vs `CustomModernCard` (16) vs `InventCardStyle` (16) — radius 24/16 varyantları bilinçli → silinmedi.
  - Sonuç: **silme yapılmadı**; ihtiyaç halinde `VIEW-PACKAGE-DURUMU.md` varyant tablosuna taşınabilir.

### Tespit — Background (DesignTokens + 3 sayfa)
- `DesignTokens.xaml:136` `InventEaseBackgroundBrush` (3-stop `MuhasibInventBgStart #E7EAEE → Mid #EFF2EA → End #F4F4E6`, Light; Dark `#12150F→#171B14→#1E221A`) — InventEase ANA DİL zemini; `195` `AppBackgroundBrush` `ImageBrush ms-appx:///Assets/background_muhasib.png` (eski OOBE mavi resim) hala mevcut.
- Mevcut kullanım:
  - `SistemKurulumView.xaml:10,18` `InventEaseBackgroundBrush` — doğru (referans).
  - `MainShellView:10,12` + `MaliDonemYonetimView:12,74` + `ShellView:11,22` `InventEaseBackgroundBrush` — doğru.
  - `ExtendedSplash.xaml:11,35` `AppBackgroundBrush` — **log dışı bırakılmış** (kullanıcı "değiştirdim" dediği halde hala resim; SplashCard `CustomElevatedCard` ile acrylic).
  - `LoginView.xaml:11,17` `AppBackgroundBrush` + anakart `CustomElevatedCard:54` + header haplar `CustomModernCard:35,41` — **log dışı**.
  - `FirmaShellView.xaml:11,18` `AppBackgroundBrush` + frost `CustomGlassPanel:116` + InfoCard `CustomModernCard:120` — **log dışı**.
- `AGENTS.md:38` + `OOBE-TASARIM-SABLONU.md:52` InventEase mührüne göre `AppBackgroundBrush` (resim) OOBE sayfalarında kullanılmaz; 3 sayfa InventEase'e geçmeli — bu oturumda **kod değişikliği yok**, yalnızca tespit loglandı (kullanıcı "silme" talimatı Custom* için verildi, background için sonraki oturumda decide).

### Kullanım şekli (Custom* stillerinin tek tek)
- `CustomModernCard` → **hap kart / pill card**: `LoginView:35` logo hapı (M + MuhasibPro, BorderThickness 1), `LoginView:41` adım göstergesi kabı (Kurulum/Giriş/Firma Seçimi/Çalışma Alanı), `FirmaShellView:120` InfoCard (ikon + başlık + UserInfo + Yeni Firma). Özellik: `CardBackgroundFillColorDefaultBrush` zemin + ince stroke + 16 radius + 0,0,0,12 margin — Invent `InventCardStyle`'ın sistem-fırçalı karşılığı.
- `CustomElevatedCard` → **yüzen anakart (gölgeli)**: `LoginView:54` ana içerik buz kapı yerine (`ContentGrid` sarmalayıcı), `ExtendedSplash:44` SplashCard (460px, Translation 0,0,32 + ThemeShadow). Özellik: `AcrylicBackgroundFillColorDefaultBrush` + `CardBackgroundFillColorTertiary` border + ThemeShadow (Invent `InventFrostPanelStyle`/`ElevatedCard` karşısında acrylic varyant).
- `CustomGlassPanel` → **cam/frost panel**: `FirmaShellView:116` ana içerik frost paneli (`InfoCard + 2 kolon`). Özellik: `LayerOnAcrylicFillColorDefaultBrush` 0.5 border — `GlassPanel:167`/`InventFrostPanelStyle:30`'ın LayerOnAcrylic varyantı.
- `CustomCompactCard` / `CompactElevatedCard` → **eklenip henüz kullanılmayan varyantlar** (0 caller, `grep` temiz) — kompakt liste/satır kartı için hazır; ihtiyaçta `FirmalarList`/`MaliDonemlerList` satırlarında değerlendirilecek; ölü değil, rezerve.
- **Token kullanımı:** `CardCornerRadius 16` (CustomModern/Glass/Elevated), `CompactCardCornerRadius 6` (Compact*), `CardPadding 10` (Modern/Elevated), `CompactPadding 12` (Compact*), `SectionPadding 16` (rezerve), `CardPictureRadius 68` (Oturum 96'da silinip geri gelen `FirmaCard` için; Cards'ta rezerve).

### Doğrulama
- `grep` Custom* caller: 5 kullanım (Login 3 + FirmaShell 2 + Splash 1) + 2 rezerve (0 caller) — tutarlı.
- `dotnet build` / `dotnet test` **bu oturumda çalıştırılmadı** (yalnız log kaydı; kod değişikliği yok, derleme etkilenmez).
- Sonraki adım: kullanıcı onayıyla 3 sayfa `AppBackgroundBrush → InventEaseBackgroundBrush` geçişi + `Custom*` vs `Invent*` varyant tablosunun `VIEW-PACKAGE-DURUMU.md`'ye işlenmesi; duplicate varyant ihtiyaca göre birleştirme.

## 2026-09-06 — Oturum 100 (SistemKurulum Custom* geçişi — senin güncellediğin gibi — ✅🧪)

### İstek (kullanıcı)
- "benim güncellediğim gibi sistemKurulumView ve alt bileşenlerini de güncelle" — Splash/Login/FirmaShell'de yaptığın `AppBackgroundBrush` + `Custom*` dilini SistemKurulum'a da uygula.

### Uygulanan (3 dosya, 6 yer — Invent → Custom)

| Dosya | Önce (Invent) | Sonra (Custom) | Neden (senin desenin) |
|---|---|---|---|
| `SistemKurulumView.xaml:10` `Page.Background` | `InventEaseBackgroundBrush` | `AppBackgroundBrush` | Splash:11 / Login:11 / FirmaShell:11 ile birlik |
| `SistemKurulumView.xaml:18` `RootGrid.Background` | `InventEaseBackgroundBrush` | `AppBackgroundBrush` | aynı |
| `SistemKurulumView.xaml:60` ana frost | `InventFrostPanelStyle` | `CustomGlassPanel` | FirmaShell:116 frost `CustomGlassPanel` |
| `SistemKurulumView.xaml:64` hero kart | `InventCardStyle` | `CustomModernCard` | FirmaShell:120 InfoCard `CustomModernCard` |
| `SistemKurulumView.xaml:115` işlem günlüğü kartı | `InventCardStyle` | `CustomModernCard` | aynı |
| `DatabaseInfoPanel.xaml:17` sistem durumu | `InventCardStyle` | `CustomModernCard` | Login alt kartları `CustomModernCard` değil ama `InventCard` yerine Custom hap kart senin varyantın |
| `DatabaseInfoPanel.xaml:171` veritabanı işlemleri | `InventCardStyle` | `CustomModernCard` | aynı |
| `SystemTestsPanel.xaml:16` sistem testleri | `InventCardStyle` | `CustomModernCard` | aynı |

- `QuickSistemDbDiagDialog.xaml` (ContentDialog) — **ELLEMedi**: dialog chrome `MuhasibInventCardBrush` + iç pale `MuhasibInventPaleBrush`; senin 3 sayfa değişikliğinde dialoglara dokunulmadığı için korundu; gerekirse `CardBackgroundFillColorDefaultBrush` varyantına geçirilebilir (sonraki oturum).

### Değişmeyen (korundu)
- Tüm iç metrik pill'ler (`MuhasibInventPaleBrush`/`MuhasibTealLightBrush`/`SuccessBg` vb.), ikonlar, `MuhasibPrimaryButtonStyle`/`GhostPillButtonStyle`, terminal `MuhasibTerminalBgBrush` — senin Login/FirmaShell güncellemende de iç tokenlar aynen korunmuştu, aynı ilke uygulandı.
- `ViewModel` / `x:Bind` / `Visibility` / `Command` / `Grid` yapısı — 0 fonksiyon değişikliği (AGENTS kural 1/5).

### Doğrulama
- `dotnet build MuhasibPro.slnx -c Release` **0 Hata** (18 uyarı: bilinen CS8625/xUnit1031/IL2072, yeni yok) — `SystemTestsPanel` dışında XAML well-formed.
- `grep` sonrası Custom* caller: **11 kullanım** (Login 3 + FirmaShell 2 + Splash 1 + SistemKurulum 5) + 2 rezerve (`CustomCompactCard`/`CompactElevatedCard` hala 0 caller).
- Invent → Custom geçişi tek yönlü değil; `Invent*` stilleri `Cards.xaml`'da duruyor (silinmedi — Oturum 99 kararı), ihtiyaçta geri dönülebilir.

## 2026-09-06 — Oturum 101 (FirmaShellView yapısı mühür — AppBackgroundBrush + header ayrı + CustomGlassPanel + CustomModernCard — ✅🧪)

### Yapı (kullanıcı mührü — her defasında anlatılmayacak, tek kaynak bu log)
- **Arka plan:** `FirmaShellView.xaml:11,18` `Page/RootGrid Background="{ThemeResource AppBackgroundBrush}"` → `Assets/Images/background_muhasib.png` (`DesignTokens.xaml:210` `ImageBrush`) — `MainWindow` `Mica` altında resim net görünür (yol `Assets/Images` düzeltildi).
- **Header ayrı (frost dışında, zemin üstünde):** `73-95` `Grid ColumnSpacing 16 Margin 0,0,0,2` — sol `Logo` (`CustomModernCard` compact `26×26` teal `M` + `15pt` MuhasibPro) / sağ `UserInfoControl` (`CustomModernCard` dış `Padding 4 Margin 0,15,0,4` + iç `Transparent` — ilerleme adımı `LoginView:35,41` `CustomModernCard Padding 4/10` ile aynı).
- **Altındaki her şey tek frost:** `103` `Border Style="{StaticResource CustomGlassPanel}" Padding 16` (`LayerOnAcrylic + CardStroke 0.5 + CardCornerRadius 16`) — içinde `StackPanel Spacing 14` → `106` üst bilgi (`Firma & Mali Dönem 20 Bold + açıklama 13` + `Yeni Firma` Ink) + `125` `PanelsGrid ColumnSpacing 12` → `134` `LeftPanel` + `137` `RightPanel` **her biri `Style="{StaticResource CustomModernCard}" Padding 16`** (frost içinde beyaz kartlar).
- **Alt bileşenlerde de CustomModernCard:** `FirmalarListControl` / `MaliDonemlerListControl` iç kartları ve `NamePasswordControl` girişleri gerektiğinde `CustomModernCard`/`CustomCompactCard` ile (tek palet `CardBackgroundFillColorDefaultBrush/CardStrokeColorDefaultBrush` — `DesignTokens` warm `#F3F2EF/#E9E5DE/#DDD8CF` + Teal `#7A9E8F` ile uyumlu).

### Not
- Bu yapı `LoginView:35,41,54` (logo+adım `CustomModernCard`, anakart `CustomElevatedCard`) ve `ExtendedSplash:44` (`CustomElevatedCard`) ile aynı dil — tek palet, sayfalar arası buton/kart farkı yok.
- Bundan sonra yeni sayfa/panel bu mühre göre yazılır; `Invent*` stilleri rezerve, silinmedi.

## 2026-09-06 — Oturum 102 (Arkaplan warm + Teal yeşil + QuickDialog + FirmaShell polish + Radio + canlı — ✅🧪)

### Arkaplan & DesignTokens (tek palet)
- `Assets/Images/background_muhasib.png` örnekleme: `tl #EDEBE7 / tr #D1D7D1 / avg #E0E0D9 / min #C4D1CD / max #F3F2EF` → `DesignTokens.xaml:18-20` `InventBg #F3F2EF/#EDEBE7/#E0E0D9`, `Pale #E9E5DE / Border #DDD8CF` (warm), `Dark #151410/#1C1914/#241E18` + `Pale #1A1612/Border #332E26`.
- Teal `background_muhasib.png` yeşili: Light `Teal #7A9E8F / Pressed #4A6B5A / Light #E6EDE6 / Border #C4D1CD` (`36-40`), Brand alias aynı, Dark `Teal #8FB3A8 / Pressed #5A8A7A / Light #2A352A / Border #4A6B5A` (`90-94`) — tek palet, her sayfada buton/ikon aynı.

### QuickDialog (`QuickSistemDbDiagDialog.xaml`)
- `13` `Background InventCardBrush + Border CardStrokeColorDefaultBrush` (ana border), `16` `PrimaryButtonStyle InventDialogInfoStyle` (`Buttons.xaml:334` mavi `#005FB8`) + `DefaultButton None` (accentBlue zorlaması kalktı), iç `34` `CustomGlassPanel` frost (`FirmaShellView:103` ile aynı) + 5 kart `62,74,86,98,110,120` `CustomModernCard` (ana border) — `Grid`ler silinmedi, `Border`lar kart içinde.

### FirmaShell polish (canlı istekler)
- Header: adım silindi → logo sola/UserInfo sağa swap (`73-94` `Column 0 Logo 26×26 14pt / Column 2 UserInfo CustomModernCard Margin 0,15,0,4 → 0,12,0,12 → 0,8,0,4` + `UserInfoControl:27` `Pale→Transparent` dış pill), logo `InventCard #FFFFFF → CustomModernCard` soft (`CardBackgroundFillColorDefaultBrush`), `F2F5F2` ayrı palet silindi (tek palet).
- Kart genişlik: üst bilgi `Margin 4,4,4,2 → 0` / frost `Padding 16,16,16,24 → 16` / `PanelsGrid ColumnSpacing 10→12` / login header/card `Padding 6/4 →10` + `Margin 0` — alt/üst/sağ/sol eşit (`ot51` resmi ile birebir).
- `CustomModernCard/Elevated/Compact` `Margin 0,0,0,12/8 → 0` (`Cards.xaml:191,200,214,229`) — ana kartın alt itmesi kalktı.

### Icon / arama gri fix
- `FirmalarListControl` header `28` `TealLight→InventPale` geri alındı (gri hiç olmadı), tile `46` `Teal → Pale → Teal` (gri iptal), `MaliDonemlerListControl` takvim `Pale→TealLight` + arama `TextBox Pale→InventCard #FFFFFF` (beyaz).

### Radio (`Cards.xaml:56`)
- Ayrı `RadioBg` paleti silindi, tek palete döndü: boş `InventPale/SoftBorder`, hover `TealLight 30%`, seçili `TealLight/Teal` → sonra resim yeşili için `F2F5F2/DDE5DD/E6EDE6/EDEBE7` ayrı açıldı, kullanıcı "ayrı palet kafa karıştırıyor" → silindi, eski `InventPale/Teal`e revert + seçili `EDEBE7` (sol taraf warm) denendi → yine tek palet `InventPale/Teal`e dönüldü (şu anki hali).

### Canlı
- `dotnet build` **0 Hata** (Info/IL2072 hariç), `login_live2.png` + `teshis_live.png` (HWND `3475000` → `1770548` → `20804` → `6095990`) — login `Sistem Veritabanı Hazır + Hızlı Giriş` / teşhis `Hazır + DOSYA/BAĞLANTI/DOĞRULAMA/TABLOLAR` aynı dil, alt/üst eşit, arama beyaz, radio teal.
