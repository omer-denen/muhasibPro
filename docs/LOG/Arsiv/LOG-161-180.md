# MuhasibPro — Oturum Günlüğü — Cilt 9 (161-180)

> Önceki cilt: `LOG-141-160.md` (dolu, 20/20). Kitap indeksi: `docs/LOG.md`.

---

## 2026-09-10 — Oturum 161 (tüm dialog zeminleri `MuhasibCardBackgroundSubtleBrush` — ✅ kod 🧪❌)

### İstek (kullanıcı)
- "Tüm dialogların background rengini bununla değiştir: `MuhasibCardBackgroundSubtleBrush`." (doğrudan emir — sınıf bazlı onay yerine geçer.)

### Bulgu
- Fırça mevcut: `DesignTokens.xaml:222` (`MuhasibInventPaleColor`'dan, tema-reaktif).
- 8 `ContentDialog` kökü tarandı; `YonetimAyarlarDialog` hedef fırçayı ZATEN taşıyordu (çalışma ağacında, untracked dosyada — önceki okumada `CardBackground...` görünmüştü, kullanıcı paralelinde çevirmiş).
- Kalan 7 kök: 5× `CardBackgroundFillColorDefaultBrush` + 2× `AcrylicBackgroundFillColorDefaultBrush` (`TenantDatabaseUpdate`, `QuickDiag` — Oturum 157'de korunmuş akrilik emsallerdi).

### Uygulanan
- 7 kök satıra satır-adresli `sed` (iç kartlara dokunulmadı — örn. `RestoreVerify:67` iç rozet aynen `CardBackground...`).
- Sonuç: 8/8 kök `MuhasibCardBackgroundSubtleBrush`.

### Doğrulama
- 8 dosya XML well-formed; kök satırlar grep'le tek tek doğrulandı; iç kart kullanımları korundu.
- `dotnet` bu ortamda kırık → **build + canlı görsel Windows'ta** (açık/koyu tema, iç kart kontrastı kullanıcıda).

### Sonraki adım
- B1 rozet + B2 tetikleme açık; 6.66/6.67 build-test-canlı borcu sürüyor.

---

## 2026-09-10 — Oturum 162 (Login sağ kolon: başlık + kart deseni — ✅ kod 🧪❌)

### İstek (kullanıcı, 3 tur)
1. LoginView'i aç; sol kolon = başlık + kart, doğru mu? Aynısını sağa yap.
2. Aynı font, sadece size büyüsün.
3. Çakışma sorusu: kart içinde zaten 22px "Kullanıcı Girişi" var → karar: dış 16px + iç silinir.

### Uygulanan (sınıf bazlı: LoginView + NamePasswordControl, kullanıcı emri)
- `LoginView.xaml`: `RightPanel` Border → StackPanel (başlık + kart); başlık `KULLANICI GİRİŞİ` 16px Semibold tertiary `CharacterSpacing 80` (sol 11px ile aynı font, büyük); `x:Name` StackPanel'e taşındı (VSM setter'ları + code-behind refsiz — `RightPanel/LeftPanel` code-behind'de 0).
- `NamePasswordControl.xaml`: iç 22px `Bold` başlık bloğu silindi (5 satır), alt açıklama korundu. Kontrol yalnız LoginView'de kullanılıyor; `Kullanıcı Girişi` metnine kod/test referansı 0.

### Ders (araç)
- Python `open()` evrensel satırsonu çevirisi `\r\n`→`\n` yapar; CRLF dosyada `split('\r\n')` boş döner + yazımda tüm dosya LF'ye dönerdi. Kural: CRLF dosyalar `newline=''` ile ham okunur/yazılır.

### Doğrulama
- 2 dosya XML well-formed; binding adları aynen (`x:Bind`/`{Binding}` dokunulmadı).
- `dotnet` kırık → **build + canlı (çift başlık kalmadı mı, 16px hiyerarşi) Windows'ta**.

### Sonraki adım
- B1 rozet + B2 tetikleme açık; 6.66/6.67 build-test-canlı borcu sürüyor.

---

## 2026-09-10 — Oturum 163 (FirmaShell liste başlıkları: ikon + chip birliği — ✅ kod 🧪❌)

### İstek (kullanıcı)
- `FirmaYönetimView` (FirmaShell) fix'leri; kullanıcı ekran görüntüsü alıp dikkat edilecek noktaları kırmızı daireyle işaretleyecek. Tespitler: (1) Firmalar başlığında ikon var, dönem başlığında yok; (2) firma sayaç chip'i tam yuvarlak değil; (3) dönem chip'i firma stilinde değil.

### Uygulanan (sınıf bazlı: 2 liste UserControl'ü, kullanıcı emri)
- `MaliDonemlerListControl`: başlığa 28×28 petrol tile + `IconCalendar` (firma `IconBuilding` tile'ının aynısı); sayaç chip'i firma stiline (zeytin tint/border/deep, Padding 7,2, 11px mono semibold).
- `FirmalarListControl`: başlık sayaç chip'i `Pill(20)` → `Circle(999)` (yükseklikten bağımsız tam yuvarlak); dönem chip'i de aynı `Circle` (birlik).
- İç kartlardaki diğer pill chip'lere dokunulmadı (kapsam: yalnız başlık sayaçları).

### Doğrulama
- 2 dosya XML well-formed; binding adları aynen; tokenler mevcut (`Circle 999`, `IconCalendar`, zeytin ailesi, monospace).
- `dotnet` kırık → **build + kırmızı-daireli ekran görüntüsüyle canlı teyit Windows'ta/kullanıcıda**.

### Sonraki adım
- Kullanıcının işaretli ekran görüntüsü gelince kalan noktalar; B1/B2 açık.

---

## 2026-09-10 — Oturum 164 (radio kart renkleri arkaplanla uyumu — ✅ kod 🧪❌)

### İstek (kullanıcı)
- `FirmaShellView` radio stilleri arkaplan resmiyle uyumlu olsun (hover/select/diğerleri).

### Analiz
- Resme bakıldı: warm kırık-beyaz zemin + silik adaçayı/bej/mavi-gri yıkamalar. Stil (`MuhasibCardRadioButtonStyle`, firma + dönem listeleri) hover'da soğuk petrol yıkıyordu (%30/45) — warm zeminde yabancı duruyordu.
- Karar: hover/pressed warm yıkamaya; seçili petrol kimliği KORUNDU (mühürlü vurgu — seçili kartın bağırması gerekir); boş `Disabled` dolduruldu.

### Uygulanan (sınıf bazlı: DesignTokens + Cards, kullanıcı emri)
- YENİ token `MuhasibWarmWashColor` (Light `#D3CCC0` / Dark `#3A342C`) + `MuhasibWarmWashBrush` (kural 3: önce token).
- `HoverOverlay` → `WarmWashBrush` (opaklıklar aynen 0.30/0.45 — artık warm derinleşme).
- `Disabled` → `CardBorder.Opacity 0.55`. `Checked` (petrol dolgu + çerçeve) aynen.

### Doğrulama
- 2 dosya XML well-formed; yeni anahtar 4 noktada bağlı; petrol fırçaya dokunulmadı (başka tüketici etkilenmez).
- `dotnet` kırık → **build + canlı (hover/selected/disabled, açık + koyu tema) Windows'ta**.

### Sonraki adım
- B1/B2 açık; 6.66/6.67 borcu sürüyor.

---

## 2026-09-10 — Oturum 165 (dönem durumları otomatik mi + neden yalnız 2027 eski — 🔍 teşhis, kod yok)

### Sorular (kullanıcı)
1. Firma seçiminde dönem durumları (Açık/Kapalı/Güncelleme Gerekli) otomatik mi alınıyor? 2027'de migration bekliyor ama "Açık" yazıyor.
2. Migration tüm dönemleri etkilemeliyken neden yalnız 2027 etkileniyor? Diğerlerine uygulanmış mı?

### Kanıt (disk, salt-okunur python-sqlite)
| DB | History | Versiyon | Damga | Sonuç |
|---|---|---|---|---|
| `db-F-0001_2025.db` | InitialApp + AddTenantIdentity | 1.1.0 | 15:55 (bugün) | GÜNCEL |
| `db-F-0001_2026.db` | InitialApp + AddTenantIdentity | 1.1.0 | 15:55 (bugün) | GÜNCEL |
| `db-F-0001_2027.db` | YALNIZ InitialApp | 1.1.28.1709 (eski şema) | 07.09 (damga kolonu yok) | ESKİ — tek bekleyen |

### Cevap 1 — hayır, tam otomatik değil
- `MaliDonemListViewModel`: `AnalyzeDbStatusAsync` YALNIZ `SelectedItem` için tetiklenir (seçili kart + ProgressRing). Seçili olmayan dönem hiç analizlenmez → `DbDurum` rozeti düşmez → kartta yalnız kayıt durumu (`Açık`) kalır.
- `CheckUpdateRequiredAsync(databaseName)` tekil çağrılır (Coordinator seçim akışı + UpdateView); firma seçiminde tüm dönemler taranmaz. E1 olayı da işlemdeki dönem için yayınlanır → tek rozet (HATALAR B1'in canlı karşılığı).
- Yani 2027 seçili değilse analiz yoktur; "Açık" yanlıştır değil, eksiktir (kayıt-durumu doğru, şema-durumu bilinmiyor).

### Cevap 2 — diğerlerine uygulanmış, atlanan yok
- Migration'lar tenant-bazlıdır: her `.db` kendi history'sini taşır, yayın (broadcast) yoktur. 2025/2026 bugün 15:55'te göçmüş (güncelleme akışı ya da geçişte auto-migrate); 2027 o akışa henüz girmemiş.
- 2027'nin `1.1.28.1709`'u SemVer-öncesi takvim şemasıdır (`DbSchemaVersions` haritası tanır — Oturum 119/131); göç edince `AddTenantIdentity` + damga alır.

### Aday fix'ler (onay bekler)
- A: firma seçiminde tüm dönemlere paralel analiz (`Task.WhenAll`, Oturum 72 deseni — per-item try/catch korunur).
- B: "N dönemde güncelleme var" toplu şeridi (liste-üstü sayaç, tek tek rozet yerine).
- C: değişiklik yok (seçimde analiz mevcut davranış kalır).

### Doğrulama
- Disk okuma salt-okunur (`mode=ro`); kod satırları yukarıda; `dotnet` kırık → davranış değişikliği yok, test yok.

---

## 2026-09-10 — Oturum 166 ("Yeni Mali Dönem Aç" görünürlüğü — ✅ kod 🧪❌)

### İstek (kullanıcı)
- Buton beyaz-üstünde-beyaz: `SecondaryCompact` (şeffaf zemin + `#DDD8CF` bordür + adaçayı metin) kartta eriyor.

### Uygulanan (sınıf bazlı: MaliDonemlerListControl, kullanıcı emri)
- Aynı stil + yerel override: zemin `PetrolTint`, bordür `PetrolBorder`, ikon/metin `PetrolDeep` (yanındaki zeytin sayaçla aynı tint-dili; ölçü/padding aynen).
- `Primary` (36px koyu) başlığa hantal geldiği için elendi; yeni stil yok.
- Durumlar: normal = tint hap; hover = bordür petrole döner (şablon); pressed = şablon koyulaştırır; hepsi tema-reaktif.

### Doğrulama
- XML well-formed; fırça anahtarları mevcut; ölçü değişmedi (başlık ritmi korunur).
- `dotnet` kırık → **build + canlı (normal/hover/pressed, açık + koyu) Windows'ta**.

---

## 2026-09-10 — Oturum 167 (2027 elle göç: yedek + AddTenantIdentity + damga — ✅ veri 🧪❌)

### İstek (kullanıcı)
- Test migration'ı yarım kalmıştı (2027 eski); "akış olması gerektiği gibi" tamamlansın → karar: 2027'yi göçür (kod değişimi yok).

### Uygulanan (uygulamanın kendi akışının birebiri)
- Önce yedek: `Backups/TenantBackups/db-F-0001_2027.db_20260910_190918_elle.backup` (`_elle` = elle işaret).
- EF `Up()` ile aynı 5 `ALTER TABLE ... ADD ... NULL` (kolon sırası dahil) + history satırı `('20260908_AddTenantIdentity','9.0.12')` (kardeş değerin aynısı).
- Damga (`TenantDbVersionFromMigrationsAsync` mantığı): `Previous=1.1.28.1709`, `Current=1.1.0` (`ForMigration` haritası), `LastUpdate=UtcNow`. Kimlik kolonları `NULL` bırakıldı — uygulamanın göçü de doldurmaz (dolduran ayrı yedek/geri-yükleme yolları); veri uydurulmadı.

### Doğrulama
- `integrity_check` önce/sonra `ok`; history + tablo şeması 2026 ile birebir; 3 dönem de 1.1.0.
- Uygulama kapalıyken, tek transaction; WAL yoktu (temiz .db).
- `dotnet` kırık → **uygulamada canlı teyit Windows'ta**: 2027 seçiminde "Güncelleme Gerekli" rozetinin sönmesi + durum "Sağlıklı" olmalı.

### Sonraki adım
- B1/B2 açık; 6.66/6.67 borcu sürüyor.

---

## 2026-09-10 — Oturum 168 (seçilide hover yok + buton hover görünürlüğü — ✅ kod 🧪❌)

### İstek (kullanıcı)
- Seçili radio kartta hover olmasın; "Yeni Mali Dönem Aç" hover'ı yok/görünmüyor.

### Kök nedenler
- VSM grupları bağımsız: `Checked` perdesi 0 yapsa da `PointerOver` sonradan 0.30 yazıyordu (çapraz-grup çakışmasında son kazanan görünürdü).
- Buton tabanı `PetrolTint` iken şablon hover'ı da `PetrolTint` yazıyordu (Oturum 166'nın yan etkisi) — yalnız bordür kıpırdıyordu.

### Uygulanan (sınıf bazlı: Cards + MaliDonemlerList, kullanıcı emri)
- `Checked` → perde `Collapsed`, `Unchecked` → `Visible` (opaklıktan bağımsız, grup-çapraz çalışır).
- Buton tabanı beyaza (`InventCardBrush`, tema-reaktif) + petrol bordür/metin korundu: normal = kontur hap, hover = tint dolar (görünür), pressed koyulaşır.

### Doğrulama
- 2 dosya XML well-formed; `dotnet` kırık → **build + canlı (seçili-hover yok, buton 3 durum) Windows'ta**.

### Açık
- `/repos/Ekrankaydi` bu ortamdan erişilemiyor (`/repos` yok) → ekran görüntüsü workspace'e konunca ayar-dialogu tasarımı (Oturum 169 adayı).

---

## 2026-09-10 — Oturum 169 (ayar dialogu ekran-görüntüsü tasarımı — ✅ kod 🧪❌)

### Referans (kullanıcı)
- `/repos/EkranKaydi/Ekran görüntüsü 2026-09-10 191004.png` (cüzdan-seçim dialogu): solda ikonlu menü + seçili hap vurgu; sağda her satır ayrı beyaz kart (beyaz-kart/bej-zemin).

### Uygulanan (sınıf bazlı: dialog + 3 panel, kullanıcı emri)
- **Menü:** `ListView.Resources` ile seçili = petrol-tint zemin + petrol-deep metin (sistem mavisi kilitlendi); seçili-hover/pressed = petrol-bordür; seçsiz-hover = `WarmWash` (Oturum 164 tokeni). İkonlar zaten vardı.
- **Kartlar:** 3 panelde tek-kart → satır-başı beyaz kart (`CustomModernCard`, Padding 16,12): Liste 4 + Saklama 4 + Genel 3 (firma + oluşturan + kayıt). Başlık blokları kartsız bölüm başlığı.
- Binding/met/kontroller aynen (VM dokunulmadı); `x:Int32` preset'ler, `IsYonetici` kilidi, uyarı rozeti korundu.

### Doğrulama
- 4 dosya XML well-formed; binding yolları çocuk VM'lerle eşleşti (sayım yukarıda).
- `dotnet` kırık → **build + canlı (menü vurgu + kart ritmi, açık + koyu) Windows'ta**.

---

## 2026-09-10 — Oturum 170 (IconShutdown çöküşü — ❌→✅ fix)

### Hata (kullanıcı, canlı)
- `XamlParseException`: `IconShutdown` bulunamadı (`AyarSaklamaPanel:67`, `InitializeComponent`).

### Kök neden
- Oturum 169 yeniden-yazımında satırlara eklediğim ikon uydurmaydı; anahtar sözlükte yok. Orijinal satırlarda ikon yoktu; diğer 6 anahtar (`Layers/Backup/Trash/Shield/Building/Layers`) doğrulandı, tek sahte bu.

### Fix
- `IconShutdown` → `IconLogOut` (çıkış/kapanış anlamı uyar).

### Ders
- Yeniden-yazımda eklenen her `StaticResource` anahtarı `Icons.xaml`'a karşı tek tek doğrulanır (XML parse yetmez — `x:Key` çözümü runtime'dadır).

### Doğrulama
- XML well-formed; 7/7 ikon anahtarı mevcut. **Canlı teyit Windows'ta** (dialog açılışı).

---

## 2026-09-10 — Oturum 171 (ayar dialogu: Kapat gitti, sağ-üst X geldi — ✅ kod 🧪❌)

### İstek (kullanıcı)
- `CloseButtonText="Kapat"` kalksın; referans resimdeki gibi sağ-üste X.

### Uygulanan (sınıf bazlı: dialog + Icons, kullanıcı emri)
- `CloseButtonText` silindi (alt buton çubuğu yok).
- Başlık ızgarasına 4. kolon + 32×32 `GhostPillButtonStyle` X (`IconClear` E711 — YENİ anahtar, kural 3; `Hide()` handler `OnKapatClick`).
- Kapanış akışı aynen (`ShowCenteredAsync` sonrası `SayfaBoyutuDegisti` okunur).

### Doğrulama
- XML well-formed; `IconClear` 2 noktada bağlı; handler imzası mevcut desenle aynı.
- `dotnet` kırık → **build + canlı (X konumu/hover/kapanış) Windows'ta**.

---

## 2026-09-10 — Oturum 172 (durum-butonu ↔ X aralığı — ✅ kod 🧪❌)

### İstek (kullanıcı)
- "Varsayılanlara dön" ile X biraz ayrılsın, sağa sabitlensin.

### Uygulanan
- X butonuna `Margin="8,0,0,0"` (ızgara 12 + 8 = ~20px nefes); sağa yaslanma `*` kolondan aynen.

### Doğrulama
- XML well-formed; `dotnet` kırık → canlı Windows'ta.

---

## 2026-09-10 — Oturum 173 (toplu log kapanışı — 📋 docs)
- Kullanıcı emri: "yapılan işlemleri logla".
- Uygulanan: KONTROL'e `Cilt 9 rötuşlar (161-172)` bölümü; ROADMAP faz tablosuna 6.66 + 6.67 satırları; AYARLAR-PLAN'a §7 uygulama-durumu (sapmalar + borç).
- LOG cilt + indeks + HATALAR zaten oturumunda işlenmişti (158-172); bu oturum yalnızca eksik halkaları kapattı.

---

## 2026-09-10 — Oturum 174 (ön muhasebe çekirdek modül araştırması — 📋 bulgu)
- Kaynaklar: TR program dokümanları (cari/stok/fatura/irsaliye/sipariş/teklif/kasa/banka/çek-senet/bordro) + global (invoicing/expense/bank-feeds/payroll/tax/multi-user) + GİB e-belge zinciri.
- Çıktı: modül diagramı + MuhasibPro eşleşmesi (çekirdek hazır, B fazı = ticari modüller).

---

## 2026-09-10 — Oturum 175 (çekirdek iskelet araştırması: açılış→dönem seçimi — 📋 bulgu)
- Kaynaklar: splash (ayrı thread + durum satırı + min süre) + login→ana-form deseni + DB-per-tenant (migration TENANT başına koşar, `MigrateAllTenants` döngüsü; bağlantı oturum-başına çözülür).
- Çıktı: iskelet diagramı + MuhasibPro eşleşmesi (akış birebir; fark: göç seçimde tetiklenir, toplu döngü yok — tasarım kararı).

---

## 2026-09-10 — Oturum 176 (güncelleme altyapısı — 📋 bulgu)
- Çıktı: tenant göç hattı diagramı (kontrol→onay→yedek→göç→doğrula→damga→geçiş + geri alma) + uygulama-güncellemesi kavramı + MuhasibPro eşleşmesi.

---

## 2026-09-10 — Oturum 177 (View bağımlılık haritası — 📋 docs)
- İstek: tüm Page/UserControl/Dialog'ların bağımlılık-sıralı diagramı bir md'ye.
- Üretim: 52 XAML tarandı (betik: xmlns öneki + code-behind `new XDialog`/`CreateNewViewAsync`); `docs/VIEW-BAGIMLILIK.md` (19 kök ağaç + 52'li topolojik yükleme sırası; 13 Page / 30 UserControl / 1 Window / 8 Dialog).

---

## 2026-09-10 — Oturum 178 (güncelleme yapısı diagramda var mı — ❓ cevap)
- Soru: VIEW-BAGIMLILIK'ta güncelleme arayış/işleme/uygulama yapısı var mı?
- Cevap: View adları var (`TenantDatabaseUpdateView`, `UpdateView`, `TenantDatabaseUpdateDialog`) ama kök/yaprak — akış XAML içermede değil, VM saga + navigasyonda: arayış = Coordinator + `CheckUpdateRequiredAsync`; işleme/uygulama = UpdateViewModel 4 adım (Yedek→Göç→Doğrula→Geri alma) + Migration/BackupManager; app-update = Settings `UpdateView`.

---

## 2026-09-10 — Oturum 179 (güncelleme altyapı denetimi — ✅ hazır, kod yok)
- İstek: eksik view varsa oluştur + altyapıyı hazırla.
- Bulgu: 3 view mevcut (UpdateView 180 + TenantDatabaseUpdateView 372 + UpdateDialog 86 satır); altyapı TAM ve bağlı — Coordinator→FirmaShell:178 seçimde; saga gövdeli (Yedek→Göç→Doğrula→OtoGeriAl, stub/NotImplemented 0); UpdateViewModel mevcut.
- Yapılacak iş çıkmadı; açık kalem Oturum 165 adayları (dönem-seçim analizi A/B/C, onay bekliyor).

---

## 2026-09-10 — Oturum 180 (açık faz envanteri — 📋 liste)
- İstek: logları incele, açık kalan tüm fazları listele.
- Çıktı: kullanıcı cevabındaki liste (kod işi 10 + kullanıcı sepeti); kaynak KONTROL ⬜ + HATALAR B + faz borçları.

---

## 2026-09-10 — Oturum 181 (iş kuyruğu kaydı — 📋 plan)
- İstek: açık fazlar loga faz olarak kaydedilsin, ilk açılışta bu sıra işletilsin.
- Uygulanan: KONTROL'e `Sıradaki iş kuyruğu` (0-9 + kullanıcı sepeti); sıra doğrulama-kapısı önce, bağımlılık sonra.
