# MuhasibPro — Oturum Günlüğü — Cilt 13 (Oturum 241-260)

> Kodlama: UTF-8 (BOM, 65001). Üst indeks: `docs/LOG.md`.

---

## 2026-09-13 — Oturum 241 (Faz 6.79: Sistem.db yaşam-döngüsü güvenliği + Faz 6.80 kuralı)

### Istek (kullanıcı)
"Açılışta Sistem.db yedekliyor musun, uygulama kapanışında WAL dosyalarını DB'ye aktarıyor musun gibi kritik işlemleri yapıyor musun?" → kapsam sorusuna cevap: **Hepsi**.

### Tarama bulgusu (dürüst kayıt — Kural 15)
- Açılışta oto-yedek çağrısı **0**; kapanışta WAL checkpoint **0**; `EnsureWeeklyBackupAsync` kodda **yok** (yalnız arşiv/LOG kaydı); `WeeklyBackupDays`/`HaftalikButunlukKontrolu`/`VacuumOnBackup` modelleri **0 tüketici** (rafta).

### Araştırma (Kural 13/14)
- **Sage 50:** otomatik yedek "dosyayı kapatınca" (gün-ilk) + saklama aralığı ayarlı; tüm kullanıcılar çıkmalı.
- **QuickBooks:** "Save backup copy automatically when I close my company file" + kaç yedek saklanacağı + "Complete verification".
- **SQLite resmi:** WAL checkpoint `TRUNCATE` kapanışta önerilir (pasif kapanış `-wal` bırakabilir).

### Uygulanan
- **Data:** `ISistemBackupManager.CheckpointWalAsync` + impl (TRUNCATE reuse, best-effort).
- **Business:** `ISistemYasamDongusuService` + `SistemYasamDongusuService` (tek sorumluluk): açılış (checkpoint + eşik-aşımı oto-yedek + FIFO) / kapanış (checkpoint her zaman + ayarlıysa yedek). Eşik `WeeklyBackupDays`, keep `GetSistemKeep` — **modelden** (Kural 7).
- **DI:** `AddSingleton<ISistemYasamDongusuService, SistemYasamDongusuService>` (yalnız Singleton'lara yaslanır; `ISistemBackupManager`+`ISistemDatabaseOperationService`+`IDatabaseSettingsProvider`+`ISistemLogService`).
- **App:** açılış kancası `StartupApplicationExtensions.DbTest` (DB geçerliyse splash adımı) + kapanış kancası `WindowHelper.TryTakeExitBackupAsync` (Sistem kolu her zaman + tenant kolu korunur).
- **Ayarlar:** kapanış toggle metni "açık dönem + sistem" (2 dosya; ayar adı korundu — migration yok).
- **Test:** `SistemYasamDongusuTests` 6 test.

### Doğrulama
- `dotnet build --no-incremental` **0 hata** (25 uyarı: bilinen eski aileler; yeni `SistemYasamDongusuTests` CS8625 `null!` ile kapatıldı)
- `dotnet test` **468/468** (462 + 6)

### Faz 6.80 kural kaydı (kullanıcı)
2-katmanlı sayfa yapısı (kök `Border` + `Image` + tema-tonlu tint ~%85-90; içerik kartları `CardBackgroundFillColorDefaultBrush`/`CardStrokeColorDefaultBrush`/`ControlCornerRadius`/`ThemeShadow`/Padding 16; sadece Fluent ThemeResource; kök Grid+Background ve >2 seviye iç içe kart yasak). `AGENTS.md` **Kural 17** + `docs/TASARIM-KURALLARI.md` + ROADMAP karar logu + Faz 6.80 planı.

**Pilot (onaylı): KurulumSplash** — kök `Grid`→`Border` (`SplashOverlay`), içine `Image` (`{ThemeResource UygulamaZeminGorseli}`, tema-bazlı) + `SolidBackgroundFillColorBaseBrush` ~0.87 tint + içerik kartı korundu; `DesignTokens` ThemeDictionaries'e `UygulamaZeminGorseli` (`light.jpg`/`dark.jpg`) eklendi. Build 0 hata + test 468/468. `light.jpg`/`dark.jpg` assetleri kullanıcı ekleyince görünür (yokken tint zemini kalır).

### Kalan
- Canlı WAL/backup turu kullanıcıda; 6.78 Adım 2 (tenant bağlama); 6.80 sıradaki view geçişleri (Splash→Login→…).

---

## 2026-09-13 — Oturum 242 (Faz 6.78 Adım 2: tenant hattı ortak restore çekirdeğine bağlandı)

### Kapsam (kullanıcı: "6.78 adım 2 tamamla")
Mevcut `RestoreVerdictEvaluator` (tenant, saf fn) ortak restore çekirdeğine bağlandı — davranış korunur, testler yeşil kalır.

### Uygulanan
- **Domain (`RestoreAnaliz.cs`):** `RestoreAnalizGirdisi`'ne `KimlikUyusmazlikBaslik`/`KimlikUyusmazlikAciklama` + türetilen `KimlikUyusmazMi` eklendi (kimlik kıyası ortak çekirdeğe taşındı; hüküm metni çağırandan gelir).
- **Business (`RestoreAnalizDegerlendirici`):** sıra tenant davranışını koruyacak şekilde güncellendi — dosya-bütünlüğü → kimliksiz → kimlik-uyuşmazlığı → sürüm → kayıp-kayıt → kurulum/makine. Yeni `KimlikUyusmazMi` dalı `RequireCode` üretir.
- **Business (`RestoreVerdictEvaluator`):** ince adaptöre indirildi — tenant'a özgü firma/dönem/ad kıyasını hesaplar, `RestoreAnalizGirdisi` kurar, hükmü `RestoreAnalizDegerlendirici.Degerlendir`'den alır. Eski gömülü sürüm/kurulum/makine dalları kaldırıldı (Kural 3/4).
- **Test:** `RestoreAnalizTests` +3 (kimlik uyuşmazlığı → RequireCode; kimliksiz kimlik-uyuşmazlığını geçersiz kılar; kimlik sürümden önce hükmeder). `RestoreVerdictTests` (10) değişmeden yeşil — tenant davranışı birebir.

### Doğrulama
- `dotnet build MuhasibPro.sln --no-incremental` **0 hata** (bilinen eski uyarı aileleri; yeni dosyalarda 0)
- `dotnet test` **471/471** (468 + 3)

### Not
- Dosya/`integrity` ekseni tenant hattında nötr geçilir (davranış korunur); tenant restore'a gerçek dosya analizi bağlanması Adım 3/4 kapsamındadır.
- Ortak çekirdek sırası tenant önceliğine göre yazıldı (kimlik, sürümden önce hükmeder). Adım 3 (Sistem hattı) bu sıraya göre `RestoreAnalizGirdisi` kuracak; `KimlikliMi` sistem tarafında her zaman `true` beslenmeli.

### Kalan
- 6.78 Adım 3 (Sistem hattı: `SistemYedekViewModel.GeriYukleAsync` analiz + hüküm tek kapı) → Adım 4 UI (analiz paneli + kod alanı); canlı tur kullanıcıda.

---

## 2026-09-13 — Oturum 243 (Faz 6.78 Adım 3: Sistem hattı restore — araştırma + uygulama)

### Kapsam (kullanıcı: "adım 3 ... sektör bazlı araştırıp altyapı ve tasarımını ona göre yap")
Sistem.db geri-yükleme tek kapıya alınır: önce analiz → hüküm → onay/kod → restore; "hükümsüz restore" kalkar.

### Araştırma (Kural 13/14 — sektör + MS Fluent)
- **QuickBooks Desktop (Verify/Rebuild/Restore):** önce Verify (bütünlük); hasar varsa Rebuild — **Rebuild yedek zorunlu**; Rebuild sonrası tekrar Verify; hâlâ bozuksa **son çare** yedekten restore. Restore mevcut company file'ın üzerine yazabilir → Intuit "mevcut dosyayı değiştirme / ayrı klasöre restore et" uyarısı verir.
- **Sage 50 (Restore):** restore **mevcut veriyi ezer** → "restore etmeden önce mevcut firmanın yedeğini al" uyarısı; "A New Company" ile üzerine-yazmadan restore seçeneği; restore öncesi **seçenekleri gözden geçirme (Confirmation) penceresi**; işlem sırasında **progress gösterilir, kesilmemeli** (kesilirse veri bozulur); **sürüm uyumsuzluğu:** yeni sürüm yedeği eski sürüme restore edilemez — aynı/yeni sürüm gerekir.
- **SQLite resmi:** canlı kopya `VACUUM INTO`/Online Backup; restore = dosya değişimi + `integrity_check`; restore öncesi mevcut dosyanın yedeği.
- **MS Fluent (ContentDialog + Progress):** her dialogda **güvenli/nondestructive Kapat** şart; **yıkıcı iş = Primary**; async onay deseni → primary kilitlenir + **işlem dialoq içinde** ilerler; `None` (Esc/perde) = iptal; aynı anda **tek dialog**; süresi bilinen işte **determinate ProgressBar**, UI bloke ise indeterminate ring; bitince **tek özet**.

### Karar
Sistem restore **tek kapı**: (1) yedek dosyası salt-okunur analiz (integrity + migration history + tablo sayısı) + (2) mevcut Sistem.db ↔ yedek **farkı** (MaliDonem/firma/kullanıcı satır farkı + tenant DB dosya varlığı) + kurulum/makine kimliği + sürüm, (3) **ortak çekirdek** `RestoreAnalizDegerlendirici` hükmü (Allow/Warning/RequireCode/Block), (4) Verify-benzeri dialog (hüküm banner + fark listesi + RequireCode ise 6-haneli tek-seferlik kod; Block'ta Primary disabled; güvenli Kapat), (5) onay/kod sonrası restore — mevcut `SistemBackupManager` zaten **safety backup + atomic restore + rollback** yapıyor; Kural 12 gereği adım rozetli akış + determinate bar + tek özet; Kural 13 yardım maddesi güncellenir.

### Uygulama sırası (kullanıcı: "altyapı → genel tasarım → restore UI; tekrar düzenleme olmasın")

**1) Altyapı (onay dışı):**
- **Domain:** `SistemSnapshot` + `SistemMaliDonemSatiri` (`DatabaseResultModel`) — MaliDonem/Firma/Kullanıcı satırları + KurulumId/MachineGuid + son migration/SemVer.
- **Data:** `ISistemSnapshotReader` (Contracts) + `SistemSnapshotReader` (Impl) — mevcut Sistem.db ve yedek dosyayı `Mode=ReadOnly;Pooling=False` ile okur; `DbSchemaVersions.ForMigration` ile sürüm; DI Singleton.
- **Business:** `ISistemRestoreAnalizService` + `SistemRestoreAnalizService` (Singleton) — dosya analizi (`IDatabaseBackupManager.AnalyzeBackupFileAsync`) + iki snapshot farkı (kayıp dönem/firma/kullanıcı + karşılıksız tenant dosyası) → `RestoreAnalizGirdisi` → ortak çekirdek `RestoreAnalizDegerlendirici`; DI.
- **DTO:** `SistemRestoreAnalizSonuc` (Dosya + Fark + Hukum + `FarkOzeti`/`FarkDetayi`).

**2) Genel tasarım — Kural 17 Katman 1:**
- `SistemDbYonetimView.xaml`: kök `Grid`+`Background` → **kök `Border`** + `Image` (`{ThemeResource UygulamaZeminGorseli}`, kullanıcının eklediği `light.jpg`/`dark.jpg`) + `SolidBackgroundFillColorBaseBrush` ~0.87 tint; içerik kartları zaten `ModernCard` (Fluent token) olduğundan Katman 2 hazırdı.

**3) Restore UI (Kural 8 sınıf onayı kullanıcı direktifiyle):**
- YENİ `Views/SistemDbYonetim/Components/Dialogs/SistemRestoreVerifyDialog.xaml(.cs)` — hüküm banner + yedek kartı + kayıp-kayıt listesi + RequireCode 6-haneli kod alanı; **Block'ta Primary disabled**; güvenli `Vazgeç` (`DefaultButton=Close`); restore'u dialog yapmaz, `GeriYukleOnaylandi` döner.
- `IDialogService.ShowSistemRestoreVerifyAsync(analiz)` + `DialogService` bağlantısı.
- `SistemYedekViewModel.GeriYukleAsync` tek kapıya alındı: analiz → log özeti → hüküm dialogu → (`!onay || Engelli` ise dur) → restore → liste tazele → tek özet (Kural 11).
- `SistemDbYonetimViewModel` composition ctor'una `ISistemRestoreAnalizService`; `SistemDbYonetimView.xaml.cs` yardım maddesi analiz/hüküm/kod akışıyla güncellendi (Kural 13).

### Doğrulama
- `dotnet build MuhasibPro.sln` **0 hata** (bilinen eski uyarı aileleri)
- `dotnet test` **482/482** (471 + 9 altyapı + 2 VM kapısı)
- Kalan: canlı Light/Dark + Mica turu kullanıcıda (sınıf onayı sonrası UI yazıldı).

---

## 2026-09-13 — Oturum 244 (Denetim Masası FirmaShell'den ayrılıp ayrı pencere oldu — Kural 14 + 17)

### İstek (kullanıcı)
"Denetim Masasını Firmashell'den ayır. esnek ve yönetilebilir bir yapı istiyorum. Denetim Masasını ayrı bir pencere aç." Onaylar: **modal** pencere · tetikleyici **yalnız FirmaShell header** · konum **`Views/DenetimMasasi/`**.

### Araştırma (Kural 13/14 — sektör + MS)
- **QuickBooks Desktop:** ayarlar `Edit > Preferences` ile **ayrı pencere** (sol kategori + sağ içerik; My/Company Preferences kapsamı; OK ile kaydet).
- **Sage 50:** ayarlar **ayrı "Settings" penceresinde**, modül bazlı gruplu; kullanıcı tercihleri ayrı; yönetici/kullanıcı kapsamı.
- **MS/Fluent:** ayarlar için ayrı pencere/page; ContentDialog tek-instance; uzun işte determinate + tek özet (Kural 12).
- **Sonuç:** "ayarları ayrı pencere" kararı sektör normu; FirmaShell'i seçim odaklı tutup ayarları bağımsız pencerede yönetmek esnek/yönetilebilir.

### Karar
`DenetimMasasiView` (UserControl, FirmaShell içi sekme) → bağımsız **Page** (ayrı `DetailsWindow`, modal, Mica). FirmaShell'den "Seçim/Ayarlar" toggle'ı + gömülü Denetim Masası kaldırılır; header'da tek buton pencereyi açar. Denetim Masası kayıtlı bir Page/VM olduğundan **her yerden** `CreateNewViewAsync<DenetimMasasiViewModel>` ile açılabilir (esneklik).

### Uygulanacak (sınıf bazlı onay alındı)
- Taşıma: `Views/ShellViews/Shell/Components/DenetimMasasi/**` → `Views/DenetimMasasi/**` (namespace `...Components.DenetimMasasi` → `MuhasibPro.Views.DenetimMasasi`); 30 dosya.
- `DenetimMasasiView` → `<Page>` + Kural 17 Katman 1 (zemin `UygulamaZeminGorseli` + tint; 8 iç sayfa şeffaf) + `MuhasibPetrol*`→Fluent accent + pencere boyutu + Kural 13 `?`.
- `FirmaShellView`: toggle + gömülü Denetim kaldır; "Denetim Masası" butonu; Kural 17 Katman 1.
- `FirmaShellViewModel`: ölü `IsAyarlarAcik` silinir.
- `Startup`: `NavigationService.Register<DenetimMasasiViewModel, DenetimMasasiView>()`.

### Uygulandı
- **Taşıma (30 dosya):** `Views/ShellViews/Shell/Components/DenetimMasasi/**` → **`Views/DenetimMasasi/**`** (denetim + `.xaml.cs` + `Bolumler/` 6 panel + `Sayfalar/` 8 sayfa); namespace `...Shell.Components.DenetimMasasi.*` → `MuhasibPro.Views.DenetimMasasi.*` (PS toplu replace, UTF-8 BOM korunarak). Eski `Components/DenetimMasasi` klasörü boşaltıldı; `rg "Components.DenetimMasasi"` = **0**.
- **`DenetimMasasiView`:** kök `<UserControl>`→`<Page>`; Kural 17 Katman 1 (resim+tint); sol nav Katman 2 kart; accent (`AccentFillColorDefaultBrush` + `TextOnAccentFillColorPrimaryBrush`); `OnLoaded`'da ekrana göre `AppWindow.Resize` (maks 1100×760, min 640×480); Kural 13 `?` yardım (5 madde). İç `_icNav` + `Frame` şeffaf.
- **`FirmaShellView`:** `Seçim/Ayarlar` toggle (`ModSecimButton`/`ModAyarlarButton`) + gömülü `denetim:DenetimMasasiView` + `SecimGorunumu` görünürlük kapısı kaldırıldı; header'a tek **"Denetim Masası"** butonu (`OnDenetimMasasiClick`); Kural 17 Katman 1.
- **`FirmaShellView.xaml.cs`:** `OnModSecim/OnModAyarlar` silindi → `CreateNewViewAsync<DenetimMasasiViewModel>(null, "Denetim Masası")`.
- **`FirmaShellViewModel`:** ölü `IsAyarlarAcik` silindi (`rg` = 0).
- **`Startup`:** Denetim Masası kaydı + using.
- Esneklik: Denetim Masası artık kayıtlı Page/VM → her yerden `CreateNewViewAsync<DenetimMasasiViewModel>` ile açılabilir.

### Doğrulama
- `dotnet build MuhasibPro.sln` **0 hata**
- `dotnet test` **482/482** (DenetimMasasiTests değişmeden yeşil; `ArchitectureTests`/`CircularDependencyTests` yeşil)
- Kural 17 durumu: ✅ `KurulumSplash` + `SistemDbYonetimView` + `FirmaShellView` + `DenetimMasasiView` (4 view). Kalan çekirdek akış: `ExtendedSplash`, `LoginView`, `ShellView`, `MaliDonemYonetimView`, `TenantDatabaseUpdateView`, `UpdateView`, `DatabaseSettingsView`, `MainShellView`.
- Kalan: canlı modal pencere turu (Light/Dark + Mica) kullanıcıda.

---

## 2026-09-13 — Oturum 245 (Kural 18: canlı testi agent yapar, onay kullanıcıya sunulur)

### İstek (kullanıcı)
"Internetten araştırıp oluşturduğun yapının içeriğini ben nereden bileceğim; doğru kurup kurmadığını anlaman için **canlı testi sen yapıp onaya sunman** gerekiyor. Benim bilmediğim/görmediğim tasarımı ben nasıl canlı test edebilirim. Bunu sade ama katı bir dille agent kuralına yaz."

### Uygulanan (kod değişikliği yok — süreç kuralı)
- **`AGENTS.md` Kural 18 (KESİN):** canlı testi agent yürütür (uygulamayı açar, akışı sürer, UIA + ekran görüntüsü alır); **kanıt zorunlu** (akış adımları + `Temp/opencode/otXXX-*.png` + UIA); **"canlı test kullanıcıda" diyerek kapatma yasak** — kullanıcı yalnızca onay/ret verir; internetten alınan referans sözlü açıklanır; ortam açamıyorsa "canlı test yapılamadı + neden" dürüstçe yazılır; **kapı:** kanıt + onay olmadan faz kapanmaz.
- **`AGENTS.md` "Her oturumun sonunda" madde 8:** canlı test + kanıt + kullanıcı onayı işlendi.
- **`docs/TASARIM-KURALLARI.md` Kapı:** Kural 18 satırı eklendi.
- **`docs/CEKIRDEK-MODUL-PLAN.md` Kesin Kurallar madde 11:** canlı test agent'ta, kanıt+onay kapısı.
- **`docs/ROADMAP.md` karar logu:** Kural 18 kaydı.

### Not
Bu kural gereği, önceki oturumlardaki "canlı test kullanıcıda" kapanışları geçersizdir; bundan sonra her biten view/özellik için agent kanıt üretip onaya sunacak.

---

## 2026-09-13 — Oturum 246 (Kural 19: referans defteri — araştırma → nereye uygulandı)

### İstek (kullanıcı)
"Çok fazlı işlem ve yeni oturumlar açıyoruz. Hangi oturumda hangi tasarımı referans almış, projeye hangi görünümü uygulamış nereden bileceğiz. **Alınan referans loglara kaydedilmeli** ki sonraki oturumlar veya başka agentlar aynı işlemi ayrı ayrı yapmasın. Bunu da kural olarak ekle. Stabiliteyi koruyalım."

### Uygulanan (kod değişikliği yok — süreç kuralı + defter)
- **YENİ `docs/REFERANSLAR.md` (referans defteri):** tablo = tarih · oturum · kaynak (site/doküman + kısa bulgu) · **uygulandığı view/özellik** · durum. Recent kaynaklarla seed edildi: QB Verify/Rebuild, Sage 50 restore, SQLite resmi, MS dialog/progress, QB Preferences + Sage Settings (ayrı pencere), MS Mica, MS Settings/Toolkit SettingsCard, ContentDialog API.
- **`AGENTS.md` Kural 19 (KESİN):** kayıtsız referansla tasarım uygulanmaz; araştırma → defter → uygulama sırası; aynı yer için tekrar araştırma yok; çelişkide kullanıcıya sorulur; `Durum` güncellenmeden iş kapanmaz.
- **`AGENTS.md` zorunlu-okuma listesine `docs/REFERANSLAR.md` eklendi** (yeni view/özellikten önce oku).
- **`AGENTS.md` "Her oturumun sonunda" madde 9:** referans defteri satırı + `Durum` güncellemesi.
- **`AGENTS.md` Kural 14 çapraz referansı:** alınan kaynak ayrıca deftere yazılır.
- **`docs/CEKIRDEK-MODUL-PLAN.md` Kesin Kurallar madde 12** ve **`docs/ROADMAP.md` karar logu** kaydı.

### Canlı test durumu (Kural 18 gereği dürüst kayıt — Denetim Masası, Oturum 244)
- Agent canlı testi **kendisi denedi** (`Temp/opencode/ot245_live.ps1`; exe launch → TransferDialog "Anladım" → login/Denetim Masası tıklama → ekran görüntüsü). **Sonuç: erişilemedi.** Uygulama bu çalıştırmada Splash sonrası **SistemDbYonetim'e yönlendirdi** (Login ekranı gelmedi); "Sisteme Giriş" butonu bulunamadı, bu yüzden FirmaShell → Denetim Masası penceresi tetiklenemedi. Ekran kanıtı: `Temp/opencode/ot245_firmashell.png` (SistemDbYonetim + Yardım dialogu görünür).
- **Engel + sonraki adım:** engel ortam/akış durumu (login otomasyonu bu yönlendirmede çalışmadı). Denetim Masası canlı turu (Light/Dark + Mica + ayrı pencere) **hâlâ açık**; bir sonraki oturumda doğru ekrandan (Login) sürülüp kanıt toplanacak. "Kullanıcı test eder" denmeyecek.

---

## 2026-09-13 — Oturum 247 (Canlı test devri: Denetim Masası turu yeni oturumda yapılacak)

### Durum
Bu oturum token sınırına yaklaştı. Canlı test **yeni oturumda** yapılacak (kullanıcı onayı). Kod değişikliği yok.

### Canlı test denemeleri (agent — Kural 18, dürüst kayıt)
Kullanılan betikler `C:\Users\Code\AppData\Local\Temp\opencode\`:
- `ot245_live.ps1` — launch → TransferDialog "Anladım" → Login/Denetim tıklama. Sonuç: Transfer geçildi, Login butonu bulunamadı; ekran SistemDbYonetim'e düştü.
- `ot246_diag.ps1` — launch + döküm. Sonuç: açılışta **TransferDialog** ("farklı kurulumdan gelen dönem veritabanları…") → butonlar `Anladım` (Aid `PrimaryButton`) + `Kapat` (Aid `CloseButton`); başka içerik yok (modal).
- `ot246_full.ps1` — full akış. Sonuç (dürüst): "Anladım" tıklandı; ardından ekran **SistemDbYonetim** (Login değil). `Güncellemeyi Uygula` **bulunamadı** (muhtemelen bekleyen göç yok), `Giriş Ekranına Devam Et` **bulunamadı**, `PasswordBox` **bulunamadı**, `Denetim Masası` **bulunamadı**. Kalan görünür butonlar: `Şimdi Yedekle` + pencere kontrolleri → sayfa SistemDbYonetim'de kaldı.
- Ekran kanıtları: `ot245_firmashell.png`, `ot246_diag.png`, `ot246_sk.png`, `ot246_goc_sonrasi.png`, `ot246_login.png`, `ot246_firmashell.png` (hepsi SistemDbYonetim/transfer aşamasını gösteriyor; Denetim penceresi açılamadı).

### Kök-neden hipotezi (henüz kanıtsız — yeni oturumda doğrulanacak)
- Splash yönlendirmesi bu kurulumda **SistemDbYonetim**'e düşüyor (Login'e değil). Neden: (a) `SplashTarget.MigrationRequired` (kod'da yeni migration var, DB'de yok → `GetPendingMigrationsAsync`), veya (b) `StartupApplicationService.IsDatabaseReady=false` dalı.
- Not: Kod'da **untracked** `Libraries/MuhasibPro.Data/Migrations/20260911181337_SeedHashDuzeltme.cs` var; DB'ye uygulanmamış olabilir. SistemDbYonetim'de "Güncellemeyi Uygula" **görünmedi** → yeni oturumda `GetPendingMigrationsAsync` sonucu ve sayfa durumu **okunmalı** (buton yoksa göç yok demektir; yönlendirme başka daldan geliyor).

### UIA tuzağı (yeni oturum için kritik)
- Konsol kodlaması **Türkçe adları bozuyor** (`Güncellemeyi Uygula` → `GǬncellemeyi...`); bu yüzden `ClickText` tam-eşleşme Türkçe metinlerde **çalışmıyor**.
- Çözüm: (1) `AutomationId` kullan (`PrimaryButton`, `CloseButton` vb.), (2) ASCII/wildcard ile buton adı (`*Yedekle*`, `*Giriş*`), (3) gerekirse `chcp 65001` + `-Encoding UTF8` betik dosyası BOM'lu.

### Yeni oturum planı (canlı test — Kural 18)
1. Uygulamayı aç; TransferDialog'u `PrimaryButton` Aid ile geç.
2. **SistemDbYonetim durumunu oku:** `DbDurumMetni`, `IsKurulumTamamlandi`, bekleyen göç var mı; `Giriş Ekranına Devam Et` butonunu Aid/ASCII ile bul → Login'e geç.
3. Login: `PasswordBox` (Aid) + giriş butonunu ASCII/wildcard ile tıkla (kullanıcı: `korkutomer`, şifre `Ok241341`; QuickLogin alternatifi).
4. FirmaShell: "Denetim Masası" butonunu bul (ASCII içerik taraması) → tıkla.
5. **Doğrula:** yeni **ayrı modal pencere** (başlık "Denetim Masası"), Kural-17 zemin (light/dark.jpg + tint), sol nav + 8 bölüm, `?` yardım; ekran görüntüsü + UIA dökümü. Light/Dark turu için Görünüm bölümünden tema değiştirip tekrar kanıt.
6. Kanıtı `LOG`'a işle ve kullanıcı onayına sun (Kural 18).

### Uygulama süreci
- Çalışan `MuhasibPro.exe` prosesi kapatıldı (temiz devir).
- Build **0 hata**; kod değişikliği yok.

---

## 2026-09-13 — Oturum 248 (Login pilotu: Fluent 2 + ana border + MVVM yardım + logo)

### İstek (kullanıcı)
"Kaldığımız yerden devam; önce dark.jpg/light.jpg uygulandı mı bak" → ardından Kural 17 view turu; Login pilot tasarımı.

### Kontrol (istek 1)
- `light.jpg` (354 KB) / `dark.jpg` (116 KB) mevcut; `DesignTokens` `UygulamaZeminGorseli` (Light/Dark) → 4 view'de kullanılıyor.

### Kullanıcı reddi + düzeltme (süreç)
- İlk tur mekanik Katman-1 sarmalamasıydı (sadece kök Border+Image+tint). Kullanıcı: "Border ekleyip geçiyorsun... bu tasarımı kabul etmiyorum" → **gerçek tasarım + araştırma** istendi.
- Kural 14 araştırması: MS Fluent 2 auth (merkezlenmiş/sade/tek CTA), Sage 50 login, Windows 11 giriş → `docs/REFERANSLAR.md`'ye 3 satır.

### Login pilotu (onaylı, tamam)
- **Katmanlama (kullanıcı tarifi):** zemin görseli → **ana border paneli** → içinde kartlar.
  - Katman 1: kök `Border` + `UygulamaZeminGorseli` + tint `SolidBackgroundFillColorBaseBrush` **Opacity 0.35** (0.55→0.35 kullanıcı onayı; "çok daha modern").
  - Katman 2: `AnaBorder` paneli (`CardBackgroundFillColorSecondaryBrush` + `CardStrokeColorDefaultBrush` + `OverlayCornerRadius` + `ThemeShadow` + Padding 24).
  - Katman 3: kartlar (`ModernCard`) + `ThemeShadow`+`Translation` (receiver `OnPageLoaded` try/catch) + aralık 12px.
- Marka **yatay** (AppIcon solda + "MuhasibPro" sağında), login **sağda** (kullanıcı isteği: "login sağda olur").
- `?` butonu **ana border'ın sağ üst köşesinde** (`YardimButton`, `AutomationProperties.AutomationId`); canlı tık → "Giriş — Yardım" açılıyor.
- Eski InventEase "buzlu anakart"/`Custom*` aliaslar kaldırıldı; `NamePasswordControl`/`QuickLoginPanel` Fluent'e çekildi.

### MVVM düzeltmesi (kullanıcı: "code-behind metodla dolu")
- `LoginView.xaml.cs`'ten `OnYardimClick`/`OnTeshisClicked` **silindi** → `LoginViewModel.TeshisCommand` + `YardimCommand`.
- Altyapı: `YardimMaddesiDto` (Business DTO) + `IDialogService.ShowYardimAsync` + `DialogService` impl (View chrome App'te) — Kural 5 zinciri.
- Code-behind'da yalnız lifecycle + gölge receiver kaldı.

### Logo (`M` → `AppIcon.png`)
- 5 kullanım: Login, ExtendedSplash, KurulumSplash, SistemDbYonetim, FirmaShell (Login canlı doğrulandı; diğerleri tur kapsamında).

### Dialog zemini (kullanıcı: "arka plan transparent")
- `YardimDialog` Background `CardBackgroundFillColorDefaultBrush` (yarı saydam) → **`SolidBackgroundFillColorBaseBrush` (opak)**; canlı doğrulandı.

### Kural güncellemesi (kullanıcı: "önce kural metnini güncelle")
- `AGENTS.md` **Kural 17**: "Katmanlı sayfa yapısı (zemin → ana border → kartlar)"; tint ~%35; ana border paneli; opak dialog zeminleri.
- `docs/TASARIM-KURALLARI.md` aynı dile göre yeniden yazıldı.
- Tüm view'larda tint 0.35'e çekildi (11 dosya).

### Doğrulama
- `dotnet build MuhasibPro.sln` **0 hata / 0 uyarı**.
- Canlı kanıt: `Temp/opencode/ot248_login_v3_1440.png`, `ot248_login_light.png`, `ot248_help2.png`, `ot248_splash_3000.png`.

### Kalan (yeni oturum)
- **Ana border paneli** kalan view'lara tek tek (MaliDonemYonetim, TenantDatabaseUpdate, Update, MainShell, ShellView, DatabaseSettings, SistemDbYonetim, FirmaShell, DenetimMasasi) + ilgili dialog opaklığı + ? butonu panel köşesi.
- Kalan 4 logonun canlı doğrulaması.
- `KONTROL/ROADMAP` güncellemesi.

---

## 2026-09-13 — Oturum 249 (Kural 17 Katman 2: FirmaShellView ana border paneli)

### İstek (kullanıcı)
"kaldığımız yerden devam edelim" → Oturum 248'in kalan listesinden **FirmaShellView** (kullanıcı onayı, Kural 8) Kural 17 Katman 2 (ana border paneli) geçişi.

### Araştırma (Kural 13/14 — içerik + tasarım)
- **İçerik (sektör):** Sage 50 "Company Selection" penceresi — şirket listesi (ad/sürüm/vergi no/mali yıl/veri yolu), "Add Company", satır-başı işlemler → mevcut FirmaShell firma+dönem seçim yapısıyla uyumlu, davranış değişikliği gerekmedi.
- **Tasarım (MS Fluent):** Windows 11 design principles — Elevation & layering (yüzeyleri üst üste bindirip hiyerarşi), Materials (Mica/Acrylic), yuvarlatılmış geometri → Kural 17 katmanlı yapı teyit edildi.
- **Referans defteri (Kural 19):** 2 satır `docs/REFERANSLAR.md`'ye **uygulamadan önce** eklendi, tamamlanınca `Durum` ✅ yapıldı.

### Uygulanan (Kural 8 sınıf onayı: FirmaShellView)
- **`FirmaShellView.xaml`:** Kök zemin (Katman 1) korundu; tüm içerik (sticky header + listeler) tek **`AnaBorder`** paneli içine alındı — `CardBackgroundFillColorSecondaryBrush` + `CardStrokeColorDefaultBrush` + 1px + `OverlayCornerRadius` + `Padding 24` + `ThemeShadow`; `PanelWrap` kenarlardan boşluklu (`Margin 24,48,24,24`; üst boşluk pencere caption düğmeleri için) ve `MaxWidth 1600`.
- `CustomGlassPanel` sarmalayıcı ve `SecimGorunumu` adlı gereksiz StackPanel kaldırıldı (Kural 4); içerik kartları (`CustomModernCard` listeleri) Katman 3 olarak doğrudan panel içinde.
- **`?` yardım butonu** (Kural 13) ana border'ın sağ üst köşesine (`YardimButton`) eklendi.
- **`FirmaShellView.xaml.cs`:** `Loaded="OnPageLoaded"` + `AnaBorderShadow.Receivers.Add(RootGrid)` (try/catch — ctor'da değil, Splash emsali).
- **`FirmaShellViewModel.cs`:** `YardimCommand` + `YardimGoster` (6 maddelik `YardimMaddesiDto` listesi; `DialogService.ShowYardimAsync`) — MVVM/Kural 5 zinciri.
- **`Cards.xaml`:** artık 0 kullanıcı olan ölü `CustomGlassPanel` stili silindi (Kural 4).

### Doğrulama
- `dotnet build MuhasibPro.sln` **0 hata**; `dotnet test` **482/482** (mimari bekçi dahil).
- **Canlı test (Kural 18 — agent yürüttü):** TransferDialog (`PrimaryButton`) → Login (hızlı giriş + giriş) → **FirmaShell**. Ekran görüntüsü: `Temp/opencode/ot249_firmashell.png` (ana border paneli + çevrede zemin + `?` panel köşesinde), `Temp/opencode/ot249_help.png` ("Firma & Mali Dönem Seçimi — Yardım" 6 madde). UIA: `Temp/opencode/ot249_firmashell_uia.txt`, `ot249_help_uia.txt`.
- **Light/Dark:** Denetim Masası → Görünüm'den tema **Light** seçildi ("Kaydedildi"), ancak yeniden başlatmada Light render yansımadı (otomasyon/kalıcılık) — **Light kanıtı eksik, dürüst kayıt**; yapı yalnız `ThemeResource`/Fluent anahtarları kullandığından ve Login pilotu (Oturum 248) aynı desenle Light doğrulandığından davranış parity'si beklenir, teyit sonraki tura bırakıldı.
- **Kural 17 durumu:** Katman 2 ✅ `KurulumSplash`, `SistemDbYonetimView`, `FirmaShellView`, `DenetimMasasiView`, `LoginView`; kalan: `MaliDonemYonetimView`, `TenantDatabaseUpdateView`, `UpdateView`, `DatabaseSettingsView`, `MainShellView`, `ShellView`, `ExtendedSplash`.

### Kalan (yeni oturum)
- Kalan view'lara Katman 2 (yukarıdaki liste) + ilgili dialog opaklığı + `?` panel köşesi.
- FirmaShell Light/Mica canlı teyidi.

---

## 2026-09-13 — Oturum 249 (devam) — Denetim Masası Windows Ayarlar yeniden tasarımı (🔨 KISMİ — yeni oturumda sürer)

### İstek (kullanıcı)
"denetim masası görünümünü Windows ayarlar görünümü gibi olsun" → "giriş sayfasını Windows 11 ayarlar giriş sayfası gibi yap" → "orta sayfa tasarımı header tasarımı da aynı olsun" → "içerikleri yazıları komple yeniden tasarla" → **"sahte ayar, kaydedildi text gibi saçma sapan işlemler istemiyorum."**

### Kararlar (kullanıcı onayı)
- **Zemin: Mica** — Denetim penceresinde opak resim zemin kaldırıldı; `DetailsWindow` Mica'sı görünür. **Kural 17 Mica istisnası** kaydedildi.
- **Ayarlar kartı: CommunityToolkit `SettingsCard`** (resmi/stabil). DevWinUI'nin ported kopyası reddedildi.
- **İçerik + metin komple yeniden tasarım**; **sahte ayar** (tüketicisiz, "ayrı fazda" notlu) ve **"Kaydedildi" metni** yasak.

### Araştırma (Kural 13/14/19 → REFERANSLAR 3 satır, 🔨)
- MS "Guidelines for app settings": tam pencere, content max ~1000–1100px, `SettingsCard`/`SettingsExpander`, `BodyStrong` bölüm başlıkları, tema ComboBox ("Light/Dark/Use system setting"), opak arka plan yok (Mica).
- MS WinUI 3 app structure + `NavigationView`: `PaneDisplayMode=Left` kabuk, `AutoSuggestBox` pane'de, opak zemin Mica'yı kapatır.
- Windows 11 Settings **Home**: üstte hesap/cihaz özeti + etkileşimli **kategori kartları ızgarası**.

### Paket çalışması (sorun → çözüm)
- **DevWinUI 10.4.1 yalnız `net10.0`** → uygulama `net8.0` olduğu için `SettingsCard` bulunamadı (WMC0001). **Bu, önceki modelin takıldığı yerdi.**
- `DevWinUI` **10.0.0** (`net8.0-windows` destekli, 10.1+ yalnız net10) yükseltildi/düşürüldü.
- **`CommunityToolkit.WinUI.Controls.SettingsControls` 8.2.251219** eklendi (net8; namespace `using:CommunityToolkit.WinUI.Controls`). WASDK 2.4.0 uyumlu.
- İlk dev-winui denemesi sırasında yazılan geçici yerli `Controls/SettingsCard` + `Styles/SettingsCard.xaml` **silindi** (Kural 4).

### Yapılan
- **`DenetimMasasiView` (kabuk):** kök `Border` (transparent — Mica), **`NavigationView` Left** (kenardan kenara; `AutoSuggestBox` pane'de; `MenuItemsSource=GorunurMenuler`), sağda büyük başlık + `?` + `Frame` (`MaxWidth 1000`). Opak `Image`+tint zemin kaldırıldı.
- **Paneller (4/6):** `AppPlatform`, `Identity`, `FirmaKayit`, `Donem` → `controls:SettingsCard` + `BodyStrong` bölüm başlıkları + **yeniden yazılmış metinler** (sade dil, "ne işe yarar"); Yönetici rozeti `Header` öğesinde korundu; **sahte satırlar** (Oturum süresi, Son giriş bilgisi) ve **"Kaydedildi" metni** kaldırıldı.
- **`GirisDashboardSayfasi` (Giriş = Ayarlar Home):** hero (PersonPicture + ad/rol + firma/dönem özeti + Yeni Firma) + **kategori kartları ızgarası** (`ItemsWrapGrid` 3×~316 + `SettingsCard IsClickEnabled`; tıklayınca bölüme gider) + **Firmalarım** kartı (işlev korundu) + firma-yok durumu.
- **VM:** `AyarlarNavigationMenu` → `Aciklama` alanı + yeni başlık/açıklamalar; `GirisDashboardViewModel` → `Kategoriler`, kullanıcı bilgisi (ad/rol/avatar), `OzetMetni`; `GirisDashboardSayfasi.xaml.cs` → `OnKategoriClick`.

### Doğrulama
- `dotnet build MuhasibPro.sln` **0 hata**.
- Canlı test (Kural 18) **yapılmadı** (bu turda uygulama kapatıldı). Elde: yeni kabuk ekran görüntüsü `Temp/opencode/ot249_denetim.png` (kenardan kenara pane + arama + büyük başlık).

### Kalan (YENİ OTURUM — devralınacak)
1. **`SistemVeritabaniAyarlarPaneli` + `YedekSaklamaAyarlarPaneli`** hâlâ eski `CustomModernCard`; `SettingsCard` + yeni metinlere çevrilecek (YedekSaklama'da sahte "Haftalık bütünlük kontrolü" kaldırılacak).
2. **Canlı test (Kural 18):** SettingsCard render + Mica + Giriş (Home) sayfası + Light/Dark ekran görüntüsü/UIA; kullanıcı onayı.
3. **Metin/temizlik borcu:** `KaydedildiMetni` VM'lerden (6 Ayar VM) + testlerden temizlik; `Identity` `SessionTimeoutMin`/`SonGirisBilgisiniGoster` (sahte) VM/model/provider/test temizliği (Kural 4).
4. **Kural 13:** nav başlıkları değişti → `DenetimMasasiView.xaml.cs` yardım maddeleri güncellenecek; (fonksiyonlu view yardımı).
5. Test seti panel/VM değişikliklerinden sonra yeniden koşulacak (en son 482/482 idi).
6. `KONTROL/ROADMAP` + `REFERANSLAR` `Durum`ları kapanışta güncellenecek.

---

## 2026-09-13 — Oturum 249 (devam 2) — Veritabanı tek view + token/tema bugfix + canlı Light/Dark

### İstek (kullanıcı)
"Oturum 249 devam" → onaylı kalan listesi; ek soru: "veritabanı ayarlarını tek view içinde Sistem ve dönem olarak göstersek daha mı iyi olur" → karar: **tek sayfa, iki bölüm/expander** (MS SettingsExpander).

### Uygulanan (Kural 8 sınıf onayı)
- **Panel dönüşümü + temizlik:** `SistemVeritabaniAyarlarPaneli` + `YedekSaklamaAyarlarPaneli` → `SettingsCard`; 6 Ayar VM'den `KaydedildiMetni` silindi; Identity sahte `SessionTimeoutMin`/`SonGirisBilgisiniGoster` (VM+model+provider+test) temizlendi (Kural 4); Yedek Saklama'dan sahte "Haftalık bütünlük kontrolü" kaldırıldı.
- **Veritabanı tek view:** Nav tek `Veritabanı` (alt menü yok); yeni `VeritabaniAyarlarPaneli` iki `SettingsExpander` (Sistem Veritabanı + Mali Dönem Veritabanları) + `VeritabaniAyarSayfasi`; `AyarBolumu` sadeleşti (3 ölü değer silindi); eski 4 dosya silindi; arama artık açıklamada da eşleşir.
- **Kural 13:** `DenetimMasasiView.xaml.cs` yardım maddeleri yeni nav/metinlere göre güncellendi.
- **Bug fix (canlı testte çıktı):** `ToolBar.xaml` geçersiz `TextFillColorTertiaryColor` → `TextFillColorTertiary`; `YonetimAyarlarDialog.xaml` `ControlFillColorSecondaryColor` → `ControlFillColorSecondary` (XamlParse → siyah/boş Denetim penceresi).
- **Tema bug fix:** `ThemeSelectorService` `IEventBus.Subscribe<AppSettingsChangedEvent>` → kayıtta platform temasını canlı uygular + `AppBackgroundRequestedTheme`'e yazar (açılışta okunur). Sahte tema ayarı gerçek oldu.

### Araştırma (Kural 14/19 → REFERANSLAR)
- MS `SettingsExpander` (grup, 1 seviye, MaxWidth/Grid glitch) + MS app settings guidelines; Sage 50 Company>Backup + QuickBooks backup (verify/saklama/kapanışta yedek). 5 satır deftere yazıldı (Durum ✅).

### Doğrulama
- `dotnet build MuhasibPro.sln` **0 hata**; `dotnet test` **481/481** (sahte ayar testi kaldırıldı: 482→481).
- **Canlı test (Kural 18 — agent yürüttü):** Transfer→Login→FirmaShell→Denetim Masası; tek `Veritabanı` nav + iki expander (Dark: `Temp/opencode/ot249c_veritabani.png`, `ot249d_top.png`, `ot249e_bottom.png`); Light canlı (`ot249f_after_light.png`) + açılışta Light (`ot249g_denetim_startup.png`) + Veritabanı Light (`ot249g_veritabani_light.png`); UIA `ot249d_top_uia.txt` vb. **Kullanıcı onayı ALINDI (2026-09-13).**

### Kalan
- Diğer view'lara Kural 17 Katman 2 turu sürüyor (MaliDonemYonetim, TenantDatabaseUpdate, Update, MainShell, ShellView, DatabaseSettings, ExtendedSplash).

---

## 2026-09-13 — Oturum 251 (Denetim Masası Giriş: Win11 karşılaştırma → RichButton tasarımı + rol/busy/canlı güncelleme denetimi — ✅🧪)

### İstek (kullanıcı, 4 tur)
1. "Denetim Masası ile Windows 11 Ayarlar (Giriş) şu an aynı mı görünüyor?" → karşılaştırma; 2. "bu şekilde tasarlayalım" → pane üstü kullanıcı (foto destekli) + MuhasibPro/Sistem.db/Güncelleme + Kullanıcıya Ait Firmalar (firma expander → altında mali dönem listesi); 3. revizyon: **RichButton kart içine alınmaz** (Win11 gibi), "ayar içinde ayar" olmaz (Ayarlar bölüm kartları kaldırılır), **arama navbar'a geri**, Giriş paneli **oransal** yerleşim; 4. "Yerel Hesap yerine kullanıcı bilgisinden Rol yaz; güncelleme + Sistem.db dinamik → busy bağla; **her Denetim Masası girişinde** güncelleme taraması — uygulamayı kilitlemeden ring/progress ile".

### Araştırma (Kural 14/19 → REFERANSLAR)
- **Canlı referans:** Windows 11 25H2 (build 26200) Ayarlar Giriş ekran görüntüsü (`ot251_winsettings.png`) — pane üstü hesap kartı, Giriş yerleşimi, seçili nav vurgusu.
- **DevWinUI RichButton:** docs + Galeri `RichButtonPage.xaml` + kaynak `RichButton.cs` (Title/SubTitle/Icon/ActionIcon/DisplayMode/Command + public Click); **kritik:** `App.xaml`'e `ms-appx:///DevWinUI/Themes/Generic.xaml` merge şart (README).
- REFERANSLAR'a 4 satır (canlı ref + kullanıcı kararı + RichButton + merge kuralı) — Durum ✅.

### Plan değişikliği (dörtlü kayıt)
- **İşlem öncesi plan:** Win11 25H2 birebir — arama üst-ortada + Giriş'te 2 sütun (sol Ayarlar kategorileri, sağ firma listesi) + top Border kartlar.
- **Plan durumu:** İlk uygulama tamamlandı (ot251e/f) ve kullanıcıya sunuldu.
- **Değişiklik sebebi:** Kullanıcı kararı — "ayar içinde ayar" gereksiz (bölümler navbar'da), arama pane'de kalsın, üst öğeler kart kabı olmadan RichButton olsun, yerleşim oransal.
- **Uygulanan sonuç:** Bu kayıttaki nihai yapı (Giriş = 3 RichButton + Kullanıcıya Ait Firmalar kartı; arama pane'de; Ayarlar kategorileri kaldırıldı).

### Uygulanan (Kural 8 onayları: DenetimMasasiView → GirisDashboardSayfasi)
- **`DenetimMasasiView.xaml`/`.cs`:** `PaneHeader` hesap kartı (PersonPicture + ad + rol) + arama pane'e geri + nav seçim vurgusu fix (`NavSeciminiAynala`: aynı örnekte dahi null→hedef; `SeciliMenu` bildiriminde de) + pencere 1380×880.
- **`DenetimMasasiViewModel`:** `GorunurMenuleriTazele` yalnız gerçek değişimde `Clear` (`SequenceEqual`); rol fallback "Kullanıcı"; ctor'a `ISistemDatabaseService`+`IUpdateService` geçişi.
- **`GirisDashboardViewModel` (yeniden):** Ayarlar kategorileri kaldırıldı; `DonemKarti`/`FirmaDashboardKarti.Donemler` + `DonemOzeti`; `IsFirmalarYukleniyor`/`BosDurumGoster` + `IsSistemDbYukleniyor` + `SistemDbAltMetni`/`SistemDbDetayMetni`; `IsGuncellemeDenetleniyor` + pencere-başına-1 `_guncellemeDenetlendi` + **20 sn tavan** + her durumda sonuç + unobserved-exception guard; `YukleAsync` **sıralı**.
- **`GirisDashboardSayfasi.xaml`/`.cs`:** üstte kart kapsız 3 `dev:RichButton` (MuhasibPro ReadOnly + sürüm; Sistem Veritabanı `Bağlı • Güncel • boyut` + ring; Güncelleme + ring) + "Kullanıcıya Ait Firmalar" kartı (Yeni Firma + `SettingsExpander` per firma → `SettingsExpander.ItemsSource` mali dönem `SettingsCard` satırları + "Gelişmiş Yönetim" ItemsHeader) + boş-durum; `App.xaml` DevWinUI Generic merge; yardım maddeleri güncellendi (Kural 13).
- **`AuthenticationService`:** seed yöneticisi (KFR satırı yok) → sentetik `Rol=Yönetici` (AyarYetkiDenetimi bootstrap kuralıyla aynı).
- **`FirmaListelemeService.GetFirmalarWithUserId`:** dönem projeksiyonu eklendi ("0 dönem" bug fix).

### Doğrulama (Kural 18)
- `dotnet build` **0 hata**; `dotnet test` **481/481**.
- Canlı (agent): login → Denetim Masası → Giriş; `ot251l_home.png` — pane **"Yönetici"**, Sistem.db **"Bağlı • Güncel • 1,04 MB"**, Güncelleme "kaynağı tanımlı değil" (kaynak ayarsız dürüst durum), firmalar expander → `2027/2026/2025` dönem satırları + "Açık" (UIA `ot251l_home_uia.txt`, `ot251l_expanded_uia.txt`). Denetim yolu kanıtı: `ot251j` (kaynak varken "Denetlenemedi • Kaynağa ulaşılamadı") vs `ot251k` (sıralı fix sonrası "Bağlı • Güncel").
- **Kullanıcı onayı ALINDI (2026-09-13).**

### Kalan
- Kural 17 Katman 2 sıradaki view'lar (MaliDonemYonetim → TenantDatabaseUpdate → Update → MainShell → ShellView → DatabaseSettings); Denetim Masası Mica istisnası sürüyor.
- Denetim Masası gerçek simge kaynağı: kurulu sürümde FeedUrl ayarlanınca "her girişte denetim" tam turu (kullanıcı FeedUrl girerse otomatik).

---

## 2026-09-13 — Oturum 251 (devam) — Ana border hairline + tek görsel/tema perdesi pilotu (✅🧪)

### İstek (kullanıcı)
"Ana border'ın kenarlığı dark'ta açık gri, light'ta beyaz olsa daha mı ön planda durur?" → referans olarak Dynamics 365 ekran görüntüsü paylaşıldı ("repos/ekrankaydi") → "light resmin üzerine dark bir rectangle perde yapsan daha stabil olmaz mı" → "uygula" → "gri yapmayacağız, mevcut kartlarımızı kullanacağız; 365 tasarımı iptal, log'a ekle".

### Araştırma / karar (Kural 14/19)
- Dynamics 365 görseli piksel ölçümü: panel kenarı **iki taraftan da AÇIK** 1px hairline (L≈253; zemin L≈228, panel L≈246). Zemin görselleri ölçümü: `light.jpg` L≈210, eski `dark.jpg` L≈46.
- Değerlendirilen renkler: **Light `#F2FFFFFF`** (beyaz hairline; panel Default dolgusuna karşı +8 rim), **Dark `#59FFFFFF`** (≈%35 beyaz; +59 rim). Yeni token: `MuhasibAnaBorderCizgiBrush` (ThemeDictionaries).
- Zemin kararı: **tek görsel `light.jpg` + tema perdesi** `MuhasibZeminPerdeBrush` (Light `#59F3F3F3` = bugünkü %35 tint'in birebir karşılığı; Dark `#D9202020` ≈%85) — iki ayrı zemin görselinden stabil.

### Uygulanan (Kural 8 sınıf sınıf)
- `DesignTokens.xaml`: hairline Color (Light/Dark) + Brush; `MuhasibZeminPerdeBrush` (Light/Dark); `UygulamaZeminGorseli` Dark → `light.jpg`.
- Pilot (canlı kanıtlı): `ExtendedSplash` SplashCard + `LoginView` AnaBorder → hairline token; Login `AnaBorder`'a eksik `Translation="0,0,32"` eklendi (gölge artık basıyor); perde her iki view'da.
- Yayılım: 9 view'da (`MainShell`, `MaliDonemYonetim`, `SistemDbYonetim`, `UpdateView`, `DatabaseSettingsView`, `TenantDatabaseUpdateView`, `ShellView`, `KurulumSplashView`, `FirmaShellView`) eski tint satırı → perde token (`edit` tool).
- Dark zemin görseli: Unsplash "soft light gradient" indirilip `dark.jpg` yapıldı; **tek görsel+perde kararı sonrası kullanılmıyor** (asset duruyor; kaldırma kararı sonraki bakımda).

### Kullanıcı kararları (kayıt)
- **365 tasarımı İPTAL** ("bir sonraki oturumda 365 tasarımına geçeceğiz" notu geçersiz).
- **Gri yön yok:** "gri yapmayacağız, mevcut kartlarımızı kullanacağız" — ana border içinde kart yapısı zaten uyumlu, mevcut kartlar korunur.

### Ders (HATALAR)
- PowerShell `Get-Content/Set-Content` toplu replace 9 XAML'de encoding bozdu (mojibake + BOM) → `git checkout` ile geri alındı, `edit` tool ile yeniden yapıldı. Detay: `HATALAR.md`.

### Doğrulama (Kural 18)
- `dotnet build` 0 hata + `dotnet test` 481/481; diff dosya başına 1-2 satır (encoding temiz).
- Canlı: Light login (`ot252_Light_login.png`), Dark splash (`ot252b_Dark_t2500.png`), Dark login (`ot252_Dark_login.png`), dark kenar zoom (`ot252_dark_login_zoom.png`); kullanıcı light'ı onayladı ("harika durdu"), dark perde çalışıldı.

### Gri rötuş + onay (aynı oturum)
- Kullanıcı: "biraz daha griye döndür" + "beyaz patlıyor gibi" → hairline değerleri yumuşak griye çekildi: **Light `#E6DFE1E4`, Dark `#45C9CCD1`** (thickness 1 kalır; ilk değerler `#F2FFFFFF`/`#59FFFFFF` idi).
- Canlı: Light login (`ot252_Light_login.png` — gri hat, parlama yok) + Dark login (`ot252_Dark_login.png`); kullanıcı onayı: **"budur"**.
- Test: **481/481**; pilot tema zorlaması temizlendi (`%LocalAppData%\MuhasibPro\ApplicationData\LocalSettings.json` silindi).

### Kullanıcı kararı (kapanış)
- **365 tasarımı İPTAL** — "bir sonraki oturumda 365 tasarımına geçeceğiz" notu geçersiz.
- **Gri yön yok / mevcut kartlar:** "gri yapmayacağız, mevcut kartlarımızı kullanacağız" — ana border içinde kart yapısı uyumlu, mevcut kartlar korunur.
- Oturum kullanıcı onayıyla kapatıldı; ana border hairline'ı kalan ana panellere yayma işi Katman 2 turuna bırakıldı.

### Kalan
- Ana border hairline'ı henüz almayan ana paneller (FirmaShell `AnaBorder`, KurulumSplash kartı) — Katman 2 turunda.
- `dark.jpg` asset kaldırılsın mı (artık kullanılmıyor) — kullanıcı kararı.
- Light zemin `light.jpg` hâlâ 5000×3333 (~63,6 MB decode) → 2560'a indirme fırsatı.

---

## 2026-09-14 — Oturum 252 (Sahte transfer uyarısı düzeltmesi + dialog tema/tek buton + DEV işareti + çift açılma engeli — ✅🧪)

### İstek (kullanıcı, 3 mesaj)
1. "Uygulamayı publish yapıp kurduk, sonra Windows'tan uninstall yaptım — bu transfer dialog neden açılıyor, benim ne işime yarıyor?" (önce teşhis, sonra düzeltme)
2. "Düzeltelim; dialog açık temada bile dark; içerik profesyonel olsun; Anladım ile Kapat aynı işi yapıyor; bloklamıyorsa anlamı yok — dev'de gerekliyse DEV-Mode yazalım."
3. "Uygulamanın çift açılmasını da engelle; zaten açık uyarısı verebilirsin."

### Teşhis (kanıtlı)
- **Veri:** tenant damgaları `9e67…`, güncel kurulum `8ae5…` (**13.09 22:24:34'te yeniden üretilmiş**); `MakineId` iki tarafta aynı (`15e9fb7d…`). Gerçek transfer değil — **Oturum 251'de pilot tema temizliği için `LocalSettings.json` silinince** kimlik kaynağı kaybolmuş, `KurulumKayitService.GetOrCreateAsync` yeni kimlik üretmiş; tarama tüm dönemleri "farklı kurulum" saymış.
- **Kod açığı:** `TenantVersionReader`/`CheckTransferAsync` makine–kurulum ayrımı yapmadan tek `HasMismatches` döndürüyor; dialog release'de de açılıyor; içerik/buton çift.
- **Tema:** OS Dark (registry `AppsUseLightTheme=0`) + uygulama Light iken dialog Dark açıyordu — ContentDialog popup katmanı kök eleman temasını izlemiyor ("miras" garantisi yok).
- **Çift örnek:** kodda tek-örnek koruması yok (grep 0).

### Plan değişikliği (dörtlü kayıt)
- **İşlem öncesi plan:** Transfer düzeltmesi (aynı makine → sessiz onarım; dialog yalnız gerçek transfer) + dialog içerik/tema düzeltmesi.
- **Plan durumu:** Kullanıcı onayladı ("Onaylıyorum, uygula").
- **Değişiklik sebebi:** Kullanıcı ek istekleri — tek buton + profesyonel içerik + DEV işareti + çift açılma engeli (kapsam büyüdü, tasarım aynı).
- **Uygulanan sonuç:** Aşağıdaki altı dosya grubu; dev-mode ise ayrı faz olarak (yalnız kimlik/transfer + teşhis araçları kapsamıyla) PLAN'a alındı.

### Uygulanan
- **Business/Data:** `TransferCheckResult` + `AlignedCount`/`AdoptedKurulumId`; `SplashRoutingService.CheckTransferAsync` — makine farklı → **gerçek transfer** (dialog listesi + olay); makine aynı → **kimlik kaybı** (sessiz onarım). Tek eski kimlik → `IKurulumKayitService.UpdateKurulumIdAsync` ile kimlik damgalardan geri alınır (o kimlikle alınmış eski yedeklerle uyum korunur); karışık kimlik → `ITenantSQLiteDatabaseService.ReAlignTenantKurulumIdsAsync` ile dönemler güncel kimliğe eşitlenir.
- **UI:** `SplashNavigator` — bildirim yalnız `#if DEBUG`; release'de gerçek transfer bilgisi geri-yükleme hükmünde (bloklayan akış) kalır; onarım logu her derlemede. `TransferDialog` — tek "Kapat" butonu ("Anladım" kaldırıldı) + profesyonel içerik (ne algılandı / ne anlama gelir / ne yapmalı) + "yalnız geliştirme derlemesinde gösterilir" notu.
- **Tema:** `DialogHelper.ApplyAppTheme` — uygulama teması runtime'dan yazılır (XAML'de hardcode yok); `ShowCenteredAsync` + `DialogService.CreateDialog` tek kapı.
- **DEV işareti:** `AppSurumBilgisi.Metin` DEBUG'da `DEV • v…` (Login/FirmaShell/SistemDbYonetim sürüm footer'ları).
- **Çift açılma:** yeni `Helpers/SingleInstanceGuard` (named Mutex + 3 sn yeniden-başlatma toleransı; ikinci örnekte `#32770` "zaten açık" uyarısı + mevcut pencereyi öne getirme denemesi) + `App` ctor kapısı.
- **Test:** `SplashRoutingTests` +2 (tek eski kimlik onarımı / karışık kimlik eşitleme) — build **0 hata** + test **483/483**.

### Canlı kanıt (Kural 18 — agent)
- **Kimlik onarımı:** tema probu (Light) + OS Dark → Login Light + `DEV • v1.1.3 …` (`ot252_login_light.png`, UIA `DEV-TEXT=…`); transfer dialogu **açılmadı**; DB kanıtı: Sistem.db GlobalAyarlar **ve** LocalSettings `KurulumId=9e67…` (onarım çalıştı; eskisi `8ae5…`).
- **Gerçek transfer:** `db-F-0001_2025.MakineId=TEST-BASKA-MAKINE` ile dialog açıldı — **açık tema**, iki paragraflı içerik + sarı "Etkilenen" satırı (`db-F-0001_2025.db • Kurulum:9e676d2d → 9e676d2d • Makine:farklı`) + tek buton (`ot252_transfer_dialog.png`); "Kapat" → Login (`ot252_transfer_after_close.png`).
- **Çift açılma:** ikinci örnek `#32770` "MuhasibPro zaten açık…" kutusu (`ot252_ikinci_ornek_uyari.png`), ikinci proses çıktı, tek pencere kaldı.
- **Temizlik:** `MakineId` geri alındı + tema probu silindi; son açılış temiz (`ot252_final_clean.png`, UIA `TRANSFER-DIALOG=False`). Uygulama kapatıldı.
- **Kullanıcı onayı bekleniyor** (kanıtlar bu sunumla iletildi).

### Kalan
- **Kullanıcı sıralaması (2026-09-14):** **sıradaki oturum = KONTROL 6.80 Katman 2 turu** (ana border hairline kalan paneller); **dev-mode (6.82) faz olarak bekliyor** — Katman 2 sonrası.
- **Dev-mode iskeleti** (kullanıcı onaylı kapsam: kimlik/transfer araçları + teşhis araçları) — `IDevModeProvider` + Denetim Masası "Geliştirici Araçları" + `DEV` log damgası; restore/göç zorlama ve silme kilidi bypass kapsam DIŞI (kullanıcı kararı).
- KONTROL 6.80 Katman 2 turu (ana border hairline kalan paneller) aynen duruyor.
- **Onay notu:** kanıtlar kullanıcıya sunuldu; kullanıcı ret vermedi ve sıradaki adımı belirledi (onay kabul edildi — itiraz olursa LOG düzeltilir).

---

## 2026-09-14 — Oturum 253 (Faz 6.80 Katman 2 turu: ana border + hairline + dialog opaklığı + Light/Dark canlı — ✅🧪)

### İstek (kullanıcı)
"faz 6.80 deki işlemleri tamamlayalım" → 4 kapsam sorusu soruldu, kullanıcı onayladı:
1. **Onay usulü:** toplu onay + her dosyada diff gösterimi (Kural 8 esnetilmedi, kayıt burada).
2. **MainShell:** sağ içerik (başlık + gövde) ana panele; NavSidebar zeminde kalır. **ShellView:** pencere chrome'u → Katman 2 eklenmez.
3. **Dialog opaklığı:** yarı saydam `AcrylicBackgroundFillColorDefaultBrush` kökleri opak `SolidBackgroundFillColorBaseBrush`'a çevrilir.
4. **Asset:** `dark.jpg` silinir, `light.jpg` 2560px'e küçültülür.

### Plan değişikliği (dörtlü kayıt)
- **İşlem öncesi plan:** KONTROL 6.80 maddeleri — kalan 8 view'a ana border + hairline + `?` panel köşesi + ilgili dialog opaklığı + asset kararı + FirmaShell Light/logo canlı teyitleri.
- **Plan durumu:** yukarıdaki 4 onayla uygulandı.
- **Değişiklik sebebi:** (a) `DatabaseSettingsView` canlı rotası yok (0 navigasyon çağrısı; yalnız `Startup.Register` + bir test) — yine de onaylı listede olduğu için dönüştürüldü, ölü-rota temizliği ayrı karara bırakıldı; (b) ShellView/DenetimMasası kararı gereği değişmedi.
- **Uygulanan sonuç:** aşağıdaki dosya listesi; KONTROL 6.80 maddeleri kapatıldı (TenantDatabaseUpdate canlı turu erişilemedi — dürüst kayıt).

### Uygulanan
- **Hairline:** `FirmaShellView` AnaBorder `CardStrokeColorDefaultBrush→MuhasibAnaBorderCizgiBrush` + `Translation="0,0,32"` (eksikti); `KurulumSplashView` SplashCard aynı hairline.
- **Katman 2 (ana border):** `MaliDonemYonetimView` (`CustomElevatedCard`→`AnaBorder`), `TenantDatabaseUpdateView`, `UpdateView`, `SistemDbYonetimView` (panel + `?` panel sağ üst köşesine taşındı), `DatabaseSettingsView`, `MainShellView` (sağ içerik paneli; sidebar zeminde). `ShellView` (chrome) ve `DenetimMasasiView` (Mica istisnası) değişmedi.
- **Gölge deseni:** her panelde `ThemeShadow` + `Loaded` receiver (ctor'da değil — Splash emsali).
- **`?` yardım (Kural 13):** yeni VM command + içerik — `MaliDonemYonetimViewModel`, `TenantDatabaseUpdateViewModel`, `UpdateViewModel`, `DatabaseSettingsViewModel`, `MainShellViewModel`; buton hepsinde ana border sağ üst köşesinde.
- **Dialog opaklığı (10 dosya):** `DeleteGuardDialog`, `RestoreVerifyDialog`, `TenantDatabaseUpdateDialog`, `TransferDialog`, `YeniDonemDialog`, `SagaPipelineDialog`, `YonetimAyarlarDialog`, `YetimYedeklerDialog`, `YedekSilOnayDialog`, `SistemRestoreVerifyDialog` kökleri opak.
- **Tema fırça süpürmesi (Oturum 230 kuralı):** `TenantDatabaseUpdateView`, `UpdateView`, `MainShellView`, `FirmalarListControl` (22 nokta), `UserInfoControl`, `NavSidebarControl`, `AnimatedInfoBorder` `{StaticResource TextFillColor/SystemFillColor/Muhasib*}`→`{ThemeResource}`. **Canlı bulgu:** FirmaShell çocukları StaticResource yüzünden çalışma-zamanı Light geçişinde beyaz-beyaz kalıyordu; süpürme sonrası düzeldi (`ot253h_firmashell_light.png`→`ot253i_firmashell_light.png`).
- **Bug fix (kendi regresyonum):** `UpdateView.GomuluUygula` ilk yazımda `KokZemin.Visibility=Collapsed` yapıyordu — KokZemin TÜM içeriği sardığı için gömülü Güncelleme boş açıldı; yalnız `ZeminGorseli`+`ZeminPerdesi` katmanları gizlenecek şekilde düzeltildi.
- **Bug fix (latent):** `MainShellView.ViewModel` DataContext'e bağlıydı ama DataContext hiçbir yerde atanmıyordu (null) → VM ctor'da `ServiceLocator` ile çözülüp `DataContext`'e verildi.
- **Ölü stil (Kural 4):** `Cards.xaml` `InventFrostPanelStyle` silindi (son tüketicisi MainShell'di); `CustomElevatedCard` silindi (son tüketicisi MaliDonemYonetimView Katman-2'ye geçti — 0 caller). `MuhasibPro.csproj`'dan silinen `dark.jpg`'nin bayat `None Remove` satırı temizlendi. (`CustomCompactCard` 0 caller — eski "rezerve" kaydı Fluent yönünde geçersiz; temizlik adayı olarak KONTROL'e not.)
- **Asset:** `Assets/Images/dark.jpg` silindi; `light.jpg` 5000×3333 → 2560×1706 (ffmpeg q2).
- **Kural 19:** `REFERANSLAR.md`'ye yayılım satırı (yeni internet araştırması yok — mevcut desen; MS layering + hairline ölçümü + Toolkit Settings kaynakları).
- **HATALAR:** gömülü-zemin gizleme dersi + StaticResource tema fırçası yinelemesi kayda geçti.

### Doğrulama (Kural 18 — agent)
- `dotnet build MuhasibPro.sln` **0 hata**; `dotnet test` **483/483**.
- **Dark kanıtları:** `ot253a_firmashell_dark.png` (yeni hairline), `ot253b_yonetim_dark.png` (`?` köşede), `ot253b_yardim_dark.png` (opak yardım), `ot253c_denetim_home_dark/veritabani_dark/guncelleme_dark.png` (gömülü UpdateView), `ot253e_sistemdb_*` (panel + `?`), `ot253f_mainshell_*`.
- **Light kanıtları:** `ot253h_gorunum_light/guncelleme_light/veritabani_light/firmashell_light/yonetim_light/deleteguard_light.png` (runtime switch), `ot253i_login_light/sistemdb_light/firmashell_light/mainshell_light.png` (açılıştan Light).
- **UIA:** `YardimButton` her panelde bulundu, yardım dialogu açıldı/kapandı; `DevamEtButton` → MainShell; Denetim nav seçimleri (`Veritabanı/Güncelleme/Görünüm`); `DEV • v1.1.3` footer.
- **Tema:** `LocalSettings.json` `AppBackgroundRequestedTheme":"Light"` (seçim doğrulandı); açılış + runtime switch iki senaryo da temiz.

### Kalan
- **Kullanıcı onayı bekleniyor** (kanıtlar bu sunumla iletildi).
- **TenantDatabaseUpdateView canlı turu:** göç bekleyen dönem yok → ekran canlı açılamadı (dürüst kayıt); kod + XAML derleme doğrulandı.
- **DatabaseSettingsView:** canlı rota yok (ölü görünüm + test bağı) → ayrı temizlik kararı (Kural 4) bekliyor.
- Sıradaki iş: **Faz 6.82 dev-mode** (kullanıcı sıralaması).

---

## 2026-09-14 — Oturum 254 (Ana tasarım dili MÜHÜR + Faz 6.83 TenantDatabaseUpdateView turu — kod değişikliği yok, 📋 kural/faz kaydı)

### İstek (kullanıcı)
"Ana tasarım dilimizi mühürle. Bundan sonra oluşturulacak View/UserControl vs. görünümler bu mimari üzerine inşa edilecek. Ana kart ve içindeki kart stili tarzını diyorum. Görünüm tarafında bundan sonraki işlemler sadece View içindeki kontrol yerleri ve araştırma sonucu eklenecek/çıkarılacak butonlar olacak. TenantDatabaseUpdateView için ayrı faz aç, unutmayalım."

### Uygulanan (kod değişikliği YOK — süreç/kural + faz kaydı)
- **MÜHÜR — `AGENTS.md` Kural 17:** `zemin → ana border (ana kart) → içerik kartları` mimarisi **kilitli** ilan edildi; bundan sonraki her View/UserControl/Dialog bu yapı üzerine inşa edilir. Görünüm işlerinde kapsam yalnızca: (a) kontrol yerleşimleri, (b) araştırma sonucu (Kural 14) eklenen/çıkarılan buton-kontroller. **Katman 2 ana border + Katman 3 iç kart stilleri (renk/radius/gölge/padding/hairline/kart arası boşluk) mührün parçası; habersiz değiştirilmez** — yalnız kullanıcı kararıyla mühür revizyonu.
- **`docs/TASARIM-KURALLARI.md`:** aynı mühür metni doküman başına işlendi.
- **`docs/ROADMAP.md`:** Faz 6.80 durumu "🔒 MÜHÜRLÜ + Katman 2 TAMAM"; karar loguna mühür satırı; **Faz 6.83** satırı açıldı.
- **`docs/KONTROL-LISTESI.md`:** **Faz 6.83 — TenantDatabaseUpdateView canlı doğrulama turu** bölümü açıldı (kontrollü test dönemi → canlı akış → Kural 18 kanıtları; neden ayrı faz: göç bekleyen dönem yokluğu).

### Not
- Bu oturum **yalnız kural/faz kaydıdır** — üretim kodu, XAML ve stil dosyalarına dokunulmadı; build/test kapısı önceki oturumun yeşilini korur (0 hata + 483/483).
- Mühür, Oturum 253'te tamamlanan Katman-2 turuyla canlı doğrulanmış stil setini (Fluent ThemeResource + `MuhasibAnaBorderCizgiBrush` hairline + `ThemeShadow` + 12px kart aralığı) sabitler.

### Kalan
- **Faz 6.83** (TenantDatabaseUpdateView canlı tur) — planlı, sırası geldiğinde uygulanır (dev-mode 6.82 ile sıralama kullanıcıda).
- Oturum 253 kullanıcı onayı (6.80 kapanışı) — kanıtlar sunulmuştu; bu oturumda da ret verilmedi.

---

## 2026-09-14 — Oturum 255 (FirmaShellView seçim deneyimi: araştırma + Faz 6.84 planı — kod değişikliği yok, 📋 plan)

### İstek (kullanıcı)
FirmaShellView içeriği için değerlendirme: üst header/logo/ayarlar/UserInfo konumları; firma arama kutusu **popup** açsın, içinde firmalar listelensin, seçilen firma **altta kart** olsun; mali dönem **RadioButton stili beğenilmiyor**; text/icon renkleri tasarımla uyumlu değil; **DevamEt butonunun solunda seçili firma+dönem** gösterilsin; firma kartındaki **sıkışık butonlar** konforlu olsun. "Değerlendir, araştırmanı yap, plan çıkar."

### Araştırma (Kural 14/19 → REFERANSLAR 8 satır, 📋 plan)
- **Sektör:** Sage 50 Company Selection (kolonlu tam liste + Add Company + satır aksiyonu) · Tally (F3 liste; yüklü şirket bilgisi ekranda kalır) · Oracle Retail Select Company (liste üstü arama, filtre tüm sonuçlarda) · Deltek (arama + sonuç sayısı).
- **MS Fluent:** Radio Buttons kılavuzu — "seçenekler bağlama göre değişiyorsa **liste kontrolü** kullan" (dinamik mali dönem listesi için radio kartı yanlış kontrol); AutoSuggestBox (yazarken filtre + zengin `ItemTemplate` + `SuggestionChosen`); Selection mode / ListView `Single`; ComboBox yalnız tek satırlık ikincil seçimler için (zengin kartlar listede kalır).
- **Sonuç:** Kullanıcının önerdiği "arama → popup liste → seçili kart" akışı sektör + MS kılavuzu ile uyumlu; radio kart stili hem kullanıcı zevki hem MS kılavuzu gereği değişmeli.

### Plan (Faz 6.84 — `KONTROL-LISTESI`'ne işlendi)
1. Header: tek kapsül → kimlik solda (logo+ürün), aksiyon sağda (Denetim Masası + UserInfo).
2. Firma seçimi: `AutoSuggestBox` (odakta dropdown, boşken tümü, yazarken filtre; zengin satır: No/kod + ünvan + il + `N dönem` + seçili işareti).
3. Seçili firma kartı (alt): detaylar + `N dönem` hapı; aksiyonlar ayrı konforlu satırda (`Düzenle`, `Mali Dönem İşlemleri`).
4. Mali dönem: RadioButton kartı bırakılır → `ListView SelectionMode=Single` (accent sol çubuk, radyo dairesi yok) + **sabit** seçili dönem detay kartı (DB adı/boyut/DB durumu/son yedek + `Yedek`).
5. CTA: alt barda solda `Firma • Yıl • durum` özeti, sağda `Çalışma Alanına Geç` (seçimsiz disabled + neden tooltip).
6. Token/renk sözleşmesi: ink siyah hap kaldırılır; olive/petrol alias'ları view'dan çıkar; accent + system renkleri; ikonlar TextFill ikincil/üçüncül.
7. Davranış korunur: `Selection` akışı, `DevamEt` guard'ları, güncelleme koordinatörü, son-seçim kaydı, Kural 13 yardım güncellemesi.

**Not (mühür):** Mali dönem seçim stilinin değişimi ve firma kartı aksiyon yerleşimi **mühür revizyonu** sayılır — kullanıcı talebiyle açıldı; onayla uygulanır.

### Plan değişikliği (dörtlü kayıt)
- **İşlem öncesi plan:** yok (yeni talep) — önce değerlendirme/araştırma istendi, kod yazılmadı.
- **Plan durumu:** Faz 6.84 taslağı hazır; **sınıf bazlı onay bekliyor**.
- **Değişiklik sebebi:** kullanıcı geri bildirimi ile radio kart stili + renk uyumu + CTA özeti + buton konforu kapsama alındı.
- **Uygulanan sonuç:** bu oturumda kod yok; REFERANSLAR + KONTROL + ROADMAP kaydı yapıldı.

### Kalan
- **Faz 6.84 sınıf onayı** (hangi view/kontrol hangi sırayla) → uygulama → build/test + Kural 18 canlı (Light/Dark) + yardım güncel.
- Dev-mode (6.82) ve TenantDatabaseUpdateView canlı turu (6.83) planlı sırada.

---

## 2026-09-14 — Oturum 256 (Kullanıcı Yönetimi ayrı faz + FirmaShellView dashboard yönü — araştırma + karar; kod yok, context devri)

### İstek (kullanıcı, zincir)
1. "Kullanıcı yönetimini ayrı faz olarak aç" + "Kullanıcı yönetimi **modal** olarak açılsın".
2. "Kullanıcı yönetim kartı ayrı olsun, ama tasarımlar iç içe geçmesin. FirmaShellView en sık kullanılacak alanlardan biri, bir nevi **uygulama giriş öncesi yönetim paneli** olacak; kullanıcı deneyimi yüksek olsun."
3. "Denetim Masası adı **Ayarlar** olarak geçsin; kod tarafı önemli değil, kullanıcı Ayarlar görsün."
4. "Onaylıyorum; araştırarak yaparsan sorun olacağını sanmıyorum."
5. **Kapanış:** "Oturumu logla, yeni oturum başlatayım; context doldu. Modüler tasarım olarak yaptığımız için **FirmaShellView çekirdek yapısı dashboard gibi çalışacak.**"

### Araştırma (Kural 14/19)
- **Sektör:** Sage 50 Company Selection · Tally Gateway (F3 liste, yüklü şirket görünür) · Oracle Retail Select Company (liste üstü arama, tüm sonuçlarda filtre) · Deltek (arama + sonuç sayısı) · QuickBooks "Users & Roles" (yalnız admin kullanıcı oluşturur/yönetir; kullanıcı + rol + aktive) · MYOB Manage Users/Roles (rol bazlı yetki tablosu).
- **MS Fluent:** Radio Buttons ("seçenekler bağlama göre değişiyorsa **liste kontrolü** kullan") · AutoSuggestBox (yazarken filtre + zengin `ItemTemplate` + `SuggestionChosen`) · Selection mode / ListView `Single` · ComboBox (zengin/çok satırlı içerik için değil) · **InfoBar** (inline, kesintisiz, aksiyonlu; "kullanıcı bilgilendirilsin/aksiyon alsın" durumları; bloklamayan durum bildirimi).
- **Sözleşme keşfi (2 paralel explore ajanı, kod yok):** `GenericListViewModel.SelectedItem` setter'ı `model.Selected` bayraklarını senkronlar (`SyncSelectedFlags`) + `EntityEvents.ItemSelected` yayınlar; `FirmaListViewModel.Query` Data katmanında `ArananTerim.Contains` ile filtreliyor (sayfa yolu `ToLower`, count yolu değil — küçük tutarsızlık notu); `FirmaDetailsArgs { FirmaId }` ile **düzenleme yolu hazır** (`CreateNewViewAsync<FirmaDetailsViewModel>`); `MaliDonemModel` durum/analiz alanları hazır (`DbGuncellemeGerekliMi`, `DbKontrolGerekliMi`, `DbDosyaYokMu`, `IsDbAnalyzing`, `DbBekleyenGuncellemeListesi`); **6.85:** `IKullaniciService` (liste/güncelle/aktif/şifre/sil) hazır, **oluşturma yok** — `IAuthenticationService.Register(email, username, password, confirm)` ek kullanıcıya izin veriyor (ilk-kullanıcı kapısı yok; duplicate e-posta/kullanıcı adı kontrolü var; `RegistrationResult` enum); `KullaniciModel`'de `ParolaHash` yok; `AyarYetkiDenetimi.KullaniciYoneticiMi` public; `IPasswordHasher<Kullanici>` Singleton; modal pencere deseni `DenetimMasasiView` (OnLoaded `AppWindow.Resize` + `CreateNewViewAsync<VM>(null, "başlık")`); `UserInfoControl` auth servisine doğrudan erişmiyor (`ShellArgs.UserInfo` → `HesapModel` üzerinden).

### Kesinleşen kararlar (onaylı — uygulama yeni oturumda)
**Faz 6.84 — FirmaShellView (giriş öncesi yönetim paneli / dashboard çekirdeği):**
1. **Header:** en solda logo; en sağda **simgeli `Ayarlar`** butonu + `UserInfoControl`. Orta alan dashboard başlığına ayrılır (modüler dashboard yerleşimi).
2. **Firma seçimi:** **seçici buton + Flyout** (içinde arama; **sonuç yoksa "Firma bulunamadı"**); seçim → flyout kapanır; altında **seçili firma kartı** (detay + `N dönem`; aksiyonlar ayrı konforlu satır: `Düzenle` → mevcut `FirmaDetailsViewModel` düzenleme yolu, `Mali Dönem İşlemleri` → mevcut yönetim penceresi).
3. **Mali dönemler:** önce **liste**; **seçilen satır açılıp kart gibi durur** (accordion); "seçildi" işareti accent; **`RadioButton` stili tamamen kalkar** (mühür revizyonu).
4. **Busy + tenant:** her dönem kendi analiz ring'i (`IsDbAnalyzing`, 20sn tavan); tenant geçişi tek kapı/`UpdateCoordinator` + `_dbGate` serileştirme; hata → seçim geri + hata bandı. Açık/Kapalı/Güncelleme gerekli/DB yok bilgileri kartta.
5. **Güncelleme bildirimi:** **InfoBar** (bloklamaz, kapatılabilir) — tek dönemde doğrudan `TenantDatabaseUpdateView`; çok dönemde "N dönemde güncelleme hazır → İncele" listesi, satır başı Güncelle ilgili sayfaya.
6. **CTA:** alt bar solda `Firma • Yıl • durum` özeti, sağda `Çalışma Alanına Geç` (uygunsa aktif; değilse disabled + neden tooltip).
7. **Token/renk:** ink siyah hap kalkar → accent seçim göstergesi; olive/petrol alias yığını view'dan çıkar; ikonlar TextFill ikincil; durumlar System renkleri.
8. **"Ayarlar" görünür ad süpürmesi:** `FirmaShellView` buton+tooltip, pencere başlığı, `FirmaShellViewModel`/`DenetimMasasiView`/`SistemDbYonetimView` yardım metinleri. Kod sınıf adları (`DenetimMasasi*`) aynı kalır.
9. **UX kabul kriteri:** klavye (ok/Enter/Esc), flyout kapanınca odak dönüşü, boş-durumlar, busy, UIA adları, tooltip kapsamı; dashboard çekirdeği modüler (Firma seçici / Dönem listesi / Güncelleme InfoBar'ı / CTA özeti ayrı bloklar).

**Faz 6.85 — Kullanıcı Yönetimi (ayrı faz, MODAL pencere, tasarım iç içe geçmez):**
1. **Ayrı modal pencere** (`DenetimMasasi` deseni: `CreateNewViewAsync<KullaniciYonetimiViewModel>(null, "Kullanıcı Yönetimi")`; `OnLoaded` boyutlandırma); FirmaShell/Ayarlar içine gömülmez.
2. **İki alan karışmaz:** (a) **Hesabım** — Kullanıcı Bilgilerim + Şifre Değiştir (küçük ayrı dialog; `UserInfoControl` menüsünden); (b) **Kullanıcı Yönetimi** — admin: kullanıcı listesi + **yeni kullanıcı** (`AuthenticationService.Register` ek-kullanıcı destekli) + düzenle (`UpdateKullaniciAsync`) + aktif/pasif (`SetAktifAsync`) + şifre belirle (`SifreBelirleAsync`) + silme guard'lı (`DeleteKullaniciAsync`); rol atama (KFR) KAPSAM NOTU: firma-bağımlı olduğu için bu fazda gösterim; atama sonraki iyileştirme.
3. `UserInfoControl` menüsü iki grup (Hesabım / Yönetim[admin]) + Oturumu Kapat; **sahte madde yok** (madde ancak yüzey hazır olunca eklenir).
4. Admin kapısı: `AyarYetkiDenetimi.KullaniciYoneticiMi` (seed yönetici + rol=Yönetici); Kural 13 yardım zorunlu.

### Devir notu (yeni oturum)
- **Sıradaki iş:** Faz 6.84 uygulaması — sınıf sırası: `Cards.xaml` (seçimli liste stili; radio emekli) → `MaliDonemlerListControl` → firma seçici+kart (`FirmalarListControl`) → `FirmaShellView` header/dashboard → VM'ler (`FirmaShellViewModel` özet+yardım, `FirmaListViewModel` filtre/boş-durum, `MaliDonemListViewModel` güncelleme toplama) → "Ayarlar" süpürmesi; sonra **Faz 6.85** modal + Hesabım.
- **Teknik notlar:** `FirmaListViewModel` count yolu `Contains` (ToLower farkı) filtre tutarlılığı için düzeltilebilir; `FirmaDetailsViewModel` düzenleme arg'ı `FirmaDetailsArgs{FirmaId}`; `KullaniciModel`'de ParolaHash yok (şifre işlemleri servis üzerinden); InfoBar severity: güncelleme = `Warning`, bilgi = `Informational`.
- Bu oturumda **üretim kodu yazılmadı** (context nedeniyle devir); build/test önceki yeşil (0 hata + 483/483).

---

## 2026-09-14 — Oturum 257 (Kaldığı yer tespiti + loglanmamış 6.84 başlangıcının kayda geçirilmesi — kod yazılmadı, kurtarma/log oturumu)

### İstek (kullanıcı)
"Son oturumda model cevap vermeyi bıraktı. Onun aldığı log gibi, son kalınan yeri tespit et ve yapılan işlemleri logla."

### Tespit yöntemi
- **LOG indeksi:** son kayıtlı oturum **256** ("Kullanıcı Yönetimi ayrı faz + dashboard yönü — kod yok, context devri").
- **Git:** HEAD `a564f0a` (Oturum 137-251 aralığı) — Oturum 252-256 işleri **commitlenmemiş**, kirli ağaçta duruyor.
- **Kirli ağaç:** `git status` → **74 dosya** değişmiş (`git diff --stat`: 1787 ekleme / 633 silme); bunun bir kısmı loglanmış (252-256), bir kısmı **hiç loglanmamış**.

### Ayrım (loglanmış ama commitlenmemiş vs. hiç loglanmamış)
- **(a) Loglanmış, commitlenmemiş (252-256, dokunma yok):** Oturum 252 transfer ayrımı (`CheckTransferAsync` makine/kurulum ayrımı, `UpdateKurulumIdAsync`/`ReAlignTenantKurulumIdsAsync`, `ApplyAppTheme`, `DEV` işareti, `SingleInstanceGuard` + testler) + Oturum 253 Katman-2 turu (6 view ana border, 5 VM `?` yardımı, 10 dialog opaklığı, Static→Theme süpürmesi, `dark.jpg` silme/`light.jpg` küçültme) + Oturum 254-256 yalnız-docs (mühür, Faz 6.83, Faz 6.84 planı, Faz 6.85, REFERANSLAR 8+2 satır). Bu oturumda bunlara dokunulmadı.
- **(b) Hiç loglanmamış — Faz 6.84 uygulamasına başlanmış (son oturumdaki model, yarıda kalmış):** kirli ağaçta 256'nın "kod yok" hükmüyle çelişen 6.84 izleri:
  - `Styles/Cards.xaml`: `MuhasibCardRadioButtonStyle` → **`MuhasibSelectionListItemStyle`** (ListView seçimli satır: accent sol çubuk + attention yıkama + `PointerOverSelected`/`PressedSelected`; `CustomElevatedCard` + `InventFrostPanelStyle` silinmiş) + `MuhasibSelectionTrueToVis` converter kaydı.
  - `FirmalarListControl.xaml(+.cs)`: eski arama + kart listesi yerine **seçici buton + Flyout** (içinde arama `FirmaAramaBox`, zengin satırlar, seçili işareti, **"Firma bulunamadı"** boş-durumu) + altında **seçili firma kartı** (detay + `N dönem` + konforlu aksiyon satırı); petrol/zeytin alias'ları → Fluent `CardBackground`/`TextFill` token'ları.
  - `MaliDonemlerListControl.xaml(+.cs)`: **güncelleme InfoBar'ı** (`Warning`, `GuncellemeOzeti` + `Güncelle`/`İncele (N)` aksiyonu + etkilenen-dönem Flyout listesi) + radio kartından seçimli-listeye geçiş başlangıcı; header sayaçları Fluent token'lara.
  - `FirmaShellView.xaml(+.cs)`: header/dashboard + CTA özeti yönünde düzenleme (148 satır diff).
  - VM'ler: `FirmaShellViewModel` → **`SecimOzeti`** ("Firma • Yıl • durum") + **`DevamEtNedenTooltip`** + toplu-analiz sonrası özet tazeleme + yardım metinleri yeni akışa göre (seçici/Flyout, accordion, InfoBar, **"Ayarlar"**); `MaliDonemListViewModel` → **`GuncellemeBekleyenDonemler`/`GuncellemeVarMi`/`GuncellemeOzeti`/`GuncellemeAksiyonMetni` + `TazeleGuncellemeBildirimi()`** (analiz/E1/yenileme sonrası); `FirmaListViewModel` → filtrede seçim koruma (boş filtrede kart/dönem kaybolmaz); `OnMaliDonemSelected` aynı-dönem yeniden seçiminde tenant geçişini tekrar tetiklemez.
  - Küçükler: `Icons.xaml` `IconChevronDown` (seçici oku); `TenantSQLiteSelectionService` aynı-tenant artık idempotent **Success** ("Zaten bu mali dönem bağlı").

### Doğrulama (bu oturum)
- `dotnet build MuhasibPro.sln` → **0 uyarı, 0 hata** (29,86 sn).
- `dotnet test MuhasibPro.sln` → **483/483** (önceki yeşille aynı; yeni test eklenmemiş).
- Canlı test YOK (kurtarma/log oturumu; Kural 18 kapısı 6.84'e ait, bu oturuma değil).

### Plan değişikliği (dörtlü kayıt)
- **İşlem öncesi plan:** Oturum 256 devri — "kod yok, Faz 6.84 uygulaması yeni oturumda (sınıf sırası LOG 256'da)".
- **Plan durumu:** uygulama kirli ağaçta **başlamış** bulundu (sınıf sırasına uygun sırada: stil → dönem listesi → firma seçici/kart → header/CTA → VM'ler), ancak **loglanmamış ve commitlenmemiş**.
- **Değişiklik sebebi:** son oturumdaki model yanıt vermeyi bırakmış; iş loga işlenememiş.
- **Uygulanan sonuç:** bu oturumda **üretim koduna dokunulmadı** (ağaç donduruldu); yalnızca Oturum 257 kurtarma kaydı + indeks + KONTROL 6.84 durum notu + ROADMAP 6.84 durum notu yazıldı.

### Kalan (yeni oturuma)
- Kirli ağaç **commitlenmedi** (onay kullanıcıda) — içinde 252-256 loglanmış işi + 6.84 yarım işi birlikte duruyor.
- 6.84'ten eksik görünenler: accordion detay kartının tamamlanması, "Ayarlar" görünür-ad süpürmesi, klavye/odak-dönüşü/UIA-tretman kabul kriterleri, **Kural 18 canlı turu (Light/Dark, busy + tenant geçişi)** + kullanıcı onayı.
- Kural 8 sınıf onayları 6.84 için hâlâ açık — yarım işin devamı onay sırasına göre (önce onay, sonra kod).

---

## 2026-09-14 — Oturum 258 (Mühür revizyonu: Light hairline beyaza + Login form sol hizası — ✅🧪)

### İstek (kullanıcı)
1. "Açık temada ana kart border rengi beyaz olabilir mi" (+ netleştirme: "dark aynı kalacak, sadece açık temada beyaza dönsün; dark bozulacaksa orta renk").
2. "Login'de logo ve adım barı dahil NamePasswordControl başlangıç hizası" (+ netleştirme: logo bloğuna girinti YOK; form, logo+adım barı bölümünün başlangıç hizasıyla aynı hizada).

### Karar/teknik not
- Token sözlükleri tema-bağımsız (`DesignTokens.xaml:47` Light / `:80` Dark ayrı) → Light `#E6DFE1E4` → `#FFFFFFFF`, Dark `#45C9CCD1` aynen; Dark bozulmaz, orta renge gerek yok. Uyarı kaydı: beyaz 1px çizgi açık zeminde düşük kontrastlıdır (Oturum 251 "patlama" dersi); kullanıcı onayıyla uygulandı, Light canlı turu kullanıcıda.
- Form: `LoginView.xaml:160` `NamePasswordControl` `HorizontalAlignment Center→Stretch` (`MaxWidth 380` + kart `Padding 20` korunur) → sol kenar kart pedine dayanır, sol kolon ritmiyle aynı hizada. Logo/adım barı değişmedi. Davranış/stil/token değişikliği yok.

### Uygulanan (sınıf bazında onaylı 2 dosya)
- `DesignTokens.xaml:46-47` + `LoginView.xaml:160`.
- `dotnet build MuhasibPro.sln` → 0 uyarı 0 hata (20,98 sn); `dotnet test` → 483/483.
- Mühür revizyonu kayıtları: `TASARIM-KURALLARI.md` + `AGENTS.md` Kural 17 + `ROADMAP.md` karar logu + `KONTROL-LISTESI.md` hairline satırı.

### Kalan
- Beyaz çizginin Light canlı turu kullanıcıda (kontrast/patlama geri bildirimi beklenir).
- Kirli ağaç hâlâ commitlenmedi (252-258 işleri; onay kullanıcıda).

---

## 2026-09-14 — Oturum 259 (Login üst hizası düzeltmesi + beyaz çizgi onayı — ✅🧪)

### İstek (kullanıcı)
Oturum 258'deki form-sola-yaslama yorumu netleştirildi: "logo ve adım barı dahil NamePasswordControl'ün üst border'i ile aynı hizada olsun; logo metinleri ve adım barı yukarı taşınacak". Form `Center`'da kalacak.

### Uygulanan (tek dosya: `LoginView.xaml`, sınıf onaylı)
- `NamePasswordControl` Oturum 258 değişikliği geri alındı (`Stretch→Center`, satır 160).
- `SolPanel` `VerticalAlignment Center→Top` (satır 86) → logo bloğu yukarı taşındı, üst kenarı `GirisKarti` üst border'ıyla aynı hizada. `GirisKarti`/kolon/`MaxWidth`/token/stil/davranış değişmedi.
- `dotnet build MuhasibPro.sln` → 0 uyarı 0 hata; `dotnet test` → 483/483.

### Karar
- Beyaz çizgi kullanıcıda canlı doğrulandı: "bu şekilde kalsın" — Light `#FFFFFFFF` kalıcı, Dark `#45C9CCD1` aynen.

### Kalan
- Kirli ağaç hâlâ commitlenmedi (252-259 işleri; onay kullanıcıda).

---

## 2026-09-14 — Oturum 260 (Faz 6.84 kod sayımı: yapılanlar doğrulandı + kalanlar raporu — kod yazılmadı, inceleme)

### İstek (kullanıcı)
"6.84'de FirmaShellView içinde birçok işlem yapıldı ama model cevap vermediği için tam loglara kaydedilmedi. 6.84 içinde yapılan işlemleri incele, kalanları raporla."

### Yöntem
`git diff HEAD` (kirli ağaç 75 dosya) + 12 adet 6.84 dosyasının satır-sayımı + `grep` süpürmeleri (radio kalıntısı, "Denetim Masası" görünür metni, Petrol/Olive/Ink kalıntısı, ölü handler). Oturum 257'nin "loglanmamış başlangıç" notu tek tek doğrulandı; build/test bu ortamda koşulamadı (dotnet WSL interop hatası) — son yeşil Oturum 259'dan (0/0 + 483/483), kod ağacı o zamandan beri değişmedi.

### Yapılanlar (kodda doğrulandı — plan m.1-7,9)
- **Stil:** `MuhasibCardRadioButtonStyle` → `MuhasibSelectionListItemStyle` (ListViewItem: accent sol çubuk model `Selected`'ına bağlı + `PointerOverSelected`/`PressedSelected` ayrı); `MuhasibSelectionTrueToVis`; `IconChevronDown`. Radio referansı kaynakta **0**.
- **Firma seçimi (m.2):** seçici buton + Flyout (arama + zengin satır + seçili işareti + **"Firma bulunamadı"**) + seçili firma kartı (`Düzenle` → `FirmaDetailsArgs` düzenleme yolu, `Mali Dönem İşlemleri` → yönetim penceresi) + "Firma seçilmedi" boş-durumu + başlıkta "Yeni Firma".
- **Dönem listesi (m.3-4):** `ListView Single` + seçimli stil; accordion detay (DB yok uyarısı + Veritabanı/Boyut + DB Durumu + Son Yedek + Yedek); satır başı analiz ring'i (20sn tavan pre-existing); ink "Aktif Seçim" hapı kalktı.
- **InfoBar (m.5):** `Warning` + `GuncellemeOzeti` + tek dönemde "Güncelle" (doğrudan sayfa), çok dönemde "İncele (N)" flyout + satır "Güncelle".
- **Header + CTA (m.1,6):** logo sol / dashboard başlığı orta / simgeli `Ayarlar` + UserInfo sağ; progress scroll dışına (sticky); CTA alt barı (`SecimOzeti` + `DevamEtButton` + neden tooltip); pencere başlığı **"Ayarlar"**.
- **VM/servis:** `SecimOzeti`/`DevamEtNedenTooltip` (+ toplu-analiz tazeleme); `Guncelleme*` bildirimi (E1/analiz/yenileme sonrası); filtrede seçim koruma; aynı-dönem guard'ı; `SwitchTenantAsync` idempotent Success + test güncel (`AyniTenant_IdempotentBasari`, eski mesaj kaynakta 0); yardım 8 maddeye güncellendi.
- **Süpürmeler:** görünür "Denetim Masası" metni **0** (kalanlar yalnız kod yorumu); 3 view'da Petrol/Olive/Ink **0**; ölü handler (`OnFirmaRadioChecked`, `SearchBox`, `ShakeDevamEtButton`…) **0**. `TopluAnalizTamamlandi` aboneliği sızıntı değil (liste VM'i shell VM'in ctor'unda üretiliyor, aynı ömür).

### Kalanlar
**Kod (küçük):** (1) Flyout açılışında arama kutusuna `Focus()` yok — klavye kullanıcısı Tab'le iner (m.8 kısmi). (2) Ok tuşlarıyla gezinme seçimi anında değiştiriyor → her vuruşta dönem reload; canlıda değerlendirilecek. (3) Firma seçili + 0 dönem için boş-durum mesajı yok (Kural 11). (4) Bayat metin: "önce soldan bir şirket seçin" (`MaliDonemlerListControl.xaml.cs:40`) + kapalı-dönem yorumu ("animasyonlu" — shake silindi). (5) `FirmaRepository` count yolu `ToLower` tutarsızlığı (pre-existing, LOG 256 notu) duruyor. (6) Sessiz `catch {}` (3 nokta, Kural 12).
**Doğrulama (büyük):** Kural 18 canlı turu hiç yapılmadı (Light/Dark: seçici/flyout/arama, accordion, InfoBar tek/çok, CTA özet/tooltip, ring, tenant geçişi, güncelleme sayfası) + UIA + onay; Kural 8 sınıf onayları açık (12 dosya + test); 6.84'e yeni test eklenmedi; REFERANSLAR 6.84 satırları 📋 plan durumunda.

### Kalan (yeni oturuma)
Yukarıdaki 6 kod maddesi (onaylı sırayla) → Kural 18 canlı turu + onay → REFERANSLAR `Durum` ✅ → commit kararı (kirli ağaç 252-260 işleri).





