# MuhasibPro — Referans Defteri (araştırma kaynağı → projede uygulandığı yer)

> **KURAL (AGENTS Kural 19):** İnternetten/sektörden alınan **her** tasarım/akış/desen referansı buraya tek satırla yazılır:
> **kaynak** (site/doküman + kısa bulgu) + **projede hangi view/özelliğe uygulandığı** + oturum/tarih.
> - Referansı kaydedilmeden tasarım uygulanmaz (araştırma → defter → uygulama).
> - Aynı view/özellik için **yeni bir referans** uygulanmadan önce bu defter okunur; mevcut kayıtla çelişki varsa kullanıcıya sorulur (Kural 15).
> - Amaç: sonraki oturumlar/agent'lar aynı araştırmayı **tekrar yapmaz**, uygulanmış bir görünümü habersiz **ezmez** (stabilite).
> - Uygulama tamamlanınca `Durum` güncellenir; vazgeçilen referans `❌` ile işaretlenir.

| Tarih | Oturum | Kaynak | Bulgu / desen | Uygulandığı yer | Durum |
|---|---|---|---|---|---|
| 2026-09-16 | 276 | **MS — Conversation design guidance** (`learn.microsoft.com/power-platform/well-architected/experience-optimization/conversation-design`) | Konuşma tasarımı: kapsamı açıkça söyle (kullanıcı kapsam-dışı istemesin) + kesintiyi yönet (Durdur/yeni soru) + fallback/yanlış-anlamada yardım dokümanına yönlendir + seçimleri onayla (işbirlikçi his) | Faz 6.92 Adım 5 — sohbet paneli: kilit metni kapsamı söyler + Durdur + "yardım maddesi yok" fallback'u + ? yardım | 📋 plan |
| 2026-09-16 | 276 | **Kullanıcı kararı — yerleşim:** MainShell varsayılan alt paneli + Denetim Masası "Yapay Zeka" bölümü (birincil aksiyonlar sağda — Kural 16; durum/ilerleme altta) | Yerleşim araştırmayla değil kullanıcı talimatıyla sabitlendi; içerik/tasarım MS konuşma kılavuzu + proje Kural 17 kart diliyle | Faz 6.92 Adım 5 — `AsistanSohbetPaneli` + `YapayZekaAyarSayfasi` | ✅ karar |
| 2026-09-16 | 276 | **Foundry Local .NET SDK quickstart** (`learn.microsoft.com/azure/foundry-local/get-started` + `microsoft-foundry/foundry-samples` C# `native-chat-completions/Program.cs`) | net8 C#: `CreateAsync(config,logger)`→`DiscoverEps()`→`DownloadAndRegisterEpsAsync((ep,%))`→`GetCatalogAsync()`→`GetModelAsync(alias)`→`DownloadAsync(%)` (önbellekteyse atlar)→`LoadAsync()`→`GetChatClientAsync()`→`CompleteChatStreamingAsync(msgs,ct)` (`chunk.Choices[0].Message.Content`)→`UnloadAsync()`; `ChatMessage` Betalgo; "EP packages may be large" | Faz 6.92 — `IAsistanSohbetService` + Foundry impl + ilk-indirme determinate ilerleme (Kural 12) | 📋 plan |
| 2026-09-16 | 276 | **NuGet paket TFM incelemesi** (`WinML 1.2.4` = `net8.0-windows10.0.18362`; `xplat 2.0.1` = `net8.0`; `Betalgo.Ranul.OpenAI 9.1.0` transitif) | WinML paketi Windows-TFM ister → Business (düz `net8.0`) referans veremez; implementasyon **App katmanında** (`DialogService` emsali: sözleşme Business, impl App); xplat yedek yol | Faz 6.92 — `FoundryAsistanSohbetService` App/Services altında | ✅ karar |
| 2026-09-16 | 276 | **`Microsoft.Extensions.AI.IChatClient` → KULLANILMADI (sapma)** | Yabancı soyutlama + kör API uyarlama riski yerine öz `IAsistanSohbetService` sözleşmesi aynı hedefi verir (Open-Closed: yeni provider = yeni impl, Business değişmez); proje sözleşme geleneğine uygun | Faz 6.92 — öz sözleşme tercih edildi | ❌ (bilinçli ret) |
| 2026-09-16 | 276 | **Xero — JAX AI finance partner** (`xero.com/us/ai-in-accounting`) | "You decide what runs on its own, and everything Xero does is recorded for your review" — AI otomatik yazmaz, kullanıcı onaylar + her şey denetlenebilir | Faz 6.92 — salt-okunur asistan ilkesi (öneri üretir, kayıt yazmaz) | ✅ karar |
| 2026-09-16 | 276 | **Foundry katalog** (runtime `GetCatalogAsync()` + curated örnek: `Qwen2.5` multilingual, `Phi-4 Mini`) | Katalog çalışma-anında listelenir; alias **ayardan** okunur (Kural 7); varsayılan `qwen2.5-0.5b` (doküman örneği, geçici) — Türkçe kalite final seçimi Windows canlı testte | Faz 6.92 — `AiAsistanSettings.ModelAlias` | 📋 plan |
| 2026-09-16 | 276 | **Phi Silica → REDDEDİLDİ** (`learn.microsoft.com/windows/ai/apis/phi-silica`) | Copilot+ donanım tabanı + Limited-Access (Microsoft unlock token) + Eki-Kas 2026'da Aion Instruct'a geçiş/Phi kaldırma takvimi → emekliye ayrılan API'ye yatırım yok | Faz 6.92 — Foundry Local seçimi | ✅ karar |
| 2026-09-16 | 275 | **DevWinUI `StepBar`** (`ghost1372.github.io/handycontrol/extend_controls/stepBar`: `StepBar : ItemsControl`, `Dock=Top` yatay, `StepIndex`, `StepBarItem.Content`, `Next/Prev`) + **MS Wizard/step-bar deseni** (REFERANSLAR Oturum 269) | Adımlar **soldan sağa** yatay şerit olarak gösterilir (daire + bağlayıcı + etiket). DevWinUI StepBar yalnız **geçerli indeksi** bilir; **adım başına uyarı/hata** durumu renklendirilemez → özel Fluent stepper (daire + bağlayıcı + etiket; renk/ikon `PostUpdateAdimDurumuConverter`) tercih edildi | `GuncellemeSonrasiView` — dikey adım listesi → soldan sağa adım şeridi | ✅ |
| 2026-09-16 | 275 | **Oturum 274 bulguları (yeniden araştırma YOK — Kural 19 "aynı işi tekrar yapma")** — üretim güncelleme sırası (staging→doğrula→eskiyi koruyarak aktive→ilk açılışta health-check→yanlışsa rollback; fail-closed) + sektör (QB/Sage "önce doğrulanmış yedek, hata'da yedekten dön; bozuk=karantina yerine rapor") + Velopack hooks (ağır DB işi `OnRestarted` sonrası, headless fast-callback'e KONMAZ) | Post-update saga akışı: dosya doğrulama → ileri-uyumluluk guard → Sistem.db migrate/verify → hata'da pre-update yedeğinden restore → dönem tarama (bozuk→yedekten restore, pending rapor, future dokunma) | `GuncellemeSonrasiView` + `IPostUpdateDogrulamaService` (Faz 6.91-D) | ✅ |
| 2026-09-16 | 275 | **KurulumSplash deseni (proje içi emsal — Kural 17/Kural 12)** | Startup-blocking tek kart: zemin→kart→adım rozetli determinate bar + % pill + durum yazısı + hata paneli/Tekrar Dene; başarıda hedefe yönlendirme | `GuncellemeSonrasiView` (güncelleme sonrası doğrulama — splash ailesi, `?` yardım muaf) | ✅ |
| 2026-09-16 | 274 | **Velopack — UpdateManager API** (`docs.velopack.io/reference/cs/Velopack/UpdateManager`) | İndirme sonrası `VerifyPackageChecksumAsync(release)` paketi feed'deki hash ile doğrular, uyuşmazsa `ChecksumFailedException` fırlatır; bu hash **indirme bütünlüğü** içindir, MITM/bozuk sunucuya karşı **güvenlik önlemi değil** (maintainer beyanı) | Faz 6.91 — bütünlük Velopack'e bırakıldı; güven kökü (Authenticode imza) sonraki tur | ✅ (bütünlük) |
| 2026-09-16 | 274 | **Velopack — Code Signing** (`docs.velopack.io/packaging/signing`) | Setup/Update/ana exe Velopack tarafından `signtool`/Azure Trusted Signing ile imzalanır; kod imzası dağıtımdan **önce zorunlu** adımdır | Faz 6.91 — release hattı güven kökü (Authenticode imza) | 📋 plan |
| 2026-09-16 | 274 | **Velopack — Preserved files** (`velopack.docs` discussion #25) | Binary klasörü `%LocalAppData%\{AppId}\current` her güncellemede **tümüyle değişir**; `%LocalAppData%\{AppId}` **uninstall'da silinir**; yalnızca `%AppData%` uninstall'dan sağ kalır → veri app kökünde tutulmaz | Faz 6.91-A — veri kökü `%AppData%\MuhasibPro`'ya taşındı + tek seferlik taşıma (`IDataPathRelocationService`) | ✅ |
| 2026-09-16 | 274 | **Velopack — Targeting a Specific Version + UpdateOptions** (`docs.velopack.io/integrating/specific-version`) | `AllowVersionDowngrade` ile bozuk sürümden geri dönüş (rollback); auto-apply **asla** downgrade yapmaz, explicit `ApplyUpdatesAndRestart` şart; downgrade delta'yı kapatır + daha yeni yerel paketleri siler; `MaximumDeltasBeforeFallback` varsayılan 10 | Faz 6.91 — bozuk sürüm rollback yolu + downgrade davranışı | 📋 plan |
| 2026-09-16 | 274 | **Auto-Update Security Design** (`comcomponent.com/en/blog/2026/04/09/000-comcomponent-autoupdate-security`) | Üretim güncelleme sırası: metadata → imza/expiry/version doğrula → **staging'e indir** → hash/size/imza doğrula → **eskiyi koruyarak aktive et** → ilk açılışta **health-check** → yanlışsa **rollback**; **fail-closed** (doğrulama düşerse devam etme); saldırılar: rollback/freeze/mix-and-match → **en yüksek görülen sürümü sakla ve daha eskisini reddet** | Faz 6.91 — doğrulama sırası + fail-closed + post-update health-check + ileri-uyumluluk guard | 📋 plan |
| 2026-09-16 | 274 | **Schema version guard** — raindex PR #2698 · memtomem `e52a478` · quilltap `version-guard.ts` · chio-store `schema_version.rs` | Şema sürümü damgalanır; açılışta **disk sürümü > binary sürümü ise reddet** (fail-closed); damga **başarılı migration SONRASI** atılır (başarısız migration damga bırakmaz); `PRAGMA application_id` + `user_version` yapısal başlık koruma sağlar | Faz 6.91-C — Sistem.db + dönem DB ileri-uyumluluk guard'ı (`DbSchemaVersions.IsNewerThanSupported`, göç/yazma YOK) | ✅ (UI ince işi 6.88) |
| 2026-09-16 | 274 | **SQLite — How To Corrupt** (`sqlite.org/howtocorrupt.html`) + **Production Hardening** (`productionhardening.org`) + **sqlprostudio** | Canlı DB'nin dosya kopyası **torn copy** üretir (WAL'deki commit'ler kaçar); güvenli yol `VACUUM INTO`/backup API; WAL + `-shm` **eşleşmiş çifttir** (restore öncesi bayat olanlar silinir); `integrity_check`/`quick_check`; **her yedek doğrulanır**, bozuk kaynaktan alınan yedek işe yaramaz; bozuk dosya **karantinaya** alınır, silinmez | Faz 6.91 — verify/restore mekaniği güçlendirme (restore-sonrası integrity_check + karantina + doğrulanmış yedek) | 📋 plan |
| 2026-09-16 | 274 | **Sage 100 upgrade checklist** (`randgroup.com`, `help-sage100.na.sage.com`) | **Pre-Migration Utility** veriyi tarar; Rebuild data/sort; **doğrulanmış** yedek; **önce kopya üzerinde test upgrade**; kullanıcılar çıkar; upgrade sonrası raporlar eski toplamlarla **mutabakat** | Faz 6.91 — göç öncesi tarama/doğrulama + göç sonrası mutabakat (tablo/satır sayımı) | 📋 plan |
| 2026-09-16 | 274 | **Sage 50 Data Conversion Wizard guide** (`cleverence.com`) | Yedek **test edilir** (sandbox'a restore edip açılır); dönüşüm yarıda kalırsa yarım dosya **açılmaz** — yedek **temiz klasöre** restore edilip yeniden denenir; **sürüm-etiketli** yedek adı; ayrı ortam; düzenli restore tatbikatı | Faz 6.91 — ön-yedek testi + restore-to-fresh + sürüm etiketli yedek + tatbikat | 📋 plan |
| 2026-09-16 | 274 | **EF Core multi-tenant dynamic migration** — Chad Golden + **MS Multi-tenancy** + ByteCrafted | Her tenant DB **ayrı** migrate edilir; tenant başına hedef sürüm izlenir, uyuşmazlıkta migrate (erişimde); tek örnek/desktop'ta runtime migration kabul (EF9 DB-geneli lock); migration yedek-önce + idempotent | Faz 6.91 — dönem DB migrate stratejisi teyidi (mevcut "erişimde yedek→göç→verify→oto-restore") | ✅ teyit |
| 2026-09-15 | 273 | **Hoppscotch — Downgrading and Restoring Backups** (`docs.hoppscotch.io`) | Yeni sürüm ilk açılışında **sürüme-özel otomatik yedek** (`backup-by-vX.Y.Z`), en yeni **3** yedeği sakla; sorun olunca downgrade + restore | Faz 6.91 — güncelleme öncesi sürüm-damgalı otomatik yedek + kurtarma | 📋 plan |
| 2026-09-15 | 273 | **Octopus — Backups & Rollbacks** + **Redgate Flyway — Roll back or fix forward** + **ispirer rollback checklist** | Rollback scripti **yedek/restore yerine geçmez**; deploy **öncesi** yedek şart; **yedek ancak kullanıcı veri değiştirene kadar geçerli** → kurtarma penceresi açılıştır; bütünlük doğrula | Faz 6.91 — pre-update yedek + post-update doğrulama/recovery penceresi | 📋 plan |
| 2026-09-15 | 273 | **Velopack — App Hooks** (`docs.velopack.io/integrating/hooks`) | `OnFirstRun`/`OnRestarted` **normal açılışta** çalışır (UI açılabilir, uygulama devam eder) → ağır iş (sistem.db senkronu, tenant pending tespiti) buraya; `OnBefore/AfterUpdateFastCallback` **başsız, UI yok, 15–30sn, aşılırsa kill** → **DB migration buraya KONMAZ** | Velopack güncelleme akışı — hook yerleşimi kuralı (uygulanacak) | 📋 plan |
| 2026-09-15 | 273 | **Velopack — Distributing Overview + SimpleWebSource** (`docs.velopack.io/distributing/overview`, `.../reference/cs/Velopack.Sources/SimpleWebSource`) | Feed = `{baseUri}/releases.{channel}.json`; **nupkg'ler feed json'uyla AYNI klasörde** olmalı. Statik host (IIS/S3/Azure) veya GitHub Releases. Delta: `vpk pack` full+delta üretir; uygulama yalnız **değişen dosyaları** indirir, delta yoksa tam paket | Velopack dağıtım — `Releases/` üretimi + repo/feed adresi | ✅ (kurulum) |
| 2026-09-15 | 273 | **Velopack — Integrating/Update Sources** (`docs.velopack.io/integrating/update-sources`, `.../overview`) | `UpdateManager("https://...")` → `SimpleWebSource`; GitHub URL → `GithubSource`; `CheckForUpdatesAsync` feed'i okur; `vpk pack/upload github` delta için önceki release'i indirir | `release.yml` (repo adresi dinamik `${{ github.repository }}`) + `UpdateFeedSourceFactory` (github→GithubSource, aksi SimpleWebSource) | ✅ (kurulum) |
| 2026-09-15 | 273 | **MS Learn — Applying Migrations (EF Core)** + **DevExpress XAF — Multi-tenant EF Core migrations** + **QuickBooks/Sage upgrade** | Runtime migration kabul edilen strateji (EF 9+ migration locking); çok kiracılıda **her tenant ayrı** migrate edilir, "erişimde otomatik güncelleme"; sektör: **önce yedek**, sonra yükselt, hata'da yedekten dön; yükseltilen dosya eski sürümde **açılamaz** | İki-katman ayrımı: app update (Velopack) ↔ tenant şema (erişimde runtime, yedek→göç→doğrula→oto-geri-al). İleri-uyumluluk guard'ı not edildi | ✅ (karar/plan) |
| 2026-09-15 | 273 | **MS — Guidelines for app settings** + **MS — Progress controls** + **QuickBooks — Update your company files** | Uzun/göç işi tek "durum hero" kartında: sürüm `eski→yeni` + rozet + tek hüküm; adım rozetli akış; `determinate` bar; içerik max ~1040px; sonuç tek InfoBar | Faz 6.87 v1 — `TenantDatabaseUpdateView` redesign (hero + 2 sütun + InfoBar) | ❌ (Oturum 273: kullanıcı reddetti — baştan tasarım) |
| 2026-09-15 | 272 | **MS — Guidelines for app settings** | İçerik sütunu ~1000–1100px; gruplar tek kart; birincil aksiyon başlıkta sağda | Faz 6.87 v1 — ana border `MaxWidth` 1040 | ❌ (Oturum 273: kullanıcı reddetti) |
| 2026-09-15 | 272 | **SAP — Status Bar / Displaying Status Information** (`help.sap.com` SAP GUI) | Status bar "sistem ve üzerinde çalıştığın işlem hakkında genel bilgi" taşır; ilk alan **sistem ID + client** (aktif bağlam). Çok-kiracılı muhasebede karşılığı: **aktif firma + mali dönem + tenant DB durumu** (yanlış döneme kayıt önlenir) | Faz 6.86 — `ShellStatusBar` sağ blok: firma · dönem · tenant noktası (birincil); Sistem.db ikincil; `StatusBarService` `IFirmaWithMaliDonemSelectedService`'ten beslenir | ✅ |
| 2026-09-15 | 272 | **MS — Status Bars (Design basics)** + **MS — Migrate WPF patterns to WinUI 3** (Oturum 270 satırları; yeni uygulama) — "status bar yalnız ana pencerede" + "dedicated footer" | Durum çubuğu **ana pencerede de** kalıcı footer olur; içerik sırası korunur (durum → ilerleme → bağlam), renkle vurgu yok | Faz 6.86 — `MainShellView` tam genişlik footer + `MainShellViewModel` besleme; detay pencereleri `ShellView` aynen; `FirmaShellView`'de yok; mesajlar pencere başına ayrı (Scoped DI) | ✅ |
| 2026-09-15 | 270 | **Kullanıcı kararı (araştırma sonucu)** — sektör/MS: "kullanıcı uygulamadayken in-app, dışındayken OS"; Chromium/Terminal: "toast hafif iş için çok ağır"; mevcut kodun **legacy** `ToastNotificationManager` + AUMID registry hack'i kullanması | **Notification kanalı = In-App message (InfoBar tabanlı)**; **OS toast + `CommunityToolkit.WinUI.Notifications` paketi tamamen kaldırılır**. Trade-off: uygulama arka planda/minimize iken bildirim görünmez | Faz 6.86 — `NotificationService` (in-app) + `ShellView` mesaj host'u + paket/`App.xaml.cs` temizliği; **çökme/başlatma bildirimi = dialog** (OS toast yolu kalkar) | ✅ karar |
| 2026-09-15 | 270 | **MS — Windows notifications overview** (`learn.microsoft.com/windows/apps/develop/notifications`) | API seçimi: **WinUI 3 / Windows App SDK / yeni + unpackaged Win32 → `AppNotificationManager`** (namespace `Microsoft.Windows.AppNotifications`, WASDK NuGet, **paket kimliği gerekmez**, aktif geliştirme); `ToastNotificationManager` (UWP WinRT) **yalnız eski UWP**, bakımda. "Eski SO/tutorial yanlış API'yi gösteriyor" | Faz 6.86 — OS toast terk edildiği için **uygulanmayacak**; mevcut legacy API'nin yanlışlığının kanıtı olarak kayıtlı | ❌ (in-app kararı) |
| 2026-09-15 | 270 | **MS — App notifications overview** (`.../notifications/app-notifications`) | Uygulama penceresi **dışında** görünen popup; uygulama kapalıyken de tetiklenir; zengin içerik (başlık/gövde/ikon/buton), **progress bar** (uzun iş), header/collection (gruplama); **sınırlama: yönetici (elevated) çalışan uygulama bildirim gönderemez/al** | Faz 6.86 — toast içerik standardı + uzun iş bildirimi + elevated uyarısı | 📋 plan |
| 2026-09-15 | 270 | **MS — Notifications design basics / UX guidance** (`.../app-notifications/app-notifications-ux-guidance`) | Bildirim = **kullanıcı uygulamada DEĞİLKEN** önemli/bağlamsal bilgi; **aşırı bildirimden kaçın**; **sessiz bildirim** (Action Center'a doğrudan) seçeneği; **Focus Session** algıla (`FocusSessionManager`) + davranışı ayarla; aksiyonlu (buton/quick reply); tıklama **ilgili bağlamda** açılmalı; arka plan işi pencere açmamalı | Faz 6.86 — "mesaj-yeri sözleşmesi" + toast ne zaman susmalı kuralları | 📋 plan |
| 2026-09-15 | 270 | **MS — Choose a notification delivery method** (`.../choosing-a-notification-delivery-method`) | Yerel (uygulama açıkken) · zamanlanmış · periyodik · push; yerel toast varsayılan **3 gün** sonra dolar → açık `ExpirationTime` ver | Faz 6.86 — yerel bildirim + süre yönetimi | 📋 plan |
| 2026-09-15 | 270 | **Chromium/Terminal #8592 + Avalonia WinUI→Avalonia migration notu** | "Toast'lar hafif bir iş için ÇOK ağır"; OS toast = kesici/ağır, hafif in-app geri bildirim için in-app yüzey; "OS toast ile in-app bildirimi **ayrı sorumluluklar** olarak modelle" | Faz 6.86 — status/InfoBar (in-app) ile OS toast'ı ayrı tutma kararının teyidi | ✅ karar |
| 2026-09-15 | 270 | **MS — Migrate WPF app patterns to WinUI 3** (`learn.microsoft.com/windows/apps/windows-app-sdk/migrate-to-windows-app-sdk/wpf-patterns-winui3`) | **StatusBar → "InfoBar + custom layout"**: *"Use InfoBar for status messaging, or add a dedicated footer area to your layout."* WinUI 3'te yerleşik StatusBar kontrolü **yok** | Faz 6.86 (birleşik) — status bar **özel alt şerit (footer)** olarak kalır + **olay/state mesajları InfoBar'a** ayrılır (hibrit) | ✅ (Oturum 271 — `ShellStatusBar` footer yeniden yazıldı; InfoBar kanalı Chunk-2) |
| 2026-09-15 | 270 | **MS — Structure a modern WinUI 3 desktop app** (`learn.microsoft.com/windows/apps/develop/ui/windows-app-sdk-app-structure`) | Modern uygulama = Mica + custom titlebar + NavigationView + **InfoBar (durum mesajları)**; *"colored status borders, toast-like popups veya custom status bar'ları InfoBar ile değiştir"*; InfoBar NavigationView içeriğinin üstüne | Faz 6.86 (birleşik) — event/state mesajları için `InfoBar` (severity) hattı | 📋 plan |
| 2026-09-15 | 270 | **MS — InfoBar guidance** (`learn.microsoft.com/windows/apps/develop/ui/controls/infobar`) | Severity renk+ikon **tema/high-contrast/renk-körlüğü/kontrast için önceden tasarlanmış** → elle hex seçme; **uzun ömürlü** app-wide durum; hızlı aç-kapa (flaş) önlenmeli; must-read/bloklayan → ContentDialog; bağlamsal → Flyout | Faz 6.86 (birleşik) — mesaj severity renkleri sistem kaynağından (hardcode `StatusColorHex` kalkar); flaş önleme | 📋 plan |
| 2026-09-15 | 270 | **VS Code — Status Bar UX** (`code.visualstudio.com/api/ux-guidelines/status-bar`, güncel 09/2026) | Sol = birincil/global, sağ = ikincil/bağlamsal; kısa etiket; ikon yalnız net metafor; **arka planda süren sessiz iş → status bar yükleme ikonu**; dikkat gerekiyorsa → progress notification; yüksek görünürlük uyarı/hata yalnız son çare | Faz 6.86 (birleşik) — status bar **ambiyans + sessiz arka plan ilerlemesi**; sesli/olay mesajı InfoBar/OS bildirimine | ✅ (Oturum 271 — footer sade durum/progress; olay mesajı Chunk-2) |
| 2026-09-15 | 270 | **MS — Status Bars (Design basics)** (`learn.microsoft.com/windows/win32/uxguide/ctrl-status-bars`) | Desenler: pencere durumu → ilerleme → bağlamsal bilgi sırası; **yalnız ana pencerede**; kısa etiket/fragment; renk-kalın-italik ile vurgu **yok**; ikon yalnız net metafor; ilerleme etiketi "…iyor" (gerund) | Faz 6.86 (birleşik) — status bar **içerik sırası + sade dil + vurgu yok** (yakışıklı/sade) | ✅ (Oturum 271 — sol durum/progress, sağ kullanıcı·DB·saat; hardcode renk 0) |
| 2026-09-15 | 270 | **MS — Progress controls** (`learn.microsoft.com/windows/apps/develop/ui/controls/progress-controls`) | Süresi bilinen işte **determinate ProgressBar** (yüzde); kullanıcı işe devam edebiliyorsa non-modal bar; süre bilinmezse indeterminate ring; tamamlanınca **tek** sonuç | Faz 6.86 (birleşik) — `IsBusy`/progress hattı: uzun iş determinate, kısa iş ring, tek özet (Kural 11/12) | 📋 plan |
| 2026-09-15 | 269 | **QuickBooks Desktop — "Update your company files"** (`quickbooks.intuit.com/learn-support`) | Sürüm yükseltmede şirket dosyası güncelleme: **otomatik yedek** (konum değiştirilebilir) → **"Update Now"** → **"Done"**; başarısızsa **yedekten geri yükle ve tekrar dene** | Faz 6.87 — Veritabanı Güncelleme akışı içerik doğrulaması (Yedek→Göç→Doğrulama + oto-geri-alma) | 📋 plan |
| 2026-09-15 | 269 | **QuickBooks Desktop — Verify/Rebuild Data** (`quickbooks.intuit.com/learn-support`) | Verify → hata bulursa **Rebuild (yedek zorunlu)** → tekrar Verify; "hiçbir sorun yok" / "hasar bulundu" net hükmü | Faz 6.87 — Doğrulama adımı + net sonuç hükmü (başarı/neden) | 📋 plan |
| 2026-09-15 | 269 | **MS — Guidelines for progress controls** (`learn.microsoft.com/windows/apps/develop/ui/controls/progress-controls`) | Süresi **bilinen** işte (kurulum/indirme/güncelleme) **determinate** ProgressBar; süre bilinmiyorsa indeterminate; tüm etkileşimi bloklayan bilinmeyen beklemede indeterminate **ProgressRing** | Faz 6.87 — Yedek/Göç/Doğrulama için **determinate** ilerleme (% / adım) + modal busy ring | 📋 plan |
| 2026-09-15 | 269 | **QB topluluk (olumsuz örnek)** — "güncelleme sırasında ekranda hiçbir şey yok, ilerleme çubuğu bile yok" + **Fluent Wizard/Stepper** (WinUI'de yerleşik yok; DevWinUI `StepBar`, Fluent-Blazor `Wizard`, Dynamics stepper) | Uzun süren güncellemede **görünür akış planı + ilerleme olmadan bırakma yasak**; sektörde çok adımlı iş **step bar / wizard** ile gösterilir | Faz 6.87 — **adım rozetli akış planı** (Yedek→Göç→Doğrulama) + determinate bar (Kural 12) | 📋 plan |
| 2026-09-15 | 269 | **MS — Assemble a modern Windows desktop app (WinUI 3)** (`learn.microsoft.com/windows/apps/develop/ui/windows-app-sdk-app-structure`) | "Kullan **InfoBar**'ı durum mesajları için — renkli durum çerçeveleri, toast benzeri popup'lar veya **özel status bar**'ları InfoBar ile değiştir"; tutarlı+erişilebilir inline; `NavigationView` içeriğinin **üstüne** yerleştir | Faz 6.86 — durum mesajı/severity hattının InfoBar'a taşınması değerlendirmesi | 📋 plan |
| 2026-09-15 | 269 | **MS — InfoBar** (`learn.microsoft.com/windows/apps/develop/ui/controls/infobar`) | `Severity` (Informational/Success/Warning/Error) **tema/erişilebilirlik için önceden tasarlanmış** renk+ikon (`InfoBar*Severity*Brush` kaynakları); `Error` idareli; `IsClosable`; ekran okuyucu işleme seviyeleri; hızlı aç-kapa (flaş) önlenmeli | Faz 6.86 — mesaj severity renkleri (hardcode hex yerine sistem/kaynak) | 📋 plan |
| 2026-09-15 | 269 | **VS Code — Status Bar UX** (`code.visualstudio.com/api/ux-guidelines/status-bar`) | Sol = **birincil/global** (durum, sorunlar), sağ = ikincil/bağlamsal; kısa etiket; ikon yalnız net metafor; arkada süren iş → yükleme ikonu; **yüksek görünürlük uyarı/hata yalnız son çare** (arka renk) | Faz 6.86 — mevcut status bar yerleşimi (sol durum / orta bağlam / sağ saat) modern doğrulama | ✅ (Oturum 271 — sol durum·progress / sağ kullanıcı·DB·saat uygulandı) |
| 2026-09-15 | 269 | **MS — "Settings for developers" / Advanced settings page** (`learn.microsoft.com/windows/advanced-settings/developer-mode`) | Geliştiriciye yönelik araç/kapılar **ayrı bir bölümde** toplanır; bölümün başında **uyarı (disclaimer) + açık onay** ("Read the disclaimer. Click Yes to accept the change") istenir; seçenekler mantıksal alt-başlıklarla gruplanır | Faz 6.82 — Denetim Masası'na yalnız DEBUG'da görünen **"Geliştirici Araçları"** bölümü + her aksiyonda onay | ✅ |
| 2026-09-15 | 269 | **MS — Dialog controls** (`learn.microsoft.com/windows/apps/develop/ui/controls/dialogs-and-flyouts/dialogs`) | Yıkıcı/"do it" aksiyon **Primary** (en solda), güvenli/nondestructive **Close** (en sağda); onay diyalogu 2 butonlu (affirm + cancel); geri alınamaz silmede `DefaultButton=None` ile **açık seçim zorlanır**; aynı anda **tek** ContentDialog | Faz 6.82 — kimlik sıfırlama/onarım ve transfer taraması onay diyalogları | ✅ |
| 2026-09-15 | 268 | **MS — Motion in Windows / Fluent 2 Motion** (kalıcı/sonsuz hareketten kaçın; hareket odak öğesinde kalsın; "no motion" sun) + **kullanıcı kararı** ("animasyon yapmak için yapılmış gibi"; "seçili kart ana kartın rengini alacak ki içerideymiş gibi duracak") | Seçili öğe için animasyon yerine **durum farkı**: seçili dönem/firma satırı **ana kart rengini** alır (`CardBackgroundFillColorSecondaryBrush` → panel üstüne binince içeride/çukur), seçimsiz saydam, seçim yalnız sol accent hub; **animasyon sınıfları tümüyle silindi** | Faz 6.84 — `MuhasibDonemSelectionListItemStyle`, `MuhasibSelectionListItemStyle`, `FirmalarListControl` seçili firma kartı; `MuhasibAnaBorderCizgiBrush` kalınlığı 1→1.5 | ✅ |
| 2026-09-15 | 268 | **Canlı ölçüm (kullanıcı bulgusu)** — "açık temada hover görünmüyor"; `ControlFillColorSecondaryBrush` Light'ta açık renkli olduğundan etkisiz kalıyordu | Liste satırı hover perdesi **tema-farkında token**'a alındı: `MuhasibHoverOverlayBrush` (Light `#14000000` / Dark `#1AFFFFFF`) + `HoverOverlay.Opacity=1` | Hover perdesi — `MuhasibDonemSelectionListItemStyle` + `MuhasibSelectionListItemStyle` | ✅ |
| 2026-09-15 | 268 | **MS — Optimize animations and media** (`learn.microsoft.com/windows/apps/develop/performance/optimize-animations-and-media`) | **"Sonsuz animasyonları seyrek kullan"** (sürekli animasyon CPU tüketir, düşük güç/boşta durumunu engeller); **bağımsız animasyon > bağımlı animasyon** (`Opacity`, `Scale/Translation`, `Canvas.Left/Top` bağımsız; `Width/Height`, `StrokeDashOffset` gibi `EnableDependentAnimation` gerektirenler UI thread'inde → yük altında takılır) | Faz 6.84 dönem kartı seçim animasyonu — `StrokeDashOffset` orbit kaldırıldı, bağımsız **Opacity** nabzına geçildi (`SelectionWaveControl`) | ❌ (268 revizyon: nabız da reddedildi — animasyon tümüyle kaldırıldı) |
| 2026-09-15 | 268 | **MS — Composition tailoring** (`learn.microsoft.com/windows/apps/develop/composition/composition-tailoring`) | Uygulama **`UISettings.AnimationsEnabled`**'a uymalı (Erişim Kolaylığı > Görüntü > Animasyonlar kapalıysa geçici animasyonlar oynamaz); `AdvancedEffectsEnabled` ile saydamlık etkileri | Faz 6.84 — `SelectionWaveControl` animasyonu kapalıysa hiç çalışmaz, statik hub kalır | ❌ (268 revizyon: kontrol silindi, animasyon yok) |
| 2026-09-15 | 268 | **MS — Motion in Windows / Fluent 2 Motion** (`learn.microsoft.com/windows/apps/design/signature-experiences/motion`, `fluent2.microsoft.design/motion`) | **"Keep the motion constrained to the element in focus"** (odak öğesi dışına hareket koyma); ani/flaş/kalıcı hareketten kaçın; **kısa süre**; "no motion" ayarı sun (WCAG) | Faz 6.84 — seçili kart accent çerçevesinin ~5sn'de bir tek-atım "nefes" nabzı (kart içinde kalır, komşuya taşmaz) | ❌ (268 revizyon: kullanıcı reddetti, animasyon yok) |
| 2026-09-15 | 267 | **MS — microsoft-ui-xaml Discussion #7432** (`github.com/microsoft/microsoft-ui-xaml`) | Stok ListView tek-seçim göstergesi: sol kenarda dikey hap (3×16, `ListViewItemSelectionIndicatorCornerRadius` 1.5, `ListViewItemSelectionIndicatorBrush`); çok-seçim modunda göstergenin kapalı olduğu tuzağı | Faz 6.84 — dönem listesi seçim stili: mavi tam-çerçeve kaldırılıp sol dikey hub (mevcut ölçü) kalır | ✅ |
| 2026-09-15 | 267 | **MS — EventTrigger / FrameworkElement.Triggers** (`learn.microsoft.com/windows/windows-app-sdk/api/winrt/microsoft.ui.xaml.eventtrigger`) | Silverlight uyumlu ama WinUI 3'te çalışır: `FrameworkElement.Triggers` içinde `RoutedEvent`'siz `<EventTrigger>` varsayılan **Loaded** olayına bağlanır; storyboard `RepeatBehavior="Forever"` ile sürekli dekoratif animasyon deseni | Faz 6.84 — ilk denemede kullanıldı; daha sonra boyut-bazlı parametreleme için **code-behind Storyboard'a geçildi** (`OrbitRingControl`) | ❌ |
| 2026-09-15 | 267 | **MS — Storyboarded animations / EnableDependentAnimation** (`learn.microsoft.com/uwp/api/windows.ui.xaml.frameworkelement.loaded`) | `Shape.StrokeDashOffset` bağımsız animasyon değildir → `EnableDependentAnimation="True"` şart; keyframe + hold ile "süpür → bekle" döngüsü | Faz 6.84 — `OrbitRingControl` dönen kuyruk (hub'dan çıkıp hub'a döner; 1.8s süpür + 1.6s bekle) | ❌ (268: dalga lehine kaldırıldı) |
| 2026-09-15 | 267 | **GitHub microsoft-ui-xaml Issue #10166** (Composition rotasyonlu gradyan çerçeve) | `CompositionRoundedRectangleGeometry` + `ShapeVisual` stroke gradyanı + `RotationAngleInDegrees` animasyonu ile gerçek dönen gradyan çerçeve mümkün; ancak her item için code-behind/SizeChanged gerekir | Faz 6.84 — değerlendirildi, **kullanıcı kararıyla storyboard yolu seçildi** (Yol A) | ❌ |
| 2026-09-14 | 261 | **Sage 50 — "Select Company Window At Startup"** (`communityhub.sage.com`) + **QuickBooks Desktop — "Open Previous Company"** (`quickbooks.intuit.com`) | Yenilik (recency) mekanizması: önceki şirketi yeniden aç / son açılanlar listesi; ana liste hep sabit sıralı (ad/yıl) — **yeniliğe göre dizme yok**; "varsayılan" bayrağı kavramı yok | Faz 6.84 — ana liste yıl-sıralı kalır; son çalışılan döneme **rozet** (vurgu, sıralama değil) | ✅ |
| 2026-09-14 | 256 | **MS — InfoBar** (`learn.microsoft.com/windows/apps/develop/ui/controls/infobar`) | Değişen uygulama durumu için **inline, bloklamayan, aksiyonlu** bildirim (ActionButton/link, kapatılabilir); kalıcı/bloklayan durumda ContentDialog; geçici ipucunda TeachingTip | Faz 6.84 — mali dönem güncelleme bildirimi (tek dönem → güncelleme sayfası; çok dönem → etkilenen liste) | ✅ |
| 2026-09-14 | 256 | **QuickBooks Desktop — Users & Roles** (`quickbooks.intuit.com/learn-support`) | **Yalnız admin** kullanıcı oluşturur/yönetir (Company ▸ Users ▸ Set Up Users and Roles); kullanıcı + rol + aktive; kendi hesap işlemleri yönetimden ayrı | Faz 6.85 — Kullanıcı Yönetimi modalı (admin kapısı `AyarYetkiDenetimi.KullaniciYoneticiMi`) + ayrı Hesabım yüzeyi | 📋 plan |
| 2026-09-14 | 256 | **MYOB — Manage Users / Manage Roles** (`myob.com/support/.../manage-users`) | Kullanıcı yönetimi ile **rol yönetimi ayrı sekmeler**; rol bazlı yetki tablosu; admin atar | Faz 6.85 — kullanıcı yönetimi/rol ayrımı (rol atama KFR nedeniyle sonraki iş) | 📋 plan |
| 2026-09-14 | 255 | **Sage 50 — Company Selection window** (`gb-kb.sage.com`) | Açılışta şirket listesi tam pencere: ad/versiyon/VKN/mali yıl/veri yolu kolonları; **Add Company**; satır başı üç-nokta aksiyonları; "satırı seç → OK" akışı | Faz 6.84 FirmaShellView firma seçimi içerik doğrulaması (liste + satır aksiyonu + yeni firma) | ✅ |
| 2026-09-14 | 255 | **Tally — Gateway of Tally** (`help.tallysolutions.com`) | Şirket seçimi F3 → şirket listesi; Gateway'de **yüklü şirket bilgisi ekranda görünür kalır** (sol panel) | Faz 6.84 — seçili firma özetinin kart olarak görünür kalması | ✅ |
| 2026-09-14 | 255 | **Oracle Retail — Select Company** (`docs.oracle.com`) | Liste **üstünde arama alanı**; filtre **tüm sonuçlarda** (ilk sayfayla sınırlı değil); terim X ile kaldırılır; yalnız yetkili şirketler listelenir | Faz 6.84 — firma araması (popup liste filtresi, tüm kayıtlarda) | ✅ |
| 2026-09-14 | 255 | **Deltek — Select a Company** (`help.deltek.com`) | Arama alanı + **sonuç sayısı** sağda; yazdıkça daralt/çoğalt; tek karakter "başlar", iki+ karakter "içerir" | Faz 6.84 — firma arama deneyimi (sayaç + yazarken filtre) | ✅ |
| 2026-09-14 | 255 | **MS — Guidelines for radio buttons** (`learn.microsoft.com/windows/apps/develop/ui/controls/radio-button`) | **"Seçenekler uygulamanın bağlamına göre değişebiliyorsa liste kontrolü kullan"**; radio = 2-7 sabit seçenek | Faz 6.84 — mali dönem RadioButton kartı → seçimli liste (mühür revizyonu; dinamik dönem listesi) | ✅ |
| 2026-09-14 | 255 | **MS — Selection mode / ListView single selection** (`learn.microsoft.com/windows/apps/develop/ui/controls/selection-modes`) | Tek seçim `SelectionMode=Single`; seçim görseli Fluent liste dili; aksiyonlar seçime göre aktif | Faz 6.84 — mali dönem ListView tek seçim + CTA aktifliği | ✅ |
| 2026-09-14 | 255 | **MS — Auto-suggest box guidelines** (`learn.microsoft.com/windows/apps/develop/ui/controls/auto-suggest-box`) | Yazarken (`TextChanged`, Reason=UserInput) filtre → `ItemsSource`; zengin `ItemTemplate`; `SuggestionChosen` ile seçim | Faz 6.84 — firma seçimi arama-öncelikli popup (zengin satır) | ✅ |
| 2026-09-14 | 255 | **MS — ComboBox guidelines** (`learn.microsoft.com` windows-dev-docs) | Çok satırlı/görselli içerik ComboBox'ta **değil** liste/grid'de; ComboBox ikincil önemdeki tek satırlık seçimler için | Faz 6.84 — firma/dönem zengin kartları liste/panelde kalır (ComboBox'a düşürülmez) | ✅ |
| 2026-09-14 | 253 | **Oturum 249+251 deseninin yayılımı** (yeni internet araştırması yok — Kural 19 "aynı işi tekrar yapma"; temel kaynaklar: MS Elevation & layering + Dynamics 365 hairline ölçümü + CommunityToolkit Settings) | Ana border `MuhasibAnaBorderCizgiBrush` hairline (Light `#E6DFE1E4` / Dark `#45C9CCD1`) + `Translation Z 32` + `ThemeShadow` (Loaded receiver) + `?` (Kural 13) panel sağ üst köşesi; dialog kökleri opak `SolidBackgroundFillColorBaseBrush` (Kural 17) | Katman 2 turu: `MaliDonemYonetimView`, `TenantDatabaseUpdateView`, `UpdateView`, `SistemDbYonetimView`, `DatabaseSettingsView`, `MainShellView` + `FirmaShellView`/`KurulumSplashView` hairline + 9 dialog kökü | ✅ |
| 2026-09-13 | 243 | QuickBooks Desktop — Verify/Rebuild Data (`quickbooks.intuit.com`) | Verify → (hasar) Rebuild **yedek zorunlu** → tekrar Verify → restore **son çare**; mevcut company file'ı değiştirme uyarısı | Sistem.db restore **tek kapı** hüküm akışı (Faz 6.78) + tenant `RestoreVerdictEvaluator` | ✅ |
| 2026-09-13 | 243 | Sage 50 — Restore (`help-sage50.na.sage.com`) | Restore mevcut veriyi **ezer** → önce yedek; **sürüm uyumsuzluğu** (yeni yedek eski sürüme restore edilemez); onay penceresi + progress; kesilmemeli | Sistem restore hüküm akışı + `SistemRestoreVerifyDialog` | ✅ |
| 2026-09-13 | 243 | SQLite resmi (`sqlite.org`) | Canlı kopya `VACUUM INTO`/Online Backup; restore = dosya değişimi + `integrity_check`; WAL checkpoint `TRUNCATE` | `TenantSQLiteBackupManager`/`SistemBackupManager` + Faz 6.79 yaşam-döngüsü | ✅ |
| 2026-09-13 | 243 | MS — Dialog controls (`learn.microsoft.com`) | Her dialogda güvenli/nondestructive Kapat şart; **yıkıcı iş = Primary**; `None` (Esc/perde) = iptal; aynı anda **tek** ContentDialog; async onay + ilerleme | Tüm ContentDialog'lar (restore/delete/güncelleme) | ✅ |
| 2026-09-13 | 243 | MS — Progress controls (`learn.microsoft.com`) | Süresi bilinen işte **determinate ProgressBar**, UI bloke ise indeterminate ring; bitince **tek** özet | Kural 12 (işlem-görseli) + restore/göç/bakım akışları | ✅ |
| 2026-09-13 | 248 | MS Fluent 2 kimlik doğrulama (`microsoft.design/articles/reimagining-our-front-door` + `techcommunity.microsoft.com` "new UX for consumer authentication") | Merkezlenmiş + sade auth ("ekran başına kavramı azalt"), cömert input alanları, **tek net CTA**, ürün rengi yalnız buton/linkte, rahatsız etmeyen arka plan görseli | `LoginView` — Fluent 2 ortalanmış sade giriş pilotu (eski buzlu kabuk kaldırıldı) | ✅ |
| 2026-09-13 | 248 | Sage 50 — "Log in to a Company" (`help-sage50.na.sage.com`) | Kullanıcı+şifre girişi; firma açma ayrı adım; şirket listesi kimlik doğrulama sonrası | `LoginView` içerik doğrulaması — adım göstergesi Kurulum→Giriş→Firma Seçimi→Çalışma Alanı | ✅ |
| 2026-09-13 | 248 | Windows 11 giriş ekranı Fluent güncellemesi (`thewincentral.com`, `windowsnews.ai`) | Yuvarlatılmış odak, tutarlı boşluk/tipografi, Fluent bileşenler | `LoginView` — Fluent kart/aralık/tipografi | ✅ |
| 2026-09-13 | 249 | Sage 50 — Company Selection window (`gb-kb.sage.com`) | Açılışta şirket listesi: ad + sürüm + vergi no + mali yıl + veri yolu sütunları; "Add Company"; satır-başı işlemler | `FirmaShellView` — firma/mali dönem seçim ekranı içerik doğrulaması (Kural 17 Katman 2 geçişi) | ✅ |
| 2026-09-13 | 249 | MS — Windows 11 design principles: Elevation & layering + Materials (`learn.microsoft.com`) | Yüzeyleri üst üste bindirip hiyerarşi kurma (elevation/layering); Mica/Acrylic malzemeler; yuvarlatılmış geometri | `FirmaShellView` — Kural 17 Katman 2 ana border paneli (zemin → panel → kartlar) | ✅ |
| 2026-09-13 | 249 | MS — Guidelines for app settings (`learn.microsoft.com/windows/apps/design/app-settings/guidelines-for-app-settings`) | Ayarlar sayfası tüm pencereyi doldurur; içerik max ~1000–1100px; `SettingsCard`/`SettingsExpander` (Toolkit); bölümler `BodyStrong`; NavigationView'de ayar son öğe/sabit; tema = ComboBox "Light/Dark/Use system setting" | Denetim Masası Windows Ayarlar görünümü (bu oturum) | ✅ |
| 2026-09-13 | 249 | MS — Structure a modern WinUI 3 app + NavigationView (`learn.microsoft.com/windows/apps/develop/ui/controls/navigationview`, `.../windows-app-sdk-app-structure`) | `NavigationView PaneDisplayMode=Left` kabuk; opak arka plan koyma (Mica görünsün); pane'de AutoSuggestBox; içerik `NavigationView.Content` | Denetim Masası kabuğu (NavigationView Left + Mica) | ✅ |
| 2026-09-13 | 249 | CommunityToolkit — `SettingsCard`/`SettingsExpander` (paket `CommunityToolkit.WinUI.Controls.SettingsControls 8.2.251219`) | Win11 `SettingsCard` (Header/Description/HeaderIcon/Content + IsClickEnabled) + `SettingsExpander` grubu; DevWinUI'nin ported kopyası yerine resmi Toolkit | Denetim Masası ayar kartları (Görünüm/Güvenlik/Firma/Dönem/Veritabanı) | ✅ |
| 2026-09-13 | 249 | MS — `SettingsExpander` (CommunityToolkit, `learn.microsoft.com/dotnet/communitytoolkit/windows/settingscontrols/settingsexpander`) + "Guidelines for app settings" | `SettingsExpander` birden çok `SettingsCard`'ı **katlanabilir grup** yapar; ilgili ayarları tek başlık altında topla; **iç içe expander 1 seviyeyi geçmesin**; bölüm başına ~4-5 ayar; `MaxWidth`'li StackPanel'i Grid'e sar (ItemsRepeater glitch'i) | Denetim Masası **Veritabanı tek view**: `Sistem Veritabanı` + `Mali Dönem Veritabanları` `SettingsExpander` bölümleri | ✅ |
| 2026-09-13 | 251 | **Windows 11 25H2 Ayarlar canlı referansı** (build 26200, birebir ekran görüntüsü `Temp/opencode/ot251_winsettings.png`) | Sol pane: en üstte **hesap kartı** (büyük avatar + ad + hesap türü), altında nav listesi; Giriş sayfası: üstte **cihaz/durum kartları** + altında firmalar bölümü; seçili nav maddesi accent pill + sol çubuk ile vurgulu | Denetim Masası kabuğu + **Giriş (Home)** sayfası — hesap pane üstü, RichButton durum satırı, firma expander + mali dönem listesi, nav seçim vurgusu | ✅ |
| 2026-09-13 | 251 | **Kullanıcı kararı — arama pane'de kalır + "ayar içinde ayar" kaldırılır:** Giriş sayfası Ayarlar bölüm kartlarını **çoğaltmaz** (bölümler zaten navbar'da); üst bilgi öğeleri kart kabı olmadan RichButton olarak durur | `DenetimMasasiView` arama konumu (pane); `GirisDashboardSayfasi` içerik daraltma | ✅ |
| 2026-09-13 | 251 | DevWinUI — `RichButton` (`ghost1372.github.io/DevWinUI/controls/richButton` + Galeri `RichButtonPage.xaml`; kaynak: `dev/DevWinUI/Controls/Native/RichButton/RichButton.cs`) | `RichButton : Control`; `Title`/`SubTitle`/`Icon`/`ActionIcon`/`DisplayMode (Normal-Subtle-ReadOnly)`/`Command`+public `Click`; Windows 11 "Windows Update" kartı deseni (ikon + başlık + son kontrol + chevron) | Denetim Masası **Giriş üst bilgi düğmeleri**: Uygulama bilgileri (ReadOnly) · Sistem.db bağlantı durumu · Güncelleme | ✅ |
| 2026-09-13 | 251 | DevWinUI paket kurulum kuralı (README) | `App.xaml`'e `<ResourceDictionary Source="ms-appx:///DevWinUI/Themes/Generic.xaml" />` merge edilmeden kontrol stilleri yüklenmez; paket `Microsoft.Graphics.Win2D` bağımlılığı getirir | `MuhasibPro/App.xaml` (RichButton stil kaynağı) | ✅ |
| 2026-09-13 | 251 | **Dynamics 365 Sales Hub elevation hairline** (kullanıcı ekran görüntüsü `repos/ekrankaydi/Ekran görüntüsü 2026-09-13 225403.png`; piksel ölçümü: sayfa zemini L≈228, panel L≈246, **kenar çizgisi L≈253 — panelden de zeminden de AÇIK 1px hairline**) + zemin görselleri ölçümü (`light.jpg` L≈210, eski `dark.jpg` L≈46) | Ana border hairline'ı: **Light `#E6DFE1E4` / Dark `#45C9CCD1`** — ilk denemede beyaz (`#F2FFFFFF`/`#59FFFFFF`) "patlıyor" geri bildirimiyle **yumuşak griye** çekildi (kullanıcı onayı: "budur"); 1px + `Translation Z 32` + `ThemeShadow` | Kural 17 **ana border** pilotu: `ExtendedSplash` SplashCard + `LoginView` AnaBorder | ✅ |
| 2026-09-13 | 251 | **Unsplash — "Dark background with a soft light gradient"** (Philipp Hubert, `unsplash.com/photos/b4N2fd_Jm6w`; Unsplash License) | Denendi; ardından **tek görsel + tema perdesi kararı** (`MuhasibZeminPerdeBrush`) verildi → ayrı dark görsel **kullanılmıyor** (`Assets/Images/dark.jpg` asset olarak duruyor; kaldırma kararı sonraki bakımda) | Kural 17 Dark zemin (vazgeçildi) | ❌ |
| 2026-09-13 | 249 | Sage 50 — Company > Backup + QuickBooks Desktop — Back up company file (`help-sage50.na.sage.com`, `quickbooks.intuit.com`) | Şirket/veri yedeği: hatırlatma + zamanlama + **yedeği doğrula (verify)** + saklanacak yedek adedi + "kapanışta otomatik yedek" | Denetim Masası Veritabanı > **Dönem** (Yedek Saklama) içerik doğrulaması | ✅ |
| 2026-09-13 | 244 | QuickBooks — Preferences (`Edit > Preferences`) | Ayarlar **ayrı pencere** (sol kategori + sağ içerik; My/Company kapsamı) | Denetim Masası'nın FirmaShell'den ayrılıp **ayrı modal pencere** olması (Oturum 244) | ✅ |
| 2026-09-13 | 244 | Sage 50 — Settings window (`help-sage50.na.sage.com`) | Ayarlar **ayrı "Settings" penceresi**, modül bazlı gruplu; kullanıcı tercihleri ayrı | Denetim Masası ayrı pencere + kullanıcı/global ayar kapsamı | ✅ |
| 2026-09-12 | 221/238 | MS — Mica / SystemBackdrop (`learn.microsoft.com`) | Pencere düzeyinde Mica backdrop; içerik şeffaflığı | `MainWindow`/`DetailsWindow` `<MicaBackdrop Kind="Base" />` (Faz 6.75) | ✅ |
| 2026-09-10 | 159/160 | MS Settings kılavuzu + Toolkit SettingsCard + sektör (saklama 3/7/14/30) | Ayar panelinde NumberBox yerine **ComboBox preset**; SettingsCard deseni | `YonetimAyarlarDialog` (Faz 6.67) | ✅ |
| 2026-08-23 | — | MS — `ContentDialog` API + `DialogShowingWithoutSmokeLayer` | Dialog perdesi yerleşim API'si; `DefaultButton=Primary` accent ezmesi tuzağı | Tüm dialog chrome'u (QuickDialog kalıbı) | ✅ |
| 2026-09-16 | 277 | MS — tema kaynakları (`ThemeResource`) + `ThemeDictionaries` çözümleme davranışı (`learn.microsoft.com` ThemeResource) | Tema-bağımlı brush yalnız element üzerinde `{ThemeResource}` ile uygulanır; `Application.Current.Resources[...]` (converter) pencere `RequestedTheme` override'ını değil uygulama/sistem temasını çözer → Light'ta koyu zemin | `GuncellemeSonrasiView` hero + adım rozetleri: renk converter'ları kaldırıldı, durum renkleri `ThemeResource` overlay Border'ları + bool görünürlüklerle seçildi | ✅ |
| 2026-09-16 | 277 | MS Copilot yerleşim evrimi (`windowsnews.ai` "Copilot Taskbar anchored to system tray" 2025-12 + `windowslatest.com` bottom-center quick view) | Asistan girişi **system tray / durum alanında tek düğme**; tıklayınca **kalıcı pane** açılır, kapalıyken ekran alanı kaplamaz ("always within reach without occupying screen real estate") | MainShell AI asistanı: statü çubuğunun **en sağında 🤖** düğmesi + statü çubuğunun üstünde **sağa hizalı Flyout** (`TopEdgeAlignedRight`); model indirme panelin içinde | ✅ |
| 2026-09-16 | 278 | **LM Studio — `lms remove` (PR #580 / issue #579) + delete bug #1603/#962** | Model silme akışı: **sessiz silme yok**; model + **disk konumu** gösterilir, **onay** istenir; yüklü model önce bırakılır ("unload first"), silinemezse **sebep** döner; modeller GB ölçeğinde → yanlışlıkla silme kabul edilemez; boş klasörler temizlenir | Faz 6.92 — Denetim "Model yönetimi" sil akışı: satır başı "Sil" + onay dialogu + yüklüyse otomatik unload + sonuç/başarısızlık mesajı (`AsistanModelDto.YukluMu`) | ✅ |
| 2026-09-16 | 278 | **MS — Dialog controls (ContentDialog)** (`learn.microsoft.com/windows/apps/develop/ui/controls/dialogs-and-flyouts/dialogs`) | Yıkıcı işlem öncesi `ContentDialog` onayı: **belirli buton metni** ("Sil"/"Vazgeç", genel "Evet/Hayır" değil) + **en az bir güvenli (kapat) buton** + yıkıcı aksiyon `PrimaryButton`; onay = işlemi yapmadan önce kesin sorma | Faz 6.92 — model sil onayı `IDialogService.ShowConfirmationAsync("Model silinsin mi?", …, "Kalıcı Sil", "Vazgeç")` (yeni dialog sınıfı açmadan, Kural 8 yüzeyi dar) | ✅ |
| 2026-09-16 | 278 | **MS — Guidelines for app settings + Toolkit `SettingsExpander`** (`learn.microsoft.com/windows/apps/design/app-settings/guidelines-for-app-settings`, `.../settingsexpander`) | Ayar sayfası: gruplar **başlık + açıklama + ikon**, aksiyon sağda (`SettingsCard`); alt-ayarlar `SettingsExpander` ile katlanır; devre dışı öğede **sebep** açıklaması; değişiklik anında yansır (onay butonu yok) | Faz 6.92 — "Model yönetimi" bölümü: disk özeti + indirilmiş model listesi + `Yenile`/`Sil` aksiyonları sağda; indirme yönergesi açıklama metni | ✅ |
| 2026-09-16 | 278 | **Foundry Local quickstart (Oturum 276 kaydı) — yeniden araştırma YOK (Kural 19)** | Model indirme akışı (EP → katalog → indir/yükle, determinate %) zaten kayıtlı | Faz 6.92 — "indirme yönergeleri" metni mevcut 276 bulgusunu kullanır (yeni kaynak gerekmedi) | ✅ (yeniden kullanım) |
| 2026-09-16 | 278 | **MS — Tutorial: Build a RAG application (Foundry Local)** (`learn.microsoft.com/azure/foundry-local/tutorials/tutorial-build-rag-app`) + **C# SDK README** (`github.com/microsoft/Foundry-Local/sdk/cs`) + **v1.1.0 release** | Yerel embedding: `qwen3-embedding-0.6b` (~495 MB) `GetEmbeddingClientAsync()` / `GenerateEmbeddingsAsync` (batch). Resmî RAG örneği: belge vektörleri + sorgu vektörü **kaba kuvvet cosine** ile top-k — "küçük derlemlerde iyi; büyük veri için ayrı vektör DB". | Faz 6.93 — `IYardimVektorUretici` (App/Foundry) + hibrit arama (ek vektör DB yok) | ✅ |
| 2026-09-16 | 278 | **"You Probably Don't Need a Vector Database for RAG" (2026)** (`baeseokjae.github.io/posts/vector-database-rag-alternative-2026/`) + **SoyLM** (`github.com/soy-tuber/SoyLM`) + **JASRAG** (`github.com/andrewlerdorf/JASRAG`) | Küçük + alana özel derlemde BM25/lexical, embedding'e yakın (SQuAD recall@10: BM25 %88 vs embeddings %91,7); vektör DB çoğu kez aşırı; **en iyi pratik = hibrit (lexical + embedding, RRF)**; FTS5 = ek altyapı yok; derlem büyüyene kadar FTS5 baseline yeterli | Faz 6.93 — `AsistanBilgi.db` dataset + hafif lexical + RRF (ağır vektör DB yok, FTS5 yok) | ✅ |
| 2026-09-16 | 278 | **SQLite FTS5 Extension — tokenizers** (`sqlite.org/fts5.html`) + **Alex Garcia — Hybrid FTS5 + sqlite-vec** (`alexgarcia.xyz/blog/2024/sqlite-vec-hybrid-search/`) + **vstash (arXiv 2604.15484)** | FTS5 tokenizer'ları: `unicode61` (diakritik siler, Unicode case), `trigram` (substring, LIKE/GLOB), `porter` (yalnız İngilizce kök) → **Türkçe kökleyici yok**. Hibrit RRF formülü `Σ 1/(k+rank)`, k=60; ağırlıklar (vektör 0,6 / lexical 0,4); Türkçe ek yapısı salt-lexical'i zayıflatır → embedding ana katkı | Faz 6.93 — `RrfBirlestirici` (k=60, w_lex 0,4 / w_vec 0,6); Turkish-aware token normalizasyonu | ✅ |
| 2026-09-16 | 278 | **On-Device Privacy-Preserving Knowledge Retrieval (replication, 2026-06)** (`doi.org/10.5281/zenodo.20694694`) + **ReperioNet** (`github.com/markusfurtlehner/ReperioNet`) | Yerel motorlar (FTS5/DuckDB-FTS/BM25/linear scan/MiniLM+HNSW/sqlite-vec) + **Türkçe (TQuAD)** dayanıklılık: hibrit (lexical+vektör) tutarlı kazanıyor; çok dilli MiniLM hibrit competitive. Türkçe için Snowball kökleyici paketi ayrı bağımlılık | Faz 6.93 — hibrit kararı teyidi; Türkçe kökleyici bağımlılığı **alınmadı** (embedding + basit normalizasyon tercih) | ✅ |

> Not: Bu defter yaşayan belgedir; yeni araştırma yapıldığında **uygulamadan önce** satır eklenir, uygulama bitince `Durum` güncellenir. Geçmiş satırlar silinmez (kaynak geçmişi).
