<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="MuhasibPro.Views.DenetimMasasi.Bolumler.DonemAyarlarPaneli"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <!-- Mali Dönem bölümü (Windows Ayarlar dili): bölüm başlıkları + SettingsCard.
         DataContext = DonemAyarlarViewModel (sayfa verir; başlık kabukta). -->
    <StackPanel Spacing="24">

        <!-- Liste Görünümü -->
        <StackPanel Spacing="4">
            <TextBlock Text="Liste Görünümü" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="2,0,0,4" />
            <controls:SettingsCard Header="Yedekler sayfa boyutu" Description="Dönem yedek listesinde her sayfada gösterilecek kayıt sayısı.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconHardDrive}" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox SelectedValue="{Binding YedekPageSize, Mode=TwoWay}" MinWidth="140" ToolTipService.ToolTip="Yedek sayfa boyutu">
                    <x:Int32>1</x:Int32>
                    <x:Int32>2</x:Int32>
                    <x:Int32>4</x:Int32>
                    <x:Int32>8</x:Int32>
                    <x:Int32>12</x:Int32>
                    <x:Int32>20</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
            <controls:SettingsCard Header="Bilinmeyen yedekler sayfa boyutu" Description="Eşleşmeyen yedek listesinde her sayfada gösterilecek kayıt sayısı.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconHelpCircle}" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox SelectedValue="{Binding BilinmeyenPageSize, Mode=TwoWay}" MinWidth="140" ToolTipService.ToolTip="Bilinmeyen sayfa boyutu">
                    <x:Int32>1</x:Int32>
                    <x:Int32>2</x:Int32>
                    <x:Int32>4</x:Int32>
                    <x:Int32>8</x:Int32>
                    <x:Int32>12</x:Int32>
                    <x:Int32>20</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
        </StackPanel>

        <!-- Güncelleme -->
        <StackPanel Spacing="4">
            <TextBlock Text="Güncelleme" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="2,0,0,4" />
            <controls:SettingsCard Description="Güncelleme taramasında tek seferde işlenen kayıt sayısı.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconDatabase}" />
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Toplu tarama boyutu" FontWeight="SemiBold" />
                        <Border Padding="8,2" CornerRadius="{StaticResource MuhasibPillCornerRadius}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" ToolTipService.ToolTip="Yalnızca yönetici değiştirebilir" VerticalAlignment="Center">
                            <TextBlock Text="Yönetici" FontSize="10" Foreground="{ThemeResource SystemFillColorAttentionBrush}" />
                        </Border>
                    </StackPanel>
                </controls:SettingsCard.Header>
                <ComboBox SelectedValue="{Binding BackfillSayfaBoyutu, Mode=TwoWay}" IsEnabled="{Binding IsYonetici, Mode=OneWay}" MinWidth="140" ToolTipService.ToolTip="Toplu tarama boyutu">
                    <x:Int32>10</x:Int32>
                    <x:Int32>50</x:Int32>
                    <x:Int32>100</x:Int32>
                    <x:Int32>250</x:Int32>
                    <x:Int32>500</x:Int32>
                    <x:Int32>1000</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
            <controls:SettingsCard Description="Şema güncellemesi başarısız olursa kaç kez yeniden deneneceği.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconRefresh}" />
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Güncelleme denemesi" FontWeight="SemiBold" />
                        <Border Padding="8,2" CornerRadius="{StaticResource MuhasibPillCornerRadius}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" ToolTipService.ToolTip="Yalnızca yönetici değiştirebilir" VerticalAlignment="Center">
                            <TextBlock Text="Yönetici" FontSize="10" Foreground="{ThemeResource SystemFillColorAttentionBrush}" />
                        </Border>
                    </StackPanel>
                </controls:SettingsCard.Header>
                <ComboBox SelectedValue="{Binding MigrationRetry, Mode=TwoWay}" IsEnabled="{Binding IsYonetici, Mode=OneWay}" MinWidth="140" ToolTipService.ToolTip="Güncelleme denemesi">
                    <x:Int32>1</x:Int32>
                    <x:Int32>2</x:Int32>
                    <x:Int32>3</x:Int32>
                    <x:Int32>4</x:Int32>
                    <x:Int32>5</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
        </StackPanel>

        <!-- Bağlantı -->
        <StackPanel Spacing="4">
            <TextBlock Text="Bağlantı" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="2,0,0,4" />
            <controls:SettingsCard Description="Veritabanı bakım işlemlerinin en fazla süresi (saniye).">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconWrench}" />
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Bakım zaman aşımı" FontWeight="SemiBold" />
                        <Border Padding="8,2" CornerRadius="{StaticResource MuhasibPillCornerRadius}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" ToolTipService.ToolTip="Yalnızca yönetici değiştirebilir" VerticalAlignment="Center">
                            <TextBlock Text="Yönetici" FontSize="10" Foreground="{ThemeResource SystemFillColorAttentionBrush}" />
                        </Border>
                    </StackPanel>
                </controls:SettingsCard.Header>
                <ComboBox SelectedValue="{Binding BakimTimeoutSec, Mode=TwoWay}" IsEnabled="{Binding IsYonetici, Mode=OneWay}" MinWidth="140" ToolTipService.ToolTip="Bakım zaman aşımı (sn)">
                    <x:Int32>30</x:Int32>
                    <x:Int32>60</x:Int32>
                    <x:Int32>120</x:Int32>
                    <x:Int32>300</x:Int32>
                    <x:Int32>600</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
            <controls:SettingsCard Description="Tek bir veritabanı komutunun en fazla süresi (saniye).">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconLoader}" />
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Komut zaman aşımı" FontWeight="SemiBold" />
                        <Border Padding="8,2" CornerRadius="{StaticResource MuhasibPillCornerRadius}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" ToolTipService.ToolTip="Yalnızca yönetici değiştirebilir" VerticalAlignment="Center">
                            <TextBlock Text="Yönetici" FontSize="10" Foreground="{ThemeResource SystemFillColorAttentionBrush}" />
                        </Border>
                    </StackPanel>
                </controls:SettingsCard.Header>
                <ComboBox SelectedValue="{Binding CommandTimeoutSec, Mode=TwoWay}" IsEnabled="{Binding IsYonetici, Mode=OneWay}" MinWidth="140" ToolTipService.ToolTip="Komut zaman aşımı (sn)">
                    <x:Int32>5</x:Int32>
                    <x:Int32>15</x:Int32>
                    <x:Int32>30</x:Int32>
                    <x:Int32>60</x:Int32>
                    <x:Int32>120</x:Int32>
                    <x:Int32>300</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
            <controls:SettingsCard Description="Veritabanı meşgulken yeni işlemin bekleyeceği süre (ms).">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconZap}" />
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Kilit bekleme süresi" FontWeight="SemiBold" />
                        <Border Padding="8,2" CornerRadius="{StaticResource MuhasibPillCornerRadius}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" ToolTipService.ToolTip="Yalnızca yönetici değiştirebilir" VerticalAlignment="Center">
                            <TextBlock Text="Yönetici" FontSize="10" Foreground="{ThemeResource SystemFillColorAttentionBrush}" />
                        </Border>
                    </StackPanel>
                </controls:SettingsCard.Header>
                <ComboBox SelectedValue="{Binding BusyTimeoutMs, Mode=TwoWay}" IsEnabled="{Binding IsYonetici, Mode=OneWay}" MinWidth="140" ToolTipService.ToolTip="Kilit bekleme süresi (ms)">
                    <x:Int32>1000</x:Int32>
                    <x:Int32>5000</x:Int32>
                    <x:Int32>15000</x:Int32>
                    <x:Int32>30000</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
            <controls:SettingsCard Description="Veritabanı bağlantıları yeniden kullanılsın mı? (Önerilen: açık)">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconLayers}" />
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Bağlantı havuzu" FontWeight="SemiBold" />
                        <Border Padding="8,2" CornerRadius="{StaticResource MuhasibPillCornerRadius}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" ToolTipService.ToolTip="Yalnızca yönetici değiştirebilir" VerticalAlignment="Center">
                            <TextBlock Text="Yönetici" FontSize="10" Foreground="{ThemeResource SystemFillColorAttentionBrush}" />
                        </Border>
                    </StackPanel>
                </controls:SettingsCard.Header>
                <ToggleSwitch IsOn="{Binding Pooling, Mode=TwoWay}" IsEnabled="{Binding IsYonetici, Mode=OneWay}" />
            </controls:SettingsCard>
        </StackPanel>
    </StackPanel>
</UserControl>
