<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="MuhasibPro.Views.DenetimMasasi.Bolumler.FirmaKayitAyarlarPaneli"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <!-- Firma bölümü (Windows Ayarlar dili): bölüm başlıkları + SettingsCard.
         DataContext = FirmaKayitAyarlarViewModel (sayfa verir; başlık kabukta). -->
    <StackPanel Spacing="24">

        <!-- Firma Kodu -->
        <StackPanel Spacing="4">
            <TextBlock Text="Firma Kodu" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="2,0,0,4" />
            <controls:SettingsCard Description="Firma kodlarının uyacağı kalıp. X bir rakamı temsil eder (örn. F-XXXX).">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconEdit}" />
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Firma kodu biçimi" FontWeight="SemiBold" />
                        <Border Padding="8,2" CornerRadius="{StaticResource MuhasibPillCornerRadius}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" ToolTipService.ToolTip="Yalnızca yönetici değiştirebilir" VerticalAlignment="Center">
                            <TextBlock Text="Yönetici" FontSize="10" Foreground="{ThemeResource SystemFillColorAttentionBrush}" />
                        </Border>
                    </StackPanel>
                </controls:SettingsCard.Header>
                <TextBox Text="{Binding FirmaKodPattern, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsYonetici, Mode=OneWay}" MinWidth="140" VerticalAlignment="Center" ToolTipService.ToolTip="Firma kodu biçimi" />
            </controls:SettingsCard>

            <controls:SettingsCard Description="Açıkken biçime uymayan firma kodları reddedilir.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconShield}" />
                </controls:SettingsCard.HeaderIcon>
                <controls:SettingsCard.Header>
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Sıkı doğrulama" FontWeight="SemiBold" />
                        <Border Padding="8,2" CornerRadius="{StaticResource MuhasibPillCornerRadius}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" ToolTipService.ToolTip="Yalnızca yönetici değiştirebilir" VerticalAlignment="Center">
                            <TextBlock Text="Yönetici" FontSize="10" Foreground="{ThemeResource SystemFillColorAttentionBrush}" />
                        </Border>
                    </StackPanel>
                </controls:SettingsCard.Header>
                <ToggleSwitch IsOn="{Binding ValidationStrict, Mode=TwoWay}" IsEnabled="{Binding IsYonetici, Mode=OneWay}" />
            </controls:SettingsCard>
        </StackPanel>

        <!-- Yeni Dönem -->
        <StackPanel Spacing="4">
            <TextBlock Text="Yeni Dönem" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="2,0,0,4" />
            <controls:SettingsCard Header="Yeni dönem durumu" Description="Yeni açılan mali dönemin başlangıç durumu.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconCheckCircle}" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox ItemsSource="{Binding DefaultDurumSecenekleri, Mode=OneTime}" SelectedItem="{Binding SeciliDefaultDurum, Mode=TwoWay}" MinWidth="180" ToolTipService.ToolTip="Yeni dönem varsayılan durumu" />
            </controls:SettingsCard>
        </StackPanel>

        <!-- Liste Görünümü -->
        <StackPanel Spacing="4">
            <TextBlock Text="Liste Görünümü" Style="{StaticResource BodyStrongTextBlockStyle}" Margin="2,0,0,4" />
            <controls:SettingsCard Header="Açık dönemler sayfa boyutu" Description="Açık dönem listesinde her sayfada gösterilecek kayıt sayısı.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconLayers}" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox SelectedValue="{Binding AcikPageSize, Mode=TwoWay}" MinWidth="140" ToolTipService.ToolTip="Açık dönem sayfa boyutu">
                    <x:Int32>2</x:Int32>
                    <x:Int32>4</x:Int32>
                    <x:Int32>8</x:Int32>
                    <x:Int32>12</x:Int32>
                    <x:Int32>20</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
            <controls:SettingsCard Header="Arşiv dönemler sayfa boyutu" Description="Arşivli dönem listesinde her sayfada gösterilecek kayıt sayısı.">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="{StaticResource IconArchive}" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox SelectedValue="{Binding ArsivPageSize, Mode=TwoWay}" MinWidth="140" ToolTipService.ToolTip="Arşiv sayfa boyutu">
                    <x:Int32>1</x:Int32>
                    <x:Int32>2</x:Int32>
                    <x:Int32>4</x:Int32>
                    <x:Int32>5</x:Int32>
                    <x:Int32>8</x:Int32>
                    <x:Int32>12</x:Int32>
                </ComboBox>
            </controls:SettingsCard>
        </StackPanel>
    </StackPanel>
</UserControl>
