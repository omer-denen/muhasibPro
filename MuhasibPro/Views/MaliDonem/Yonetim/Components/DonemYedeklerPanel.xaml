<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="MuhasibPro.Views.MaliDonem.Yonetim.Components.DonemYedeklerPanel"
    x:Name="DonemYedeklerKok"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MuhasibPro.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="TrueToVis" TrueValue="Visible" FalseValue="Collapsed" />
        <converters:BoolToVisibilityConverter x:Key="FalseToVis" TrueValue="Collapsed" FalseValue="Visible" />
    </UserControl.Resources>

    <Border Style="{StaticResource CustomModernCard}" Padding="16,14">
        <StackPanel Spacing="12">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Spacing="1" VerticalAlignment="Center">
                    <TextBlock Text="Dönem Yedekleri" FontSize="14" FontWeight="SemiBold" Foreground="{ThemeResource MuhasibTextPrimaryBrush}" />
                    <TextBlock FontSize="11" Foreground="{ThemeResource MuhasibSageTertiaryBrush}">
                        <Run Text="{Binding ElementName=DonemYedeklerKok, Path=Root.SelectedDonem.DatabaseName, FallbackValue='—'}" /><Run Text=" • " /><Run Text="{Binding Yedekler.Count, FallbackValue='0'}" /><Run Text=" yedek" />
                    </TextBlock>
                </StackPanel>
                <Button Grid.Column="2" ToolTipService.ToolTip="Seçili dönemin VACUUM INTO yedeğini al" Click="OnYedekAlClick" Style="{StaticResource PrimaryCompactButtonStyle}" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <FontIcon Glyph="{StaticResource IconAdd}" FontSize="11" Foreground="{ThemeResource MuhasibOnInkBrush}" />
                        <TextBlock Text="Şimdi Yedekle" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource MuhasibOnInkBrush}" />
                    </StackPanel>
                </Button>
            </Grid>
            <ItemsControl ItemsSource="{Binding PagedYedekler}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource InventPillMutedStyle}" Padding="12,10" Margin="0,0,0,8">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                               
                                <StackPanel Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding BackupFileName}" FontSize="12" FontWeight="SemiBold" FontFamily="{StaticResource MuhasibMonospaceFontFamily}" Foreground="{ThemeResource MuhasibTextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{Binding BackupDisplayName}" FontSize="11" Foreground="{ThemeResource MuhasibSageTertiaryBrush}" />
                                        <TextBlock Text="{Binding LastBackupDate}" FontSize="11" Foreground="{ThemeResource MuhasibSageTertiaryBrush}" />
                                        <TextBlock Text="{Binding BackupFileSizeDisplay}" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" />
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                    <Button Tag="{Binding}" Click="OnGeriYukleClick" Style="{StaticResource GhostPillButtonStyle}" ToolTipService.ToolTip="Bu yedeği döneme geri yükle">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <FontIcon Glyph="{StaticResource IconUpload}" FontSize="11" Foreground="{ThemeResource MuhasibSuccessBrush}" />
                                            <TextBlock Text="Geri Yükle" FontSize="11" Foreground="{ThemeResource MuhasibSuccessBrush}" />
                                        </StackPanel>
                                    </Button>
                                    <Button Tag="{Binding}" Click="OnYedekSilClick" Style="{StaticResource GhostPillButtonStyle}" ToolTipService.ToolTip="Yedeği kalıcı olarak sil" VerticalAlignment="Center">
                                        <FontIcon Glyph="{StaticResource IconTrash}" FontSize="12" Foreground="{ThemeResource MuhasibDangerBrush}" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid ColumnSpacing="8" Margin="0,4,0,0" Visibility="{Binding YedekHasPagination, Converter={StaticResource TrueToVis}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Content="‹" Style="{StaticResource GhostPillButtonStyle}" Click="OnYedekPrevClick" IsEnabled="{Binding YedekCanPrev}" />
                <TextBlock Grid.Column="1" Text="{Binding YedekPageInfo}" FontSize="11" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <Button Grid.Column="2" Content="›" Style="{StaticResource GhostPillButtonStyle}" Click="OnYedekNextClick" IsEnabled="{Binding YedekCanNext}" />
            </Grid>
            <Border Padding="16,14" CornerRadius="{StaticResource MuhasibCardCornerRadius}" Background="{ThemeResource MuhasibInventPaleBrush}" BorderBrush="{ThemeResource MuhasibSoftBorderBrush}" BorderThickness="1" Visibility="{Binding HasYedek, Converter={StaticResource FalseToVis}}">
                <StackPanel Spacing="4" HorizontalAlignment="Center">
                    <FontIcon Glyph="{StaticResource IconDatabase}" FontSize="20" Foreground="{ThemeResource MuhasibSageTertiaryBrush}" HorizontalAlignment="Center" />
                    <TextBlock Text="Henüz yedek yok" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" HorizontalAlignment="Center" />
                    <TextBlock Text="İlk yedeğinizi almak için “Şimdi Yedekle”ye basın." FontSize="11" Foreground="{ThemeResource MuhasibSageTertiaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
