<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="MuhasibPro.Views.MaliDonem.Yonetim.Components.DonemYedeklerPanel"
    x:Name="DonemYedeklerKok"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MuhasibPro.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="TrueToVis" TrueValue="Visible" FalseValue="Collapsed" />
        <converters:BoolToVisibilityConverter x:Key="FalseToVis" TrueValue="Collapsed" FalseValue="Visible" />
    </UserControl.Resources>

    <Border Style="{StaticResource CustomModernCard}" Padding="16,14">
        <StackPanel Spacing="12">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Spacing="1" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                        <TextBlock Text="Dönem Yedekleri" FontSize="14" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                        <ProgressRing Width="12" Height="12" IsActive="{Binding IsYedeklerYukleniyor}" Foreground="{ThemeResource MuhasibPetrolBrush}" Visibility="{Binding IsYedeklerYukleniyor, Converter={StaticResource TrueToVis}}" />
                    </StackPanel>
                    <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}">
                        <Run Text="{Binding ElementName=DonemYedeklerKok, Path=Root.SelectedDonem.DatabaseName, FallbackValue='—'}" /><Run Text=" • " /><Run Text="{Binding Yedekler.Count, FallbackValue='0'}" /><Run Text=" yedek" />
                    </TextBlock>
                </StackPanel>
                <Button Grid.Column="2" ToolTipService.ToolTip="Seçili dönemin VACUUM INTO yedeğini al" Click="OnYedekAlClick" Style="{StaticResource PrimaryCompactButtonStyle}" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <FontIcon Glyph="{StaticResource IconAdd}" FontSize="11" Foreground="{ThemeResource MuhasibOnInkBrush}" />
                        <TextBlock Text="Şimdi Yedekle" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource MuhasibOnInkBrush}" />
                    </StackPanel>
                </Button>
            </Grid>
            <ItemsControl ItemsSource="{Binding PagedYedekler}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource InventPillMutedStyle}" Padding="12,10" Margin="0,0,0,8">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                               
                                <StackPanel Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding BackupFileName}" FontSize="12" FontWeight="SemiBold" FontFamily="{StaticResource MuhasibMonospaceFontFamily}" Foreground="{ThemeResource TextFillColorPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{Binding BackupDisplayName}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                        <TextBlock Text="{Binding LastBackupDate}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
                                        <TextBlock Text="{Binding BackupFileSizeDisplay}" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                    <Button Tag="{Binding}" Click="OnGeriYukleClick" Style="{StaticResource GhostPillButtonStyle}" ToolTipService.ToolTip="Bu yedeği döneme geri yükle">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <FontIcon Glyph="{StaticResource IconUpload}" FontSize="11" Foreground="{ThemeResource SystemFillColorSuccessBrush}" />
                                            <TextBlock Text="Geri Yükle" FontSize="11" Foreground="{ThemeResource SystemFillColorSuccessBrush}" />
                                        </StackPanel>
                                    </Button>
                                    <Button Tag="{Binding}" Click="OnYedekSilClick" Style="{StaticResource GhostPillButtonStyle}" ToolTipService.ToolTip="Yedeği kalıcı olarak sil" VerticalAlignment="Center">
                                        <FontIcon Glyph="{StaticResource IconTrash}" FontSize="12" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid ColumnSpacing="8" Margin="0,4,0,0" Visibility="{Binding YedekHasPagination, Converter={StaticResource TrueToVis}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Style="{StaticResource GhostPillButtonStyle}" Click="OnYedekPrevClick" IsEnabled="{Binding YedekCanPrev}" Width="32" Height="32" Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" ToolTipService.ToolTip="Önceki sayfa">
                    <FontIcon Glyph="{StaticResource IconChevronLeft}" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Button>
                <TextBlock Grid.Column="1" Text="{Binding YedekPageInfo}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <Button Grid.Column="2" Style="{StaticResource GhostPillButtonStyle}" Click="OnYedekNextClick" IsEnabled="{Binding YedekCanNext}" Width="32" Height="32" Padding="0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" ToolTipService.ToolTip="Sonraki sayfa">
                    <FontIcon Glyph="{StaticResource IconChevronRight}" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Button>
            </Grid>
            <Border Padding="16,14" CornerRadius="{StaticResource MuhasibCardCornerRadius}" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Visibility="{Binding IsYedekListesiBos, Converter={StaticResource TrueToVis}}">
                <StackPanel Spacing="4" HorizontalAlignment="Center">
                    <FontIcon Glyph="{StaticResource IconDatabase}" FontSize="20" Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" />
                    <TextBlock Text="Henüz yedek yok" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
                    <TextBlock Text="İlk yedeğinizi almak için “Şimdi Yedekle”ye basın." FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
