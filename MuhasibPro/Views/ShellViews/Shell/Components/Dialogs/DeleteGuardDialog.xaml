<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="MuhasibPro.Views.ShellViews.Shell.Components.Dialogs.DeleteGuardDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PrimaryButtonText="Dönemi Sil"
    PrimaryButtonStyle="{StaticResource InventDialogDangerStyle}"
    CloseButtonText="Vazgeç"
    DefaultButton="Close"
    Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
    BorderThickness="1"
    CornerRadius="{StaticResource ControlCornerRadius}">

    <ContentDialog.Title>
        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Width="36" Height="36" CornerRadius="{StaticResource MuhasibCardCornerRadius}" Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}" VerticalAlignment="Center">
                <FontIcon Glyph="{StaticResource IconAlertTriangle}" FontSize="18" Foreground="{ThemeResource SystemFillColorCriticalBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1" Spacing="1" VerticalAlignment="Center">
                <TextBlock Text="Guard Kontrolü" FontSize="10" Foreground="{ThemeResource TextFillColorTertiaryBrush}" CharacterSpacing="60"/>
                <TextBlock Text="Mali Dönem Silme" FontSize="16" FontWeight="Bold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            </StackPanel>
        </Grid>
    </ContentDialog.Title>

    <StackPanel Spacing="16" MinWidth="400">

        <!-- Guard Info — YeniMaliDonem ile aynı açık Info banner -->
        <Border
            Padding="12"
            CornerRadius="{StaticResource MuhasibInventInnerCornerRadius}"
            Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}"
            BorderBrush="{ThemeResource SystemFillColorAttentionBrush}"
            BorderThickness="1">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon
                    Glyph="{StaticResource IconAlertTriangle}"
                    FontSize="24"
                    Foreground="{ThemeResource MuhasibPetrolDeepBrush}"
                    VerticalAlignment="Center" />
                <StackPanel>
                    <TextBlock
                        x:Name="DbDosyaAdiText"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    <TextBlock
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                        <Run Text="Dönem: " />
                        <Run x:Name="DonemYiliText" Text="" />
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Error Message -->
        <Border
            x:Name="ErrorBorder"
            Padding="10"
            CornerRadius="{StaticResource MuhasibInventInnerCornerRadius}"
            Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}"
            BorderBrush="{ThemeResource SystemFillColorCriticalBrush}"
            BorderThickness="1"
            Visibility="Collapsed">
            <TextBlock
                x:Name="ErrorText"
                FontSize="12"
                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                TextWrapping="Wrap" />
        </Border>

        <!-- Guard Checks -->
        <Border
            Padding="12"
            CornerRadius="{StaticResource MuhasibInventInnerCornerRadius}"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1">
            <StackPanel Spacing="8">
                <Grid>
                    <TextBlock
                        Text="1. Aktif dönem kilit kontrolü:"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock
                        HorizontalAlignment="Right"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                        Text="✅ Güvenli" />
                </Grid>
                <Grid>
                    <TextBlock
                        Text="2. Oturum/Heartbeat kontrolü:"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock
                        Text="✅ Başka kullanıcı bağlı değil"
                        HorizontalAlignment="Right"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource SystemFillColorSuccessBrush}" />
                </Grid>
                <Grid>
                    <TextBlock
                        Text="3. Yetki kontrolü:"
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock
                        Text="✅ Veritabani_Sil İzni Var"
                        HorizontalAlignment="Right"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource SystemFillColorSuccessBrush}" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- Confirmation Code Input -->
        <StackPanel Spacing="8">
            <TextBlock
                FontSize="12"
                FontWeight="SemiBold"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}">
                <Run Text="Onay Kodu: Silme işlemini onaylamak için lütfen dönemin yılını (" />
                <Run x:Name="ConfirmCodeHintText" Text="" FontWeight="Bold" />
                <Run Text=") yazınız:" />
            </TextBlock>
            <TextBox
                x:Name="ConfirmCodeBox"
                PlaceholderText="Örn: 2027"
                FontSize="14"
                FontFamily="{ThemeResource MuhasibMonospaceFontFamily}"
                Padding="12,10"
                CornerRadius="{StaticResource MuhasibPillCornerRadius}"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" />
        </StackPanel>

        <!-- Yedek tercihi -->
        <CheckBox
            x:Name="YedekleriDeSilCheckBox"
            IsChecked="True"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
            ToolTipService.ToolTip="İşaret kalkarsa yedekler diskte korunur ve 'Silinen Dönem Yedekleri' listesinde gösterilir">
            <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{ThemeResource TextFillColorPrimaryBrush}">
                <Run Text="Dönemin yedeklerini de sil" FontWeight="SemiBold" /><Run Text=" (işaretsizse yedekler korunur)" />
            </TextBlock>
        </CheckBox>
    </StackPanel>
</ContentDialog>