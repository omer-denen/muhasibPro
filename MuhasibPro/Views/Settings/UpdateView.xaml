<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MuhasibPro.Views.Settings.UpdateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:MuhasibPro.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource AppBackgroundBrush}">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="TrueToVis" TrueValue="Visible" FalseValue="Collapsed" />
        <converters:StringToVisibilityConverter x:Key="StringToVis" />
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Spacing="12" Padding="24,18,24,24" MaxWidth="720" HorizontalAlignment="Center">

            <!-- Başlık -->
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4" VerticalAlignment="Center">
                    <TextBlock Text="Güncellemeler" FontSize="20" FontWeight="Bold" Foreground="{StaticResource MuhasibTextPrimaryBrush}" />
                    <TextBlock Text="Uygulama sürümü ve güncelleme kaynağı" FontSize="13" Foreground="{StaticResource MuhasibSageSecondaryBrush}" TextWrapping="Wrap" />
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="6" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <Button Content="Şimdi Kontrol Et" Style="{StaticResource SecondaryCompactButtonStyle}" Command="{x:Bind ViewModel.CheckNowCommand, Mode=OneWay}" IsEnabled="{x:Bind ViewModel.IsCheckButtonEnabled, Mode=OneWay}" />
                    <TextBlock Text="{x:Bind ViewModel.LastCheckText, Mode=OneWay}" FontSize="11" Foreground="{StaticResource MuhasibSageTertiaryBrush}" HorizontalAlignment="Right" />
                </StackPanel>
            </Grid>

            <Pivot>
                <!-- Güncelleme sekmesi -->
                <PivotItem Header="Güncelleme">
                    <StackPanel Spacing="12" Margin="0,12,0,0">
                        <!-- Durum kartı -->
                        <Border Style="{StaticResource CustomModernCard}" Padding="16">
                            <StackPanel Spacing="10">
                                <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                    <FontIcon Glyph="{x:Bind ViewModel.StatusIconGlyph, Mode=OneWay}" FontSize="20" Foreground="{ThemeResource MuhasibPetrolBrush}" VerticalAlignment="Center" />
                                    <StackPanel Spacing="1" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" FontSize="14" FontWeight="SemiBold" Foreground="{ThemeResource MuhasibTextPrimaryBrush}" />
                                        <TextBlock Text="{x:Bind ViewModel.VersionText, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" />
                                    </StackPanel>
                                </StackPanel>

                                <ProgressBar Minimum="0" Maximum="100" Value="{x:Bind ViewModel.ProgressPercentage, Mode=OneWay}" Foreground="{StaticResource MuhasibPetrolBrush}" Visibility="{x:Bind ViewModel.ProgressVisible, Mode=OneWay, Converter={StaticResource TrueToVis}}" />
                                <TextBlock Text="{x:Bind ViewModel.ProgressText, Mode=OneWay}" FontSize="11" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" TextWrapping="Wrap" />
                                <TextBlock Text="{x:Bind ViewModel.ProgressDetails, Mode=OneWay}" FontSize="11" Foreground="{ThemeResource MuhasibSageTertiaryBrush}" TextWrapping="Wrap" />

                                <!-- Hata bandı -->
                                <Border Padding="10" CornerRadius="{StaticResource MuhasibInventInnerCornerRadius}" Background="{ThemeResource MuhasibDangerBgBrush}" BorderBrush="{ThemeResource MuhasibDangerBrush}" BorderThickness="1" Visibility="{x:Bind ViewModel.HasError, Mode=OneWay, Converter={StaticResource TrueToVis}}">
                                    <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource MuhasibDangerBrush}" TextWrapping="Wrap" />
                                </Border>

                                <!-- Güncelleme detay + aksiyon -->
                                <StackPanel Spacing="8" Visibility="{x:Bind ViewModel.UpdateCardVisible, Mode=OneWay, Converter={StaticResource TrueToVis}}">
                                    <TextBlock Text="{x:Bind ViewModel.UpdateSize, Mode=OneWay}" FontSize="11" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" />
                                    <TextBlock Text="{x:Bind ViewModel.ReleaseDate, Mode=OneWay}" FontSize="11" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" />
                                    <TextBlock Text="{x:Bind ViewModel.ChangelogUrl, Mode=OneWay}" FontSize="11" Foreground="{ThemeResource MuhasibPetrolDeepBrush}" TextWrapping="Wrap" Visibility="{x:Bind ViewModel.ChangelogUrl, Mode=OneWay, Converter={StaticResource StringToVis}}" />
                                    <Button Content="{x:Bind ViewModel.UpdateButtonText, Mode=OneWay}" Style="{StaticResource MuhasibPrimaryButtonStyle}" Command="{x:Bind ViewModel.UpdateActionCommand, Mode=OneWay}" IsEnabled="{x:Bind ViewModel.IsUpdateButtonEnabled, Mode=OneWay}" HorizontalAlignment="Left" />
                                </StackPanel>

                                <!-- Detay görünümü (indirme/kurulum dışı) -->
                                <StackPanel Spacing="8" Visibility="{x:Bind ViewModel.DetailsVisible, Mode=OneWay, Converter={StaticResource TrueToVis}}">
                                    <TextBlock Text="{x:Bind ViewModel.UpdateSize, Mode=OneWay}" FontSize="11" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" />
                                    <TextBlock Text="{x:Bind ViewModel.ChangelogUrl, Mode=OneWay}" FontSize="11" Foreground="{ThemeResource MuhasibPetrolDeepBrush}" TextWrapping="Wrap" Visibility="{x:Bind ViewModel.ChangelogUrl, Mode=OneWay, Converter={StaticResource StringToVis}}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </PivotItem>

                <!-- Ayarlar sekmesi -->
                <PivotItem Header="Ayarlar">
                    <StackPanel Spacing="12" Margin="0,12,0,0">
                        <!-- Kaynak kartı -->
                        <Border Style="{StaticResource CustomModernCard}" Padding="16">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Güncelleme kaynağı" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource MuhasibTextPrimaryBrush}" />
                                <TextBox Text="{x:Bind ViewModel.FeedUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="https://github.com/kullanici/repo" FontSize="12" />
                                <TextBlock Text="GitHub repo adresi veya Velopack feed adresi. Değişiklik otomatik kaydedilir." FontSize="11" Foreground="{ThemeResource MuhasibSageSecondaryBrush}" TextWrapping="Wrap" />
                                <Button Content="Bağlantıyı Dene" Style="{StaticResource SecondaryCompactButtonStyle}" Command="{x:Bind ViewModel.CheckNowCommand, Mode=OneWay}" HorizontalAlignment="Left" />
                            </StackPanel>
                        </Border>

                        <!-- Seçenekler kartı -->
                        <Border Style="{StaticResource CustomModernCard}" Padding="16">
                            <StackPanel Spacing="10">
                                <TextBlock Text="Seçenekler" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource MuhasibTextPrimaryBrush}" />
                                <ToggleSwitch Header="Açılışta otomatik kontrol" IsOn="{x:Bind ViewModel.AutoCheckEnabled, Mode=TwoWay}" />
                                <ToggleSwitch Header="Bildirimleri göster" IsOn="{x:Bind ViewModel.NotificationsEnabled, Mode=TwoWay}" />
                                <ToggleSwitch Header="Beta sürümleri dahil et" IsOn="{x:Bind ViewModel.IncludeBetaVersionsEnabled, Mode=TwoWay}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </PivotItem>
            </Pivot>

        </StackPanel>
    </ScrollViewer>
</Page>
